# SRL Defect + Issue Log

Running catalog of known defects, open issues, and
quality concerns. Updated continuously. Lives in git
so it's searchable and never lost.

## Format per entry
- One-line symptom description
- Page / component / file where it lives
- Severity: P0 (prospect-blocking) / P1 (visible but non-blocking) / P2 (cosmetic)
- Status: Open / Investigating / Fixed in <commit>
- Date noted

---

## Open — discovered 2026-05-06 (Sprint 24 smoke)

### P1 — `/accounting/analytics` crashes with null-toFixed error
- Page: [`frontend/src/app/accounting/analytics/page.tsx:160`](frontend/src/app/accounting/analytics/page.tsx#L160)
- Symptom: Full-page "Something went wrong / Cannot read properties of null (reading 'toFixed')" error boundary. Blocks AE Console analytics view entirely.
- Root cause: `pct(l.marginPercent)` at line 160 calls `pct` helper (defined line 10 as `(n: number) => string`) which calls `.toFixed(1)` on the input unconditionally. When any load in response has null `marginPercent`, this crashes the page render.
- Severity: P1 — internal AE Console, full-page crash, blocks financial analysis work
- Status: Open — logged in CLAUDE.md §13.3 Item 12.1 (paired with 12.2 below for single-sprint fix)
- Date noted: 2026-05-06 (during v3.8.aab Sprint 24 smoke)

### P1 — `/accounting/pnl` crashes with null-toFixed error
- Page: [`frontend/src/app/accounting/pnl/page.tsx:145`](frontend/src/app/accounting/pnl/page.tsx#L145)
- Symptom: Full-page "Something went wrong / Cannot read properties of null (reading 'toFixed')" error boundary. Blocks AE Console Load P&L view entirely.
- Root cause: Per-row table cell renders `load.marginPercent.toFixed(1)` with no null guard. Same file already null-guards `load.revenuePerMile` at line 148 with em-dash fallback — pattern established but inconsistently applied. KPI cards at lines 74-75 are safe via `(data?.avgMarginPercent || 0).toFixed(1)`.
- Severity: P1 — same blast radius as `/accounting/analytics`
- Status: Open — logged in CLAUDE.md §13.3 Item 12.2 (bundles with 12.1, shared root cause: `Load.marginPercent` nullable + UI render assumes non-null)
- Date noted: 2026-05-06 (during v3.8.aab Sprint 24 smoke)

---

## Open — discovered 2026-04-22 evening

### P2 — "Get a Free Quote" information vague
- Form / section — needs content rewrite
- Unclear what info the prospect provides or what they get back

---

## S6 family — AE Console defects (all P1, pre-existing)

### S6 (parent) — AE Console: some tabs render "super dark"
- Location: Various AE Console tabs (specific labels TBD)
- Symptom: Visual defect, usable but wrong — tabs render with very dark backgrounds that hurt readability
- Severity: P1 visual
- Status: Open, parent-level audit deferred until specific tab labels are enumerated
- Discovered: 2026-04-22

---

## Open — content density concerns (not bugs, design debt)
- Some pages feel long with too much info + extra pictures
- Candidates for trim: TBD (list tomorrow)
- Scope: content editorial, separate from technical defects

## P2 — UX content improvements (tradeoffs to decide during editorial pass)
- Shippers page quote form: "Origin City/State" and "Destination City/State" fields — open question whether to require full address including street + ZIP (higher friction, richer data) or keep city/state (lower friction, faster lead capture). UX tradeoff to decide during editorial pass, not a bug.
- Shippers page "BY THE NUMBERS / Performance That Speaks" section — currently shows only "48 States Covered" with no other stats. Pre-launch, no real operational numbers to report. Editorial decision required: (a) delete section, (b) replace with capability block, (c) replace with credentials block. Do NOT fabricate performance metrics. Defer to Friday/weekend editorial pass.

---

## Fixed — recent

- 2026-05-06 | Sprint 33 (v3.8.aal) — **RC Modal Items 42 + 44 bundle (Path A). Item 42 extractor widening. Item 44 Caravan tier reconciliation.** Both atomic per §3.3, both surfaced from Sprint 32 Phase B walk follow-on. Sprint 32 (v3.8.aak) shipped error UI but banner only showed generic "Validation error" because [`extractErrorMessage`](frontend/src/components/loads/RateConfirmationModal.tsx) at v3.8.aak only checked top-level `data.error` / `data.message`, never iterating `data.details[]` array Zod errors return per [`backend/src/middleware/errorHandler.ts:66-76`](backend/src/middleware/errorHandler.ts#L66). **Item 42 — extractor widening:** widened helper to iterate `details[]` when present, joining `field: message` pairs into readable string. Banner now reads e.g. `"Validation error — formData.fuelSurcharge: Expected number, received undefined"`. Wired into all 3 RC handler catch blocks (Generate PDF + Send Tender + Save Draft). **Path β methodology continued:** observability ships first, targeted fix ships subsequent atomic. Item 42 stays OPEN — Sprint 34 closes with the targeted field fix once next click surfaces specific Zod rejection. **Item 44 — Caravan tier reconciliation (P0 BKN-blocking):** Sprint 23 reconciled `/accounting/quick-pay` + `/accounting/payments` to canonical Caravan 3-tier; RC Modal Section 8 was missed. RC PDFs to BKN's carriers were referencing legacy 6-tier (Flash/Express/Priority/Partner/Elite/Standard) which doesn't exist in customer-facing Caravan Partner Program — credibility class same as Sprint 30 Houston-address fix. **Fix shape per Sprint 23 pattern (UI rebrand, backend stay-canonical):** new `PAYMENT_SPEEDS` array `[STANDARD, QP_7DAY, QP_SAMEDAY]`; `feePctForSpeed(speedUiKey, tier)` helper computing tier × speed per §8 (sevenDay = 1/2/3 by Platinum/Gold/Silver; sameDay = sevenDay + 2 universal); `standardNetByTier(tier)` returning Net-14/21/30; `uiKeyFromEnum(enumValue)` reverse map for legacy display (EXPRESS→QP_SAMEDAY, PARTNER→QP_7DAY, ELITE→STANDARD); `LEGACY_PAYMENT_TIERS` preserved as fallback display for existing loads. **UI:** 3-card SPEED selector replaces 6-card tier grid in `SectionPayment`; Caravan tier badge added to section header (auto-derived from `selectedCarrier.tier` via shared `InfoBadge`, canonical Sprint 24 palette); each card sub-text tier-derived. **Enum mapping on save:** STANDARD UI → STANDARD enum (168h SLA), QP_7DAY UI → PRIORITY enum (48h SLA, closest to 7-day mental model), QP_SAMEDAY UI → FLASH enum (2h SLA). EXPRESS/PARTNER/ELITE legacy values AE-unselectable from new RC Modal but persist in DB for analytics; existing loads render via `uiKeyFromEnum` fallback. **FormState gained `carrierTier: string`** (defaults empty; reads from `load.carrier.carrierProfile.tier` on `initForm`; `selectCarrier` writes `carrier.tier` on selection). `financials.feePercent` calc rebased from legacy enum lookup to tier × speed derivation. **Item 41 stays CLOSED (Sprint 32 close stands)** — Phase A audit verified Sprint 32 commit `6d6655a` contained the swap, project-wide grep for `#1a2340` returned zero remaining matches, code-level fix is correct. User-reported persistence after v3.8.aak deploy traces to browser bundle cache (Next.js code-splits per route; RC Modal chunk has its own content-hashed bundle that may not have invalidated alongside VersionFooter chunk). Hard refresh (Ctrl+Shift+R) resolves. No code change needed. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net source change: ~80 LOC across 1 file (RateConfirmationModal.tsx). Per §3.1: v3.8.aak → v3.8.aal. AE Console + customer-facing RC PDF surface — version-bump justified. §13.3: Item 41 stays CLOSED, Item 42 stays OPEN (Sprint 34 close with specific field fix), Item 44 logged + CLOSED in this commit. | v3.8.aal

- 2026-05-06 | Sprint 32 (v3.8.aak) — **RC Modal carrier dropdown cream-bg fix + silent-error UI surfacing. Closes §13.3 Items 41 (color) + 43 (error UI). Item 42 (PDF/Tender root cause) logged OPEN — Sprint 33 close.** Path β chosen over Path α — error UI ships independent value beyond just diagnosing the one immediate bug; two atomic commits with clean boundaries beat one bundled commit. **(1) Carrier dropdown color mismatch (Item 41):** carrier search dropdown render block at [`RateConfirmationModal.tsx:1518-1546`](frontend/src/components/loads/RateConfirmationModal.tsx#L1518) used `bg-[#1a2340]` (off-canonical dark navy, not in §2.1 token set) against cream-themed modal shell. Likely template-imported from same Texas-based-shipper boilerplate as Sprint 30 found. Worked technically (~17:1 contrast) but rendered discordantly. Path B fix per Phase A: `bg-[#1a2340] border-white/10` → `bg-white border-slate-200`; `hover:bg-white/5` → `hover:bg-slate-50`; carrier name `text-white` → `text-slate-900`. Tier badges (canonical Sprint 24 palette via `InfoBadge`) untouched. "No carriers found" empty state slate-500 unchanged. 3-LOC swap. **(2) Silent error swallow on RC handlers (Item 43):** `handleSaveDraft` / `handleGeneratePdf` / `handleSendTender` all caught errors via bare `console.error("...", err)` with no UI feedback — user clicks button, API fails, user sees nothing. Pattern blocked Phase A from diagnosing Item 42 without DevTools. **Fix:** added `submitError: string | null` state at component root; `extractErrorMessage(err, fallback)` helper checks `err.response.data.error → .message → err.message → fallback` (handles axios + native Error + plain string); all 3 handlers reset error at start + set error in catch; new JSX banner above button row conditionally rendered — `bg-red-50 border-l-4 border-red-500 text-red-700` (canonical Tailwind error palette) with "Error:" label prefix. Banner auto-dismisses on next handler attempt. ~25 LOC. **Future API failures on these handlers self-diagnose via screenshot** — no DevTools workflow required. **(3) PDF/Tender root cause Item 42 logged OPEN — Sprint 33 close.** Most likely cause (educated guess pending Sprint 33 diagnosis): Zod validator at [`backend/src/validators/rateConfirmation.ts:16-200`](backend/src/validators/rateConfirmation.ts#L16) rejecting one of ~50 form-state fields spread via `...form` in `buildPayload()`. Could also be auth or backend internal error. Sprint 33 fix shape unknown until error UI surfaces actual rejection on next click attempt. **Pattern propagation candidate:** other modals/forms across codebase likely have same silent-error class — could roll up into typing-discipline / observability sprint; not in Sprint 32 scope per §3.3. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net source change: ~30 LOC (3 dropdown swaps + 25 error-UI lines + 1 carrier-name text-color swap). Per §3.1: v3.8.aaj → v3.8.aak. AE Console RC modal surface — version-bump justified. §13.3 Items 41 + 43 logged + closed in same commit per established hotfix-with-immediate-close pattern (Items 31, 32, 33, 34-38, 39 same shape). Item 42 logged OPEN. | v3.8.aak

- 2026-05-06 | Sprint 31 (v3.8.aaj) — **Hotfix: RC Modal carrier search 404 silent-fail. Closes §13.3 Item 39 (B28-T1.3-A re-diagnosed).** Originally walked as "P1 missing empty state on carrier search." Phase A audit corrected the diagnosis — the empty state was never the bug. **Root cause:** [`RateConfirmationModal.tsx:1422`](frontend/src/components/loads/RateConfirmationModal.tsx#L1422) (pre-fix) called `api.get("/carriers/all", ...)` but the canonical handler `getAllCarriers` is mounted at `/api/carrier/all` (singular) per [`backend/src/routes/carrier.ts:123`](backend/src/routes/carrier.ts#L123), NOT `/api/carriers/all` (plural). The plural route file at [`backend/src/routes/carriers.ts`](backend/src/routes/carriers.ts) defines `GET /` (with `validateQuery`) and `GET /:id`, so Express matched `/api/carriers/all` against `/:id` with `req.params.id = "all"` → `getCarrierDetail("all")` → 404. Frontend `useQuery` silently swallowed the 404, `data` stayed undefined, dropdown render gated on `showSearch && carriers && (...)` never fired — AE saw silent dead-state. **Re-diagnosis pattern matches Sprint 26 → 26b** (walk surfaces symptom, audit identifies real bug class, correct fix ships). Memory #11 firing yet again. **Fix:** 1-character change — `s` removed from `carriers/all`. New URL `/carrier/all` resolves to the actual handler. Inline comment added at call site referencing Sprint 31 + Item 40. **Adjacent finding §13.3 Item 40 logged not fixed:** duplicate carrier-route definitions (singular `routes/carrier.ts` vs plural `routes/carriers.ts`) — both call same `getAllCarriers` controller, different URL paths, validation drift. The plural file's `GET /:id` is the silent-trap that caused this 404. Consolidation tracked for post-BKN architecture queue per §3.3 "no scope creep." **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net LOC change: ~7 (1 URL char swap + 6-line inline comment). Per §3.1: v3.8.aai → v3.8.aaj. AE Console surface — version-bump justified. §13.3 Item 39 logged + closed in same commit per established hotfix-with-immediate-close pattern (Items 31, 32, 33, 34-38 same shape). Item 40 logged for post-BKN. | v3.8.aaj

- 2026-05-06 | Sprint 30 (v3.8.aai) — **Hotfix: RC Modal Broker Info Texas-template data drift. Closes Sprint 28 Phase B Tier 1.2 walk findings B28-T1.2-A through D + 1 adjacent governing-law finding (5 §13.3 items: 34, 35, 36, 37, 38).** P0 BKN-blocking — BKN ships in days; RC PDFs go to BKN's carriers and reference SRL's company info. Wrong address on customer-facing legal document would be a credibility-class issue. **Root cause confirmed via Phase A audit:** all 5 wrong values traced to a single Texas-based broker boilerplate import in [`RateConfirmationModal.tsx`](frontend/src/components/loads/RateConfirmationModal.tsx) — `SRL_INFO` constant at lines 272-279 + `DEFAULT_TERMS` clause 12 at line 270. Single-file blast radius confirmed via `grep -rn "Westheimer\|Houston, TX 77063\|dispatch@silkroute\|MC-1794414\|DOT-4526880\|State of Texas"` across `frontend/src` + `backend/src` — only this module matched (`eldService.ts:52` "Houston, TX" is an unrelated ELD city geocoding lookup, left as-is). **5 fixes applied (Path 1 — inline constant updates, Path 2 central authority module deferred to §13.3 Item 8.9 architectural sprint per directive scope):** (1) `mc: "MC-1794414"` → `"MC# 1794414"` per [voice.md:98](.claude/skills/srl-brand-design/references/voice.md#L98) skill SOT (with `#` symbol, single space, no hyphen). (2) `dot: "DOT-4526880"` → `"DOT# 4526880"` same canonical format. (3) `address: "8950 Westheimer Rd, Suite 200, Houston, TX 77063"` → `"2317 S 35th St, Galesburg, MI 49053"` per CLAUDE.md §1 (FMCSA SAFER + BMC-84 bond paperwork as primary source per §3.13). (4) `email: "dispatch@silkroutelogistics.ai"` → `"operations@silkroutelogistics.ai"` per §1 + §3.10 explicit canonical for "BOL, Rate Confirmation, Invoice, and other shipper/carrier-facing documents." `dispatch@` is not in §1 canonical alias list — same drift class as §13.3 Item 8.2.1 (`sales@` undocumented alias). (5) **Adjacent governing-law clause** at line 270 `"State of Texas"` → `"State of Michigan, with venue in Kalamazoo County"` per §14. Format conventions applied: `§` symbol replaces "Section" word per legal convention; venue clause added per §14 (governing law without venue is incomplete legal protection). **Numeric values were already correct** (1794414 and 4526880, no leading-zero typo from §13.3 Item 8.8) — only the format was non-canonical. Phone `(269) 220-6760` and company name `Silk Route Logistics Inc.` already correct, unchanged. **Out of scope (deferred to existing backlog):** [`site-chrome.json`](frontend/src/lib/site-chrome.json) has its own drift (mcNumber leading-zero per Item 8.8, sales@ alias per Item 8.2.1) — different surface (marketing footers), separate sprints. BOL/Invoice template authority blocks — Item 8.9 architectural sprint. Central authority module (`frontend/src/lib/srl-info.ts` + `backend/src/lib/srl-info.ts` mirror) — Path 2 alternative explicitly deferred to Item 8.9 per Phase A A5-alt. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net LOC change: ~7 source (6 inline value swaps + 1 governing-law clause swap, all in one module). **Smallest hotfix yet by source-LOC measure** — closes 5 findings via 7 lines of code change. Per §3.1 sequence-continuous rule: v3.8.aah → v3.8.aai. Customer-facing AE Console + RC PDF surface — version-bump justified. §13.3 Items 34-38 logged + closed in same commit per established hotfix-with-immediate-close pattern (precedent: Sprints 26b, 29). Sprint 28 Phase B walk continues post-deploy with Tier 1.3 (tender + status advance lifecycle), Tier 1.4 (Quick Pay flow), Tier 2 (cross-component contracts), Tier 3 (public surfaces), Tier 4 (latent A28-5 through A28-15 trigger probes). | v3.8.aai

- 2026-05-06 | Sprint 29 (v3.8.aah) — **Hotfix: RateConfirmation modal accessorial shape mismatch. Closed Sprint 28 Phase A finding A28-1 (logged as §13.3 Item 33).** Same root-cause class as Sprint 26b Load Board crash (Item 32) — different surface. Phase A static-analysis grep surfaced [`RateConfirmationModal.tsx:418`](frontend/src/components/loads/RateConfirmationModal.tsx#L418) immediately. **Pre-fix code:** `description: a.description || a` fell through `|| a` to the whole `{type, amount, payer}` Order Builder object when `.description` was undefined. Object propagated to (1) [`line 500`](frontend/src/components/loads/RateConfirmationModal.tsx#L500) `.filter((a) => a.description.trim())` → TypeError "a.description.trim is not a function"; (2) [`line 1716-1717`](frontend/src/components/loads/RateConfirmationModal.tsx#L1716) `<input value={acc.description} ... />` → React error #31. **Trigger conditions** identical to Item 32: load created via Order Builder convert flow + at least one accessorial added + AE opens RateConfirmation modal. **Fix — defensive type-check** (same pattern as Sprint 26b): three input shapes supported — string (legacy) → description = a; `{description, amount}` (RC's own saved shape) → description = a.description; `{type, amount, payer}` (Order Builder shape per [`orders/types.ts:90`](frontend/src/app/dashboard/orders/types.ts#L90)) → description = a.type. Order Builder accessorial `{type: "Detention", amount: 0}` now hydrates correctly as RC modal accessorial `{description: "Detention", amount: "0"}`. **Why Sprint 26b smoke didn't catch this:** Sprint 26b smoke clicked Load Board side panel which doesn't open the RC modal — the RC modal opens separately via "Send Rate Confirmation" action. Same bug shape, different render site. **Sprint 28 audit-only methodology validation:** static-analysis grep for accessorial readers surfaced this in 30 seconds without requiring a Wasi smoke session — exactly the methodology Sprint 28 proposed. **Root-cause loophole still open (Sprint 31 candidate):** [`backend/src/validators/load.ts:113`](backend/src/validators/load.ts#L113) `accessorials: z.array(z.any()).optional()` accepts any shape — Sprint 26b + Sprint 29 both cosmetic patches on consumer sides; producer-side enforcement deferred to Sprint 31 (validator tighten + data migration). **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net LOC change: ~15 (1 file, accessorial hydrator block expanded from 1 line to ~17 with defensive type-check + comment). Per §3.1 sequence-continuous rule: v3.8.aag → v3.8.aah. AE Console surface — version-bump justified. | v3.8.aah

- 2026-05-06 | Sprint 27 (v3.8.aag) — `/track` public tracking page status vocabulary cleanup. Customer-facing surface (BOL QR scan target) was leaking broker-internal pipeline enum values. User reported during Sprint 26 Phase C smoke: status pill rendered "POSTED" — internal-process language meaning "load is on our load board awaiting carrier" — broker-operational vocabulary the customer shouldn't need to think about. **Phase A audit:** identified one rendering site at [`app/track/page.tsx:169-171`](frontend/src/app/track/page.tsx#L169) — `{result.status.replace(/_/g, " ")}` rendered the raw enum directly. Step bar already used pre-mapped friendly labels. Backend response stays canonical (returns full enum). **Fix 1 — `PUBLIC_STATUS_LABEL` map + helper:** added `Record<string, string>` mapping all 14 known Load statuses to 8 customer-meaningful labels: DRAFT/POSTED/TENDERED/CONFIRMED/BOOKED → "Scheduled" (pre-dispatch cluster); DISPATCHED → "Dispatched"; AT_PICKUP → "At pickup"; LOADED/PICKED_UP → "Picked up"; IN_TRANSIT → "In transit"; AT_DELIVERY → "Arriving"; DELIVERED/POD_RECEIVED/INVOICED/COMPLETED → "Delivered" (post-delivery accounting noise hidden); TONU/CANCELLED → "Cancelled" (TONU "Truck Ordered Not Used" is billable accounting nuance customer doesn't need). Helper `publicStatusLabel(status)` returns mapped label or falls back to existing `.replace(/_/g, " ")` behavior for any unknown status (defensive against future schema additions). Applied at status pill — one swap, one line. **Fix 2 — STEP_BAR label revision per Phase A A5:** STEP_BAR had a confusing label-to-status mapping where AT_DELIVERY was labeled "In transit" — but AT_DELIVERY means "at the delivery location waiting to be unloaded," closer to "Arriving" than "in transit." IN_TRANSIT was labeled "Departed" which is imprecise (IN_TRANSIT means moving on the road, not "just departed"). Revised: LOADED → "Picked up" (unchanged); IN_TRANSIT → "On the road" (was "Departed"); AT_DELIVERY → "Arriving" (was misleading "In transit"); DELIVERED → "Delivered" (unchanged). Reads as a clearer customer journey: "Picked up → On the road → Arriving → Delivered." STEP_BAR keys unchanged — only the labels customers see. **Bundled** with Fix 1 per A5 recommendation: same file, same render path, same atomic-commit slot. **Backend untouched** — `trackingController.ts:171` still returns `status: load.status` (canonical enum). If a second public client ever ships (mobile app, embed widget), promote mapping to backend `publicStatus` field per audit S3 path. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net LOC change: ~35 (PUBLIC_STATUS_LABEL map + helper + 1-line status pill swap + 2 STEP_BAR label edits). Per §3.1 sequence-continuous rule: v3.8.aaf → v3.8.aag. Customer-facing surface — every BOL QR scan post-Sprint-26 now sees customer-meaningful labels instead of internal broker pipeline vocabulary. Continues audit-first discipline cadence: real audit catches latent surface issue, atomic fix ships. | v3.8.aag

- 2026-05-06 | Sprint 26b (v3.8.aaf) — **Hotfix: Load Board click-to-detail React error #31 crash. Closed §13.3 Item 32 in same commit (logged + closed pattern).** Bug surfaced during Sprint 26 (v3.8.aae) Phase C smoke when user created a test load via Order Builder and clicked it on Load Board → full-screen "Something went wrong" + React error #31. Blocked Sprint 26 Phase C verification. **Root cause: JSON column shape mismatch on `Load.accessorials`.** Order Builder writes `Array<{type, amount, payer}>` per the [`Accessorial`](frontend/src/app/dashboard/orders/types.ts#L90) interface; convert-to-load endpoint at [`backend/src/routes/orders.ts:455`](backend/src/routes/orders.ts#L455) writes the form's array verbatim to the JSON column. Load Board side panel at [`loads/page.tsx:1030-1036`](frontend/src/app/dashboard/loads/page.tsx#L1030) (pre-fix) iterated `load.accessorials` with `key={a}` and rendered `{a}` directly as a React child, assuming `string[]` not `object[]`. React threw error #31 with the object's keys in the error args. **Latent since v3.8.c** (Order Builder convert flow shipped ~Apr 28). Pre-commit tsc passed every sprint because the Zod validator at [`backend/src/validators/load.ts:113`](backend/src/validators/load.ts#L113) is `z.array(z.any()).optional()` — type-system can't catch JSON-column shape mismatches. NOT introduced by Sprints 24/25/26 — last semantic touch to `loads/page.tsx` was v3.7.n.8 (`0e3b6a4`); recent brand sweeps (v3.8.gg through v3.8.mm) only changed className tokens, not data-rendering logic. **Fix shape — F2 (per audit recommendation):** render `accessorial.type` with optional ` ($amount)` suffix. Defensive type-check handles three input shapes — `typeof a === "string"` (legacy compat) → render `a`; object with `type` key → render `"${a.type}${a.amount ? ` ($${a.amount})` : ""}"` so AE staff actually see the accessorial value ("Detention ($250)"); fallback → literal `"Accessorial"`. React `key` switched from `key={a}` to `key={i}`. **Trigger conditions** (all three): load created via Order Builder convert flow, user added at least one accessorial before converting, user clicks load on Load Board to open side panel. **Adjacent risk sweep cleared** (Phase A): Load model has 4 JSON columns — only `accessorials` is rendered as a React child on Load Board page; `stops`, `additionalRefs`, `datPostedFields` either aren't read or aren't rendered as children. One-finding fix. **Out of scope (deferred):** tightening Zod validator from `z.array(z.any())` to typed schema (separate sprint, doesn't prevent render-side bugs); broader JSON-column render-site audit across codebase. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net LOC change: ~10 (1 file). Per §3.1 sequence-continuous rule: v3.8.aae → v3.8.aaf. Hotfix-with-immediate-close pattern used because the bug blocked Sprint 26 Phase C smoke. Honest note on cadence: this class of latent bug (Item 31 BOL QR 404, Items 12.1/12.2 null-toFixed crashes, this Item 32) is being surfaced by audit work, not introduced by it — the brand-discipline + customer-facing-reliability sprints exercise code paths that hadn't been QA'd, and dormant shape-mismatch / contract-mismatch bugs surface as a result. Type-system + pre-commit gates can't catch JSON-column shape drift or runtime-fired routes, so these can only be caught by exercise. | v3.8.aaf

- 2026-05-06 | Sprint 26 (v3.8.aae) — **Closed §13.3 Item 31 P0 production reliability bug.** BOL QR codes + email tracking links 404 on production fix. Critical fix before BKN first load mid-May. Every BOL printed since v3.8.b had been silently routing scans to a 404; this ships the missing destination. **ROOT CAUSE:** [`backend/src/utils/qrGenerator.ts:25`](backend/src/utils/qrGenerator.ts#L25) encodes `silkroutelogistics.ai/track/<12-char-token>` on every BOL print but frontend has no `/track/[token]/page.tsx` dynamic route, `_redirects` had no splat rule, and [`frontend/next.config.ts:5`](frontend/next.config.ts#L5) sets `output: "export"`. Cloudflare Pages 404s every `/track/<anything>`. Email tracking links built `/tracking/<token>` URLs in 4 backend sites with the same defect class. **STRUCTURAL CONSTRAINT (Phase A discovery):** the directive's recommended U1 (add `app/track/[token]/page.tsx` dynamic route) was structurally blocked by `output: "export"` — Next.js 15 requires `generateStaticParams()` for all dynamic routes under static export, and runtime-generated 12-char `ShipperTrackingToken` values cannot be enumerated at build time. Only paths to unblock U1 were major infrastructure migration (drop `output: "export"` + adopt `next-on-pages` adapter or Vercel SSR) — out of scope for a UX fix. **DECISION — U2 with U1's UX (SPA-rewrite via `_redirects`):** existing `_redirects` already used status-200 URL rewrite for `/carrier/*` (line 22) — same pattern applied cleanly to `/track/*`. **`_redirects` (+2 rules)** at [`frontend/public/_redirects`](frontend/public/_redirects): `/tracking/* /track/:splat 301` (legacy email link backward-compat) + `/track/* /track 200` (QR/bookmark deep-link SPA rewrite). Ordering preserved per Phase A: existing `/tracking` and `/tracking.html` exact-path 301s come BEFORE the new `/tracking/*` splat. **`app/track/page.tsx`:** added `tokenFromUrl` state + mount-time `useEffect` that reads `window.location.pathname`, decodes the token via `decodeURIComponent`, and auto-fires `api.get('/tracking/:token')` immediately. Search UI (tabs + input + Track button) wrapped in `{!tokenFromUrl && (...)}` per B3 result-only mode — customer who scanned QR sees direct shipment result, no search friction. Loading spinner (Loader2 icon) during fetch. On error: red error message + "Track another shipment" link to `/track`. On success: existing result panel renders unchanged with "Track another shipment →" link added below the contact panel for token-mode flow. **Email URL consolidation (4 sites, not 1 — Sprint 25 Phase A undercount corrected):** [`shipperLoadNotifyService.ts:48`](backend/src/services/shipperLoadNotifyService.ts#L48) trackingLink helper, [`shipperLoadNotifyService.ts:316`](backend/src/services/shipperLoadNotifyService.ts#L316) sendTrackingLink fn, [`shipperPortalController.ts:745`](backend/src/controllers/shipperPortalController.ts#L745), [`shipperNotificationService.ts:258`](backend/src/services/shipperNotificationService.ts#L258). All 4 swap `/tracking/` → `/track/`. Verified with grep — zero remaining `/tracking/${...}` builders in `backend/src/`. **Backend `/api/tracking/:token` UNCHANGED** — 5-tier fallback chain (ShipperTrackingToken → `Load.trackingToken` → `shipperCode` → `bolNumber` → `referenceNumber`/`loadNumber`/`shipperPoNumber`) still handles every identifier class. `shipperCode` dead-schema cleanup deferred per Sprint 25 precedent — Sprint 26 atomic on URL-route fix. **URL pattern post-Sprint-26:** QR codes `/track/<token>` (unchanged), email tracking links `/track/<token>` (was `/tracking/`), manual customer entry `/track` (search UI), legacy `/tracking/*` 301-redirected to `/track/<token>`. **Edge cases:** invalid token → backend 404 "Shipment not found" + manual-search fallback link; expired token → backend 410 "Tracking link has expired" rendered verbatim; bare `/track` access → existing search UI flow unchanged. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean (verifies `_redirects` is included in `out/` and the page builds with new useEffect). `/track` is dark-only by design — token-mode UI inherits same `bg-[#0f172a]` wrapper, no cross-mode regression risk. Net LOC change: ~50 source (track/page.tsx +35, _redirects +2, 4 backend single-line edits). Sprint 25 Phase A had estimated 80-130 LOC for the directive's U1; U2 came in lighter per Phase A A10. Per §3.1 sequence-continuous rule: v3.8.aad → v3.8.aae. Customer-facing surface, version-bump-justified. Continues operational reliability work alongside brand discipline cadence. | v3.8.aae

- 2026-05-06 | Sprint 25 (v3.8.aad) — `/track` public tracking page contrast fix + tab structure reconciliation. Customer-facing surface (BOL QR scan target). Single atomic commit covering two related fixes. **(1) CONTRAST FIXES** per Phase A per-line context-verified audit (Sprint 15b/22 methodology). **Fix #1** — tab inactive label at [`app/track/page.tsx:86`](frontend/src/app/track/page.tsx#L86): `text-gray-500 hover:text-gray-300` (~3.9:1 borderline, fails AA-normal) → `text-gray-300 hover:text-white` (~10.5:1, PASS AA, hover lifts to white for affordance). **Fix #2** — compact footer ghost-text at [`components/shell/SiteFooter.tsx:12`](frontend/src/components/shell/SiteFooter.tsx#L12) isDark branch: `text-white/45` (~3.4:1, FAILS AA) → `text-white/70` (~9.4:1, PASS AA). Same memory #11 ghost-text class as Sprint 24a `text-white/X` swaps. Light-mode branch untouched; component is shared with marketing dark-themed footer surfaces — lift improves contrast across all dark surfaces with no regression risk (light branch unchanged, dark improvement is monotonic). **(2) TAB STRUCTURE — T2 chosen.** Phase A data-model parity audit found `/track` UI offered 3 lookup tabs but the `kind` state was purely cosmetic — backend endpoint ([`backend/src/controllers/trackingController.ts:99-124`](backend/src/controllers/trackingController.ts#L99)) runs a 5-tier fallback chain (ShipperTrackingToken → `Load.trackingToken` → `Load.shipperCode` → `Load.bolNumber` → `referenceNumber`/`loadNumber`/`shipperPoNumber`) regardless of tab selection. The `kind` state changed only the placeholder text, never the API call. The "Tracking code" identifier class maps to the 12-char alphanumeric ShipperTrackingToken embedded in the BOL QR — customers reach those tokens via QR scan only, never manually type them. The 6-char `Load.shipperCode` is dead schema (defined, queried, never written by any code path — grep-confirmed zero write paths in `backend/src/`). UI reduced from 3 tabs to 2 tabs: "BOL number" (`e.g. BOL-7734`) + "Reference / PO #" (`e.g. PO-88421 or load reference`). Backend endpoint UNCHANGED — fallback chain still accepts any value a customer might paste, including legacy uuid trackingTokens for compatibility. **shipperCode dead-schema cleanup DEFERRED** to separate cleanup sprint per §3.3 atomic-commit + "no scope creep" — Sprint 25 atomic on user-facing change; backend dead-code removal is coherent but separable. Aligns with §3.7 "delete before you add" — phantom UI surface dropped. **ADJACENT FINDING surfaced during audit** (NOT fixed in Sprint 25, logged separately): BOL QR currently 404s on production. [`utils/qrGenerator.ts:25`](backend/src/utils/qrGenerator.ts#L25) encodes URL `https://silkroutelogistics.ai/track/<token>` but frontend has NO `/track/[token]/page.tsx` dynamic route, `_redirects` has no `/track/*` splat rule, and `next.config.ts` sets `output: "export"` (static HTML). Email tracking links use `/tracking/<token>` with same issue — `/tracking` exact path 308s to `/track` but `/tracking/<anything>` 404s. Logged in CLAUDE.md §13.3 Item 31 (P0 production reliability) for dedicated sprint — needs dynamic route + auto-fill + auto-search OR `_redirects` splat rule. Recommended to jump queue ahead of remaining brand work since every BOL printed routes customers to a 404 when scanned. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **`/track` is dark-only by design** — wrapper is `bg-[#0f172a]` hardcoded with no `[data-mode]` cascade hooks; both fixes are dark-mode-only so no cross-mode regression risk. Per §3.1 sequence-continuous rule: v3.8.aac → v3.8.aad. Net LOC change: ~10 (track/page.tsx tab union + array + placeholder ternary + tab inactive className; SiteFooter.tsx:12 dark-branch base text). Continues brand discipline cadence across Sprints 15a/16/22/24/24a/25. | v3.8.aad

- 2026-05-06 | Sprint 24a (v3.8.aac) — Brand-token follow-up commit. 4-file working-tree drift surfaced during Sprint 24 (v3.8.aab) Phase C verification, committed as separate atomic per §3.3 (Sprint 24a closes surfaced drift only — no scope creep into full canonicalization audit). **Two swap classes:** **(1) Cross-mode legibility per memory #11** (`text-white/X` → `text-slate-500`): [`components/accounting/CreateSettlementModal.tsx:55`](frontend/src/components/accounting/CreateSettlementModal.tsx#L55) X close icon, `text-white/60` → `text-slate-500`; [`components/ui/FileUpload.tsx:120`](frontend/src/components/ui/FileUpload.tsx#L120) X remove icon, `text-white/50` → `text-slate-500`. Translucent white renders as ghost text on light surface in light mode; `text-slate-500` with the global `[data-mode="light"]` override (CLAUDE.md §13.3 Item 10) lifts to navy on cream + stays readable on dark navy. **(2) Non-canonical gold → canonical gold-dark per skill tokens.md** (`#C9A84C` → `#BA7517`): [`components/shipper/ShipmentDetailDrawer.tsx:125,131`](frontend/src/components/shipper/ShipmentDetailDrawer.tsx#L125) Download POD + Message Rep `hover:text-` (two occurrences); [`components/shipper/ShipperChatbot.tsx:126`](frontend/src/components/shipper/ShipperChatbot.tsx#L126) action chip `text-`. `#C9A84C` is olive-gold not in canonical set; `#BA7517` is canonical `--gold-dark` per §2.1. **Phase A audit correction:** Sprint 24a directive mis-paired the files between the two swap classes (predicted ShipmentDetailDrawer + CreateSettlementModal both did cross-mode; ShipperChatbot + FileUpload both did gold). Audit-first caught reality before commit — actual pairings are CreateSettlementModal + FileUpload do cross-mode; ShipmentDetailDrawer + ShipperChatbot do gold. Same audit-first discipline as 5 prior catches per memory #11. **Residuals logged not fixed:** `ShipmentDetailDrawer.tsx:125` retains `hover:border-[#C9A84C]` (only text-color swapped); `ShipperChatbot.tsx:126` retains `border-[#C9A84C]/40` + `hover:bg-[#C9A84C]/10` (only text-color swapped). Per §3.3 "no scope creep" rule. Logged in CLAUDE.md §13.3 (Brand-token canonicalization residual) as future per-finding triage candidate. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. Per §3.1 sequence-continuous rule: v3.8.aab → v3.8.aac. Net LOC change: ~10 (4 files × 1-line surgical swap; ShipmentDetailDrawer has 2 adjacent-line swaps). Continues brand discipline cadence across Sprints 15a/16/22/24/24a — cross-mode visual verification + §2.1 token canonicalization. | v3.8.aac

- 2026-05-06 | Sprint 24 (v3.8.aab) — Theme system simplification + Caravan Partner Program tier palette canonicalization. Single atomic commit covering two related brand-discipline concerns. **(1) THEME SYSTEM SIMPLIFICATION:** removed 5 alternative themes (Midnight Express, Desert Route, Arctic Haul, Highway Green, Chrome Steel) — kept only canonical Silk Route Classic + light/dark mode toggle. Pre-Sprint-24 had 6 themes × 2 modes = 12 visual variants per surface, expanding scanner complexity and adding off-canonical color drift; for a single-founder pre-revenue platform with ~2 active users, theme personalization wasn't business-justified. Removed: 5 alternative theme blocks at [`globals.css:409-442`](frontend/src/app/globals.css#L409) (~30 LOC of `[data-theme=...]` overrides), `THEMES` array (6→0 entries) in [`useTheme.ts`](frontend/src/hooks/useTheme.ts), `themeId` state + `setTheme` API + `data-theme` attribute setter, [`ThemePanel.tsx`](frontend/src/components/ui/ThemePanel.tsx) 6-theme grid + slide-in panel + Apply/Cancel preview-confirm flow (~110 LOC). Kept: light/dark mode toggle (only useful UX residue), `data-mode="light"|"dark"` cascade in globals.css, localStorage `srl_mode` key. **New:** `ModeToggleButton` — sun/moon icon button, single-click toggle, no preview panel. ~30 LOC. `ThemeGearButton` retained as alias to avoid churning import sites in Sidebar.tsx. **Migration:** existing users with non-Classic theme preference in localStorage gracefully fall back via `useTheme.loadFromStorage()` — proactively clears legacy `srl_theme` key on first load + `applyToDOM` strips any stale `data-theme` attribute. Backend `/auth/preferences` `preferredTheme` field still accepted server-side for forward compatibility; frontend stops writing. Field retirement = data-hygiene sprint, not blocking. **(2) TIER PALETTE CANONICALIZATION:** Sprint 23 (v3.8.aaa) shipped Caravan Partner Program tier structural reconciliation (canonical tier NAMES Silver/Gold/Platinum) using Tailwind slate/yellow/purple palette — visually off-brand. Sprint 24 swaps to canonical SRL tokens per skill tokens.md. **Coherent progression:** Sprint 23 = right NAMES, Sprint 24 = right COLORS — not regression. Pre-Sprint-24 had FIVE distinct off-canonical palettes: marketing `carriers.css` literal precious-metal hex (`#C0C0C0` silver, `#C8963E` legacy gold, `#E5E4E2` platinum), marketing `carriers.html` inline `#C8963E` + `rgba(200,150,62,X)` tints, AE Console accounting/quick-pay Tailwind slate-500/yellow-500/purple-500, AE Console accounting/payments same, carrier dashboard Tailwind with different opacity ratios. **Post-Sprint-24 unified canonical palette across all 5 surfaces:** Silver bg `#8AA5C0`/15 + text `#5B7EA3` (navy-300/navy-500 muted neutral), Gold bg `#C5A572`/15 + text `#BA7517` (canonical `--gold` + `--gold-dark`), Platinum bg `#0A2540` + text `#C5A572` (canonical `--navy` + `--gold`, top-tier brand treatment). **Cross-mode contrast verification per memory #11:** Silver/Gold ~3:1 borderline on cream (passes UI 3:1 + AA Large for tier-badge decorative role), ~6-7:1 PASS on dark navy; Platinum ~7:1 PASS in both modes (navy bg owns its contrast). Files updated (5 surfaces): [`carrier/dashboard/page.tsx`](frontend/src/app/carrier/dashboard/page.tsx) `TIER_COLORS`, [`accounting/quick-pay/page.tsx`](frontend/src/app/accounting/quick-pay/page.tsx) `CARAVAN_TIER_BADGE`, [`accounting/payments/page.tsx`](frontend/src/app/accounting/payments/page.tsx) `CARAVAN_TIER_BADGE`, [`shared/css/pages/carriers.css`](frontend/public/shared/css/pages/carriers.css) `.tier-card.{silver,gold,platinum}` (border + icon + svg) — also dropped 5 dead `.tier-card.bronze` rules (BRONZE tier retired pre-v3.7.a per CLAUDE.md §11), [`public/carriers.html`](frontend/public/carriers.html) inline tier headers (lines 240-241) + bulk swap `rgba(200,150,62,X)` → `rgba(197,165,114,X)` (33 occurrences across comparison table for visual coherence with new tier headers). **Per §3.1 sequence-continuous rule:** v3.8.aaa → v3.8.aab. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **Net LOC change:** ~240 (mostly removal — 5 theme blocks + ThemePanel 6-theme grid + bronze CSS rules deleted; tier palette swaps are 1-to-1 token replacements). **Public-facing impact:** theme picker disappears for every authenticated user with Sidebar; tier badges across AE Console + carrier portal surfaces show canonical palette; marketing `/carriers` tier table shows canonical palette matching authenticated surfaces — eliminates the off-brand visual disconnect between marketing and product. | v3.8.aab

- 2026-05-06 | Sprint 23 (v3.8.aaa) — Quick Pay tier reconciliation. AE Accounting Console Quick Pay Queue + Carrier Payments UI updated from legacy 5-tier `PaymentTier` display (Flash/Express/Priority/Partner/Elite) to canonical Caravan Partner Program 3-tier structure (Silver/Gold/Platinum) per memory #7 + CLAUDE.md §8 + [`frontend/public/carriers.html:217-649`](frontend/public/carriers.html#L217). Resolves operational risk surfaced 2026-05-04: AE Accounting Console showed different tier nomenclature than carrier-facing silkroutelogistics.ai/carriers, creating cognitive load + error risk during BKN first load processing (mid-May). **STRUCTURAL MISMATCH (not a rename):** legacy `PaymentTier` enum at [`schema.prisma:171`](backend/prisma/schema.prisma#L171) (FLASH/EXPRESS/PRIORITY/PARTNER/ELITE/STANDARD) encoded payment SPEED only. Canonical `CarrierTier` enum at [`schema.prisma:66`](backend/prisma/schema.prisma#L66) (SILVER/GOLD/PLATINUM/GUEST/NONE) encodes carrier loyalty TIER × payment SPEED orthogonally: Silver Net-30/3% 7-day/5% same-day, Gold Net-21/2%/4%, Platinum Net-14/1%/3%. **PATH 2 SCOPE** per directive Phase A decision #1: UI displays canonical `CarrierTier` badge from `CarrierProfile.tier` + speed-bucket label derived from legacy `PaymentTier` + actual fee% from `quickPayFeePercent` field. Visible truth on UI, honors legacy data. Single atomic commit, ~95 LOC. Path 1 (UI cosmetic translation only) rejected as too cosmetic. **Path 3 (full data model refactor splitting `paymentTier` into `paymentSpeed` + `tierAtTimeOfPay` + Prisma migration + DB backfill) explicitly OUT OF SCOPE — backlog as separate sprint candidate when SLA-policy review happens.** Backend (1 file, ~10 LOC): [`accountingController.ts:643`](backend/src/controllers/accountingController.ts#L643) `getPayments` + [`:1245`](backend/src/controllers/accountingController.ts#L1245) `getPaymentQueue` extend `carrier.select` to include `carrierProfile: { select: { tier: true } }` so UI renders Silver/Gold/Platinum badge from authoritative `CarrierProfile.tier` source. **Backend SLA hours UNCHANGED** per directive decision #4: FLASH=2h, EXPRESS=24h, PRIORITY=48h, PARTNER=72h, ELITE=120h, STANDARD=168h preserved as operational deadline encoding — UI relabels speed bucket only, SLA hour reconciliation requires SLA-policy review (separate sprint). Frontend (2 files, ~85 LOC): [`accounting/quick-pay/page.tsx`](frontend/src/app/accounting/quick-pay/page.tsx) replaced `TIER_INFO` with `CARAVAN_TIER_BADGE` (Silver/Gold/Platinum/Guest/None palette matching [`carrier/dashboard/page.tsx:23-27`](frontend/src/app/carrier/dashboard/page.tsx#L23) per directive decision #7) + `SPEED_LABEL` (Same-Day / 7-Day Quick Pay / Standard derived from legacy `PaymentTier`) + `CARAVAN_RATE_MATRIX` constant for the legend bar; replaced "Payment Tiers:" legend with full 3-tier × 3-speed rate matrix (canonical source of truth for AE staff resolving carrier disputes); added "Speed" column; Tier column now shows canonical `CarrierTier` badge; Fee column shows actual fee% alongside dollar amount; `feePercentLabel()` helper for fallback when `quickPayFeePercent` null on legacy records. [`accounting/payments/page.tsx`](frontend/src/app/accounting/payments/page.tsx) same `CARAVAN_TIER_BADGE` + `SPEED_LABEL` constants; "Speed" column added; Tier column shows canonical badge. STANDARD tier handling per directive decision #3: graceful fallback to "Standard" speed + carrier's canonical tier badge. **v3.7.a tierMap pattern reuse:** the existing translation at [`accountingController.ts:3911-3918`](backend/src/controllers/accountingController.ts#L3911) (FLASH/EXPRESS→SILVER, PRIORITY/PARTNER→GOLD, ELITE→PLATINUM) was added v3.7.a for analytics aggregation; Sprint 23 extends the same translation philosophy to UI display layer — show canonical truth without rewriting the legacy enum data. **No DB writes** per directive decision #5 — pure UI + API-include refactor, safe regardless of existing record state. M-milestone tier promotion logic untouched per directive decision #6. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. Per §3.1 sequence-continuous rule: v3.8.zz → v3.8.aaa (first double-letter post-zz overflow per §3.1 "never roll the minor at z"). | v3.8.aaa

- 2026-05-05 | Sprint 22 (v3.8.zz) — Surfaced visual bugs from morning walkthrough. Three fixes in one atomic commit. **(1) /accounting/invoices page wrapper bg cross-mode rendering correctness.** [`accounting/invoices/page.tsx:194`](frontend/src/app/accounting/invoices/page.tsx#L194) page wrapper AND [`:278`](frontend/src/app/accounting/invoices/page.tsx#L278) sticky thead used non-canonical `bg-[#0a0e1a]` literal hex (slightly darker than canonical `#0F1117`). The non-canonical hex was NOT in [`globals.css:124-130`](frontend/src/app/globals.css#L124-L130) `[data-mode="light"]` override list (which only covers `#0F1117`/`#161921`/`#080C18`/`#1C1F2B` etc.) — but `text-white`/`text-slate-{300,400,500}` ARE in the override list at [`:162-173`](frontend/src/app/globals.css#L162-L173). Net: in light mode, page bg stayed dark navy while text remapped to dark, producing dark-on-dark invisibility. Same architectural pattern as Sprint 14 dashboard layout posture fix — non-canonical bg literals break cross-mode cascade. **Fix:** swap both `bg-[#0a0e1a]` → `bg-[#0F1117]` (canonical, in override list). Mode toggle works for free via existing globals.css cascade. **Single-file scope confirmed** — pattern check across `/accounting/{aging,credit,disputes,fund,payments,pnl,quick-pay,quickpay-revenue,reports}` confirmed all use canonical `bg-[#0F1117]` already. Bug isolated to `invoices/page.tsx` only (NOT a shared-shell issue). **(2) CommandPalette.tsx Cmd+K trigger button light-mode invisibility.** [`components/ui/CommandPalette.tsx:224`](frontend/src/components/ui/CommandPalette.tsx#L224) used `text-white/50` + `hover:text-white/70` which have NO `[data-mode="light"]` override (only `text-white` is covered at globals.css:162). In light mode, `bg-white/5` correctly remapped to white card surface but `text-white/50` stayed at `rgba(255,255,255,0.5)` = invisible on white. **Fix:** `text-white/50` → `text-slate-400`, `hover:text-white/70` → `hover:text-white` (both have light-mode overrides in globals.css cascade). **(3) HTML entity rendering bug** ("Gail &amp; Rice" customer name displayed literally on /accounting/invoices). **Root cause:** pre-v3.8.d.2 `sanitizeInput` middleware HTML-escaped string DB values (CLAUDE.md §11). v3.8.d.2 fixed the middleware AND ran a one-time decode for **loads table** ([`backend/scripts/decode-encoded-load-fields.ts`](backend/scripts/decode-encoded-load-fields.ts)) but **customer table was NOT in that script's scope**. React JSX renders strings as text nodes without entity decoding, so legacy escaped values print literally. **Fix (Sprint 22 scope = F1 frontend defensive):** new shared util [`frontend/src/lib/htmlEntities.ts`](frontend/src/lib/htmlEntities.ts) mirrors the existing backend util at [`backend/src/utils/htmlEntities.ts`](backend/src/utils/htmlEntities.ts). Apply `decodeHtmlEntities()` in `customerName()` helper at invoices/page.tsx:57-67. **Backend serializer-layer decode (F2)** and **customer-table DB cleanup migration (F3)** deferred to separate sprints with explicit DB-migration authorization. F1 closes the surfaced bug now and handles future legacy data automatically without DB migration. **Memory #11 lesson reinforced for the 5th time today:** scanner-driven sweeps optimize for density-per-file and miss high-visibility low-density bugs (page headings, single buttons, single data fields). Human-eye walkthrough is primary truth-test. **Version letter note:** continuous sequence per §3.1 — picked up from `.yy` (parallel work landed v3.8.nn–.yy between Sprint 21 close and Sprint 22 start) → next available is `.zz`. Past `zz` uses double-letters (`.aaa`, `.aab`, …) per §3.1. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. 5 files touched: `lib/htmlEntities.ts` (new, 38 LOC), `accounting/invoices/page.tsx` (3 edits), `components/ui/CommandPalette.tsx` (1 edit), `components/ui/VersionFooter.tsx` (bump + comment), `docs/regression-log.md`. | v3.8.zz

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v9: cross-component layout resolution per Sprint 21 directive. **74 P0 retirement (395 → 321)** by closing the documented FP class from Sprints 18+19+20 audits where shared React components in `frontend/src/components/` render correctly inside dashboard/accounting/admin layout wrappers but v8's `layoutBgContext` only fires for files in `frontend/src/app/` route directories — when `pass3Tailwind` processes a component file, `findLayoutBgForPage` returned null, `findAncestorBg` fell back to `#FFFFFF` default, and reported white-on-white for `text-white` on `bg-white/5` overlays that actually render against dashboard dark navy at runtime. **v9 introduces:** `resolveImportPath(specifier, fromFile)` handling TypeScript path aliases (`@/X` → `frontend/src/X`) and relative imports, trying `.tsx`/`.ts`/`/index.tsx`/`/index.ts` suffixes; `parseComponentImports(file)` regex-matching `(?:from|import)\s*\(?\s*['"]([^'"]+)['"]\s*\)?` (catches static, dynamic, require), filtering to imports resolving into `frontend/src/components/`, following barrel re-exports; `resolveBarrelReExports(indexFile)` parsing `export { Name } from "./Path"` AND `export * from "./X"` (per Phase A confirmation #3 — both named and namespace re-exports); `buildComponentContextMap(tsxFiles)` walking app/ files at scanner startup, building initial `Map<componentAbsPath, Set<appFilePath>>` from app-route imports, then transitively propagating through component-to-component imports (BFS, depth cap `MAX_COMPONENT_GRAPH_DEPTH = 5` per Phase A #4), final conversion from app-file paths → route-prefix arrays via existing `deriveRoutePrefix`-style logic; `findLayoutBgForPage` extended with `componentContextMap` parameter, multi-context first-match-wins per directive A2 confirmation, tracks `multiContextCount`. **v9 simplifications per Phase A:** string-literal import paths only, transitive depth cap = 5, first-match-wins for multi-context, no JSX-prop-passed component refs (v10), no render-prop pattern resolution (v10), no server vs client component disambiguation (v10). **Counters surfaced in summary:** `componentContextMapSize` (49 components resolved), `componentContextOrphanCount` (6 orphan components — no app consumer found, fall through to v8 default), `tailwindComponentContextFiringCount` (39 firings during pass3Tailwind — shared component file resolved via component-context map rather than #FFFFFF default), `tailwindComponentMultiContextCount` (28 multi-context applications — components imported by multiple route trees). **Re-run delta vs Sprint 20 v8 baseline:** total findings 14,237 → 14,220 (−17), P0 **395 → 321** (−74), P1 12,752 → 12,809 (+57 — borderline FP cases moved P0 → P1 after correct layout context applied), P2 unchanged at 1,090. **39 component-context firings retired ~74 P0 directly** plus side-effects through ancestor resolution. **Brand-mechanical scanner workstream COMPLETE after v9.** Remaining v10+ candidates listed in scanner header comment are narrow extensions for specific edge cases (CSS-in-JS, render-props, server/client disambiguation, etc.) — not blocking for normal repo audit. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Sprint 20 (v3.8.mm) — Long-tail dashboard residue per-finding triage. **46 swaps applied across 27 files** via Sprint 19 perl swap-applier with v2 window-search refinement. Smaller scope than Sprint 19 — Sprint 19 already retired the high-density rule-matching findings; Sprint 20 residue is OUTLIER + FP. Honest scope. **Methodology refinement (v2 swap-applier):** scanner v8 reports JSX opening-tag line, but className token often appears 2-5 lines later inside multi-line JSX. v1 applier searched only the exact line and missed many swaps; v2 searches `+/-5` line window around scanner-reported line. 46 vs ~4 applied with same swap proposal set. v2 applier saved at [`/tmp/apply_swaps_v2.pl`](../../tmp/apply_swaps_v2.pl) (will need to be re-derived in future sprints). **Files touched** (27 with swaps applied): `dashboard/contract-rates/page.tsx` (5), `dashboard/exception-config/page.tsx` (4), `dashboard/carriers/page.tsx` (4), shipper portal `settings`/`invoices` + carrier portal `my-loads`/`settings` (3 each), `dashboard/fuel-tables` + `dock-scheduling` (3 each), `dashboard/scorecard` + `fleet` (2 each), long tail of 1-swap files. **Onboarding 6 swap proposals SKIPPED** — 5 confirmed bg-navy custom utility scanner FPs documented in Sprint 15b (element parent has `bg-navy` Tailwind plugin class scanner doesn't recognize, real rendering is text-white on dark navy nav PASS). Line 501 amber finding may have shifted from Sprint 18 fix; not re-applied to avoid double-correction. **Cream-context gold preventive repair: SKIPPED** per Phase A confirmation (Sprint 19 already covered). **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **Net P0 retirement vs 444 baseline:** ~40-46 expected. **Sprint 20 closes the diminishing-returns mechanical sweep tail.** Brand-mechanical workstream functionally complete after this commit. **Sprint 21+ direction options:** (1) Scanner v9 enhancement to resolve ~95 shared-component FPs (cross-file React component composition — already documented in v7/v8 deferred candidate list); (2) CSS residue per-finding (~140 remaining post-Sprint-17); (3) OUTLIER per-finding triage (~88 visual-judgment cases — decorative chevrons, conditional className branches); (4) Narrative work (`/shippers` vertical positioning rewrite). | v3.8.mm

- 2026-05-04 | Sprint 19 (v3.8.ll) — Per-finding dashboard + portal residue triage. **162 swaps applied across 71 files** using the Sprint 15b/16/18 perl swap-applier with line-targeted edits driven by scanner v8 `file:line:bg:token` metadata. **Major surprise:** [`dashboard/carriers/page.tsx`](frontend/src/app/dashboard/carriers/page.tsx) (55 P0, 40 swaps applied) was MISSED in Sprint 15b's top-11 cutoff AND explicitly excluded from Sprint 16 portal scope (it's an AE Console carrier-management page, not the carrier portal). Single-largest residue file in the codebase. Lesson logged: top-N file filtering misses long-tail files that individually count but cumulatively matter — applies to future sweep planning. **Other notable files swept** (smaller counts): `dashboard/dock-scheduling/page.tsx` (11), `dashboard/exception-config/page.tsx` (10), `dashboard/lead-hunter/tabs/ProfileTab.tsx` (10), `dashboard/fleet/page.tsx` (7), `dashboard/claims/page.tsx` (6), `dashboard/contract-rates/page.tsx` (5), `dashboard/track-trace/tabs/ExceptionsTab.tsx` (5), `dashboard/waterfall/tabs/TendersTab.tsx` (5), long tail of 60+ files with 1-4 P0 each. **Shape mapping same as Sprint 15b/16:** A (text-{slate\|gray}-{500-900} on dark → text-slate-400 — Sprint 14 collateral retired in files Sprint 15b's top-11 didn't cover); B (text-white on light card → text-[#0A2540]); C (text-{slate\|gray}-{300-500} on light → -700); D (text-{color}-{400\|500} status badge on light → -700); E (text-white/N translucent on dark → text-slate-400); gold-residual (text-[#C5A572] on bg-[#FAEEDA] cream → text-[#BA7517] gold-dark, applied preventively across all in-scope files). **Sprint 19 OUTLIERS deferred** (~50-60 across): text-gray-300/text-slate-300 decorative icons where -500/-700 swap creates wrong visual weight (chevrons/dividers); conditional className branches (cn(isActive ? ... : ...) patterns); text-white/N translucent in "barely visible placeholder" contexts; 4 text-[#0F1117]-on-gradient-bg sidebar avatar findings (Sprint 16 already documented as v9 scanner FPs). These residue stay for Sprint 21 per-finding work or v9 scanner enhancement. **Methodology validation:** applying Sprint 15b/16 perl methodology to Sprint 19's broader scope confirmed the rule-set is robust. No new over-correction classes surfaced (gold-on-cream repair applied PREVENTIVELY this time before scanner re-run, eliminating the Sprint 15a-style mid-Sprint catch). **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **Net P0 retirement vs 600 baseline:** ~150-170 expected (162 swaps − ~10 cases where scanner trail had multiple findings on same line and only one matched). **Post-Sprint-19 remaining ~430-450 P0 partition:** ~140 CSS residue (Sprint 21), ~65 shared-component v8 scanner FPs (v9 scanner), ~50-60 OUTLIERS deferred this sprint, ~150-180 long-tail single-occurrence findings in files this sprint didn't reach (docs surfaces, edge-case form modals, etc.). **Sprint 19 substantially closes the structured-sweep workstream. Sprints 20+ are per-finding triage + scanner v9 + narrative work.** | v3.8.ll

- 2026-05-04 | Sprint 18 (v3.8.kk) — Small-scope auth/error/track P0 sweep. **10 genuine fixes applied across 9 files** — far below directive's ~220 projection because Sprint 14 Phase C analysis (which produced the ~220 estimate) was stale by the time Sprints 15b/16/17 retired most of the projected dashboard/portal/CSS findings. Honest realistic scope after filtering: ~80 P0 outside dashboard/portal/CSS — of which **~65 are scanner v8 FALSE POSITIVES due to cross-component layout context limitation.** Files fixed (10 swaps): [`auth/login/page.tsx:387`](frontend/src/app/auth/login/page.tsx#L387) TOTP secret display `text-[#C9A84C]` → `text-[#BA7517]` canonical gold-dark; [`auth/error.tsx:16`](frontend/src/app/auth/error.tsx#L16) + [`onboarding/error.tsx:16`](frontend/src/app/onboarding/error.tsx#L16) error icons `text-red-400` → `text-red-700` (composited red tint Shape D); [`app/error.tsx:23`](frontend/src/app/error.tsx#L23) root error ID `text-white/30` → `text-slate-400` (Shape E translucent); [`onboarding/page.tsx:501`](frontend/src/app/onboarding/page.tsx#L501) Sprint 15b residue `text-amber-500` → `text-amber-700`; [`track/page.tsx:176`](frontend/src/app/track/page.tsx#L176) public-track icon `text-white/30` → `text-slate-400`; [`components/auth/LoginSplash.tsx:189`](frontend/src/components/auth/LoginSplash.tsx#L189) feature icon `text-[#d4a574]` non-canonical warm gold → `text-[#BA7517]` canonical; [`components/invoices/CreateInvoiceModal.tsx:88`](frontend/src/components/invoices/CreateInvoiceModal.tsx#L88) + [`components/contacts/ContactsPanel.tsx:250`](frontend/src/components/contacts/ContactsPanel.tsx#L250) close-X icons `text-gray-400` → `text-gray-700`; [`components/invoices/InvoiceLineItemsEditor.tsx:96`](frontend/src/components/invoices/InvoiceLineItemsEditor.tsx#L96) hover-delete `hover:text-red-400` → `-700`. **Scanner false positives DOCUMENTED EXPLICITLY (~65 across):** [`components/layout/Sidebar.tsx`](frontend/src/components/layout/Sidebar.tsx) (8 P0 — renders inside `app/dashboard/layout.tsx` `bg-[#0F1117]` dark navy), [`components/orders/LineItemsSection.tsx`](frontend/src/components/orders/LineItemsSection.tsx) (13 — Order Builder dashboard), [`components/loads/RateConfirmationModal.tsx`](frontend/src/components/loads/RateConfirmationModal.tsx) (11), [`components/loads/CreateLoadModal.tsx`](frontend/src/components/loads/CreateLoadModal.tsx) (11), [`components/invoices/BatchActionsBar.tsx`](frontend/src/components/invoices/BatchActionsBar.tsx) (5), [`components/ui/CommandPalette.tsx`](frontend/src/components/ui/CommandPalette.tsx) (4), [`components/ui/StatCard.tsx`](frontend/src/components/ui/StatCard.tsx) (3), [`components/ui/FormElements.tsx`](frontend/src/components/ui/FormElements.tsx) (3), [`components/ui/ClickToCall.tsx`](frontend/src/components/ui/ClickToCall.tsx) (2), [`components/MarcoPolo.tsx`](frontend/src/components/MarcoPolo.tsx) (2), [`components/auth/LoginBrandPanel.tsx`](frontend/src/components/auth/LoginBrandPanel.tsx) (1), [`components/ui/ThemePanel.tsx`](frontend/src/components/ui/ThemePanel.tsx) (1), and others. **Root cause:** scanner v8 layout-fallback resolution (Sprint 13 v8) only fires for files in `app/` route directories; components in `frontend/src/components/` render in dashboard dark contexts at runtime but scanner can't resolve that statically because the import graph isn't traversed. Real rendering of e.g. `<Sidebar>` inside `<DashboardLayout bg-[#0F1117]>` is `text-slate-400` on dark = ~5:1 PASS, `hover:text-white` on dark = high-contrast. Force-fixing these to `text-slate-700` would create dark-on-dark readability bugs in real dashboard rendering — rejected as net-negative per Sprint 17 lesson reinforced. **v9 scanner candidate (already in v7/v8 deferred list as "cross-file React component composition"):** extend `findAncestorBg` to walk import graph and propagate layout context through component boundaries, OR use Next.js app-router conventions to associate components with their consuming routes. Until v9, these 65 component-level findings remain as documented scanner FPs and must NOT be force-fixed. **Honest scope acknowledgment:** Sprint 18 ships small (10 fixes vs projected 150-180) because correct work beats padded retirement count. Same atomic-commit discipline as Sprint 15a (which also shipped small after over-correction risk surfaced). **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **Net P0 retirement vs 609 baseline:** ~10-12 expected (10 swaps − ~0 false-positive overlap + maybe 1-2 cascade reclassifications). **Remaining 595-600 P0 partition:** ~140 CSS residue (Sprint 21), ~210 single-occurrence dashboard residue + outliers (Sprint 19), ~65 documented shared-component scanner FPs (v9 scanner work, not bulk-fixable), ~39 portal residue (Sprint 19), ~125 long-tail single-instance findings across miscellaneous files. **Sprint 18 closes the brand-mechanical-sweep workstream.** Remaining residue is per-finding triage (Sprint 19) and v9 scanner enhancement (component layout resolution). Sprints 18+ entering narrative work — `/shippers` vertical positioning rewrite per next directive. | v3.8.kk

- 2026-05-04 | Sprint 17 (v3.8.jj) — Final mechanical CSS-side P0 sweep. **76 P0 retirement (685 → 609)** across 5 cluster shapes mirroring v3.8.t/u/y methodology applied to remaining CSS files. **Var-level fixes:** `--gold-dark: #B8862E → #BA7517` (canonical gold-dark per CLAUDE.md §2.1 LEGACY-ALLOWED, balanced ~5:1 on cream + ~3.3:1 on dark navy) at console.css/utilities.css/3 marketing CSS files; added `--green-dark: #15803D`, `--amber-dark: #92400E`, `--red-dark: #991B1B` to console.css `:root`. **Note: initial attempt at `#854F0B` (~9:1 on cream)** created new P0s on dark navy (~1.7:1 dark-on-dark); caught mid-Sprint via scanner re-run, reverted to balanced `#BA7517`. **Targeted bulk seds:** (a) 6 status-badge selectors in `compliance-carrier.css` (`.status-badge-{green\|amber\|red}` + `.timeline-badge-{passed\|flagged\|failed}`) swapped `color: var(--{color}) → var(--{color}-dark)`; (b) `communications.css` 4 badges (`.tier-GOLD`, `.stage-Active`, `.icon-email-out`, `.comm-type-label`) swapped `color: var(--gold) → var(--gold-dark)`; (c) marketing CSS files (about/blog/careers/faq/carriers/terms/privacy/security-policy/auth login+register) — literal `color: #C8963E → #854F0B` (legacy dark-gold per §2.1, ~9:1 on white PASS — note: this was BEFORE the var-level revert from #854F0B, applied only to literal hex which has no dark-bg context risk because marketing pages render on cream/white only). **CSS-E perl bulk:** all CSS files matched `background: #C8963E[^}]*color: #FFFFFF\|var(--white)` → navy text on gold (per v3.8.t Cluster B fix extended to remaining buttons across `.btn-send`/`.btn-new-convo`/`.contact-item.active`/etc.). **Chrome rule explicit-color additions:** `.footer`, `.navbar`, `.nav`, `.navbar.scrolled`, `.mobile-overlay`, `.mobile-menu`, `.trust-bar` selectors in 11 marketing CSS files received explicit `color: var(--fg-on-navy)` declarations via perl rule-block-aware insert where previously cascade-inheriting body color on dark bg → invisible navy-on-navy. Sprint 3 v3.8.t fixed this in `utilities.css` for chrome HTML structure; Sprint 17 fixes it at page-CSS level for belt-and-suspenders + reduced surface for future cascade regressions. **Marketing CSS perl rule-block aware sweep:** `color: var(--gray-400) → var(--gray-600)` on rules with light bg (`#F1F5F9` / `#F8FAFC` / `#FFFFFF` / `var(--white)` / `var(--gray-{50,100,200})`) — closes secondary-text legibility on `.comparison-table th`, `.tier-NONE`, `.status-DISMISSED`, `.progress-step-num`, `.lh-table th` and similar. **Remaining ~140 CSS-side P0** after Sprint 17 partition: ~12 `var(--gold)` badge residuals on cream (need per-selector swap to `var(--gold-dark)` outside `communications.css` which was already done), ~8-12 cascade-inherit dark navy on non-chrome selectors (`.timeline-step`, `.tier-card::before` etc. — per-selector add `color: var(--fg-on-navy)`), ~5 `#C8963E` text-on-white in marketing pages where `.btn-primary` swap didn't catch, ~4 bg-gold + text-white residuals, ~2 white-on-translucent-white overlay patterns, long tail of single-occurrence outliers. These are diminishing-returns per-finding work for Sprint 21 (post-Sprint-18 marketing Tailwind sweep + narrative work). **Methodology:** mostly perl-based bulk where context allows (rule-block-aware patterns prevent cross-context over-correction); var-level changes for tokens used consistently. Caught one over-correction class mid-Sprint (`#854F0B` on dark navy) via incremental scanner re-runs — confirms perl rule-block-awareness is necessary for CSS bulk sweeps just like Sprint 15a's gold-on-cream lesson confirmed it for Tailwind. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. **Public-facing impact:** AE Console accounting/communications/compliance pages, marketing pages (about/blog/careers/faq/carriers/contact/etc.), and chrome elements (header/footer/navbar/mobile-menu) now render with brand-conformant readable contrast at the CSS layer. **Remaining 609 P0 partition:** ~140 CSS residue (Sprint 21), ~220 marketing/auth/onboarding Tailwind (Sprint 18), ~210 single-occurrence dashboard residue + outliers (Sprint 19), ~39 portal residue (Sprint 19). | v3.8.jj

- 2026-05-04 | Sprint 16 (v3.8.ii) — Shipper/carrier portal Tailwind P0 sweep via per-file context-sensitive edits (Sprint 15b methodology) extended to portal scope. **332 swaps applied across 29 portal files**; 38 single-occurrence outliers + scanner false positives skipped. Portal context: layouts use `bg-[#F7F8FA]` light gray intentionally per Sprint 13 audit (shipper portal serves procurement contacts; carrier portal serves Caravan Partner Program tier display + daily dispatch). Fixes target text-color choices that work against light bg — NOT a layout posture flip like Sprint 14 did for AE Console. **Top files (32 with findings):** [`carrier/dashboard/page.tsx`](frontend/src/app/carrier/dashboard/page.tsx) (36 swaps — Caravan Partner Program tier-overview surface), [`shipper/dashboard/tracking/page.tsx`](frontend/src/app/shipper/dashboard/tracking/page.tsx) (38), [`carrier/dashboard/settings/page.tsx`](frontend/src/app/carrier/dashboard/settings/page.tsx) (25), [`carrier/dashboard/compliance/page.tsx`](frontend/src/app/carrier/dashboard/compliance/page.tsx) (24), [`carrier/dashboard/available-loads/page.tsx`](frontend/src/app/carrier/dashboard/available-loads/page.tsx) (21), [`carrier/dashboard/scorecard/page.tsx`](frontend/src/app/carrier/dashboard/scorecard/page.tsx) (21), [`carrier/dashboard/payments/page.tsx`](frontend/src/app/carrier/dashboard/payments/page.tsx) (21), [`carrier/dashboard/my-loads/page.tsx`](frontend/src/app/carrier/dashboard/my-loads/page.tsx) (18), [`carrier/dashboard/documents/page.tsx`](frontend/src/app/carrier/dashboard/documents/page.tsx) (17), [`carrier/dashboard/messaging/page.tsx`](frontend/src/app/carrier/dashboard/messaging/page.tsx) (12), 22 more portal files with smaller counts. **Fix shapes applied:** P1 (`text-{slate\|gray}-{400\|500}` on light → `text-{slate\|gray}-700`, ~216 retired — dominant secondary-text legibility cluster); P2 (`text-[#C9A84C]` non-canonical gold on light → `text-[#BA7517]` gold-dark, ~76 retired — continues Sprint 15a gold canonicalization to portal scope which the earlier `[#C9A84C] → [#C5A572]` sed didn't reach because portal scope wasn't included in Sprint 15a's bulk; aligns with §2.1 token discipline); P3 (`text-white` residual on light card → `text-[#0A2540]` canonical SRL navy, ~12); P4 (`text-{color}-{400\|500}` status badge on light → `text-{color}-700`, ~30 across emerald/amber/red/green/blue/yellow/indigo); P5 (`text-{slate\|gray}-{200\|300}` light-on-light decorative → `text-{slate\|gray}-{500\|700}` per role — chevrons/icons get `-500` to preserve subtle-indicator role, label/body text gets `-700` default); E (`text-white/N` translucent on light → `text-slate-500`, 1 instance). **Caravan Partner Program tier badges PRESERVED** at [`carrier/dashboard/page.tsx:24-26`](frontend/src/app/carrier/dashboard/page.tsx#L24-L26) `TIER_COLORS` (Silver/Gold/Platinum) — uses `text-slate-600`, `text-yellow-700`, `text-purple-700` which are not in swap rules (shade 600/700 on light is already PASS). Note: current tier visual palette (yellow/purple Tailwind tokens) doesn't match brand-skill canonical (SRL gold `#C5A572` for Gold; navy+gold for Platinum) — tier visual palette reconciliation is brand-skill scope separate from contrast scope. Sprint 16 fixes contrast only. **Quick Pay tier name reconciliation** (memory #26 canonical Silver/Gold/Platinum) explicitly OUT of Sprint 16 scope per directive. Sprint 16 fixes contrast on whatever tier names exist today. **Methodology:** scanner v8 produces `(file, line, bg, token)` tuples via existing `JsxStackEntry` walk + layout-fallback resolution; perl swap-applier (`/tmp/apply_swaps.pl`) reads tuples and applies word-boundary regex swap on the target line only. NO bulk-sed across files. Each swap is line-targeted with bg-context confirmed by scanner output. Same risk-mitigation pattern as Sprint 15b after Sprint 15a's gold-on-cream over-correction class proved bulk-sed unsafe for context-sensitive clusters. **Scanner false positives logged (4 skipped):** `text-[#0F1117]` inside dark sidebar/nav at [`shipper/register:65,95`](frontend/src/app/shipper/register/page.tsx#L65), [`CarrierSidebar:79`](frontend/src/components/carrier/CarrierSidebar.tsx#L79), [`ShipperSidebar:99`](frontend/src/components/shipper/ShipperSidebar.tsx#L99) — element parent has gradient (`bg-gradient-to-br from-[#C5A572]`) or non-Tailwind hex bg the scanner doesn't resolve, so walker lands on nav's `bg-[#0F1117]` and reports dark-on-dark. Real rendering is text-on-gold-avatar or similar correct contrast. Defer to Sprint 19 per-finding triage OR v9 scanner gradient bg recognition. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. Net retirement vs 1,016 P0 baseline: ~332 expected (Phase C confirms exact delta). **Public-facing impact:** shipper portal (procurement-facing tracking/messages/documents/quote/invoices/settings/analytics) and carrier-facing Caravan Partner Program + dispatch board (load board, my-loads, available-loads, payments, scorecard, compliance, settings) now render with brand-conformant readable contrast. **Remaining P0 after Sprint 16** projected ~676: ~216 CSS-side residue (Sprint 17), ~220 marketing/auth/onboarding-rest (Sprint 18), ~235 single-occurrence + edge cases + outliers across other surfaces (Sprint 19 per-finding triage). | v3.8.ii

- 2026-05-04 | Sprint 15b (v3.8.hh) — Per-file context-sensitive Tailwind P0 sweep across 11 dashboard files using per-line edits with bg-context awareness from scanner v8 `file:line:bg:token` metadata. Replaces unsafe bulk-sed methodology that proved over-correcting in Sprint 15a (gold-on-cream class caught + repaired mid-commit). **Files processed (11):** [`dashboard/loads/page.tsx`](frontend/src/app/dashboard/loads/page.tsx) (45 P0 baseline), [`onboarding/page.tsx`](frontend/src/app/onboarding/page.tsx) (35), [`dashboard/fuel-tables/page.tsx`](frontend/src/app/dashboard/fuel-tables/page.tsx) (33), [`dashboard/scorecard/page.tsx`](frontend/src/app/dashboard/scorecard/page.tsx) (25), [`dashboard/routing-guide/page.tsx`](frontend/src/app/dashboard/routing-guide/page.tsx) (25), [`dashboard/phone-console/page.tsx`](frontend/src/app/dashboard/phone-console/page.tsx) (24), [`dashboard/contract-rates/page.tsx`](frontend/src/app/dashboard/contract-rates/page.tsx) (19), [`dashboard/drivers/page.tsx`](frontend/src/app/dashboard/drivers/page.tsx) (18), [`dashboard/sops/page.tsx`](frontend/src/app/dashboard/sops/page.tsx) (17), [`dashboard/tagging-rules/page.tsx`](frontend/src/app/dashboard/tagging-rules/page.tsx) (16), [`dashboard/lead-hunter/page.tsx`](frontend/src/app/dashboard/lead-hunter/page.tsx) (9 — PURE DARK). **Fix shapes applied per-finding (~197 total swaps):** Shape A `text-{slate\|gray}-{500\|600\|700}` on dark → `text-slate-400` (Sprint 14 collateral retired); Shape B `text-white` on light card surface → `text-[#0A2540]` (canonical SRL navy); Shape C `text-{slate\|gray}-{300\|400\|500}` on light → `text-{slate\|gray}-700`; Shape D `text-{color}-{400\|500}` status badge on light → `text-{color}-700` (green/red/blue/amber/yellow); Shape E (NEW) `text-white/N` translucent on dark → `text-slate-400`; gold-residual `text-[#C5A572]` on light → `text-[#BA7517]` gold-dark (continues Sprint 15a cream-context repair pattern). **Methodology:** scanner v8 produces `(file, line, bg-context, token)` tuples via existing `JsxStackEntry` walk + layout-fallback resolution; perl swap-applier (`/tmp/apply_swaps.pl`) reads tuples and applies line-targeted edits with `(?<![\w\-])TOKEN(?![\w\-])` word-boundary regex on the target line only. NO bulk-sed across files. NO bulk-sed within file. Each swap is line-targeted with bg-context confirmed by scanner output. Bulk-sed rejected after Sprint 15a's gold-on-cream over-correction class proved the risk empirically — same Tailwind token (e.g. `text-gray-600`) is correct on light cards and wrong on dark dashboard wrappers within the same file, so any bulk approach over-corrects. **Application breakdown:** loads (41 sed line-targeted), onboarding (29 sed line-targeted), lead-hunter (12 bulk-in-pure-dark-file safe), fuel-tables (22 perl-applied), scorecard (18), routing-guide (21), phone-console (16), contract-rates (9), drivers (11), sops (9), tagging-rules (9). **Lead Hunter at 9 → 0 P0** — pure-dark file, all 8 `text-slate-600` + 1 `text-red-400/60` swapped. Closes the visual cluster the user surfaced via screenshots that triggered the entire Sprint 14/15a/15b sequence (modal dropdown contrast in light-mode runtime). **Scanner false positives logged:** onboarding `<nav>` at [lines 251, 362, 367, 370](frontend/src/app/onboarding/page.tsx#L251) uses `bg-navy` custom Tailwind utility (defined via Tailwind plugin or `extends.colors` config) that scanner v8's `TAILWIND_PALETTES` doesn't recognize. Findings resolved against next-up ancestor `bg-[#F8FAFC]` light instead of true dark navy. Real rendering is correct (text-white on dark navy nav). NOT fixed in Sprint 15b because changing those would break correct rendering. v9 candidate: extend `TAILWIND_PALETTES` to read `tailwind.config.ts` extends + plugin definitions for project-custom utilities. **Pre-commit verification:** backend `tsc --noEmit` clean, frontend `next build` clean. Net retirement vs 1,206 P0 baseline: ~190 P0 expected retirement (197 swaps − ~7 false positives in onboarding nav). Scanner Phase C confirms exact delta. **Remaining P0 after Sprint 15b** projected ~1,016: ~345 shipper/carrier portal (Sprint 16 scope, light gray `#F7F8FA` surfaces), ~216 CSS-side residue (Sprint 17), ~220 marketing/auth/onboarding-rest residue (Sprint 18), ~235 single-occurrence + edge cases across other dashboard files. | v3.8.hh

- 2026-05-04 | Sprint 15a (v3.8.gg) — Bulk Tailwind P0 sweep, **safe-pattern subset**. Drains the lowest-risk clusters from the 494 in-scope Tailwind real-readability backlog surfaced by Sprint 14 Phase C; ships the user-flagged Lead Hunter modal light-mode runtime bug as part of broader §2.1 token-discipline work. **Cluster S1** — `bg-[#1e293b]` (Tailwind slate-800, non-canonical) → `bg-[#0F1117]` (canonical SRL navy from tokens.md, in `globals.css` `[data-mode="light"]` override list). 7 instances across [`dashboard/lead-hunter/page.tsx`](frontend/src/app/dashboard/lead-hunter/page.tsx) (Add Prospect + Send Outreach modals — the screenshot-flagged bug), [`admin/users/page.tsx`](frontend/src/app/admin/users/page.tsx) (3 dropdown options + delete-confirm modal), [`onboarding/page.tsx`](frontend/src/app/onboarding/page.tsx) (facility-picker dropdown). Default-mode contrast was acceptable (text-white on dark slate-800 = ~14:1 PASS) but in light-mode runtime the existing override layer remapped `text-slate-400` to dark warm grey `var(--srl-text-secondary)` while the modal `bg-[#1e293b]` stayed dark (not in override list) → dark-on-dark contrast bug. After fix, light-mode override at [`globals.css:188`](frontend/src/app/globals.css#L188) remaps the new `bg-[#0F1117]` to cream and the labels remap correctly. **Cluster S2** — non-canonical gold `[#C9A84C]` → canonical `[#C5A572]` (`--gold` per CLAUDE.md §2.1) bulk sed across all `frontend/src/app/{dashboard,accounting,admin,onboarding}/**/*.tsx` files; ~200 occurrences (mix of `bg-` and `text-` and `border-` and arbitrary opacity variants like `/10`, `/20`). Mostly token-discipline work outside scanner P0 awareness — only 14 of these were in scanner P0 — but advances brand-skill conformance broadly. **Cluster S7-legacy** — `text-[#854F0B]` (legacy dark-gold from non-canonical palette) → `text-[#C5A572]` (canonical gold), 19 instances across CRM tabs / orders / track-trace components where the token was used as accent on dashboard dark navy bg. **Over-correction repair** discovered mid-execution: the bulk S2 sed inadvertently flipped `text-[#854F0B]` (paired correctly with `bg-[#FAEEDA]` cream-tint badges, ~9:1 contrast) to `text-[#C5A572]` which on `#FAEEDA` cream gives only 2.13:1 — a NEW P0 bug class. Repair applied via perl regex within ~120-char window of `bg-[#FAEEDA]`: `text-[#C5A572]` → `text-[#BA7517]` (canonical gold-dark, ~5:1 on cream). Caught at 15+ spots across CRM, orders, track-trace, waterfall, and other badge surfaces. **Sprint 15a deliberately DEFERS** the larger context-sensitive clusters: S3 (98 `text-slate-400`/`text-gray-400` on light surfaces), S4 (112 `text-white` on light surfaces — `bg-[#FFFFFF]`/`#F3F4F6`/`#F9FAFB`/`#F8FAFC`), S5 (23 status badges `text-{color}-400/-500` on light), S7-main (187 DARK-on-DARK collateral from Sprint 14 — `text-gray-600`/`text-slate-600`/`text-gray-700`/`text-gray-900` on dashboard's now-correctly-dark layout). These are NOT bulk-sed safe because the same token (e.g. `text-gray-600`) is correct on light cards and wrong on dark dashboard wrappers within the same file; per-line edits driven by scanner output file:line metadata are required. The Sprint 15a over-correction class (gold-on-cream from S2 bulk sed) confirmed the risk empirically — bulk sed on context-sensitive tokens MUST be paired with a context-aware repair pass. Sprint 15b queued to do the per-line work for top files (loads/page.tsx 45, fuel-tables/page.tsx 33, onboarding/page.tsx 35, scorecard 25, routing-guide 25, contract-rates 19, etc.) representing ~250 of the 420 deferred findings. Pre-commit verification passed: backend `tsc --noEmit` clean, frontend `next build` clean (40 files changed, 254 insertions / 254 deletions = pure surgical color swaps, no layout/JSX changes per v3.8.t/u/y/dd methodology). Net Tailwind P0 retirement this commit: ~33 (S1 7 + S2 14 + S7-legacy 19) per scanner Phase C re-run. ~200 §2.1 token-discipline improvements not directly scanner-visible but advance brand conformance for future audits and future contributors. Files unchanged: shipper/carrier portals (Sprint 19 scope), marketing pages, auth pages, root layout, all CSS files. | v3.8.gg
- 2026-05-04 | Sprint 14 (v3.8.ff) — AE Console dashboard layout posture fix: hydration-flash on default-mode rendering. Three AE Console layouts (`app/dashboard/layout.tsx`, `app/accounting/layout.tsx`, `app/admin/layout.tsx`) had their outermost wrappers using inline `style={{ background: 'var(--srl-bg-base)', color: 'var(--srl-text)' }}` which resolves to `#F5F3EF` light cream + `#1A1714` dark text in default-mode rendering (per `:root, [data-mode="light"]` in [`globals.css:11-40`](frontend/src/app/globals.css#L11-L40)). But the AE Console UI is built with `text-white`, `text-slate-400` secondary text, and dark hex Tailwind backgrounds (`bg-[#0F1117]`, `bg-[#161921]`) — all assuming a dark ancestor. Real runtime rendering AFTER `useTheme.loadFromStorage()` hydrates and sets `data-mode="light"` on `<html>` was correct because of the extensive `[data-mode="light"]` override layer in [`globals.css:103-300`](frontend/src/app/globals.css#L103-L300) that remaps `text-white` → `var(--srl-text)` (line 162) and dark hex bgs → `var(--srl-bg-base)` (line 188). However during SSR + initial paint (before client hydration), no `data-mode` attribute is set on `<html>` so the override layer doesn't fire — net flash-of-broken-content (FOBC) where text-white sits on cream. Sprint 13 v8 scanner surfaced 181 text-white-on-cream pairs + 165 collateral slate-400/gray-400 findings from this exact pattern. **Architectural fix: replace inline style with Tailwind classNames `bg-[#0F1117] text-white`** on the outermost wrapper of each layout. During SSR/initial paint, the literal dark hex renders directly, giving text-white the high-contrast ancestor it expects → no FOBC. After hydration with `data-mode="light"` set, the existing `[data-mode="light"] .bg-[#0F1117]` override at globals.css:188 remaps to `var(--srl-bg-base)` cream and `[data-mode="light"] .text-white` at line 162 remaps to dark text — mode toggle continues to work with zero new globals.css edits. The v8 scanner findings were partial false positives in steady-state rendering, but the FOBC bug they pointed at was real. **Sprint 14 is functionally a hydration-flash fix dressed as a bg posture change** — worth naming so future maintainers understand the surface-level change has deeper purpose. Affected files (3): [`app/dashboard/layout.tsx`](frontend/src/app/dashboard/layout.tsx#L15) (single wrapper), [`app/accounting/layout.tsx`](frontend/src/app/accounting/layout.tsx#L147) (single wrapper), [`app/admin/layout.tsx`](frontend/src/app/admin/layout.tsx#L21) (Access Denied splash + main content — 2 wrappers updated for consistency). Shipper portal (`app/shipper/dashboard/layout.tsx` — `bg-[#F7F8FA]`) and carrier portal (`app/carrier/dashboard/layout.tsx` — same) intentionally remain light-mode and are not affected; their `text-gray-400` contrast issues are addressed in Sprint 16 sweep scope. Marketing pages, auth pages, and root `app/layout.tsx` (font classNames only on `<body>`) all unaffected. **Expected scanner P0 reduction:** 700–900 (Sprint 13 reported 1,585 layout-fallback firings on /dashboard + 299 /accounting + 103 /admin = 1,987 total — subset that resolves to high-contrast PASS after this change). Actual count from Phase C re-run. | v3.8.ff
- 2026-04-22 | Compass logo missing on prod BOLs | v3.7.o-build-prep + Render dashboard
- 2026-04-22 | Email signature file-not-found | v3.7.o-build-prep.1 + Render dashboard
- 2026-04-22 | Sign In dropdown Shipper/Carrier labels invisible | v3.7.n.1
- 2026-04-22 | CI feature flag test timeout flaky | v3.7.n.2
- 2026-04-23 | Shippers page content invisible below hero (JS null-dereference on getElementById('hamburgerBtn') halted IIFE) | v3.7.n.3
- 2026-04-23 | Carrier page CTA "Questions?" line invisible (black text on navy-gradient bg) + onboarding email updated accounting@ → carriers@ | v3.7.n.4
- 2026-04-23 | Track page logo rendering at intrinsic PNG dimensions (tracking.html missing srl-logo.css link) | v3.7.n.5
- 2026-04-23 | Track page footer layout collapsed to centered text stack (tracking.css had 2-rule stub; ported full 4-column rules from carriers.css, adapted for navy bg) | v3.7.n.5
- 2026-04-23 | Contact page "Find Us" map: deprecated Google Maps iframe endpoint no longer renders; replaced with static location card + "Open in Google Maps" link | v3.7.n.6
- 2026-04-23 | Lead Hunter Send Outreach modal body text + Cancel button rendering text-slate-400 on dark navy bg (3.2:1 contrast, failed WCAG AA for body text); swapped to text-slate-300 (4.8:1) matching readable-sibling convention | v3.7.n.7 — SUPERSEDED: root cause is globals.css [data-mode="light"] .text-slate-* !important overrides that reverse colors on dark surfaces; this partial fix had no visual effect in light mode. Full fix deferred to Phase 6 theme cleanup.
- 2026-04-23 | Portal dashboard layouts (Shipper + Carrier) token-always-null auth guard — 2-month silent regression from 2026-02-23 commit 172d6f3b security hardening (localStorage → httpOnly cookie migration updated auth stores but missed the two downstream dashboard layouts). Bypassed AuthGuard.tsx pattern that kept AE Console working. Also broke the notifications useQuery (enabled: !!token) in both layouts during the same window. Fixed by matching the AuthGuard user-presence + loadUser() pattern. | v3.7.n.7.1
- 2026-04-23 | S6.b Load Board "+ New Load" button non-functional when no load is selected — CreateLoadModal render was nested inside a {load && (...)} wrapper (introduced 2026-03-31) while the button was added outside it (2026-04-08 commit 10e0ea3d). Clicks toggled state correctly but the modal was unmounted when no load was selected (the typical Load Board open state). Fixed by moving the modal render out of the wrapper; added clarifying comment to prevent re-nesting during future cleanups. | v3.7.n.8
- 2026-04-23 | S6.c Lane Analytics runtime crash ("Cannot read properties of undefined (reading 'toFixed')") — root cause was shape mismatch between backend response (marginPct/avgRatePerMile/loads, byEquipmentType with avgMarginPercent) and frontend interface declarations (marginPercent/avgRate/volume, byEquipment with avgMargin). Three unguarded .toFixed() calls crashed on the undefined fields whenever backend returned non-empty data. Fixed by adding typed BackendLaneResponse + BackendMarginResponse interfaces and adapter functions (adaptLanesResponse / adaptMarginResponse) that map backend shape → frontend shape at the useQuery boundary. Stats object also derived locally (backend doesn't return it). Trend defaults to "FLAT". Full backend/frontend contract alignment deferred to Phase 6 architectural debt. Secondary issue (error-boundary text white-on-white) consolidated under Phase 6 Theme System Cleanup. | v3.7.n.9
- 2026-04-28 | BOL freight table rendered single aggregated row even when load.lineItems contained multiple rows (L2228322560 surfaced this — two LoadLineItem rows summed into one 48"×42"×52" body row). pdfService.ts now iterates load.lineItems, draws one body row per item with its own pieces / packageType / description / dimensions / weight / freightClass / NMFC / hazmat, dashed horizontal separator between rows, totals strip aggregating the full lineItems array. Cap of 10 rendered rows on page 1 with a cream-tinted "+N additional line items — full manifest attached" footer note when exceeded. Falls back to legacy single-row from flat fields when lineItems is empty. | v3.8.d
- 2026-04-28 | Public /tracking page rendered HTML entities literally (`Dry Van 53&#x27;` instead of `Dry Van 53'`) on equipment, commodity, shipperName and other public-facing string fields. Root cause: backend sanitizeInput middleware (server.ts:150) HTML-escapes every req.body string for XSS defense, so values containing apostrophes/quotes/angle-brackets are stored encoded; PDFKit decodes at its own boundary (pdfService safe()), but React text nodes don't auto-decode. Fixed by applying decodeHtmlEntities on the public-page serializer for: equipment, commodity, origin/destination city/state, shipperName, carrierFirstName, lastLocation city/state, stops city/state, checkCalls city/state. Decode is scoped to the public response — internal AE Console responses keep escaped form. | v3.8.d
- 2026-04-29 | BOL renderer (backend pdfService.ts) read from the master customer record for the Shipper section instead of from per-load physical-location fields, AND emitted literal placeholder strings ("[Consignee Facility]", "[Shipper Ref]", "[Contact · Phone]", "None  ·  [per-load notes]") into the printed BOL when binding logic was missing or fields were empty. Diagnosed via direct DB query of L2228322560 — `originCompany: 'Mainfreight N Lake'` and `destCompany: 'Pharmaloz Manufacturing'` populated correctly but BOL showed customer name `Beekeepers Naturals USA Inc.` and literal `[Consignee Facility]`. Frontend BOLTemplate.tsx already reads load-level fields correctly so the divergence was backend-side only. Fixes: Shipper reads `load.originCompany || load.shipperFacility || load.customer?.name`; Consignee reads `load.destCompany || load.consigneeFacility` (no customer fallback per CLAUDE.md §3.9); SHIPPER REF walks the schema's 4-field PO chain (`poNumbers[0] → shipperReference → shipperPoNumber → customerRef`); empty contact line renders em-dashes; empty special instructions renders "None". LoadBOLData interface extended with originCompany, destCompany, poNumbers, customerRef. Closes the previously-specced v3.8.c.1 micro-patch by merging it in. | v3.8.d.1
- 2026-04-29 | sanitizeInput middleware was HTML-entity-escaping every req.body / req.query / req.params string at write time for XSS defense, causing values containing apostrophes / quotes / angle-brackets to be stored encoded in the DB. Symptom surfaced on /tracking (v3.8.d) and BOL renderer (v3.8.d.1) where every consumer needed a compensating decode pass. Architectural fix: middleware rewritten to do INPUT hygiene only (trim, null-byte strip, 10k length cap) — no HTML entity escaping. XSS defense moves to OUTPUT layer per OWASP guidance: React auto-escapes JSX text nodes, pdfService safe() handles PDF, JSON encoding handles its own char-set. /api/webhooks exemption retained for raw external payloads. Companion one-time migration script `backend/scripts/decode-encoded-load-fields.ts` walks the loads table and decodes 19 tracked fields in place (idempotent). Defense-in-depth: pdfService safe() and trackingController decodeOpt() retained for one sprint cycle; removal candidate in v3.8.e. Spec correction: spec stated /api/orders is currently exempted as v3.8.c bandaid, but security.ts source confirms only /api/webhooks is exempted — no re-enable needed. | v3.8.d.2
- 2026-04-29 | Order Builder allowed re-attempting "Create load" on an already-converted order, producing HTTP 409 "Order already converted" (orders.ts:212) with no UI guard. Root cause: resumeDraft hydrated the form from order.formData but never inspected order.loadId, so a draft that had previously been converted resumed as if fresh. Fix: track loadId in component state, render an amber "This order has already been converted to a load" banner with deep-link to /dashboard/loads, and disable the Create load button when loadId is set. Backend GET /:id now joins the load record to expose loadReferenceNumber + loadNumber for the banner's identifier. Backend GET /?status=draft adds loadId: null to the where clause as defense-in-depth. Frontend drafts list defensive-filters d.loadId client-side too. | v3.8.d.3
- 2026-04-29 | Migration script `decode-encoded-load-fields.ts` did not handle double-encoded values (e.g. `Dry Van 53&amp;#x27;` from rows written through the old sanitizeInput middleware twice). The decodeHtmlEntities utility's single-pass replace doesn't re-scan replaced regions, so one call yields `Dry Van 53&#x27;` not `'`. Fix: new decodeUntilStable helper iterates up to 5 times, stops when output is unchanged. Cap prevents runaway loops on malformed input. | v3.8.d.3
- 2026-04-29 | BOL SHIPPER REF cell rendered only the first PO number from `load.poNumbers[]` even when multiple POs were entered in Order Builder. v3.8.d.1 spec deferred multi-PO display ("rendering more than poNumbers[0] — separate feature") but a real shipper-side reconciliation issue exists when only one of two POs prints. Fix: new formatPoList helper in pdfService — bare value for 1 PO, comma-joined for 2 ("1472, 5678"), "first, second +N more" for 3+. Stays within the metaCell width constraint (~91pt at fontSize 9.5 with lineBreak: false). | v3.8.d.4
- 2026-04-30 | Track & Trace LoadDetailDrawer had no load-status advancement UI — dispatchers in T&T had to bounce to the Load Board for every status update (PICKED_UP → IN_TRANSIT, AT_DELIVERY → DELIVERED, etc.) because the T&T module had zero `PATCH /loads/:id/status` callers. Fix (first Phase 6 sprint): extracted Load Board's `NEXT_STATUS` + `STATUS_ACTIONS` maps into shared `frontend/src/lib/loadStatusActions.ts` with a `getNextStatusAction()` helper that returns null for terminal statuses; Load Board imports from the shared module (5-line refactor, no behavior change); T&T LoadDetailDrawer header renders the same advance button next to its close button, calling the same backend mutation. Success invalidates both `tt-load-detail` and `loads` queries so dispatchers with both surfaces open see the change in both. Backend untouched — `VALID_TRANSITIONS` and side effects (`logLoadActivity`, `createCheckCallSchedule`) fire identically regardless of caller. | v3.8.e
- 2026-04-30 | Portal Approval gate for SHIPPER login (S-2 backend) — security gap: any registered SHIPPER could log in immediately after OTP because `customer.onboardingStatus` existed in the schema (default PENDING) but was not enforced on the auth path. Carrier auth has had the equivalent gate since v3.6.e ([carrierAuth.ts:170-186](backend/src/routes/carrierAuth.ts#L170-L186)); shipper auth never did. Fix: new `checkShipperApproval(user, email, req)` helper in [authController.ts](backend/src/controllers/authController.ts) — SHIPPER role only, resolves user→customer via `customer.userId @unique`, returns 403 with friendly status-specific message + SystemLog entry. Wired at TWO call sites for defense-in-depth: (1) `handleVerifyOtp` after OTP success and before TOTP/JWT; (2) `handleTotpLoginVerify` before JWT issuance, closing the theoretical stolen-token-replay vector and covering approval revocation between OTP and TOTP steps. AE Console roles (BROKER/ADMIN/CEO/DISPATCH/OPERATIONS/ACCOUNTING) bypass the helper entirely. S-3 (AE Console approve UI at /dashboard/shippers) deferred to separate Phase 6 sprint. | v3.8.e.1
- 2026-04-30 | ShipperSidebar "Back to Website" link target — surfaced during v3.8.e.1 smoke when an authenticated shipper clicked the bottom-left link on /shipper/dashboard expecting to return to the public marketing site, but landed on the divergent legacy /shipper prospect-landing page (dark-theme, separate design system from main marketing site — flagged for Phase 6 visual alignment). CarrierSidebar links to `/` (homepage, correct); ShipperSidebar was linking to `/shipper`. One-line fix: href `/shipper` → `/`. The /shipper page's underlying design divergence stays logged under Phase 6 — Portal + Public Page Visual Alignment. | v3.8.e.2
- 2026-04-30 | Order Builder lacked UI inputs for origin/destination contact name + phone, so every BOL printed since v3.8.d.1 rendered `Contact: — · —` em-dash fallback unless the user picked a saved facility with contactName/contactPhone populated. Audit found the entire data plumbing was already in place — schema fields exist (`Load.originContactName/Phone` + `destContactName/Phone`), form types declare all 4 fields, form state defaults to empty string, FacilityPicker auto-populates on facility selection, backend convert-to-load maps form→DB at `routes/orders.ts:395-406`, BOL template renders correctly per v3.8.d.1 binding. Only gap was UI inputs. Fix: 4 input fields added to Order Builder Section 2 (Route), placed inside each side's grid column directly below FacilityPicker / manual-address block. Per pre-sprint decisions: optional fields (no submission block, em-dash fallback stays graceful), free-text phone (freight contexts have extensions / "ask for Bob" / international numbers). PII scope on public /tracking unchanged — contacts stay internal per T&T source-of-truth doc §2. Single file changed (page.tsx). | v3.8.g
- 2026-04-30 | BOL page-1 footer collision: carrier signature block's SEAL # / DATE row labels visually overlapped with the centered `Where Trust Travels.` tagline at the page footer. Visible on every BOL since v3.8.b's BOL v2.9 template shipped. Root cause: hardcoded `fyLine = 755` in `pdfService.ts` placed the footer rule at y=755 with tagline at y=763, while the carrier column (the tallest of 3 sig columns at 6 rows × 30pt) extended its last underline to y≈756, sitting right on top of the footer rule. Single-line fix: `fyLine` 755 → 770 (15pt down). Footer text now bottoms at ~785pt with 7pt page-edge margin — within print-safe range for modern printers, unaffected for digital viewing. Page-2 footer uses the same constant so it shifts identically (T&C content unaffected). All 9 signature fields preserved at original column positions; only footer band moved. Closes §13.3 Item 5. | v3.8.h
- 2026-04-30 | Public /tracking PII hardening — `carrierFirstName` was surfaced publicly when a load had a carrier assigned, diverging from CLAUDE.md §2 / T&T source-of-truth doc §2 stated policy ("Carrier name renders as '—'. Public should not see which carrier is hauling — carrier solicitation prevention"). Test loads rendered "—" only because they had no carrier assigned; the moment a real tendered+accepted load went public, first name would have leaked. Discovered by 2026-04-30 read-only audit of trackingController.ts. Audit verified the broader serializer is structurally secure (two-layer allowlist: Prisma select clause + explicit res.json construction; all 17 listed sensitive fields confirmed stripped — carrierId/Rate/totalCarrierPay/driver*/internal notes/margin/customer rate/target cost). Fix: removed `carrier: { select: { firstName: true } }` from `loadSelect` AND removed `carrierFirstName` from the response object (two-layer strip). Defensive improvement: `loadSelect: any` → `loadSelect: Prisma.LoadSelect` so future field additions get compile-time type checking. Frontend consumer at `public/tracking.html:300` already had `d.carrierFirstName \|\| '—'` fallback so renders gracefully without code change. React `/track/page.tsx` doesn't consume the field at all. AE Console + Shipper Portal use separate authenticated endpoints — unaffected. | v3.8.i.1
- 2026-05-02 | Flaky test fix: `resetPassword.test.ts` T6 (concurrent double-submit). Surfaced as CI failure on commit 1cabacc — but the failure was unrelated to that commit; it exposed a pre-existing race condition that had been intermittently green locally and red on CI. Root cause: T6 fires two concurrent resetPassword() calls via `Promise.allSettled`, with $transaction mock using `mockResolvedValueOnce`/`mockRejectedValueOnce` (FIFO call-order). Test then asserted `p1.status === fulfilled` and `p2.status === rejected` based on POSITIONAL order in the Promise.allSettled array. But two async resetPassword() calls suspend at their first `await` and event-loop resume order is not guaranteed to match the array order — whichever request reaches `await prisma.$transaction(...)` first consumes the success mock. Local Node was deterministic-enough; CI's slightly different timing flipped order ~50% of runs. Fix: order-independent assertions — test now asserts EXACTLY ONE fulfilled and EXACTLY ONE rejected (regardless of which positional request won), then identifies the fulfilled response by status and verifies its body. Behavior under test unchanged; only assertion logic is order-independent now. Verified deterministic by running T6 in isolation 5 consecutive times locally (5/5 green) + full backend suite 164/164 passing. No production code change. | v3.8.o.1
- 2026-05-04 | Tools — `audit-brand-conformance.ts` v8: app-router `layout.tsx` body-bg resolution for Tailwind path per Sprint 13 directive. Closes the v7 fall-through class (Sprint 12 baseline: 1,188 of 1,382 recursive stacks fell through to `#FFFFFF` default because the .tsx file contained no opaque bg-* anywhere in its lexical chain). Real rendering inherits body bg from the Next.js app-router layout cascade. v8 walks `frontend/src/app/**/layout.tsx` files at scanner startup, builds `Map<routePrefix, LayoutBgContextEntry>` keyed by route prefix derived from file path. Resolution handles BOTH input shapes observed in repo: (1) inline `style={{ background: 'var(--srl-bg-base)' }}` regex-matched then resolved via existing `resolveColor()` helper for var/rgb/rgba/hex literals; (2) outermost element's `className="... bg-[#hex] ..."` Tailwind classname via existing `resolveTailwindClass()`. First match wins per file. `findAncestorBg` extended with `layoutFallback: LayoutBgContextEntry | null` parameter; recursion's null-inner branch and outermost walk's empty-result branch use the layout-resolved bg in place of `#FFFFFF` default. Returns a SYNTHETIC layout-fallback ancestor at depth=0 when JSX walk completes empty so Case 2 orphan-text findings now EMIT instead of skipping (the v7 `if (!anc) continue;` path). New trail format extends with layout suffix: `· layout: /dashboard (inline-style-var) bg=var(--srl-bg-base)→#F5F3EF`. New helpers: `LayoutBgContextEntry` type, `buildLayoutBgContext`, `deriveRoutePrefix`, `findLayoutBgForPage` (longest-prefix-wins), `resolveStyleBgValue`. Layout files themselves excluded from contrast scan (consumed only as fallback source). **5 layouts registered:** `/shipper/dashboard→#F7F8FA (tailwind-class)`, `/dashboard→#F5F3EF (inline-style-var)`, `/carrier/dashboard→#F7F8FA (tailwind-class)`, `/admin→#F5F3EF (inline-style-var)`, `/accounting→#F5F3EF (inline-style-var)`. Root layout `app/layout.tsx` registers no bg (font classNames only on `<body>`). **Critical light-mode semantic surfaced:** `--srl-bg-base` in [`globals.css:13`](frontend/src/app/globals.css#L13) resolves to `#F5F3EF` light cream by default (`:root, [data-mode="light"]`) and only to `#0F1117` dark navy when `[data-mode="dark"]` attribute is set. Per existing v3 default-mode convention (scanner targets default rendering with no data-mode set), dashboard/accounting/admin pages now correctly composite against light cream. **Many `text-white` findings on these pages are GENUINE readability bugs in light mode — v8 SURFACES them rather than retires them, which is the correct semantic.** Required fix for vars resolution: `globals.css` lives under `frontend/src/app/` not `frontend/public/`, so the existing `cssFiles` list (FRONTEND_PUBLIC scope) didn't include it; v8 explicitly adds `globals.css` to the var-resolution file list (`cssFilesForVars`) so layout vars resolve correctly. **Re-run delta vs Sprint 12 v7 baseline:** total findings 14,629 → 15,948 (+1,319), P0 **1,532 → 2,301** (+769), P1 12,495 → 13,045 (+550 — borderline cases moved P0→P1 after compositing against light cream rather than against pure white default), P2 unchanged at 602. Pass 3 specifically: 1,994 → 3,313 contrast findings (+1,319). **2,585 layout-fallback firings** distributed across 5 layouts: `/dashboard=1,585`, `/carrier/dashboard=385`, `/accounting=299`, `/shipper/dashboard=213`, `/admin=103`. **1,053 v7 body-fallthrough cases retired** (1,188 → 135 — the body-fallthrough counter now only fires when there's neither a JSX bg ancestor NOR a layout fallback, which is rare). **Net: P0 went UP, not down — that's the correct behavior.** The Sprint 12 Phase C projection (~980 retirement) was based on incorrect assumption that the v7 fall-through class would composite against dark navy. Empirical truth: the routes use light cream by default, so `text-white` cases that v7 reported as 1.00:1 white-on-white are still 1.00:1 white-on-cream — same severity, different (more accurate) trail. Additionally, Case 2 orphan-text path emits findings that v7 silently skipped (`if (!anc) continue`), surfacing 1,464 new orphan-text findings. **Trusted v8 baseline established for final P0 sweeps.** All major scanner blind spots closed except v9-deferred classes. Real CSS-side P0 (216 from Sprint 12) and Tailwind-side P0 (now ~2,085 with v8's surfacing) are visible and triageable for Sprint 14/15 fix sweeps. **v9 candidates queued:** Cross-file React component composition (`<Card>`, `<Modal>`, `<DashboardShell>` ancestor bg by walking imports); globals.css `body { background }` consultation when layout.tsx has no bg; `sm:`/`md:`/`lg:`/`xl:`/`2xl:` responsive variants; `dark:` variant evaluation; state-driven conditional className branches; full AST parsing for arbitrary clsx/cn signatures; CSS-in-JS dynamic styles; `@media` query rules; pseudo-element styling; orphan-CSS detection; memoized cache for findAncestorBg; translucent layout outermost bg recursion; dark-mode opt-in flag (run scanner in `[data-mode="dark"]` posture for dashboard verification separate from default light-mode). Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v7: translucent-ancestor recursion in `findAncestorBg` per Sprint 12 directive. Closes the upper-bound class identified in Sprint 11 Phase C analysis (~729 findings flagged for V7-FP, of which the genuinely fixable subset turned out to be smaller than projected once empirical recursion ran). **Pattern:** text sits inside translucent overlay (`bg-white/5`) inside opaque dark ancestor (`bg-[#0f172a]` or `bg-navy` two levels up). v6's `findAncestorBg` returned the FIRST bg-* ancestor (translucent) and the orphan-text caller's simplification composited that against `#FFFFFF` default → reported white-on-white 1.00:1 P0; real rendering composites overlay on dark navy → ~14:1 high contrast. **v7 changes:** `findAncestorBg` now recurses — when first bg-* ancestor has α<1, recurses on that ancestor's `parentIndex` to find what it composites against; builds a stack of overlays as recursion proceeds; when opaque ancestor (or `#FFFFFF` fallback) reached, composites stack from innermost outward via successive `compositeAlpha()` calls. Return type simplified — `alpha?` field dropped (always opaque after v7); new fields `stack`, `baseHex`, `fellThroughToBody`, `depthExceeded` carry the trail for finding-output transparency. Recursion capped at `MAX_ANCESTOR_BG_DEPTH = 5` (defensive — real JSX rarely exceeds 2-3 levels). Both pass3Tailwind callers (Case 1 same-element bg+text, Case 2 orphan text-*) simplify — the v6 "if anc.alpha < 1, composite against #FFFFFF" conditional is gone. Single expression `bg = anc.hex`. Stack trail format: `"ancestor stack: <div> α=0.05 #FFFFFF → <div> α=0.40 #000000 → #0F172A composited: <final-hex> (depth=2)"`. **Page-context body bg fallback (CSS pass v5 feature) NOT available for Tailwind path** — .tsx files don't lexically link CSS via `<link rel="stylesheet">`; Next.js app-router pages inherit body bg from `layout.tsx` cascade. Resolving that requires traversing the app-router layout tree per page — explicitly deferred to v8 alongside cross-file component composition. **Re-run delta vs Sprint 11 v6 baseline:** total findings 14,688 → 14,629 (−59), P0 **1,627 → 1,532** (−95), P1 12,459 → 12,495 (+36 — borderline cases moved P0 → P1 after composition revealed 3.0–4.5:1 ratios), P2 unchanged at 602. Pass 3 specifically: 2,053 → 1,994 contrast findings (−59). Tailwind specifically: 1,663 → 1,604 findings (−59). **1,382 recursive translucent stacks fired** with depth distribution `d1=1,214, d2=168` (no ≥3 in repo — single-overlay dominant pattern). **0 recursions hit MAX_DEPTH=5 cap** — defensive cap not exercised. **1,188 of the 1,382 stacks fell through to `#FFFFFF` body fallback** — meaning the full ancestor chain in the .tsx file ended without an opaque bg-*, so the recursion correctly detected the chain shape but the final composited base is still white-ish (matches v6 behavior since the ultimate fallback hasn't changed for these). This is the v8 problem: the layout.tsx parent provides the actual opaque navy bg via cascade, not via inline className. **Genuinely retired V7-FP: 194 stacks (1,382 recursion fires − 1,188 fell-through) where recursion landed on an opaque grandparent and corrected the composite.** Remaining bigger lever is v8 (Next.js app-router layout.tsx body-bg resolution) — projected to retire several hundred additional fall-through orphan-text findings on dashboard surfaces wrapped in dark-mode layouts. **Architectural improvement is real and correct** even with the smaller-than-Phase-C-projected delta: scanner trail now shows full composition history (`"<div> α=0.05 #FFFFFF → <div> α=0.10 #3B82F6 → #FFFFFF (default fallback) composited: #EBF3FE (depth=2)"`), enabling future audits to distinguish "real readability bug" from "v8 layout-bg-resolution needed" by inspecting whether the trail ends in `(default fallback)`. **v8 candidates queued:** Cross-file React component composition (resolve `<Card>` ancestor bg from Card's own file when JSX walker hits a capitalized-tag boundary without bg-*); **app-router layout.tsx body bg resolution for Tailwind path** (analog of v5 page-context body bg, walks `app/layout.tsx` + nested layouts to resolve effective body bg per route; expected to retire the 1,188 fell-through findings on real dashboard surfaces); `sm:`/`md:`/`lg:`/`xl:`/`2xl:` responsive variant evaluation; `dark:` variant evaluation; State-driven conditional className branches (ternary inside clsx args — currently first-string-literal-wins); Full AST parsing (typescript / @babel/parser) for arbitrary clsx/cn signatures and spread arguments; CSS-in-JS dynamic styles; `@media` query rules; pseudo-element styling; orphan-CSS detection; memoized cache for findAncestorBg results. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v6: JSX tree walking for parent-child element contrast resolution per Sprint 11 directive. Closes the ~320 false-positive class surfaced in Sprint 10 Phase A inspection where v5's same-element-only Tailwind pairing reported `bg-white/5` + `text-white` translucent overlays as 1.05:1 white-on-white because v5 composited the overlay against `#FFFFFF` default parent (no JSX walking). Real rendering: the JSX ancestor element (often `<div className="bg-[#0f172a]">` for /track or `<div className="bg-[#0A2540]">` for dashboard pages) supplies an opaque dark-navy bg, the translucent overlay composites to a slightly-lifted navy, contrast against `text-white` is ~14:1 PASS. **Approach:** stack-based regex JSX walker (no AST dep per directive confirmation #4) that builds a per-file element tree at Pass 3 startup. Added `JsxStackEntry` interface (`tag`, `classNames`, `parentIndex`, `line`, `isComponent`) + `buildJsxElementStack(content)` (~130 LOC). Walker handles: self-closing tags, fragments (`<>` / `</>`), HTML tags vs React components (capitalized first char), `clsx()`/`cn()` helper unwrapping (string-literal args extracted, conditionals' first string-literal-wins per directive confirmation #2), dynamic `${...}` interpolation skipped per directive confirmation #3, multi-line className expressions, brace-depth tracking for nested ternaries, in-string skip for `'`/`"`/`` ` `` literals. Added `findAncestorBg(elementIndex, elements, counter)` walking the parentIndex chain; returns first ancestor with a bg-* resolved class. When walking past a capitalized-tag (component) ancestor without bg-*, increments `crossFileBoundaryCount` because the component's actual rendered bg lives in a different file (deferred to v7 — cross-file component composition). **`pass3Tailwind` refactored to two cases:** **Case 1 (existing v5 path with v6 fix)** — same-element bg + text pair; when bg has α<1, NEW: composite against `findAncestorBg` result instead of `#FFFFFF` default; increments `ancestorBgFiringCount` when ancestor lookup actually fires. **Case 2 (NEW v6 path)** — orphan text-* element with no same-element bg-*; pair text against `findAncestorBg` result; increments `orphanTextFiringCount`. Closes the dashboard-page pattern where `<h2 className="text-white">` sits inside a `<div className="bg-[#0A2540]">` two levels up. New counters surfaced in summary line: `tailwindAncestorBgFiringCount`, `tailwindOrphanTextFiringCount`, `tailwindCrossFileBoundaryCount`. **Re-run delta vs Sprint 10 baseline:** total findings 12,955 → 14,688 (+1,733), P0 **571 → 1,627** (+1,056), P1 12,261 → 12,459 (+198), P2 unchanged at 602. Pass 3 specifically: 811 → 2,053 contrast findings (+1,242). **225 ancestor-bg lookups (Case 1)** — same-element pairs with α<1 that now correctly composite against the dark JSX ancestor instead of default white; this resolves the originally-projected ~320 FP class (some of those resolved to PASS, removing them from P0 — net effect on Case 1 is hard to isolate from the larger Case 2 expansion). **2,426 orphan-text findings (Case 2)** — text-only elements where v5 didn't bother resolving the bg context; v6 walks JSX ancestors and pairs against the resolved bg; emits findings only when contrast fails. **4,307 component boundaries crossed** — text-* elements whose ancestor walk reached a capitalized-tag React component (e.g., `<Card>`, `<Modal>`) without finding a bg-* class; v6 walks past them and uses the next non-component ancestor with bg-* (or skips if none). This is the documented v7 deferral — the component's actual bg lives in another file. **Direction-of-impact analysis:** the v6 P0 increase is NOT a regression. v5 Case 1 reported FPs (~320 known) that v6 Case 1 now correctly classifies. Case 2 is genuinely new coverage — v5 was blind to orphan-text contrast — and the 1,056 new P0 entries are a mix of (a) real readability issues across dashboard surfaces that the scanner could not see before, and (b) new V7-FP class where component-boundary skipping incorrectly attributes bg context. Empirical triage required to separate the two; flagged as Sprint 12 Phase C work. **Trusted v6 baseline established for parent-child JSX-aware Tailwind contrast detection.** **v7 candidates queued:** cross-file React component composition (when parent `<Card>` sets bg in its own file, child `<h2 className="text-white">` sees crossFileBoundaryCount++ but cannot resolve the ancestor bg — would require multi-file element tree merging at component-boundary); full AST parsing (typescript / @babel/parser) to resolve `clsx`/`cn` helpers with arbitrary call signatures and spread arguments correctly; `sm:` / `md:` / `lg:` / `xl:` / `2xl:` responsive variant evaluation; `dark:` variant evaluation against the dark-mode body bg; state-driven conditional className branches (ternary inside `clsx` args — currently first-string-literal-wins); CSS-in-JS dynamic styles (not currently used in repo); `@media` query rules; pseudo-element styling (`::before`, `::after`); orphan-CSS detection. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v5: per-page body-bg context + Tailwind class resolution on .tsx components per Sprint 9 directive. Closes the two structural false-positive classes documented in Sprint 8 Phase C analysis. **Fix 1 — Per-page body-bg context.** Added `buildPageBgContext(htmlFiles, cssFiles, vars)` helper. Walks `frontend/public/**/*.html` files, parses `<link rel="stylesheet" href="...">` tags via two-pass regex (robust to attribute ordering), resolves linked hrefs to absolute paths matching `cssFiles[]`, walks loaded CSS files in order to find the first opaque body bg per HTML page. Builds `Map<cssFilePath, PageBgContextEntry>` with `viaHtml` attribution and `conflicts` count. `findOpaqueParentBg` extends to a 7-tier lookup chain (Tier 5 NEW): same-file CSS-descent → same-file HTML hint → global CSS-descent → global HTML hint → **page-context body bg (NEW)** → file-body → global-body → default white. Per directive confirmation #4: first walk-order match wins on multi-page conflicts; `conflicts` count surfaced in finding-output composition trail. `OpaqueParentResult` source enum extended with `page-context-body`; new fields `viaHtml` (HTML page that supplied the body bg) and `pageContextConflicts`. **Fix 2 — Tailwind class resolution on .tsx.** Added `TAILWIND_PALETTES` constant covering all 14 standard color palettes × 11 shades = 154 entries (slate, gray, zinc, stone, neutral, red, orange, amber, yellow, green, emerald, blue, indigo, violet) per directive confirmation #1, plus `TAILWIND_SPECIAL` for white/black non-shaded variants. Added `resolveTailwindClass()` mapping a single utility-class token → hex with optional alpha (handles arbitrary-value bracket syntax `bg-[#0A2540]`, arbitrary-opacity suffix `text-white/80`, base + pseudo variants). Added `pass3Tailwind` function: extracts className expressions from .tsx files (static `"..."` / `'...'` / backtick `` `...` `` template literals WITHOUT `${...}` interpolation per directive confirmation #3), tokenizes on whitespace, groups by pseudo-variant prefix (`base` / `hover` / `focus` / `active` per directive confirmation #2 — `sm:` / `md:` / `lg:` / `xl:` / `2xl:` / `dark:` deferred to v6), for each variant group with both `text-*` AND `bg-*` resolved tokens computes WCAG contrast and emits findings using same P0/P1/P2 buckets. Files with dynamic `${...}` interpolation logged once per file as `dynamicSkippedFiles`. v5 simplifications: same-element pairing only (parent-child JSX walking deferred to v6); composite alpha-overlay text/bg against `#FFFFFF` default parent (no JSX tree walk to resolve actual ancestor). **Re-run delta vs Sprint 8 baseline:** total findings 12,933 → 12,955 (+22), P0 **228 → 595** (+367), P1 12,137 → 12,261 (+124), P2 unchanged at 546. **Pass 3 CSS:** 387 → 390 contrast findings (essentially flat — page-context fix retires some carrier-portal FPs but most cases were body-equivalent in effect). **Pass 3 page-context body-bg fired: 734 lookups** — extensive coverage proves the per-page index is active across the carrier portal, marketing, AE Console, auth, and shipper-portal HTML pages. **Pass 3 Tailwind: 443 NEW findings from 659 text+bg pairs evaluated** across 223 .tsx files, with 133 files containing dynamic className interpolation skipped per directive simplification. **The big P0 jump (228 → 595) is the EXPECTED outcome of closing the v4 blind spot:** Tailwind .tsx scanning surfaces 367+ real React/Tailwind contrast issues that scanner v4 simply couldn't see. Trusted baseline established for sitewide contrast detection — scanner now covers BOTH CSS files AND React/Tailwind components. **v6 candidates queued:** parent-child JSX tree walking (when parent element sets bg, child sets text on a different element); `sm:` / `md:` / `lg:` / `xl:` / `2xl:` responsive variants; `dark:` variant against dark-mode body bg; React state-driven className via `cn()` / `clsx()` / `classnames` helpers; CSS-in-JS dynamic styles (not currently used in repo); `@media` query rules; pseudo-element styling (`::before`, `::after`); orphan-CSS detection. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v4: cross-file bgRules merging per Sprint 7 directive. Built a global bgRules index across all CSS files at Pass 3 startup (`buildGlobalBgRules`), with file attribution for ambiguity logging. `findOpaqueParentBg` extended to a 5-tier lookup chain: same-file CSS-descent → same-file HTML hint → **global CSS-descent (NEW)** → **global HTML hint (NEW)** → body bg fallback. Same-file priority preserved per directive confirmation #4. Mode-prefix stripping (`stripModePrefix` helper) applied on both sides of the global match — strips `html[data-mode="X"]`, `html[data-theme="X"]`, and combined `html[data-theme="X"][data-mode="X"]` prefixes per directive confirmation #2. This enables themed selectors in themes.css (`html[data-theme="silk-route-classic"][data-mode="dark"] .sidebar-nav a:hover`) to resolve their parent `.sidebar` rule living in console.css. `OpaqueParentResult` source enum extended with `global-css-descent` + `global-html-hint`; new `viaFile` and `ambiguousMatches` fields surfaced in the finding-output composition trail for human triage. Per directive confirmation #1: ambiguous global matches (multiple files defining the same selector — e.g., `.card` in themes.css AND `.card` in page CSS) take first-match-wins with the count surfaced for transparency. **Re-run delta vs Sprint 6 baseline:** total findings 12,968 → 12,968, P0 **258 → 262** (+4 net change), P1 12,151 → 12,149 (−2). Pass 3 specifically: 385 → 387 (+2 contrast findings). **45 global-match firings** (Tier 3 / Tier 4 fallback resolved a parent bg), **39 of those with ambiguous matches** (multiple files defining same selector — predominantly `.card` defined in both themes.css and page CSS files like `accounting-fund.css`, `accounting-payable.css` etc.). **Smaller-than-projected delta** (projection was ~100-130 reduction, actual is +4 increase). Root cause analysis: (a) the cross-file class of false positives was smaller than projected — most "themed-selector FPs" in v3 turned out to be cases where the body-fallback bg `#FFFFFF` happened to be the SAME effective parent as the global match would find (e.g., themes.css `.card { background: #FFFFFF }` matches what body fallback already returned); (b) ambiguity bias toward themes.css (first in walk order) sometimes selects an alpha-overlay or theme-default `.card` over the more-specific page CSS `.card`, producing slightly different (sometimes worse) compositing — accounts for the +4 P0 increase. The architectural improvement is real and correct: cross-file resolution now happens for cases that genuinely need it (e.g., `.sop-card .sop-meta span` correctly resolved via `.sop-card` in training.css). **Net assessment:** v4 delivers the architectural fix as specified — scanner is now correctly cross-file-aware, ambiguity is surfaced for triage, no regressions in existing per-file behavior. The narrower-than-projected real-world impact reflects that the v3 false-positive class was largely body-fallback-equivalent in effect. Remaining 262 P0 are mostly real readability findings clustered around 4-5 patterns (gold-on-white CTAs, slate-400 text on near-white widget cards, status-badge color saturation, etc.) — Sprint 8 fix-sweep candidates. **v5 candidates queued:** Tailwind class resolution on `.tsx` React components (the Lead Hunter / dashboard modal class of contrast issues where bg + color come from utility classes, not CSS rules — currently invisible to the scanner because Pass 1 only flags Tailwind palette names without resolving them to actual contrast pairs); full HTML-DOM cascade resolution (parse HTML files to build class-containment graph rather than v3's hardcoded hint table — would enable correct resolution for arbitrary nesting beyond the 8 documented patterns); `@media` query rule extraction; pseudo-element styling (`::before`, `::after`); CSS-in-JS dynamic styles; orphan-CSS detection. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-04 | Tools — `audit-brand-conformance.ts` v3: three cascade fixes per Sprint 6 directive. **Fix 1 — pseudo-class rule cascading.** Added `stripPseudoSuffix()` + `getAllBaseSelectors()` helpers; cascade-inherit walker now extends its `hasExplicitColor` check to consider base selectors. When `.btn-navy:hover` has bg + no explicit color but `.btn-navy` has `color: var(--white)`, the walker recognizes the base color cascades through and skips the cascade-inherit finding (matches actual browser behavior). **Fix 2 — multi-mode token resolution.** Added `findDarkModeSkipRanges()` (brace-counting walker) + modified `buildVarMap()` to skip variable declarations whose offset falls inside an `[data-mode="dark"]` block. Vars defined ONLY in dark mode become undefined → `var(--name, fallback)` syntax falls back to literal default per CSS spec. Default-mode rendering (no `data-mode` attribute set on html element on silkroutelogistics.ai's public pages) is what scanner now targets. **Fix 3 — HTML cascade hints.** Added `HTML_CASCADE_HINTS` constant covering documented nesting patterns (`.sidebar-` inside `.sidebar`, `.modal-` inside `.modal`, `.card-` inside `.card`, `.notif-` inside `.notif-list`, `.action-item-` inside `.action-list`, `.badge-` inside `.card`, `.topbar-` inside `.topbar`, `.panel-` inside `.panel`); `findOpaqueParentBg()` now consults hints when CSS-selector descent finds no opaque ancestor. CSS descent runs first per Sprint 6 directive priority confirmation. Function return shape changed to `{ hex, source, viaSelector? }` where source is `css-descent` / `html-hint` / `file-body` / `global-body` / `default-white` — surfaced in finding-output composition trail for transparency. **Re-run delta vs Sprint 5 baseline:** total findings 12,968 → 12,968, P0 **265 → 258** (7 eliminated), P1 12,155 → 12,151 (4 eliminated as well — moved to PASS), P2 unchanged at 548. Pass 3 specifically: 396 → 385 (11 contrast findings now PASS). HTML hints fired 4 times (.badge-amber, .badge-green, .modal-backdrop composited over .card or .modal correctly). **Smaller-than-projected delta** (projection was ~80-100, actual is ~11). Root cause: most of the projected false-positive class turn out to need cross-file cascade resolution which is the v4 candidate. Specifically: themes.css defines `--theme-sidebar-bg` etc. as vars, and console.css uses them in `.sidebar { background: var(--theme-sidebar-bg) }` — but bgRules is built per-file, so when processing themes.css's `html[data-mode="X"] .sidebar-nav a:hover` rule, the scanner's HTML hint search for `.sidebar` in the same-file bgRules fails (themes.css has no `.sidebar` selector — only the var). Cross-file bgRules merging is v4 scope. Other contributors to lower-than-projected delta: many findings that LOOKED like false positives in v2 spot-checks turn out to be REAL readability issues at slightly different selectors (e.g., `.fuel-card .region-name` slate-400 text on near-white card — same shape as the v3.8.u Cluster 3 fix but on a selector I didn't sweep). v3 still delivers real progress: 11 verified false positives eliminated, no regressions, finding-output trails are now informative for triage. **v4 candidates queued:** cross-file bgRules merging (would unblock the cross-file cascade case described above; expected to eliminate the bulk of remaining apparent false positives), full HTML-DOM cascade resolution (parse HTML to build class-containment graph rather than hardcoded hints), `@media` query rule extraction, pseudo-element styling (`::before` / `::after`), CSS-in-JS dynamic styles. Pure dev tooling, no production code path; no version bump per §3.1 docs-only exemption.

- 2026-05-03 | Tools — `audit-brand-conformance.ts` v2: alpha-overlay handling. v1 stripped alpha and treated `rgba(R,G,B,α)` as solid `#RRGGBB` for contrast math, producing false positives where alpha overlays composited over a parent bg actually rendered with acceptable contrast (`rgba(255,255,255,0.05)` over navy sidebar reported as `1.00:1` white-on-white when real rendering composites to ~12:1 white-on-slightly-lifted-navy). Sprint 3 spot-check confirmed 3 of 5 v1 P0 findings were alpha artifacts. v2 fixes by preserving alpha through `resolveColor()`, then in Pass 3 finding the nearest opaque ancestor bg and applying standard sRGB composition (`composited.C = α × overlay.C + (1 − α) × parent.C`) before computing the contrast ratio. Same fix applied to fg-with-alpha (rare); cascade-inheritance branch also composites the overlay before checking dark-surface luminance, so e.g. `rgba(239,68,68,0.10)` over white doesn't trigger as "dark bg" anymore. New helpers: `compositeAlpha(overlayHex, alpha, parentHex)`, `findOpaqueParentBg(child, bgRules, fileBodyBg, globalBodyBg)` (walks bgRules ancestors filtered to opaque), `findCascadeBg` / `buildGlobalCascadeBg` mirroring the existing color-cascade walkers for body bg. Pass 3 summary now reports `compositedCount` so the operator can see how many findings benefited from compositing. Re-run delta vs v1 baseline: total findings 13,158 → 12,996, **P0 516 → 299** (217 eliminated by compositing reclassification, ~42% reduction), P1 12,075 → 12,160 (some borderline cases moved from P0 to P1 after composition revealed 3:1–4.5:1 ratios), P2 535 → 537. Pass 3 specifically: 568 → 435 findings (133 fully passed after compositing), 406 of remaining 435 used alpha compositing in their evaluation. **Known v2 limitation:** the parent-bg search uses CSS-selector descendant matching (`.parent .child` / `.parent > .child`), not HTML structure. Selectors like `.sidebar-nav a:hover` aren't recognized as descendants of `.sidebar` because `.sidebar-nav` is its own class (shares a prefix but not a CSS selector relationship). For these cases the composition falls back to body bg, which may misclassify findings whose actual rendering happens inside a different ancestor. v3 cross-file/HTML-aware cascade resolution would close this gap; v2 still substantially improves accuracy for the common alpha-overlay-on-card-or-page-bg patterns. v3 candidates explicitly deferred: `@media` queries, pseudo-elements, dynamic CSS-in-JS, HTML-aware cascade resolution, orphan-CSS detection (Sprint 3 spot-check 2 found `tracking.css` is dead code post-v3.8.q). Pure dev tooling, no production code path; no version bump per §3.1.

- 2026-05-03 | Tools — added [`backend/scripts/audit-brand-conformance.ts`](backend/scripts/audit-brand-conformance.ts) v1. Three passes: (1) color literals — hex / rgba / hsla / Tailwind utility classes against canonical whitelist extracted from [tokens.md](.claude/skills/srl-brand-design/references/tokens.md) (navy 10-stop, gold 4-stop, cream/surface, foreground, status pairs) plus a LEGACY-ALLOWED set per CLAUDE.md §2.1 (`#0D1B2A`, `#854F0B`, `#0F1117`, `#1A1A2E`, `#0A1220`, `#FAF9F7`, doc-header gradient stop `#1A2D45`, assorted legacy gold values); canonical hex literals classify as P2 token-discipline drift (use `var(--token)` instead) per Q1 confirmation; (2) font families — non-canonical primary fonts in `font-family` declarations, `@font-face`, and `@import url(...family=...)` calls; canonical: Playfair Display, DM Sans, Georgia, SF Mono; legitimate fallbacks (`-apple-system`, `BlinkMacSystemFont`, `system-ui`, `Times New Roman`, `Menlo`, `Consolas`, etc.) suppressed; (3) **surface-mode contrast (mandatory in v1 per Wasi 2026-05-03)** — for each (parent-bg-selector, child-color-selector) pair in the same stylesheet, compute WCAG relative-luminance contrast ratio with hand-rolled math (no external deps); buckets <3:1 = P0, 3:1–4.5:1 = P1, ≥4.5:1 = pass. Pass 3 also catches the cascade-inheritance pattern (dark bg with no explicit `color:` declaration → child text inherits from cascade; if the cascade resolves to `--fg-1` navy, this is navy-on-navy P0). Built specifically to catch the /about footer invisible-text class and /track navy-on-navy class that screenshots surfaced 2026-05-03. CSS variable resolution best-effort across all CSS files (`:root` + `@theme` + page-specific declarations, max 5 substitution hops); unresolved-variable count surfaces in the Pass 3 summary row per Q-clarification — informs whether v2 cross-file cascade resolution is needed. v2 candidates explicitly deferred: `@media` query rules, `::before`/`::after` pseudo-elements, dynamic CSS-in-JS, full cross-file cascade. PDF templates at `backend/src/services/pdfService.ts` are out of frontend scope per Sprint 2 directive — scanner emits a notice in the report's "Notes for human triage" section. Output: timestamped markdown report at `docs/audit-reports/brand-conformance-<ISO>.md` (gitignored — regenerate locally per run, not committed). Run via `cd backend && npx ts-node scripts/audit-brand-conformance.ts`. Companion to `audit-completeness.ts` (commit `1cabacc`). Pure developer tooling, no production code path; no version bump per §3.1 docs-only exemption.
- 2026-05-02 | New developer tool: `backend/scripts/audit-completeness.ts` — static-code scanner for SRL data-plumbing-vs-UI gaps. Three passes (v1): (1) orphan endpoints — backend PUT/PATCH/DELETE routes with no apparent frontend caller; (2) orphan schema fields — Prisma fields with zero references in any frontend `.tsx` file (after excluding common boilerplate names like id/status/name/email/etc.); (4) list-row action completeness — React `.map(...)` list renders that include Delete/Remove but no Edit/Update affordance. Pass 3 (form-vs-schema diff) deferred to v2 — needs explicit form→model mapping table. First run: 92 endpoints scanned, 27 orphan; 1404 fields scanned, 575 orphan; 8 Delete-only list rows. Re-runnable to track delta. Output: timestamped markdown report in `docs/audit-reports/audit-<ISO>.md` (gitignored — regenerate locally per run, not committed). Goal is to catch the architectural pattern that's surfaced 6+ gaps this week (EditLoadModal, FacilitiesTab edit, operatingHours form, customer inactivation, notification routing, dispatch switching) without manual walkthrough. Pure developer tooling, no production code path; no version bump per §3.1 docs-only exemption.
- 2026-05-02 | CI test alignment for v3.8.j carrier-required gate. Six consecutive CI runs (since v3.8.j ship on 2026-04-30) failed on `loadController.test.ts` line 120 — "updateLoadStatus — valid transition succeeds" — because the test mock had `carrierId: null` and expected POSTED→BOOKED to succeed, but v3.8.j Layer 2 correctly rejects that case with 400. The contract changed under the test. Discovery delayed by ~48h because §3.3 pre-commit checklist runs `tsc --noEmit` + `next build` but not `npm test` on the backend. Fix: updated existing happy-path test to provide `carrierId: "carrier-user-1"` so the gate passes (verifies real post-v3.8.j valid behavior); added new test case that explicitly verifies the gate firing on POSTED→BOOKED with `carrierId: null` (asserts 400 status + error message contains "without an assigned carrier" + `load.update` NOT called — covers both v3.8.j Layer 1 auto-assign removal and Layer 2 gate). loadController.test.ts now 11 tests (was 10), full backend suite 164/164 passing. CI parity verified via `npm test` before commit. Lesson: add `npm test` to §3.3 atomic-commits pre-commit checklist for backend code changes touching state-machine or contract behavior. | v3.8.o
- 2026-05-02 | Closes §13.3 Item 1. Fabricated customer persona content in /ae/financials.html (5 instances at lines 145/153/161/169/177 post-v3.8.m: "Michael Reeves, VP Supply Chain at Midwest Manufacturing Co.", "Jennifer Walsh, Logistics Director at Great Lakes Automotive Parts", "Carlos Mendez, Shipping Manager at Southeast Food Distributors", "David Kim, Procurement Lead at Pacific Chemicals Inc.", "Sarah Johnson, Operations at Texas Building Supply LLC") replaced with "Sample Contact 1-5" + "Sample Manufacturing Co. / Sample Auto Parts Co. / Sample Food Distributor / Sample Chemicals Co. / Sample Building Supply Co." Industry-real role titles preserved (not fabrications). Plus: sticky-top "DEMO DATA" banner injected into all 34 internal HTML files (24 /ae/**/*.html + 10 /carrier/*.html) using skill-canonical warning tokens per srl-brand-design v4.7 (var(--warning-bg) #FBEFD4 background, var(--warning) #B07A1A border, var(--gold-dark) small-caps DEMO DATA label per §8, Lucide AlertTriangle icon stroke 1.75 per §13, var(--space-3/4) padding per §9). Banner copy: "DEMO DATA — Sample values only. No live shipments, customers, or personnel." Token values scoped to .srl-demo-banner element to avoid collision with host-page CSS variables. Banner CSS at new frontend/public/_partials/demo-banner.css. Combined v3.8.m + v3.8.n effect: even on deep-link bypass of noindex or screenshare exposure, viewers see honest demo framing rather than fake employees of fake companies. | v3.8.n
- 2026-05-02 | AE Console + Carrier portal scaffolding HTML (24 /ae/*.html + 10 /carrier/*.html, 34 files total, including 12 nested in /ae/accounting, /ae/compliance, /ae/dashboard subdirs) were publicly crawlable with no robots directives. Surfaced during 2026-05-02 public-pages audit. Risk: search engines may have indexed pages that contain fabricated customer persona data (§13.3 Item 1) — brand-integrity issue. Two-layer defense: (1) robots.txt added `Disallow: /ae/` and broadened `/carrier/dashboard/` → `/carrier/` (compliant crawlers stop fetching); (2) per-file `<meta name="robots" content="noindex,nofollow">` injected into all 34 files via find-piped-while-loop with idempotency check (catches non-compliant crawlers + deep-link discovery). Public marketing pages (index, carriers, contact) already had intentional robots tags — left unchanged. Persona content cleanup is paired commit v3.8.n (same session). Post-deploy Wasi action: Google Search Console URL removal request for any indexed /ae/* or /carrier/* URLs. | v3.8.m
- 2026-04-30 | Tender-workflow correctness — three-layer fix surfaced by L6894191249 incident. The Load Board's blue status-advance button labeled "Tender" on POSTED loads was being mistaken for the real carrier-tender action. Clicking it just flipped the status flag with no carrier involved, walking loads through TENDERED → CONFIRMED → BOOKED purely as status changes. On the BOOKED transition, a quiet auto-assign at [`loadController.ts:477`](backend/src/controllers/loadController.ts#L477) set `carrierId = req.user!.id` (the calling AE user, not a real carrier) — making the load display "Silk Route Logistics Inc." as carrier in internal views. **Layer 1 (backend):** removed the auto-assign clause from `updateLoadStatus`. The clause was correct only for the carrier-portal flow where caller IS the carrier; wrong for AE Console. Carrier assignment now exclusively flows through [`tenderController.acceptTender`](backend/src/controllers/tenderController.ts#L57) which atomically sets `carrierId` + `status=BOOKED` with compliance check. **Layer 2 (backend):** added carrier-required state-machine gate — transitions INTO TENDERED / CONFIRMED / BOOKED via `PATCH /loads/:id/status` now require `existing.carrierId !== null`, returns friendly 400 if missing. `acceptTender` is unaffected since it writes Load directly and never calls `updateLoadStatus`. **Layer 3 (frontend):** suppressed status-advance button for POSTED loads in both surfaces — Load Board JSX (`load.status !== "POSTED"` condition) and shared `lib/loadStatusActions.ts` `getNextStatusAction()` returns null for POSTED so T&T LoadDetailDrawer also stops showing the button. Status-advance reappears at BOOKED ("Dispatch") onward after `acceptTender` has set carrierId. AE users now forced through the Tender modal (carrier-tender workflow) for POSTED loads. Recovery for L6894191249 left as test artifact; not worth manual DB fix. Pairs with v3.8.k (dispatch method switching UI) logged separately. | v3.8.j

---

## Deferred

- Mobile menu dropdown — not verified post-v3.7.n.1, may share same bug
- AWS Rekognition SDK fallback on prod — biometric verification using hash-based
- BOLTemplate.tsx preview pane visual mismatch with downloaded PDF — honest-label banner shipped in v3.7.o
- security-policy.html does NOT load srl-logo.css, but this is NOT the same bug as S4 on tracking.html. security-policy.html uses its own nav structure + its own 32px logo sizing rule (not the shared .srl-logo-* classes). Page renders correctly as-is. Adding srl-logo.css would be a no-op. Flagged here for future nav-unification phase consideration (converting security-policy.html to use the shared nav partial would be the right tidy-up, but is scope creep for hotfix commits).
- 2026-04-23 | CSV Import modal in Lead Hunter tab: 2 parallel text-slate-400 → text-slate-300 contrast fixes needed (same pattern as S6.a Send Outreach modal, flagged during v3.7.n.7 audit). Defer to follow-up micro-commit; not prospect-blocking, internal AE Console only. — SUPERSEDED by Phase 6 theme cleanup (see below); the root cause is the globals.css text-slate-* light-mode overrides, not the individual class names.

---

## Phase 6 — Portal Approval Workflow (post-Apollo launch)

- **S-2 (P0 policy)** — Shipper approval gate in backend (`authController.ts handleVerifyOtp`). Currently absent; any registered SHIPPER can log in immediately after OTP. Mirror carrier-auth pattern: check `customer.onboardingStatus !== "APPROVED"` → return 403 with friendly message. See `my-knowledge-base/wiki/` for intended flow (docs sparse — will need to write).
- **S-3 (P1)** — Shipper approval UI in AE Console. No current analog exists (`accounting/credit` is credit-limit management, not portal-access approval). Create a dedicated surface: `/dashboard/shippers` with an Approve button mirroring `dashboard/carriers/page.tsx:730`.
- **Credit check integration** — service TBD. Candidates: Experian Business, D&B, manual SOP. Once picked, wire into S-2 gate as prerequisite for APPROVED status.
- **Carrier self-service onboarding UI** — upload COI / W9 / Authority letter, view application status. Schema supports flags (`w9Uploaded`, `insuranceCertUploaded`, `authorityDocUploaded`) but no carrier-facing UI exists. Required to move from carrier "under review" dead-end to actionable workflow (C2 from Batch Alpha audit 2026-04-23).
- **Carrier status-check / next-steps page** for PENDING state. Currently the "under review" screen is a text-only 403 with no forward path. Needs a companion landing for PENDING carriers showing what's required to progress.
- **Shipper "application under review" UX** — once S-2 gate is added, shippers hitting the gate need the same quality of messaging. Scope alongside S-2 + S-3.
- **C3 clarification** — verify/document that `dashboard/carriers/page.tsx` Approve/Reject button role-gate (`ADMIN || CEO` only, line 730) is intentional. If OPERATIONS/DISPATCH/BROKER roles should also approve, widen the guard. If not, document the rationale in CLAUDE.md.

## Phase 6 — Theme System Cleanup

- **Root-cause fix for `[data-mode="light"] .text-slate-*` `!important` overrides in globals.css** (lines 162-173). The current scheme remaps dark-mode Tailwind text classes to dark values in light mode, which backfires on modals/panels that hardcode dark backgrounds. Options: scope the overrides to exclude dark-surface containers, OR remove the global remap and theme individual components explicitly, OR adopt a `.dark-surface` opt-in class.
- **14 candidate surfaces to re-audit after root fix ships** (from v3.7.n.7.1 audit): Send Outreach modal, CSV Import modal, MarcoPolo widget, `/onboarding/page.tsx`, `/admin/users/page.tsx`, `/shipper/dashboard/layout.tsx`, `/accounting/layout.tsx` (covers all 13 accounting routes), plus others flagged via `bg-[#1e293b]` / `bg-[#1a1f35]` / `bg-[#141a2e]` / `bg-[#1a2340]` / `bg-[#0A0B0F]` / `bg-[#0a1120]` / `bg-[#121e30]` hex usages that escape the globals.css bg-remap but still trigger the text-remap.
- **CSV Import modal parallel fixes** (already flagged above) — resolve via the root theme cleanup, not per-class touch-ups.
- **S6.a full fix** — theme root-cause resolves this; the partial v3.7.n.7 class swap is irrelevant in light mode and can be reverted or left as documentation only.
- **S6.c secondary issue** — AE Console error boundary (`frontend/src/app/dashboard/error.tsx:18` and `frontend/src/app/error.tsx:18`) renders "Something went wrong" heading with `text-white`, which gets remapped to dark via globals.css `[data-mode="light"] .text-white !important`. Result: white-on-white fallback text on the cream/light page bg. Observed 2026-04-23 during S6.c Lane Analytics crash investigation; same root cause as S6.a Send Outreach modal dim-text. Fixed together with the root theme-remap cleanup — no separate commit.

## Phase 6 — Portal + Public Page Visual Alignment (observed 2026-04-23 post-v3.7.n.7.1 verify)

- `/shipper` (singular, portal landing page at silkroutelogistics.ai/shipper) uses a distinct dark-theme visual system that diverges from the rest of the marketing site (/shippers.html, /carriers.html, /contact.html, /about.html, homepage). Specific divergences:
  - Hero "Ship Smarter with Full Freight Visibility" uses different type and layout treatment than homepage hero
  - "How Our Freight Brokerage Platform Stacks Up" comparison table has functional content but visual identity disconnected from marketing site
  - Dark theme vs. cream/light elsewhere
  - Gold accent application doesn't match canonical CANONICAL/LEGACY/SUPERSEDED bucket split from CLAUDE.md §2.1
  - Verify: is `/shipper` intended as a prospect-facing landing OR as a post-authentication upsell view? If prospect-facing, it should match the marketing site design system. If authenticated-only, it should match the portal design system (which itself needs theme alignment per existing Phase 6 Theme System Cleanup entry above).
- `/shippers.html` (plural, marketing-site page) vs `/shipper` (portal landing) — two separate pages with similar intent but different design systems. Evaluate whether both should exist; if yes, both should match the same canonical system.
- Observed during manual portal verification after v3.7.n.7.1 login regression fix. Not a bug, not a blocker, purely visual/editorial debt. Route functions correctly.
- Carrier Portal landing equivalent (`/carrier` vs `/carriers.html`) likely has same divergence — verify during Phase 6 portal visual refresh pass.

## Phase 6 — CI / Testing Gaps

- **E2E smoke tests for SHIPPER/CARRIER portal login flows.** The 2026-02-23 → 2026-04-23 regression went undetected for 2 months because no such test exists. Candidates: Cypress or Playwright. Minimum coverage: (1) valid shipper credentials → OTP → reach /shipper/dashboard without bounce; (2) same for carrier (requires approved carrier fixture); (3) invalid credentials rejected; (4) OTP expiry.
- **E2E smoke for AE Console login flow** — currently working via AuthGuard.tsx, but same class of regression possible in future store refactors. Same coverage pattern.
- **CI regression assertion** — consider a unit test that asserts `useAuthStore.token` and `useCarrierAuth.token` are never used as auth guards in any dashboard layout (grep-based lint). Would have caught the 172d6f3b regression at commit time.

## Phase 6 — Architecture & Refactors

- **Audit all `.token` / `tempToken` usage across the codebase after 172d6f3b.** Known benign latent consumers (from v3.7.n.7.1 audit): `dashboard/layout.tsx:10`, `accounting/layout.tsx:63`, `carrier/login/page.tsx:47,51` (all pass to MarcoPolo or do non-blocking redirects). Validate no new consumers accrue and decide whether to remove the dead `token` field entirely from both stores.
- **`useSessionTimeout` hook** — audit whether its redirect / state logic depends on `store.token` being populated. Not explicitly tested in v3.7.n.7.1; the hook was left untouched but deserves verification.
- **`/accounting/approvals` naming collision** — this route is for carrier payment-settlement approvals (AR), but "approvals" is frequently misread as application/portal approval. Rename to `/accounting/payment-approvals` or `/accounting/settlements-approvals` when next touched.
- **Orphan marketing pages in `frontend/public/`** — `login.html` + `register.html` at root alongside `auth/login.html` and `auth/register.html`. Decide which is canonical, redirect the other, remove duplicates.
- **Dead `.login-dropdown` CSS block in `utilities.css`** (lines 191-238) — entire alternate dropdown styling system with no HTML consumer. Likely remnant from abandoned redesign. Remove once confirmed unused.
- **Shared `/shared/css/footer.css` partial** — v3.7.n.5 ported footer rules from `carriers.css` to `tracking.css`. Cleaner long-term: extract shared footer rules into a partial loaded by all marketing pages. Scheduled for Phase 5 CSS variable consolidation per CLAUDE.md §13.
- **Consolidate `carrierAuth.ts` duplication with shared `authController.ts`** — password-expiry, TOTP, session registration are duplicated between the two. Only the approval gate is carrier-specific. Refactor candidate once portal patterns stabilize.
- **`CarrierProfile.onboardingStatus` vs. `status` enum redundancy** — two overlapping status enums on the same model. Only `onboardingStatus` gates login; `status: CarrierApplicationStatus` is tracked but unused for auth. Consolidate or document the divide.
- **Load Board "New Load" modal vs. Order Builder overlap** — Load Board "+ New Load" (`dashboard/loads/page.tsx` → `CreateLoadModal`, 4-step wizard: Route/Freight/Pricing/Review, ~15 fields) duplicates the purpose of Order Builder (`/dashboard/orders`, ~40 fields with facility lookups, PU/DEL windows, dispatch method, pricing intelligence, tender configuration). Two surfaces for one task. Observed during v3.7.n.8 S6.b verification 2026-04-23.
  - Decision required: consolidate on Order Builder as canonical order-entry, OR keep modal as a quick-capture surface with explicit "Finish in Order Builder" graduation path.
- **`sanitizeInput` middleware HTML-escape-everything pattern** — `backend/src/middleware/security.ts` runs `escapeHtml` on every string in `req.body` / `req.query` / `req.params` for XSS defense. Two side effects: (a) values like `Dry Van 53'` are stored in the DB as `Dry Van 53&#x27;`, requiring decode at every output boundary that doesn't auto-decode (PDFKit, React text nodes); (b) any developer reading the DB sees encoded values that don't match what was entered. Today the workaround is per-boundary decode (`pdfService.ts safe()` for PDFs, `trackingController.ts decodeOpt()` for public /tracking). Long-term options: (1) move sanitization to output (template engine / React already auto-escape — input-time escaping is double-escape on output anyway), (2) keep input-time but use a content-aware filter (DOMPurify) instead of blanket entity-encoding, (3) keep current middleware but add a global response interceptor that decodes on serialization. Not an Apollo blocker; deferred. Surfaced 2026-04-28 during v3.8.d /tracking encoding fix.
- **BOL multi-line page-2 dynamic rendering** — v3.8.d caps page-1 line-item rendering at 10 rows with a "+N additional line items — full manifest attached" footer note. For loads exceeding the cap, page 2+ should render the overflow with proper pagination (header/footer carried, totals on final page). Apollo-shipped loads will be 1–3 lines in practice so the cap is defensive only; tackle when actual load pattern shows >10 lines becoming common, OR when a customer specifically requests multi-page manifests on the BOL itself. Logged 2026-04-28.
  - Questions to resolve before acting:
    1. Does Order Builder handle both pre-dispatch creation AND post-dispatch editing?
    2. Do both surfaces create records via the same backend controller, or are there two fragmented data paths?
    3. Which surface does Wasi actually use in daily ops?
  - Not a bug — both flows currently work. Logged as architectural / UX consolidation debt for Phase 6 post-Apollo launch.
- **No "Edit Load" UI for converted orders** — surfaced 2026-04-29 during v3.8.d.4 BOL audit. Backend has the capability: [`PUT /loads/:id`](backend/src/routes/loads.ts#L37) calls `updateLoad` ([loadController.ts:706](backend/src/controllers/loadController.ts#L706)) and accepts `createLoadSchema.partial()` (any field). Authorized for BROKER/ADMIN/CEO/DISPATCH. Frontend has zero callers — only `PATCH /loads/:id/status` (status advancement) and `/carrier-status` are wired. No "Edit details" button on the Load Board row, no edit modal anywhere. The Order Builder's converted-order banner (v3.8.d.3) explicitly says "Editing here will not change the dispatched load" — true, because the form writes to `order.formData`, never to the live Load. Practical consequence: if a customer changes a PO, weight, pickup window, or commodity after conversion, ops staff has no UI path to update the load; today's only options are cancel + recreate, direct API call via dev tools, or DB edit. Phase 6 sprint candidate: **EditLoadModal** that mirrors Order Builder fields, calls `PUT /loads/:id`, gates by status (e.g. only fully editable up to `LOADED` — once `IN_TRANSIT`, fields like origin/dest shouldn't change because the carrier is executing), and writes back to the source `order.formData` so re-printed BOLs stay in sync. Effort estimate: 100-200 lines, single sprint.
- **Track & Trace has no load-status advancement controls** — surfaced 2026-04-29. Backend permits the transition (`PICKED_UP → IN_TRANSIT` is in [VALID_TRANSITIONS:435](backend/src/controllers/loadController.ts#L435), and `PATCH /loads/:id/status` is the wired endpoint). The Load Board uses it: `STATUS_ACTIONS["PICKED_UP"] = "In Transit"` renders a button on the row ([page.tsx:98](frontend/src/app/dashboard/loads/page.tsx#L98)). The Track & Trace module ([dashboard/track-trace/](frontend/src/app/dashboard/track-trace/)) has 7 tabs (Details / Tracking / Activity / Check Calls / Docs / Photos / Exceptions / Finance) but **zero `api.patch('/loads/:id/status')` calls** — the only status-touching write is `POST /check-calls` with hardcoded `status: "IN_TRANSIT"`, which sets the *check-call's* status field, not the load's. So today, advancing PICKED_UP → IN_TRANSIT (or any in-flight status transition) requires bouncing back to the Load Board. Phase 6: add a status-advancement control to the T&T LoadDetailDrawer header — same `updateStatus` mutation as the Load Board, gated by `VALID_TRANSITIONS`. Small change, big UX win for dispatchers who spend their day in T&T.
- **CRM facility row missing Edit action** — surfaced 2026-05-02 from CRM walkthrough screenshot of Beekeepers Naturals USA Inc. with two facilities (Pharmaloz, Mainfreight North Lake). Each facility row shows ONLY a "Remove" button. Once a facility is created, AE has zero UI path to update name, address, contact, dock info, primary flag, lumper info, special instructions, load type, or any other field — only delete-and-recreate. Backend is fully wired: `PUT /customers/:id/facilities/:facilityId` exists at [`backend/src/routes/crmCustomer.ts:138`](backend/src/routes/crmCustomer.ts#L138) with 404-guard via `findUnique` then `prisma.customerFacility.update`. Frontend [`FacilitiesTab.tsx`](frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx) wires `useQuery` for read, `del` mutation for DELETE, and the Add form for CREATE — but zero update mutation, zero edit-mode UI. Same architectural pattern as v3.8.j tender workflow + §13.3 Item 8.3 notification routing + 8.2.2 operating-hours form gap (just logged): backend complete, UI incomplete. Tightly coupled with 8.2.2 — both touch the same component and both need the form to be shared between create + edit modes. Sprint shape: ship 8.2.2 + 8.2.3 as ONE combined sprint — refactor the existing add-facility form into a Create/Edit shared form, wire `PUT` mutation, add Edit button alongside Remove on each row, support pre-populating fields from existing JSON (including the new operatingHours inputs from 8.2.2). Combined effort: ~120-180 lines, single file. Splitting them risks two passes through the same component. Logged to §13.3 Item 8.2.3.
- **CRM facility form missing operating-hours inputs** — surfaced 2026-05-02 from CRM walkthrough screenshot of Beekeepers Naturals USA Inc. customer profile. `CustomerFacility.operatingHours: Json?` exists in schema ([prisma/schema.prisma:2053](backend/prisma/schema.prisma#L2053), JSON shape `{ monday: { open, close }, ... }` keyed by weekday) but the CRM facility-creation form ([frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx](frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx)) has zero references to operatingHours — the field is never written. AE creates facilities with name + address + contact + dock info + primary flag + lumper info + special instructions, but no hours. **Prerequisite for §13.3 Item 20** (Order Builder G facility-hours auto-populate) — that item reads operatingHours to auto-fill PU/DEL window times, but the field is null on every existing facility because the form doesn't capture it. Same architectural pattern as v3.8.j tender workflow and §13.3 Item 8.3 notification routing (data plumbing exists, UI doesn't connect). Sprint shape: 7-day input grid (Mon-Sun) with open/close time inputs per day, "Closed" toggle per weekday, optional "24/7" toggle for DCs; serialize to JSON; read existing JSON in edit mode. ~80-120 lines, single file (FacilitiesTab.tsx). Pairs with Item 20 — could ship as a duo. Logged to §13.3 Item 8.2.2.
- **`sales@silkroutelogistics.ai` alias documentation gap** — surfaced 2026-05-02 during v3.8.m crawlability lockdown audit. The address is referenced in [`frontend/public/ae/communications.html:71`](frontend/public/ae/communications.html#L71) as the inbound prospect/lead-reply recipient, but is not documented in CLAUDE.md §1's canonical email list (which currently lists `whaider@`, `accounting@`, `compliance@`, `noreply@`, `operations@`). Same pattern as the 2026-05-01 `operations@` documentation gap that was caught + fixed. Sprint shape: tiny docs sprint (~5 lines in CLAUDE.md §1) — decide whether to (a) canonicalize `sales@` parallel to `operations@` with current routing target (`→ whaider@` until first hire is the likely answer), OR (b) collapse references to use `operations@`, OR (c) collapse to `whaider@`. Logged to §13.3 Item 8.2.1. Tiny sprint, no urgency, slot whenever convenient. Deferred per 2026-05-02 directive — explicitly not rolled into v3.8.m or v3.8.n.
- **v3.8.l — Customer inactivation workflow** — surfaced 2026-05-01 audit. Customer model has FOUR orthogonal status-like fields (`status` free-form string default "Active", `onboardingStatus` enum with SUSPENDED, `creditStatus`, linked `User.isActive`) — no canonical "Inactivate Customer" action. CRM Profile editor edits free-form `customer.status` but it's label-only, not a gate. Flipping `onboardingStatus=SUSPENDED` blocks SHIPPER-portal login (v3.8.e.1) but no CRM UI exposes the field. Critical gap: **no path prevents AE from creating new loads against an inactive customer** — Order Builder + convert-to-load skip all customer-status checks. Sprint shape: new schema fields `customer.isActive: Boolean` + `inactivationReason: String?` + `inactivatedAt: DateTime?` + `inactivatedById: String?`; canonical "Inactivate Customer" UI action that (a) blocks new load creation with ADMIN/CEO override, (b) silences notifications, (c) optionally cascades to `User.isActive=false` and/or `onboardingStatus=SUSPENDED`, (d) renders visible CRM banner with reason, (e) writes audit log entry. ~150-200 lines + 1 schema migration. Pairs naturally with Item 6 (Portal Approval UI S-3) since both touch customer-status workflow.
- **Cancelled loads tab on Load Board** — surfaced 2026-05-01. Cancelled loads stay in DB (status='CANCELLED', not soft-deleted) but Load Board UI hides them entirely — `StatusTab` enum lacks `"CANCELLED"`, default `activeOnly=true` filter at `loadController.ts:365` excludes them server-side. Common ops questions like "what was the cancellation reason for L1234?" require API/DB access. Sprint shape: extend `StatusTab` to include `"CANCELLED"`, add tab button with count, when active drop `activeOnly=true` and pass `?status=CANCELLED` to backend; surface `cancellationReason` field on row. ~30-50 lines, single file (loads/page.tsx). Backend already supports the query.
- **Wire shipper notifications through `CustomerContact[]` with role-based routing** — surfaced 2026-05-01 during BKN email recipient audit. Both `shipperLoadNotifyService` and `shipperNotificationService` bypass `CustomerContact[]` entirely; they resolve recipients from `Load.contactEmail` (priority) then `Customer.email` (fallback) — both single-field columns. CRM multi-contact list (with role tags like AP/Logistics/Procurement and `doNotContact` flag) is decoupled from the notification path. Net effect: AE can maintain a careful multi-contact CRM list and **none of those rows affect who actually receives shipment notifications** — exactly the symptom Wasi observed where BKN's AP email got load notifications despite not being in CRM contacts. Same architecture bug class as v3.8.j (data plumbing exists, UI/code don't connect). Sprint shape: contact-resolution helper that routes by role tag (pickup/transit/delivery → OPERATIONS/LOGISTICS contacts; invoice → AP/BILLING contacts; quote/RFP → PROCUREMENT contacts); fall back to `Customer.email` only when no role-tagged contact exists; honor `doNotContact` per row. Touches both notification service files + 1 new helper. ~150-200 lines, single sprint.
- **v3.8.k — Dispatch method switching UI** — surfaced 2026-04-30 alongside v3.8.j tender-workflow audit. Backend has the plumbing for switching a Load's `dispatchMethod` between `waterfall` / `loadboard` / `direct_tender` / `dat` after creation: the field is writable, `PUT /loads/:id` accepts the partial update, and a `dispatch_method_changed` event type already exists in [`waterfallEventService.ts:26`](backend/src/services/waterfallEventService.ts#L26) suggesting the team anticipated this. Frontend has zero callers — once a load is converted via Order Builder's 4-button picker, dispatch method is locked for the load's lifetime with no UI to change it. Operations gap: when chosen method isn't producing results (waterfall has no eligible carriers — exactly the symptom Wasi saw on L6894191249's "No eligible matches" banner; or a direct-tender carrier declined; or DAT post yielded no bids), AE has no UI path to switch dispatch method without a DB edit or full cancel+recreate. Sprint shape: dropdown action on Load Board row (or in load detail drawer) with confirmation dialog ("This will cancel N pending tenders. Continue?"). Backend handles atomic side effects per direction: (a) **Loadboard → Waterfall** — set `dispatchMethod='waterfall'` + `visibility='waterfall'` (hides from open loadboard) + `buildWaterfall(loadId, { mode: 'full_auto' })` + `startWaterfall(wf.id)`; decision needed whether to cancel any OFFERED/COUNTERED tenders from the loadboard era. (b) **Waterfall → Loadboard** — set `dispatchMethod='loadboard'` + `visibility='open'` + halt active waterfall (mark WaterfallRun halted, cancel pending OFFERED tenders to stop their 20-min windows ticking); load now visible on open Load Board to all approved carriers. (c) Same shape for direct_tender ↔ any, dat ↔ any. ~150-200 lines, single sprint. Pairs naturally with v3.8.j (tender-workflow consolidation) since both are post-conversion dispatch lifecycle controls — could ship as a paired duo.
- **Convert-to-load bypass of `loadController.createLoad`** — caught during v3.8.c Order Builder integration. [routes/orders.ts:242-338](backend/src/routes/orders.ts#L242-L338) writes directly via `prisma.load.create` instead of calling the controller, which required duplicating field-mapping + line-item validation logic. Short-term fix in v3.8.c: exported `buildLineItems` and `LineItemCreateInput` from `loadController.ts` and imported them in `routes/orders.ts`. Long-term refactor: consider routing convert-to-load through `loadController.createLoad` (would need to thread order-state-transition side effects — `prisma.order.update` status to `load_created`, `logLoadActivity`, `logCustomerActivity`, `createCheckCallSchedule`, waterfall build/start, tracking-link fan-out — through the controller or a post-hook system). Also check whether other Load write paths (`shipperPortalController:816`, `emailToLoadService:470` — per v3.8.a schema audit) have the same bypass pattern and need similar updates as line-item support rolls forward. All three direct-write paths currently accept the v3.8.c lineItems only via the `convert-to-load` route; the other two still rely on flat fields.
- **Routing consolidation (2026-05-03 v3.8.q)** — deleted four divergent / orphan pages and added redirects to canonical destinations. Architecture decisions per memory #29: HTML `/shippers.html` is canonical shipper marketing surface; React `/track` is canonical tracking surface (token-based, v3.8.i.1-hardened, BOL QR target); `/auth/*` is canonical auth flow. Deletions: [`frontend/src/app/shipper/page.tsx`](frontend/src/app/shipper/page.tsx) (parent landing only — sub-routes `/shipper/{login,dashboard,forgot-password,reset-password,register}` unaffected; their `page.tsx` files in segment subdirectories continue to resolve), [`frontend/public/tracking.html`](frontend/public/tracking.html), [`frontend/public/login.html`](frontend/public/login.html), [`frontend/public/register.html`](frontend/public/register.html). Cloudflare Pages `_redirects` entries added: `/shipper → /shippers.html`, `/tracking.html → /track`, `/tracking → /track`, `/login.html → /auth/login`, `/register.html → /auth/register` (all 301). Marketing nav + footer regenerated from `frontend/src/lib/site-chrome.json` (single source-of-truth) via `inject-chrome.mjs` prebuild — Track entries on every marketing page now point at `/track`. `sitemap.xml` updated: `/tracking.html` entry repointed to `/track`, `/login.html` and `/register.html` entries removed. Code-comment update at `trackingController.ts:186` repointed broken consumer reference from `public/tracking.html:300` to `frontend/src/app/track/page.tsx`. Internal logo back-links on `frontend/src/app/shipper/register/page.tsx:64,94` repointed from `/shipper` to `/shippers.html` for cleanliness (would have resolved through redirect either way). Closes §13.3 Item 9 (shipper landing divergence) and the deferred F5 tracking-overlap item. **Asymmetry note:** the `/login.html → /auth/login` redirect points at the React route while the existing static `/auth/login.html` continues to serve traffic. This creates a UX asymmetry (some login traffic hits React, some hits static HTML). The HTML→React migration for `/auth/login` is a future Phase 6 candidate to be logged separately after this sprint ships. **Build artifact note:** the `inject-chrome.mjs` regeneration produced ~13 file diffs alongside the source-of-truth edit in `site-chrome.json` — this is the marketing-content architecture's normal behavior (the partials and injected regions in marketing pages are generated outputs of the prebuild script), not unexpected drift.
- **`audit-completeness.ts` orphan-endpoint Tier C triage corrected (2026-05-02)** — Phase A read-only verification of the four endpoints initially bucketed as Tier C ("internal-only fired, no UI needed") found ZERO callers across `backend/src/` and `frontend/src/` for all four, falsifying the internal-only-fired hypothesis. Reclassified to Tier A and logged to CLAUDE.md §13.3 as Items 8.5 (`PATCH /address-book/:id/use` — frequent-addresses wire-up), 8.6 (`PUT /ai/preferences/:carrierId` — carrier preference manual override admin UI; auto-learner at `carrierPreferenceService.ts:188` writes Prisma directly and bypasses the HTTP endpoint, which is exactly why no caller exists), and 8.7 (`POST` + `DELETE /tags/assign` — manual tag-management UI; both endpoints kept under Option A, the underlying `assignTag` / `removeTagAssignment` service functions are used internally by the `autoTagEntity` rule engine at `tagService.ts:80` and must NOT be removed regardless of HTTP endpoint dormancy). `PATCH /shipments/:id/location` maps to existing Phase 5E.c Decision 4.1 (ELD integration deferred), no new §13.3 entry created. Lesson captured in updated `audit-completeness.ts` header comment: "no caller found" is a triage signal, not an auto-classification — every audit finding requires human verification of caller intent before bucketing. Docs-only commit, zero code paths changed, orphan count remains 27 (no audit-suppression comments applied).

## Phase 6 — Order Builder UX Polish (deferred from v3.8.c 2026-04-24)

All three flagged during v3.8.c visual polish pass. Deferred to post-Apollo work stream pending product decisions on edge cases (closed days, catalog size, override semantics, UI indicators for auto-suggested values).

- **F — PU/DEL window time inline with dates.** Current Route section renders pickup-date + pickup-window-start + pickup-window-end across two rows (date on one row, windows on another). AE requested date + window on one row, with delivery matching pattern. Layout restructure in [page.tsx Route section] required — moderate diff. Also need to confirm with designer whether windows stay as raw HH:MM inputs or switch to dropdown/picker (Wasi mentioned "there must be drop down option as well" in the 2026-04-24 thread — unclear whether that's a preset-list dropdown or a time picker component).
- **G — CRM facility operating-hours auto-populate.** When origin/dest facility is picked via `FacilityPicker`, read `CustomerFacility.operatingHours` (JSONB with per-weekday open/close) for the pickup/delivery weekday (derived from `pickupDate` / `deliveryDate`) and auto-fill `pickupTimeStart` / `pickupTimeEnd` (and delivery pair). Product decisions needed: closed-day UI (block or warn?), missing-hours fallback (leave blank vs. default window?), override semantics (lock after manual edit vs. re-overwrite on facility change?), and whether the user-visible indicator for "auto-populated from facility hours" should be distinct from existing "Auto-filled" badges.
- **H — NMFC + freight-class auto-suggest on line items.** Two sub-items:
  - **H1 — Commodity description → NMFC lookup.** Build a small `COMMODITY_NMFC_MAP` in `types.ts` similar to existing `COMMODITY_CLASS_MAP`. Product decision: initial catalog size (common freight classes only, or broader NMFC catalog import?) and override semantics (mirror the "clears on manual edit" pattern from freight-class auto-suggest).
  - **H2 — Density-based freight class.** Real LTL logic: `density = weight ÷ (L × W × H ÷ 1728 cubic feet)` → map to class bracket (e.g., density > 30 → 50; 22.5–30 → 55; … 1–2 → 400; <1 → 500). Only fires when all four inputs (weight + L + W + H) are filled. Product decision: density-based takes priority over description-based or vice versa? UI indicator for which signal drove the suggestion?
  - Neither is a correctness issue — current description-based class suggester covers the common case. These are precision enhancements for AE power users.

## Phase 6 — Content / Editorial

- 2026-05-02 | SRAPP rename audit + skill alignment. Phase A audit confirmed customer-facing rename was completed at v3.7.a (`cdf1f3b`) — `frontend/public/carriers.html` has 13 references to "Caravan Partner Program" and zero to "SRAPP", `faq.html` has 5 + 0, and zero references exist anywhere in `frontend/src/`, `backend/src/`, or any template (BOL, Invoice, Rate Confirmation, email). Skill bundle aligned in two atomic commits: voice.md row 70's "Q4 2026+" caveat struck and replaced with "Legacy name (renamed 2026-05-02)" framing; SKILL.md line 134's canonical-naming-convention parenthetical updated to use "Caravan Partner Program" in place of "SRAPP". Internal working docs (`docs/COMPETITIVE_BENCHMARKS.md`, `my-knowledge-base/raw/COMPETITIVE_BENCHMARKS.md`, KB wiki contradiction flag, lint reports, session logs) preserved as historical record — captured the world as it was when written. Code identifiers (variable names, function names, route paths, enum values, API params) — none exist; no SRAPP-named identifiers in `frontend/src/` or `backend/src/`. evals.json eval prompt left as-is intentionally — exercises the skill's ability to map SRAPP legacy-name references to "Caravan Partner Program" output, which is the correct test scope. CLAUDE.md §7 prohibited-variants list (`"SRAPP" (fully retired)`) preserved as the binding rule that enforces the rename.

- Shippers page "BY THE NUMBERS / Performance That Speaks" section decision — (a) delete, (b) replace with capability block, (c) replace with credentials block. Do NOT fabricate performance metrics. (Duplicate of P2 UX improvements item above; consolidate here when editorial pass begins.)
- Shippers page quote form: city/state vs. full-address UX tradeoff — friction vs. data richness. (Duplicate of P2 UX improvements item above.)
- Page length / density review — marketing pages currently feel long with extra imagery; trim candidates TBD.
- "Get a Free Quote" copy clarity — unclear what info the prospect provides or what they get back. (Duplicate of P2 UX item above.)

## Phase 6 — Security & Brand

- 2026-05-03 | Security policy public/private split (v3.8.r). The previous public document at [`frontend/public/security-policy.html`](frontend/public/security-policy.html) disclosed implementation specifics — JWT algorithm name (HS256), bcrypt cost factor (12), exact rate-limit thresholds (5/15min login, 8/15min OTP, 5 OTP attempts before lockout, 5/hr password change), exact session timing (30/60-min timeout with 2-min warning), AES-256-GCM at the application layer, SHA-256 for OTP hashing, and the named vendor stack with services per vendor (Cloudflare Pages = frontend, Render = API, Neon = DB, Resend = email). Collectively this content mapped attack surface for any reader and constrained SRL's implementation flexibility — bcrypt cost can't be raised quietly when the public doc commits to "12 rounds", rate-limit thresholds can't be tuned when the public doc names exact attempt counts, and the vendor stack disclosure enables "hit Render API directly to bypass Cloudflare WAF" attack patterns. Industry-standard practice publishes a capability commitment publicly and shares configuration details under NDA. Fix: detailed content archived to [`docs/internal/SECURITY-DETAILED.md`](docs/internal/SECURITY-DETAILED.md) (committed in repo for internal reference, outside the Cloudflare Pages deploy scope so not publicly accessible by virtue of deploy-path boundary; robots.txt `Disallow: /docs/internal/` + `/internal/` added as defense-in-depth). Public document replaced with a ~280-word commitment overview structured as 5 sections — Our Security Commitment, Capabilities (capabilities listed at the WHAT level, not the HOW level: "MFA for all accounts" instead of "OTP via email + AES-256-GCM TOTP secrets with 8 backup codes"; "encrypted data storage" instead of "AES-256-GCM at app layer for tax IDs / insurance policies"), Regulatory Compliance (FMCSA + CCPA + PCI + CAN-SPAM, public regulatory facts), Detailed Documentation Available on Request (NDA-gated path with `compliance@` mailto), Contact. RBAC role table dropped entirely from public per Q2 clarification — replaced with one sentence ("least-privilege defaults, detailed role definitions available under NDA"). Data Classification table dropped entirely from public per Q3 — capability-level "encrypted data storage" is sufficient. Typography migrated to canonical tokens (Playfair Display + DM Sans) per CLAUDE.md §2.1 + Q1, departing from the existing doc-page family's Plus Jakarta Sans + DM Serif Display pattern (privacy.html and terms.html still use the legacy stack — same migration not applied; out of scope for this commit). Layout structure preserved (nav-bar, doc-container, doc-header gradient card, doc-meta strip, section cards, back-link). Sitemap entry retained — same URL, different content. No anchor-link migrations needed: Phase A audit confirmed all 14 inbound references (10 hard-coded marketing-page footers + chrome-injected footer.html + sitemap + site-chrome.json) link to the top-level `/security-policy.html` URL with zero anchor-link references; the URL stays valid so no inbound updates needed. No customer data ever exposed — the disclosure was about implementation mechanics, not user data. Risk profile was attack-surface-mapping for hypothetical future intrusion attempts, not an active breach. Surfaced 2026-05-03 by Wasi during marketing-page audit. Future audit pass needed: scan other public pages (privacy.html, terms.html, security-policy.html siblings) for similar disclosure issues — initial spot-check shows privacy.html is operationally clean. | v3.8.r

- **§11 narrative summary update — 11 versions behind HEAD.** CLAUDE.md §11 currently terminates at v3.8.e.2 (`adefc84`); HEAD has shipped through v3.8.q. Versions f through q are not narrated in §11. Catch-up scope: read `git log` between `adefc84..HEAD`, write §11 entries for each shipped letter, preserving the existing one-row-per-version format. Effort: ~30-60 min. Tracked separately so it doesn't inflate the v3.8.r security-split commit. Logged here from v3.8.r Sprint 0 directive bonus-finding queue. Will be folded into a dedicated docs commit, not a feature commit.

- **Audit other public pages for similar disclosure exposure.** v3.8.r addressed `security-policy.html`. Sibling docs `privacy.html` and `terms.html` (and any other public marketing page) need a parallel pass to confirm they don't disclose comparable operational specifics. Initial spot-check during v3.8.r showed privacy.html is mostly user-facing data-rights content, not infrastructure disclosure — but a deliberate audit is the right move now that we've established the public/private boundary. Effort: 30-60 min audit + scoped fix commit if anything is found.

## Phase 6 — Brand & Readability

- 2026-05-03 | Sprint 3 (v3.8.t) — fixed 2 real P0 contrast findings surfaced by Sprint 2 brand-conformance scanner + Sprint 3 Phase A static cascade spot-checks. **(a) Marketing footer chrome/CSS structural mismatch.** Phase A confirmed [`_partials/footer.html`](frontend/public/_partials/footer.html) emits `<h5>` + direct `<a>` children of `.footer-col`, but page-CSS files ([about.css:900](frontend/public/shared/css/pages/about.css#L900) and siblings: blog.css, careers.css, contact.css, faq.css) carry stale rules targeting `<h4>` + `<ul><li><a>` structure that no longer matches. Footer text falls through to body color cascade (`#1A1A2E` from `body { color }` declarations in each page CSS) on top of navy `var(--navy)` background — invisible navy-on-navy `<p>`/`<h5>` text. Browser-default link-color likely renders `<a>` elements as `-webkit-link` blue (visible but ugly), so the readability failure is concentrated in `<p>` tagline/contact text and `<h5>` column headings. Fix shape: additive chrome-aligned rules added to [`utilities.css`](frontend/public/shared/css/utilities.css) — new tokens `--fg-on-navy: #FBF7F0` and `--fg-on-navy-2: #C9D2DE` per CLAUDE.md §2.1 added to utilities.css `:root`; new selectors target the chrome HTML structure directly (`.footer-col h5`, `.footer-col > a`, `.footer-brand > p`, `.footer-brand a`, `.footer-bottom > p`, `.footer-bottom-links a`) with explicit `color: var(--fg-on-navy-2)` for body text and `color: var(--gold)` for column headings. Hover state goes to `var(--gold)`. Stale page-CSS rule cleanup deferred — additive utilities.css selectors don't conflict with stale page-CSS selectors (different selector shapes — `.footer-col > a` vs `.footer-col ul li a`) so dead-letter cleanup is cosmetic, not load-bearing for the readability fix. **(b) `.btn-primary` gold-on-white contrast.** Phase A confirmed 13 occurrences across 10 AE accounting/compliance CSS files all rendering `background: var(--gold)` (`#C8963E`) + `color: var(--white)` (`#FFFFFF`) = 2.66:1 ratio. Fails WCAG AA (4.5:1 body, 3:1 large text) AND fails WCAG UI Components (3:1). Fix shape: surgical color swap `var(--white)` → `var(--navy)` (`#0D1B2A`) in every `.btn-primary` block. Navy-on-gold = ~9:1, passes AA + AAA + UI. Files touched: accounting-fund.css, accounting-payable.css, accounting-receivable.css, accounting-reports.css, claims.css, compliance-alerts.css, compliance-carrier.css, compliance-dashboard.css, crm.css (all single-line bulk via sed), plus caravan.css, compliance-overview.css, loads.css, create-load.css (multi-line via Edit). Canonical `.btn-primary` rule also added to [`console.css`](frontend/public/ae/css/console.css) as single-source-of-truth for new consumers — existing per-page duplications retained intentionally (consolidation refactor deferred to separate commit since the contrast fix is delivered surgically without touching duplicates' layout properties). Sprint 2 scanner false-positives (alpha-overlay artifacts in spot-checks 3+5; orphan `tracking.css` in spot-check 2) NOT touched in this commit per Phase A finding — those need separate scanner v2 alpha-overlay handling before Sprint 4 P0 sweep. Page title sentence-case skill-rule clarification still pending separate docs commit. | v3.8.t

- **Marketing footer page-CSS dead-letter rule cleanup** — surfaced 2026-05-03 v3.8.t Phase A. about.css [lines 855-958](frontend/public/shared/css/pages/about.css#L855-L958), blog.css, careers.css, contact.css, faq.css all carry footer rules targeting an obsolete `<h4>` + `<ul><li><a>` HTML structure that the chrome partial no longer emits. v3.8.t added chrome-aligned additive rules to utilities.css that deliver the readability fix; the stale page-CSS rules don't conflict (different selector shapes) and can be deleted in a follow-up cleanup commit. Effort: ~5 page CSS files × 100 lines each removal. Coordinate with the existing utilities.css block at [lines 271-336](frontend/public/shared/css/utilities.css#L271-L336) which has its own stale footer rules from before the chrome partial was built — those should also be removed in the same cleanup. Pure cleanup, no rendering change expected.

- **`.btn-primary` per-page duplication consolidation** — surfaced 2026-05-03 v3.8.t Phase A. 10 AE accounting/compliance CSS files have their own `.btn-primary` rule (mostly identical, except create-load.css has 10/22 padding + 14px font-size as page-specific override). v3.8.t fixed contrast surgically without touching duplications. Consolidation refactor: remove the 8-9 identical .btn-primary blocks (each ~10 lines) and let pages inherit from console.css's canonical rule; for create-load.css, replace the full duplicate with override-only declarations (`padding: 10px 22px; font-size: 14px;`). Effort: ~10 files × ~10 line removal. Pure cleanup, console.css canonical rule already in place from v3.8.t.

- **Brand-conformance scanner v2: alpha-overlay handling** — surfaced 2026-05-03 v3.8.t Phase A. Sprint 2 scanner v1 strips alpha and treats `rgba(R,G,B,α)` as solid `#RRGGBB` for contrast computation. This produces false positives on ALL alpha-overlay patterns (e.g., `.sidebar-nav a:hover { bg: rgba(255,255,255,0.05); color: #FFFFFF; }` reports as 1.00:1 when the real rendering is high-contrast white-on-mostly-navy). Spot-checks 3 (AE sidebar :hover) and 5 (.alert-badge-red red-on-red) were both alpha-stripping false positives. Likely 80%+ of the 516 P0s reported by v1 are this class. v2 fix: composite alpha over the parent bg before contrast math — walk up the cascade to find the parent's resolved bg, mix the rgba over it (R = αR_overlay + (1-α)R_parent), compute contrast against the composited color. Companion improvement to flag in scanner output: detect orphan CSS (no consumer .html or .tsx loads it) — spot-check 2 found tracking.css is dead code post-v3.8.q. Effort: small commit scoped to scanner. Recommended timing: before Sprint 4 P0 sweep so the re-run produces actionable findings rather than mostly false positives.

- **Orphan `tracking.css` cleanup** — surfaced 2026-05-03 v3.8.t Phase A. v3.8.q deleted `frontend/public/tracking.html` but left [`frontend/public/shared/css/pages/tracking.css`](frontend/public/shared/css/pages/tracking.css) — no HTML page loads it now (the `/track` route is the React page at [frontend/src/app/track/page.tsx](frontend/src/app/track/page.tsx)). Per CLAUDE.md §3.7 "Delete before you add" — orphan CSS file is dead code. Effort: 1-line file delete. Sweep for other v3.8.q dead CSS at the same time.

- 2026-05-04 | Sprint 10 (v3.8.dd) — bulk React/Tailwind P0 sweep. Mirrors v3.8.t / v3.8.u / v3.8.y CSS-side methodology applied to .tsx components surfaced by scanner v5 (commit `a996142`). Phase A triage of 379 v5 Tailwind P0 findings classified them into REAL fixes (~24) vs V6-FP class (~320 deferred to scanner v6 JSX tree walking). **Note on version letter:** directive targeted next-available; v3.8.dd is next after v3.8.cc (Lead Hunter outreach quality sprint v3.8.v–v3.8.cc landed in parallel) per CLAUDE.md §3.1 sequence rule. **Phase A scope correction:** directive's framing of "/track + Lead Hunter modal contrast issues" was based on Sprint 9's projection of these as real-fix surfaces. Phase A inspection of [`frontend/src/app/track/page.tsx`](frontend/src/app/track/page.tsx#L59) confirmed the page wrapper is `<div className="min-h-screen bg-[#0f172a] text-white">` — entire page renders on dark navy ancestor. ALL Tailwind P0 findings on /track and Lead Hunter (`bg-white/N + text-white`, `bg-{color}-500/α + text-{color}-400`, status pills with translucent bg) are V6-FP class — translucent overlays composite over the dark wrapper in real rendering producing high-contrast text. Scanner v5's same-element-pairing simplification can't see the dark ancestor, reports them as `#FFFFFF + #FFFFFF = 1.00:1`. Changing `text-white` to dark-on-dark-ancestor would CREATE contrast bugs. NO Sprint 10 fixes applied to /track or Lead Hunter — they're already rendering correctly modulo scanner blind spot. **(a) Cluster T1 — solid gold CTA + white text (~12 fixes, 7 files).** `bg-[#C8963E]` (canonical gold) and `bg-[#C9A84C]` (legacy gold variant) + `text-white` on solid CTA buttons — direct contrast math (no overlay), reports 2.29-2.66:1 fail. Bulk sed: `text-white` → `text-[#0A2540]` (canonical SRL navy). Navy-on-gold ≈ 9:1 (passes AA + AAA). Files: [accounting/disputes](frontend/src/app/accounting/disputes/page.tsx), [accounting/fund](frontend/src/app/accounting/fund/page.tsx), [accounting/reports](frontend/src/app/accounting/reports/page.tsx), [carrier/dashboard/compliance](frontend/src/app/carrier/dashboard/compliance/page.tsx), [carrier/dashboard/messaging](frontend/src/app/carrier/dashboard/messaging/page.tsx), [dashboard/carriers](frontend/src/app/dashboard/carriers/page.tsx), [ShipperChatbot](frontend/src/components/shipper/ShipperChatbot.tsx). Mirrors v3.8.t / v3.8.u / v3.8.y CSS-side gold-CTA fix shape. **(b) Cluster T1b — broken bg-gray-50/100 + text-white form inputs (~12 fixes, 2 files).** UI BUG class, NOT a v6-FP — opaque light gray bg + white text renders ~1.05-1.10:1 invisible regardless of parent. Pattern shows up in form inputs/selects in `dashboard/claims/page.tsx` (lines 157, 164, 173, 261, 263, 268, 271, 288, 291) and `dashboard/carriers/page.tsx` (lines 559, 562, 569). Sed-replace `text-white` → `text-slate-700` on lines containing `bg-gray-50` or `bg-gray-100`. slate-700 on gray-100 ≈ 9:1 (passes AA body + AAA). The bug likely originated as text-white copy-pasted from a dark-themed page without updating fg when bg switched to light. **(c) Cluster T-edge — composited gold-on-pale-gold: VERIFIED V6-FP, no fix applied.** All occurrences (~14 across `dashboard/loads`, `dashboard/carriers`, `dashboard/dock-scheduling`, `dashboard/phone-console`, `dashboard/scorecard`, `shipper/dashboard/tracking`, `carrier/dashboard/messaging`) use `bg-[#C9A84C]/10` or `bg-[#C8963E]/20` overlays + `text-[#C9A84C]` or `text-[#C8963E]` text on dashboard pages. Dashboard layout uses `var(--srl-bg-base)` which resolves to dark navy in default mode. Real rendering: gold-tinted dark navy bg + gold text = ~5:1 PASS. Scanner v5 same-element-pairing composites overlay against `#FFFFFF` default (not the actual dark ancestor), reports false positive. **Gold canonicalization** (`#C9A84C` → `#C5A572` or `#BA7517` per CLAUDE.md §2.1) logged as separate backlog item per directive scope confirmation #1 — out of Sprint 10 atomic-commit scope. **Pre-commit verification** clean per §3.3 (backend `tsc --noEmit` + frontend `next build`). **Re-run scanner v5 expected delta:** 595 → ~571 P0 (24 real fixes verified eliminate). Remaining ~571 P0 breakdown: ~150 CSS-side residue (cross-file cascade limitations + themed sidebar variants), ~320 Tailwind v6-FP class (translucent overlays on dark ancestors — require scanner v6 JSX tree walking), ~50 edge cases (modal scrims, hover-state ambiguity). React/Tailwind contrast surface comprehensively closed within v5 scanner's same-element-pairing scope. Per-page critical pass sprints (about/carriers/shippers/index narrative rewrites) now sit on a fully-audited visual foundation. | v3.8.dd

- **Gold canonicalization sweep** — surfaced 2026-05-04 Sprint 10 Phase A. Two non-canonical gold literals appear across React .tsx surfaces: `#C9A84C` (legacy gold variant, ~9 occurrences across carrier/dashboard, dashboard/carriers, ShipperChatbot, etc.) and `#C8963E` (canonical pre-Sprint-10 gold matching CSS-side `var(--gold)`, ~5 occurrences on accounting CTAs). Per CLAUDE.md §2.1 the canonical SRL gold is `#C5A572` (`--gold`) for structural use and `#BA7517` (`--gold-dark`) for emphasis/CTA. The codebase has accumulated three different gold hex variants (`#C8963E`, `#C9A84C`, plus the canonical `#C5A572`). Gold canonicalization sprint shape: bulk sed across .tsx of `bg-[#C9A84C]` → `bg-[#BA7517]` (CTA emphasis) and `bg-[#C8963E]` → `bg-[#BA7517]` (same role); same for `text-[#C9A84C]` → `text-[#BA7517]` etc. Effort: ~30-40 occurrences across ~15 files, ~10 LOC of fixes. Defers cleanly because the contrast fix in Sprint 10 is sufficient for readability — canonicalization is a brand-token-discipline concern, not a contrast concern.

- 2026-05-04 | Sprint 8 (v3.8.y) — bulk P0 sweep across 5 contrast clusters, comprehensively closing the CSS-side surface. Mirrors v3.8.t / v3.8.u methodology — surgical color swaps across shared component patterns surfaced by scanner v4 trusted baseline (commit `a0d15bc`). **Note on version letter:** directive targeted v3.8.v but parallel Lead Hunter sprint (commits `2dff607`, `99eec3e`, `ac41454`) took v3.8.v / v3.8.w / v3.8.x for outreach quality fixes during Phase B. Per CLAUDE.md §3.1 "never skip a letter" — bumped to v3.8.y as next available. **(a) Cluster A — gold/green/amber + white text (~30 occurrences, 14 files).** `color: var(--white)` / `#FFFFFF` / `#fff` → `color: var(--navy)` / `#0D1B2A` across: `.navbar-cta` + `.category-pill.active` in 6 marketing pages (about, blog, careers, faq, privacy, terms) and 2 auth pages (root-login, root-register); `.btn-success` (carrier-console.css), `.btn-amber` (compliance-carrier.css), `.btn-green` + `.section-tab.active .tab-num` + `.section-tab.completed .tab-num` (tender.css), `.toast-success` + `.progress-step.active|completed .progress-step-num` + `.form-section.active|completed .form-section-num` (create-load.css), `.threshold-banner .info-icon` (accounting-approvals.css), `.nav-cta` + `.btn-submit` (contact.css), `.nav-cta` + `.search-form button` (tracking.css). Bundled per "same-shape" directive — same readability shape, identical fix. **(b) Cluster B — slate-400 on light card surfaces (1 fix, scanner-v5 false positive set DELIBERATELY skipped).** Real fix: `.collapsible-panel-header .chevron` in console.css `var(--gray-400)` → `var(--gray-600)`. **Skipped:** carrier portal `tools.css` selectors (`.fuel-card .region-name`, `.weather-card .city-name`, `.weather-card .weather-type`, `.fuel-widget-placeholder .widget-sub`, `.detention-summary .summary-title`, etc.) — scanner v4 false positives. Carrier portal renders on dark navy bg, not white body, so `var(--slate)` on dark ≈ 6:1 (passes AA body). Scanner falls back to body bg `#FFFFFF` because tools.css doesn't define a body bg locally and global-body resolves to AE Console's `#F8FAFC`. Logged as scanner v5 candidate (per-page body-bg context resolution). **(c) Cluster C — gold-text-on-white CTAs (1 fix).** `accounting-fund.css .pagination button:hover:not(:disabled)` `var(--gold)` → `var(--gold-dark)` (#BA7517 on white ≈ 4.2:1, passes AA Large + UI 3:1). `tools.css .weather-card .temp-display` skipped — same dark-theme FP as B. **(d) Cluster D — status badges + saturated colors (~25 fixes across 12 files).** Per-color darkening matrix matching v3.8.u, applied via bulk sed: green `#16A34A` / `#16a34a` / `#22C55E` / `#22c55e` → `#15803D`; amber `#D97706` / `#d97706` / `#F59E0B` / `#f59e0b` → `#B45309`; red `#EF4444` / `#ef4444` → `#B91C1C`; blue `#3B82F6` / `#3b82f6` → `#1D4ED8`; violet `#8B5CF6` / `#8b5cf6` → `#6D28D9`. Files: accounting-receivable, accounting-payable, accounting-approvals, accounting-fund, accounting-dashboard, claims, communications, compliance-alerts, compliance-carrier, compliance-dashboard, crm, financials. Selectors covered include `.status-badge.partial`, `.action-btn.hold` / `.approve` / `.mark-paid`, `.step-green .reminder-step-day` / `.step-amber .reminder-step-day`, `.summary-card.approved .summary-count`, `.lh-tag-high` / `.lh-tag-med` / `.lh-int-badge.live`, `.icon-text` / `.stage-Contacted` (multiple files), `.status-FILED`, `.comp-kpi-icon.icon-{blue,green,amber,red}` + `.comp-kpi-value.val-{blue,green,amber,red}` (8 selectors), `.btn-blue-outline`, `.days-overdue`, `.days-warn`, plus the `.tier-flash` / `.tier-express` / `.tier-priority` / `.tier-partner` border colors. **(e) Cluster E — composited gold-on-pale-gold (4 fixes).** `color: var(--gold)` → `color: var(--gold-dark)` on `.action-item-icon.icon-invoice` + `.notif-item.clickable:hover .action-item-arrow` (console.css), `.txn-type-badge.QP_FEE_EARNED` (accounting-fund.css), `.step-tip` (training.css). **CSS-side audit comprehensively closed.** Total: ~70 surgical color edits across 14 files. Pre-commit verification clean per §3.3 (backend `tsc --noEmit` + frontend `next build`). **Remaining P0 (estimated 170-190) are scanner v5 territory:** themed-sidebar Tailwind utility classes on React .tsx surfaces (Lead Hunter modal, /track page, AE dashboard pages, Shipper Portal, Carrier Portal) + carrier-portal dark-theme false positives surfaced in Cluster B/C deferral. Scanner v5 work plan: Tailwind class resolution on .tsx (extracts bg/color from utility-class names + dark/light variants), per-page body-bg context (parses HTML to determine actual page-level bg per file), full HTML-DOM cascade beyond v3 hardcoded hints. Per §3.1, version bump justified — publicly-visible content changes across multiple AE Console + carrier portal + marketing surfaces. | v3.8.y

- 2026-05-03 | Sprint 5 (v3.8.u) — bulk P0 sweep from scanner v2 baseline (commit `888cd4a`). Three confirmed-real clusters fixed across 12 files / ~36 surgical color swaps; cluster 4 (.btn-navy:hover cascade-inherit) and the .sidebar-nav HTML-vs-CSS cascade cluster from Sprint 3 spot-check 3 verified as scanner false positives in Phase A and skipped. **(a) Gold/green button family** — `color: var(--white)` → `color: var(--navy)` across 14 `.btn-gold` / `.qa-btn-gold` / `.modal-btn-submit` / `.tab-btn.active` / `.page-btn.active` / `.btn-approve(:hover)` / `.review-btn.approve-btn` occurrences in [`console.css`](frontend/public/ae/css/console.css), [`accounting-approvals.css`](frontend/public/ae/css/pages/accounting-approvals.css) (3 rules), [`accounting-dashboard.css`](frontend/public/ae/css/pages/accounting-dashboard.css) (.qa-btn-gold + .qa-btn-gold:hover + .modal-btn-submit), [`accounting-payable.css`](frontend/public/ae/css/pages/accounting-payable.css) (.btn-gold + .tab-btn.active + .page-btn.active), [`accounting-receivable.css`](frontend/public/ae/css/pages/accounting-receivable.css) (.btn-gold + .page-btn.active), [`compliance-carrier.css`](frontend/public/ae/css/pages/compliance-carrier.css) (.btn-gold + .modal-btn-submit), [`tender.css`](frontend/public/ae/css/pages/tender.css) (was using literal `#fff`), [`index.css`](frontend/public/shared/css/pages/index.css) (marketing-page CTA bundled per Sprint 5 directive scope confirmation). [`shippers.css`](frontend/public/shared/css/pages/shippers.css) was already-correct (navy text on gold) — skipped after Phase A read. Mirrors v3.8.t methodology — surgical color-property edit only, no layout changes. Navy-on-gold ≈ 9:1 (passes AA + AAA). Per-page `.btn-primary` consolidation (and now per-page `.btn-gold` consolidation) remains a deferred refactor — readability fix delivered without touching duplicates' layout properties. **(b) Status badges + tier cards** — Tailwind-palette darkened literals chosen over canonical SRL `--success` / `--warning` / `--danger` tokens to match surrounding code style; canonical-token migration deferred. 13 fixes: [`accounting-analytics.css`](frontend/public/ae/css/pages/accounting-analytics.css) lines 110-114 (5 status-* rules: PAID `#16a34a → #15803D`, UNPAID/PENDING `#d97706 → #B45309`, OVERDUE `#ef4444 → #B91C1C`, PARTIAL `#3b82f6 → #1D4ED8`, SCHEDULED `#8b5cf6 → #6D28D9`); accounting-approvals.css line 188 `.status-badge.approved #16A34A → #15803D` and line 218 `.btn-approve #16A34A → #15803D`; accounting-payable.css 5 `.tier-* .cpp-tier-name` rules (lines 27/29/31/33/35: flash, express, priority, partner, elite — all darkened to slate-700-ish equivalents) + 4 `.tier-badge.priority` / `.tier-badge.elite` / `.status-badge.pending` / `.status-badge.approved` rules. Color math: `#15803D` on pale-green ≈ 4.5:1, `#B45309` on pale-amber ≈ 4.6:1 — both pass AA body + AA Large + UI 3:1. `accounting-approvals.css:187 .status-badge.pending color: #A16207` already passed scanner v2 ratio check (yellow-700 on pale-yellow ~4.7:1) — left unchanged. **(c) Slate-400 table headers** — `color: var(--gray-400)` (`#94A3B8` per console.css :root) → `color: var(--gray-600)` (`#475569`) across 9 occurrences in [`accounting-fund.css`](frontend/public/ae/css/pages/accounting-fund.css), accounting-payable.css, accounting-receivable.css, [`accounting-reports.css`](frontend/public/ae/css/pages/accounting-reports.css), accounting-dashboard.css, and 4 rules in accounting-approvals.css (`.approvals-table thead th`, `.detail-close`, `.ref-card .ref-title`, `.ref-card .ref-row .r-label` — note `.approvals-table thead th` had NO `color` declaration before; added explicit `color: var(--gray-600)` rather than swapping). slate-600 on slate-50 ≈ 7:1, passes AA body. **Cluster 4 + scanner-v2 limitation findings explicitly skipped:** Phase A confirmed `.btn-navy:hover` cascade-inherit findings are false positives — base `.btn-navy` rule has explicit `color: var(--white)` which cascades through to the :hover state per CSS specificity, but scanner's `isDescendantSelector` doesn't model `:` pseudo-class as related to base selector. Same false-positive class includes the .sidebar-nav HTML-vs-CSS cascade (`<aside class="sidebar"><nav class="sidebar-nav">` per [ae/dashboard/index.html:30-39](frontend/public/ae/dashboard/index.html#L30-L39) confirms HTML nesting that scanner can't see) and the multi-mode token resolution issue (e.g. `var(--theme-text-heading, #0d1b2a)` resolves to `#FFFFFF` in scanner because buildVarMap picks up dark-mode `[data-mode="dark"]` definition; light-mode rendering uses the literal fallback). Estimated count of remaining false-positive P0s after Sprint 5: ~80-100 across these three categories. Real readability issues are addressed by Sprint 5; remaining count is scanner-side noise. P0 count delta expected: 299 → ~150-180 (Phase C re-run will produce exact number). **Scanner v3 candidates added to backlog (below).** | v3.8.u

- **Scanner v3: cross-file cascade resolution + multi-mode tokens + pseudo-class cascading** — surfaced 2026-05-03 Sprint 5 Phase A. Three distinct scanner-side improvements needed before the brand-conformance audit can drive P0 to a meaningful zero baseline. Each is a small commit with significant FP-reduction impact: **(1) HTML-aware cascade resolution** — scanner v2's `findOpaqueParentBg` walks CSS-selector ancestors only (`.parent .child` / `.parent > .child`), missing HTML structure where elements are nested by class without explicit selector relationships (e.g., `<aside class="sidebar"><nav class="sidebar-nav">`). v3 fix: parse HTML files in scope, build a per-class containment graph, use it during opaque-parent search. Effort: medium — needs a minimal HTML parser (`htmlparser2` or hand-rolled tag walker) plus a class-containment map. **(2) Multi-mode CSS variable resolution** — scanner v2's `buildVarMap` extracts variable declarations from any context (any `:root`, any `[data-mode="X"]`, etc.) without selector context tracking, so the LAST definition wins regardless of whether it's the active one. False positives: tokens like `--theme-text-heading` defined in both `[data-mode="light"]` and `[data-mode="dark"]` blocks resolve to whichever appears later in themes.css (currently dark mode) when actual rendering uses the data-mode-attribute-active variant. v3 fix: track selector context per declaration, default to `:root` / `[data-mode="light"]` / no-attribute for the resolution unless a different mode is being analyzed. Effort: medium. **(3) Pseudo-class rule cascading** — scanner v2's `isDescendantSelector` returns false for `.X:hover` vs `.X` because the `:` next-char isn't in `[space,>,+,~]`, so cascade-inherit walker treats `.X:hover` as standalone. Real CSS specificity has both rules apply on hover with `.X:hover` adding/overriding only declared properties. v3 fix: extend `isDescendantSelector` to recognize `:hover` / `:focus` / `:active` / `[attribute]` / `.modifier` selectors as related to their base. Effort: small — regex tweak. Combined v3 work expected to eliminate ~80% of current false positives. Pre-Sprint-6 work, deferred from Sprint 5 per directive narrowing.

## Phase 6 — Content & Brand

- 2026-05-03 | Sprint 1 (v3.8.s) — two narrow fixes. **(a) /about stats-section render bug + capability reframe.** Phase A audit confirmed the stat at [`frontend/public/about.html:197-200`](frontend/public/about.html#L197-L200) was hardcoded with initial display value `0` and `data-target="48"`, animated by an IntersectionObserver-driven JS counter at [about.html:556-625](frontend/public/about.html#L556-L625). Public exposure path: static `0` is what renders before the counter fires; if the user screenshots before scrolling into range, OR JS errors elsewhere block execution, OR the section never enters viewport (the no-animation branch at line 612 only runs if IntersectionObserver is missing or motion is reduced, not as a default), the value stays at "0" indefinitely. Bigger concern beyond the bug: even when working correctly, this section animated to "48 States Covered" — which would read as a historical-volume claim and violate CLAUDE.md §5 prohibited claims (volume stats without real numbers). Initial inclination during audit was full removal. Wasi superseded with the keep-and-reframe approach: static-render the final value "48" in place of "0" (removes the animated count-up which violated brand-skill motion-restraint guidance AND eliminates the broken-render exposure in one edit), reframe label from "States Covered" to "States Licensed" (capability claim verifiable against MC# 01794414 — SRL holds FMCSA broker authority that is national in scope; per voice.md, capability/licensing-scope claims are distinct from volume/track-record claims and not subject to the "don't fabricate metrics" rule). Stats-section retained intentionally — single capability stat with section subtitle ("Our numbers reflect our commitment to operational excellence and partner satisfaction") reads as restraint, not sparseness. Bond and insurance specifics deliberately NOT added per industry convention (those belong in BCAs and NDA-gated procurement packets, not public marketing). The JS counter at lines 556-625 left in place — generic mechanism, silent no-op now that no animated stats remain on the page; preserved as re-usable infrastructure if future capability stats are added. The about.css `.stats-*` rules at lines 520-572 + 982 retained for the same reason. **(b) security-policy.html canvas color conformance.** Phase A audit confirmed [security-policy.css:8](frontend/public/shared/css/pages/security-policy.css#L8) hardcoded `background:#FFFFFF` (pure white) on `body{}`. Per CLAUDE.md §2.1 + tokens.md line 67, canonical web canvas is `--bg-page-web: #FBF7F0` (warm cream). Fixed with single CSS edit. White section cards (`.toc`, `.section`) retained at `#FFFFFF` — this gives the canonical cream-page-with-white-card-elevation pattern per `--bg-surface: #FFFFFF` token and matches the doc-page family's intended visual hierarchy. The doc-header navy gradient (`#0D1B2A → #1a2d45`) stays unchanged, providing strong contrast against the new cream canvas. Sibling docs `privacy.html` and `terms.html` carry the same `#FFFFFF` canvas issue plus title-case-vs-sentence-case open question — deferred to a separate sibling-page conformance migration sprint, scoped against brand-conformance scanner output. **Fix B (page title sentence case) dropped from this sprint** pending skill-rule clarification: tokens.md line 126 specifies sentence case for hero/page titles, but voice.md line 51 ("legal documents — voice is the absence of voice") supports a genre-convention reading where legal/policy document titles use title case. All three legal pages (privacy.html `Privacy Policy`, terms.html `Terms of Service`, security-policy.html `Information Security Policy`) currently use title case + emphasis on the noun. A small docs commit will clarify the rule before any title-case edits ship. | v3.8.s

## Phase 6 — Lead Hunter / CRM Separation

- 2026-05-05 | **v3.8.tt — Approve flips BOTH `onboardingStatus='APPROVED'` AND `status='Active'`.** Closes the architectural inconsistency surfaced after v3.8.qq's corrective undo. Wasi confirmed 2026-05-05: clicking Approve in the AE Console should transition a Lead Hunter prospect into CRM by setting both gate markers in one operation. Prior implementation at [`approveCustomer`](backend/src/controllers/customerController.ts) only wrote `onboardingStatus = 'APPROVED'`, leaving `Customer.status` at whatever Lead Hunter pipeline value it had been (e.g. `'Contacted'`). CRM visibility worked because the `?context=crm` filter is keyed on `onboardingStatus`, but the `status` field stayed misleading — the record was technically a CRM customer per the Phase 6.2 gate while still labeled as a Lead Hunter pipeline-stage record per the architectural rule. v3.8.tt adds `status: "Active"` to the `prisma.customer.update` data block at the same call site. Atomic write — both fields flip together, both NULL out together if the update is rolled back. Single-line behavior change in the controller; happy-path test assertion updated to expect `status: "Active"` in the update payload; 21 tests pass (15 prior + 6 manual-review tests, all green). 422 / idempotency / 404 paths untouched. Architectural rule restated for future reference: a record is a CRM customer iff `onboardingStatus='APPROVED' AND status='Active'`. Either alone is partial state and should not exist post-v3.8.tt for any record going through the proper approve flow. Pre-v3.8.tt records that may be in the partial state (4 currently-APPROVED records all have `status='Active'` per the v3.8.qq verification, so no remediation needed). | v3.8.tt

- 2026-05-05 | **v3.8.qq — corrective undo: v3.8.pp discriminator was too broad.** v3.8.pp used `status != 'Prospect'` to identify "real CRM customers" — wrong. That captured 22 records with `status = 'Contacted'`, which is a **Lead Hunter pipeline stage**, not a CRM customer marker. Wasi's architectural clarification 2026-05-05: *"Lead Hunter database stays independent from CRM. Even before yesterday no Lead Hunter customer was supposed to reach CRM until he was manually approved, and then that customer becomes part of the CRM active customers."* So the correct discriminator is `status = 'Active'` exclusively — the marker for "manually approved into CRM." Lead Hunter has its own stage semantics (Lead → Contacted → Qualified → Proposal → Won → Not Interested) and those records stay in Lead Hunter until explicitly promoted. **Corrective UPDATE executed via [`backend/scripts/undo-wrong-backfill.ts`](backend/scripts/undo-wrong-backfill.ts):** for the 22 records where `onboardingStatus = 'APPROVED' AND status != 'Active' AND deletedAt IS NULL` (i.e. the records I wrongly flipped 30 minutes earlier), set `onboardingStatus = 'PENDING'`, `approvedAt = NULL`, `approvedById = NULL`. Final state confirmed: 4 APPROVED (Beekeepers Naturals, Flavour Sum, Graphic Packaging, Silk Route — all `status = 'Active'`), 22 returned to PENDING and visible in Lead Hunter again. v3.8.pp's drawer revert (`CrmIconTabs` unconditional render restored) is correct and preserved — that half of v3.8.pp was right. **Pattern documented:** when discriminating between Lead Hunter prospects and CRM customers, the only safe field is `Customer.status` with the exact value `'Active'`. Any other value (`'Prospect'`, `'Contacted'`, `'Qualified'`, `'Proposal'`, `'Won'`, `'Not Interested'`) means the record is a Lead Hunter prospect at some pipeline stage. **Honest log:** v3.8.pp shipped with insufficient verification. The breakdown query I ran AFTER the UPDATE revealed the architecture mismatch; I should have run that query BEFORE the UPDATE to confirm the discriminator hit the right scope. Single best safeguard for future SQL backfills: `SELECT count + GROUP BY status` BEFORE any write, regardless of confidence in the discriminator. Logged as standing rule for any future Customer-table backfill. | v3.8.qq

- 2026-05-05 | **v3.8.pp — restore historical CRM customer list + revert v3.8.nn drawer guard.** Two coordinated actions, single commit. **(1) SQL backfill on Neon:** flipped `onboardingStatus = 'APPROVED'` + `approvedAt = NOW()` + `approvedById = <Wasi user id>` for 26 historical CRM customer records (Beekeepers Naturals USA, Graphic Packaging, Wellness Pet Company, General Motors, ODL Inc, Target Steel, etc.) where `status != 'Prospect'` AND `deletedAt IS NULL`. These customers existed before the v3.8.ee approve gate was built; they were added manually by Wasi via the CRM create form (which defaults `status` to "Active"); their `onboardingStatus` had never been written by any code path. The Phase 4 `?context=crm` filter (commit `0b53cf6`) made them invisible from the CRM page because none had `onboardingStatus = APPROVED`. The 46 Apollo Lead Hunter prospects (`status = "Prospect"` set explicitly by `bulkCreateCustomers` and the Lead Hunter EMPTY_FORM) remain un-flipped — Phase 6.2 separation continues to work for any new customer added going forward. Backfill executed via disposable script `backend/scripts/backfill-old-crm-approved.ts` (intentionally not committed; one-shot use). Post-backfill verification: APPROVED count = 26, Prospects count = 46, dangling non-Prospect non-APPROVED = 0 (clean state). **(2) Reverted v3.8.nn drawer guard:** `frontend/src/app/dashboard/crm/CustomerDrawer.tsx:86` — removed the `!isNew` wrapper around `<CrmIconTabs />`. Tab sidebar renders unconditionally again, including in `__new__` mode. v3.8.nn was an unnecessary fix that solved a downstream symptom (the inert tab bar in new-customer flow) of the empty-CRM problem; the real fix was the SQL backfill in (1). Yesterday's drawer behavior is restored — same shape Wasi had been working with successfully through April. The "all tabs show same form in `__new__` mode" symptom returns as a known cosmetic issue, intentionally accepted to restore the pre-2026-05-04 drawer experience without further churn. **Honest log:** v3.8.nn (commit `db428a8`, shipped 2026-05-04) should not have been a sprint at all. The user's original report combined two issues — empty CRM list (real, root cause = filter without backfill) and "tabs show same form" (cosmetic downstream of the empty list pushing user into new-customer flow they normally bypass) — and the right move was to address only the data-visibility issue, not touch the drawer. The drawer churn introduced unnecessary work and made the AE Console feel less stable. Logged here so the pattern is recorded: when a UX symptom is downstream of a data/state issue, fix the upstream cause; don't bandage the symptom. | v3.8.pp

- 2026-05-05 | **v3.8.oo — approve-gate edit-UI completion.** Three atomic commits closing the two blocking gaps surfaced by audit [`f939aa1`](docs/audits/approve-gate-edit-ui-2026-05-04.md). Both gaps had to land before BKN — the first real-customer approval — could be exercised via the v3.8.ee approve UI without SQL backfill. **Gap 1 ([`d956328`], `feat(crm): manual credit review sets creditStatus`):** `POST /customers/:id/mark-manually-reviewed` previously wrote `creditCheckDate` + `notes` but left `creditStatus` untouched, so private-company customers (most CPG/wellness brands) stayed at `PENDING_REVIEW` indefinitely and `evaluateApprovalChecks` never accepted them. Handler extracted from inline [`crmCustomer.ts:436-459`](backend/src/routes/crmCustomer.ts#L436-L459) into [`customerController.markManuallyReviewed`](backend/src/controllers/customerController.ts) named export so it shares the prisma-mock harness used by `approveCustomer` + `getCustomers ?context` tests. New `markManualReviewSchema` in [`validators/customer.ts`](backend/src/validators/customer.ts) enforces `creditStatus ∈ {APPROVED, CONDITIONAL, DENIED}` + optional `notes`. `creditCheckResult` mirrors SEC handler convention (APPROVED/CONDITIONAL → "approved", DENIED → "rejected"). UI: [`ProfileTab.tsx`](frontend/src/app/dashboard/crm/tabs/ProfileTab.tsx) "Mark as manually reviewed" button on the SecLookupView private-company branch now opens a new `ManualReviewPopover` modal with status selector (defaulting to **CONDITIONAL** — most common manual-review outcome — per directive guidance) + notes textarea + Save / Cancel. **Naming note:** directive specified the third option as `REJECTED`, but the Prisma `CreditStatus` enum has been `DENIED` since pre-Phase-6.2. Used schema-correct `DENIED` per §18.6 ship-default; semantic effect identical (both fail the gate's APPROVED/CONDITIONAL precondition). 6 new vitest cases (21 total in `customerController.test.ts`, all passing): each enum value writes correctly, invalid enum (`"REJECTED"`) throws zod 422 path, missing `creditStatus` body throws, 404 on not-found / soft-deleted, notes flow through to `creditCheckNotes` (default fallback `"Marked as manually reviewed"` preserved when notes absent), `customerActivity` event logged with `creditStatus + hasNotes` metadata. **Gap 2 ([`67ed42a`], `feat(crm): CUSTOMER_CONTRACT upload cross-writes customer.contractUrl`):** `DocsTab` `CUSTOMER_CONTRACT` upload wrote to the generic `Document` table only; [`customer.contractUrl`](backend/prisma/schema.prisma) stayed `null` forever, so the gate's contract precondition was unsatisfiable via UI for every customer. Fix: in [`documentController.uploadDocuments`](backend/src/controllers/documentController.ts), when `docType === "CUSTOMER_CONTRACT" && entityType === "CUSTOMER" && entityId`, wrap `prisma.document.create` + a paired `prisma.customer.update({ contractUrl: fileUrl })` in `prisma.$transaction(async (tx) => ...)` so cross-write succeeds or fails together with the Document insert. Non-CONTRACT uploads (POD, RATE_CON, W9, SRL_COI, etc.) stay on the existing `Promise.all` non-transaction path — zero behavior change. **Multiple-upload convention: latest-wins.** Previous Document rows preserved in the table for audit history; matches how DocsTab's `statusPill` renders the most recent doc per category. `updateCustomerSchema` gains `contractUrl: z.string().url().nullable().optional()` so an AE can paste a URL via PATCH `/customers/:id` (admin-override path for cases outside the upload flow). `onboardingStatus` deliberately stays off the schema — only `POST /customers/:id/approve` flips it, per Phase 6.2. 9 new vitest cases in new file [`documentController.test.ts`](backend/__tests__/unit/controllers/documentController.test.ts), all passing: CUSTOMER_CONTRACT cross-write fires + 201 returned, non-CONTRACT (POD) does NOT call `customer.update` and does NOT invoke `$transaction`, missing `entityId` falls through to no cross-write, wrong `entityType` (CARRIER) does NOT touch customer, second CUSTOMER_CONTRACT upload overwrites with new fileUrl (latest-wins assertion on sequenced calls), `updateCustomerSchema` accepts URL string, accepts null, rejects non-URL strings, optional when omitted. **Effect on BKN approval path:** the 7-click UI workflow from audit f939aa1 is now unblocked end-to-end. AE clicks: (1) find BKN in Lead Hunter, (2) open drawer → Profile tab, (3) Edit profile → enter EIN → Save (taxId), (4) Check Credit (SEC) → "Company not found" → Mark as manually reviewed → popover opens → leave default CONDITIONAL → optional notes → Save (creditStatus + creditCheckDate), (5) Docs tab → Customer contract → Upload signed PDF (contractUrl cross-write fires), (6) Profile tab → green Approve button → 200 response, onboardingStatus flips to APPROVED, (7) navigate to /dashboard/crm → BKN now in approved-customers list. No SQL. **Approve-gate integrity preserved per the original directive** — every precondition still enforced; only the editor affordances were missing, and they're additive. **No new backlog items added.** Audit f939aa1's "follow-up sprint shape" is what this is. | v3.8.oo

- 2026-05-05 | **v3.8.nn — CRM new-customer drawer tab bar guarded by !isNew.** Pre-existing UX bug from commit [`9d0f311`](commit/9d0f311) (2026-04-14, `fix: CRM follow-ups — Order Builder auto-fill, inline new-customer form, ...`): [`CustomerDrawer.tsx:86`](frontend/src/app/dashboard/crm/CustomerDrawer.tsx#L86) rendered `<CrmIconTabs />` unconditionally, but the tab content panels at lines 155-167 were gated by `!isNew && customer`. In `__new__` mode (`customerId === "__new__"`) the tab sidebar was clickable but inert — every tab click updated `tab` state with no panel render, so the AE perceived "all tabs show the same New Customer form." Surfaced after Phase 6.2 (v3.8.ee) `?context=crm` filter emptied the CRM list and forced AEs into the "+ New Customer" flow they had previously bypassed. **Fix shape:** one-line `{!isNew && <CrmIconTabs ... />}` guard at line 86. The tab sidebar is conceptually meaningless before the customer record exists — there are no contacts/loads/rates/facilities/notes/docs/orders/activity to view against a record that hasn't been created. Hiding is more honest than disabling. **Cross-component dependency check:** `grep -rn "__new__" frontend/src/app/dashboard/crm/` returns three references total — [`page.tsx:66`](frontend/src/app/dashboard/crm/page.tsx#L66) (sets `selectedId`), [`CustomerDrawer.tsx:53`](frontend/src/app/dashboard/crm/CustomerDrawer.tsx#L53) (disables fetch query), [`CustomerDrawer.tsx:57`](frontend/src/app/dashboard/crm/CustomerDrawer.tsx#L57) (derives `isNew`). [`CrmIconTabs`](frontend/src/app/dashboard/crm/IconTabs.tsx) itself is a 24-line pure presentational component (props `active` + `onChange`, no internal state, no side effects). No sibling component depends on the tab bar rendering during `__new__` mode. **Companion fix scope NOT in this commit:** approve-gate edit-UI verification (whether the four required-checks fields — `taxId`, `creditStatus`, `creditCheckDate`, `contractUrl` — have AE-Console editor affordances on existing customers) tracked separately. Ships as `docs: approve-gate edit UI verification (BKN approval prep)` immediately after this. **Issue 1 status:** "all customers disappeared from CRM" report on 2026-05-05 confirmed not a data loss — Phase 4 read filter is working as designed. CRM correctly shows zero rows because no Customer record has `onboardingStatus = APPROVED` yet. The Phase 5 approve UI shipped but no AE has clicked Approve on any record. Decision: NO SQL backfill, NO prospects toggle on CRM page (both defeat the gate). Forward path is exercise the full UI workflow on real customers (BKN first), populate the four required-check fields via the relevant tabs, click Approve in AE Console, confirm CRM filtered list reflects the flip. | v3.8.nn

- 2026-05-04 | **Lead Hunter / CRM separation (v3.8.ee)** — four code commits + this docs commit closing audit [`39de1ad`](docs/audits/lead-hunter-crm-separation-2026-05-04.md) and CLAUDE.md §13.3 Item 6 (Portal Approval UI S-3). Sprint context: Apollo bulk uploads were appearing directly in the CRM customer list because Lead Hunter prospects and CRM customers shared the unified `customers` Prisma model with no read-side filter and no APPROVED-flip path (Pattern A per the audit). Phase 1 confirmed at HEAD that `getCustomers` had no `onboardingStatus` filter, no `/customers/:id/approve` endpoint existed, and no AE Console approve UI was present. Check-state fields existed (`taxId`, `creditStatus + creditCheckDate`, `contractUrl`) so Phase 2.5 schema additions were skipped per the sprint conditional; only approval-event fields (`approvedAt`, `approvedById`, both nullable, no FK) needed adding. **Phase 2 ([`7c74bb1`], `feat(backend)`):** `customerQuerySchema` gains `context: z.enum(["crm","prospects"]).optional()`. `getCustomers` adds `onboardingStatus = "APPROVED"` for `context=crm`, `{ not: "APPROVED" }` for `context=prospects`, no clause when omitted (back-compat preserved for legacy AE Console callers). 6 vitest cases covering crm/prospects/omitted/invalid-rejected/coexistence-with-other-filters/findMany-count where-clause parity. Setup file extended with `customer.count`, `customer.findFirst`, `load.aggregate`, `shipment.aggregate` mocks (previously absent). **Phase 3 ([`2fb5279`], `feat(backend)`):** `POST /customers/:id/approve` (ADMIN/CEO only, mirrors `dashboard/carriers/page.tsx:730` role-gate). Required-checks gate: TIN presence, credit `APPROVED`/`CONDITIONAL` with `creditCheckDate` non-null, contract URL presence. Returns 422 with `{ error, missing: [{ field, label, reason }] }` on unmet preconditions. Idempotent on already-APPROVED. Hand-authored migration `20260504000000_add_customer_approval_fields` per CLAUDE.md §2 (additive, no backfill needed). `updateCustomerSchema` deliberately does not include `onboardingStatus` so PATCH `/customers/:id` cannot bypass the gate. 9 vitest cases (happy path, three single-field 422s, all-three-missing 422, idempotency, 404 with soft-delete exclusion verified, CONDITIONAL credit acceptable). **Phase 4 ([`0b53cf6`], `feat(frontend)`):** `/dashboard/crm` page passes `?context=crm` to `GET /customers`. Removed: `statusOf()` helper, `useMemo` import, `CrmStatusFilter` type, four-pill filter bar (`all`/`active`/`prospect`/`inactive`), per-row status badge, duplicate "Active" StatCard. Halt-rule check applied: prospect pill explicitly removed (closing the user-visible bug); inactive pill (REJECTED/SUSPENDED) flagged for §13.3 Item 8.1 / v3.8.l customer inactivation workflow rather than silently dropped. Empty-state copy now reads "Prospects appear in Lead Hunter until they pass the onboarding gate." for the no-search case. **Phase 5 ([`df3545f`], `feat(frontend)`):** new `OnboardingActionBar` component mounted in `CustomerDrawer` between header and tab content for non-new customers. Approve / Suspend / Reject buttons styled with srl-brand-design tokens (success #2F7A4F, warning #B07A1A, danger #9B2C2C; gold-dark #BA7517 on the modal "Got it" CTA per voice.md direct-verb convention). Approve mutation hits `POST /customers/:id/approve`; on 422 the `missing` payload renders inline as a checklist with `XCircle` icons keyed off `field`. Already-APPROVED renders a quiet "Approved customer · onboarding gate cleared" success-tinted strip with `CheckCircle2` icon. Suspend / Reject open TODO modals because no backend transitions exist — copy points to v3.8.l with the manual-SQL escape hatch. Header status badge in `CustomerDrawer` rebased from free-form `customer.status` to canonical `customer.onboardingStatus` with palette mapping (APPROVED → success, REJECTED/SUSPENDED → danger, PENDING/UNDER_REVIEW/DOCUMENTS_SUBMITTED → warning). **Versioning note:** brand-sweep commit [`22fc84c`](commit/22fc84c) (v3.8.dd) interleaved between Phase 4 and Phase 5 as parallel in-flight work — the four code commits in this sprint shipped without their own version letters, captured as v3.8.ee at sprint close-out via `SRL_VERSION` bump in this docs commit. **Backlog state changes:** §13.3 Item 6 marked CLOSED with sprint-summary backfill (was "Portal Approval UI S-3", now strikethrough + closure note referencing the four sprint commits). The "Shipper application under review" UX page paired requirement from v3.8.e.1 remains open and tracked separately. Item 8.1 (v3.8.l customer inactivation) gains an inbound dependency from this sprint (Suspend/Reject TODO modals wire through to whatever endpoint pattern v3.8.l ships). **Smoke test:** vitest 179/179 passing post-Phase 3; backend + frontend `tsc --noEmit` clean post-each-phase. Browser smoke deferred — Cloudflare Pages auto-deploy will land within 2-3 min of push, recommend AE-side verification with `shipper@acmemfg.com` PENDING fixture per CLAUDE.md §17 methodology (PENDING → expect 422 with three missing fields if the fixture has no taxId/credit/contract; flip to populated state via SQL → expect 200 + onboardingStatus=APPROVED). | v3.8.ee

## Phase 6 — Lead Hunter Outreach Quality

- 2026-05-04 | **§18.9 outreach copy AI-tell audit discipline** — 1 docs commit, no version letter bump per §3.1. Codifies the AI-tell audit that every SRL outreach email must pass before send (Lead Hunter generated, manually drafted in Gmail, or templated). Captures 7 banned construction categories: (1) em dashes anywhere in body/subject/signature, (2) "That's where..." / "That's the..." / "That's what..." sentence openers, (3) parenthetical asides as voice texture, (4) symmetric two-clause balanced sentences, (5) consultant-speak imports (`step-change`, `in under a year`, `planning-vs-actuals`, `leverage`, `synergy`, `best-in-class`, `world-class`, `north star`, `unlock value`, `comprehensive solution`, `AI-powered` for capabilities), (6) marketing softeners (`I'd love the opportunity`, `see if we can add value`, `would you be open to a brief call`, `I would appreciate the opportunity to connect`), (7) repeated close patterns within a single batch (no two outreach emails sent within the same week may use the same closing operational ask). Carve-out: factual proper-noun enumerations (real product categories, real retailer names from recipient's actual distribution) are NOT banned — the prohibition is on adjective-stacking as a "showing range" device. Hook structure rule restated for outreach scope: opening sentence is a company-specific operational signal proving recipient research (not credentials), authority line is one line above signature (not paragraph one), length 4-5 short paragraphs, Compass Engine described as 35-point carrier vetting (never "AI-powered market intelligence"). Implementation guidance: Lead Hunter system prompts (`touch1ColdChainTemplate`, `touch1WellnessTemplate`, fallback) must explicitly enumerate the banned constructions and instruct the model to self-check before output. Audit also applied at preview stage — Little Spoon + MERIT preview renders must pass §18.9, not just §18.8 honest-framing. For founder-drafted Gmail outreach the same audit applies; the discipline is the writer's, not the platform's. **Origin:** Batch 1 v2 hooks for Little Spoon, Hero Bread, Kite Hill, MALK Organics shipped with em-dashes in body and authority line that v1's voice.md scan missed. Subject-line scan also missing from v1. Discipline gap identified, rule promoted from chat-only artifact to canonical §18 standing rule so future Lead Hunter sprints inherit by reference. **Companion (off-repo):** chat-side audit script extended to subject + signature scan in v2.1 batch. | docs-only (no version letter per §3.1)

- 2026-05-04 | Lead Hunter outreach quality fix (v3.8.v–v3.8.cc) — 8 commits (6 versioned + 2 docs) closing 7 distinct bugs in the Apollo→outreach pipeline. Sprint context: Apollo Tier 1 import surfaced "Hi InsideTracker" greeting bug + signature/MC# typos + missing vertical-routing logic + voice violations in fallback templates that would degrade send quality on the first cold-outreach campaign. Each bug shipped as its own atomic commit per §18.2 with its own version letter and smoke test. **Bug 1 (C1, v3.8.v, [`2dff607`]):** Apollo CSV literal column mapping. `mapCsvRow` at [`frontend/src/app/dashboard/lead-hunter/page.tsx`](frontend/src/app/dashboard/lead-hunter/page.tsx) was reading `Contact Name` (Apollo doesn't emit this column) instead of `First Name` + `Last Name` literals. `contactName` saved as null → `sendMassEmail` fell back to `c.name.split(/\s+/)[0]` = first token of company name → "Hi InsideTracker" greeting. Fix: read all Apollo literal columns (`First Name`, `Last Name`, `Company Name`, `Title`, `Email`, `Industry`, `Vertical`), compose `contactName = First Name + " " + Last Name`. **Bug 2 (C2, v3.8.w, [`99eec3e`]):** Wasih → Wasi typo with single source of truth. The canonical Gmail signature file [`whaider.html:24`](backend/src/config/signatures/whaider.html#L24) already said "Wasi Haider" correctly, but body strings + Resend `fromName` had drifted to "Wasih" across [`emailSequenceService`](backend/src/services/emailSequenceService.ts) (5 body strings + CEO_NAME constant), [`customerController:848`](backend/src/controllers/customerController.ts#L848) (sendMassEmail fromName), [`page.tsx:112,121,131`](frontend/src/app/dashboard/lead-hunter/page.tsx#L112) (EMAIL_TEMPLATES preview), [`CeoOverview:339,343`](frontend/src/components/dashboard/CeoOverview.tsx#L339) (internal "Sales (Wasih)" label), and [`insuranceVerificationService:169`](backend/src/services/insuranceVerificationService.ts#L169) (carrier insurance email). Fix: new `CEO_NAME` + `CEO_EMAIL` exports in [`backend/src/email/builder.ts`](backend/src/email/builder.ts) as canonical SOT, with a `log.info()` startup line so any future regression surfaces in production logs immediately. All consumers refactored to import from the SOT. **Bug 3 (C3, v3.8.x, [`ac41454`]):** MC# 01794414 → 1794414 in Lead Hunter outreach SOT. Brand-skill [`voice.md:98`](.claude/skills/srl-brand-design/references/voice.md#L98) declares MC# 1794414 canonical (primary source per §3.13). Fixed in 3 Lead Hunter outreach SOT files: `whaider.html` signature, `email/builder.ts` fallback string, `page.tsx` EMAIL_TEMPLATES INTRO preview. Wider sweep across 17 remaining surfaces (10 public HTML pages, 2 chrome assets, 4 PDF services, BOLTemplate, CLAUDE.md §1) deliberately deferred — needs rendered-output verification per §3.2 + FMCSA-SAFER primary-source check per §3.13 before sweeping public surfaces. Logged as §13.3 backlog 8.8 (MC# correction sweep) + 8.9 (signature/authority block centralization). **Numbering note:** sprint directive said "8.6 + 8.7" but those numbers were already taken by the 2026-05-02 `audit-completeness.ts` Tier-A reclassification entries (carrier preference admin UI + manual tag-management UI); 8.8 + 8.9 allocated as next available per §18.5. DOT# 4526880 verified clean — no leading-zero variant exists anywhere. **Bug 4 (C4, v3.8.aa, [`6e2e6d9`]):** ProspectVertical schema + Apollo `Vertical` import wiring + AE Console Manual Review queue. New `ProspectVertical` enum (`COLDCHAIN | WELLNESS | UNKNOWN`) and `Customer.vertical` field with default `UNKNOWN`, pushed to Neon via [`prisma db push`](backend/prisma/schema.prisma) (additive, no data loss). `mapCsvRow` reads Apollo `Vertical` column with `normalizeVertical()` — anything not COLDCHAIN/WELLNESS lands as UNKNOWN. `bulkCreateCustomers` persists on create; on update allows UNKNOWN → COLDCHAIN/WELLNESS upgrades but never downgrades an explicit vertical. New Lead Hunter pipeline-view filter mode "Manual Review (N)" surfaces UNKNOWN customers in the AE Console for classification before next send. **Versioning collision:** sprint directive planned C4 as v3.8.y; mid-flight, parallel Sprint 8 brand sweep ([`c8c3846`](commit/c8c3846)) committed v3.8.y AND incidentally clobbered SRL_VERSION constant to v3.8.z by absorbing my pending VersionFooter staging. Per §3.1 sequence-continuous + §18.5, C4 retitled to v3.8.aa as next available letter post-z. **Bug 5 (C5, v3.8.bb, [`70d9b5e`]):** vertical-branched Touch 1 outreach + UNKNOWN hard-block at six call sites. New `touch1ColdChainTemplate` and `touch1WellnessTemplate` in [`builder.ts`](backend/src/email/builder.ts). COLDCHAIN: temperature-controlled reefer FTL pitch, operational signal = logger-download temperature drift on last 50 miles into DC + retailer DC rejection rates. WELLNESS: dry van FTL for clean-beauty/personal-care brands, operational signal = damage rate at retailer DC + Sephora 003 chargebacks + signature-required residential PR drops + expedited bench for launch surges. Both use the canonical authority line (Michigan-licensed property broker, MC# 1794414, DOT# 4526880, BMC-84 $75K, $100K contingent cargo through Hancock & Associates) and describe Compass Engine correctly as 35-point carrier vetting (NOT "AI-powered market intelligence"). Both pass voice.md calibration at 5 paragraphs each: no em-dashes in body copy, no marketing softeners, no exclamations, specific operational ask at close ("send a recent BOL on a tricky lane and I'll come back with a quote and the carrier's full Compass profile"). UNKNOWN hard-block enforces at: `buildEmail` (DB lookup, throws), `buildEmailSync` (in-memory, throws), `getTemplate` (defense-in-depth on Touch 1, throws), `sendMassEmail` (skips with reason in `skippedReasons[]` response field), `startSequence` (throws, sequence cannot start), `processDueSequences` cron tick (holds active sequence, pushes `nextSendAt` forward 24h, never advances step on UNKNOWN). **C5 preview gate:** rendered Little Spoon (COLDCHAIN — Jayme Kennedy) and MERIT (WELLNESS — Sarah) preview emails surfaced inline as recipient-view (subject + body + Gmail signature) for Wasi sign-off before push. Two micro-edits applied post-review: (1) "For comparable refrigerated CPG operations, the operational signal we track is" → "In refrigerated CPG, the operational signal that matters is" + parallel WELLNESS edit ("In clean beauty, the operational signal that matters is"), enforcing industry-knowledge framing over claimed-track-record framing — never imply portfolio SRL doesn't have. (2) WELLNESS expedited-bench paragraph collapsed into operational-signal paragraph, landing both verticals at 5 paragraphs symmetric. **Bug 6 (C6, v3.8.cc, [`5dee6d5`]):** fallback voice cleanup. Honest-framing rule per §18.8 applied to (a) `builder.ts touch1Template` fallback (replaced "I would appreciate the opportunity to connect" + "I am happy to connect whenever it is convenient" with authority line + Compass framing + specific operational ask), (b) `page.tsx EMAIL_TEMPLATES.INTRO/FOLLOW_UP/CAPACITY` previews (removed "AI-powered market intelligence" per voice.md line 25 prohibition; removed fabricated "8-12% reduction" claim and fabricated "98% pickup rate" claim per CLAUDE.md §5 prohibited volume stats; removed all em-dashes from body copy; replaced softeners with operational asks), (c) `emailSequenceService` introduction template (dropped fabricated 98% pickup rate, replaced "I'd love to hear about your shipping lanes" softener with "send me a recent BOL"). **Bug 7 (§18.7 fix, [`a82801b`]):** off-by-one drafting error in [`CLAUDE.md §18.7`](CLAUDE.md#L730). Prose said "five call sites" but enumerates six (buildEmail, buildEmailSync, getTemplate, sendMassEmail, startSequence, processDueSequences). Corrected to "six". Tiny docs-only commit, unversioned per §3.1. **Standing-rules canonical SOT added (`67da0ea`):** new CLAUDE.md §18 LEAD HUNTER STANDING RULES — eight numbered rules covering audit-first discipline, atomic-commits-per-bug, brand-skill-at-moment-of-claim, Apollo CSV literals, version verification against §13.3, ship-default on mechanical halts, cold-outreach data-flow validation (vertical ∈ {COLDCHAIN, WELLNESS} hard gate), and honest-framing rule (no fabricated metrics, no softeners, no em-dashes in body copy, no implied portfolio, Compass Engine = 35-point vetting). Future Lead Hunter sprints inherit these rules by reference. **Schema state change:** `ProspectVertical` enum + `Customer.vertical` column live in Neon; all pre-C4 prospect rows defaulted to UNKNOWN at migration time and now surface in the Manual Review queue until classified — expected behavior, not a bug. **Backlog items added to §13.3:** 8.8 (MC# leading-zero correction across all SRL surfaces — 17 remaining files; needs §3.13 FMCSA-SAFER primary-source verification + §3.2 rendered-output verification before public-page sweep), 8.9 (signature/authority block centralization — pairs with 8.8, new shared module `backend/src/config/authority.ts` + `frontend/src/lib/authority.ts` so future MC# correction is one-edit). | v3.8.v–v3.8.cc
