# docs/claude/archive/ — closed, historical, log

Records of work that shipped and decisions already taken. Nothing here is a
standing rule; where a rule survived a closed item, the rule stayed in
`CLAUDE.md` or moved to `docs/claude/` and only the account moved here.

Kept rather than deleted because the accounts are load-bearing under
§19 Sub-pattern 15 — a stale "NOT built" list commissions duplicate work, and a
deleted account cannot be checked against a claim. § numbers are preserved
exactly as they were; see `../README.md`.
