# §13.3 — closed items and arc records

Split out of `backlog.md` on 2026-09-23. Every item here carries positive evidence
of closure — a struck-through title, or `CLOSED` / `SUPERSEDED` in its opening.

Kept rather than deleted: these accounts are load-bearing under §19 Sub-pattern 15,
where a claim about what the code does has to be checkable against the record of what
was actually done. Several items here are also the only written account of an
incident and the rule it produced.

Nothing here is a standing rule. Where a rule survived a closed item, the rule is in
`CLAUDE.md` or `docs/claude/`; only the account is here. Section numbers unchanged.

---

3. ~~**EditLoadModal — post-conversion load edit UI.**~~ **CLOSED 2026-05-30 in v3.8.alu.** Pre-alu the backend `PUT /loads/:id` (`loadController.updateLoad`, authorized BROKER/ADMIN/CEO/DISPATCH) had full capability — poster/employee auth, status guard blocking COMPLETED/CANCELLED/TONU, customerId-lock on INVOICED/COMPLETED/POD_RECEIVED, ~30 editable fields — but ZERO frontend callers. When a customer changed a PO/weight/window/commodity post-conversion, ops had no UI path (cancel+recreate, raw API, or DB edit only). **Shape shipped:** new focused FLAT [`EditLoadModal`](frontend/src/components/loads/EditLoadModal.tsx) (deliberately NOT mirroring the 796-LOC 4-step `CreateLoadModal` wizard — too heavy + duplicative) over the commonly-edited post-conversion fields: route (origin/dest city/state/zip/company), schedule (PU/DEL dates + time windows), freight (weight/pieces/equipment/commodity/freight-class), financials (customer/carrier rate — gated to `canSeeMargin` margin-roles), special instructions. Pre-fills from the selected load; builds a `createLoadSchema.partial()`-compatible payload (numbers coerced, dates → ISO, empty strings omitted so an untouched field is never blanked; `specialInstructions` sent even when empty to allow clearing); PUTs `/loads/:id`; invalidates `["load", id]` + `["loads"]`. Edit button (Pencil icon, gold) added to the Load Board detail-panel action cluster, gated `canCreate && !["COMPLETED","CANCELLED","TONU"].includes(status)` — **mirrors the backend gate** so the AE never hits a 400 from the button. **Scoping decision on the "status-by-field permission matrix":** the backend does NOT enforce per-field status locks beyond (a) no-edit-at-all on terminal statuses and (b) customerId-lock on INVOICED-class. So the frontend keeps it simple — all listed fields editable on any non-terminal load, backend stays authoritative on the customerId edge. A richer per-field matrix (e.g. lock origin/dest after AT_PICKUP, lock pickup window after DISPATCHED) is **banked** — it would require backend enforcement first (the validator + updateLoad would own the matrix; the modal would mirror it), and is not BKN-blocking. **Scope:** ~250 LOC new component + ~15 LOC page wiring (import + showEdit state + Edit button + modal mount + Pencil icon). No backend change, no schema. Gates: frontend tsc + next build clean (backend tsc defensively clean). The Order Builder converted-order banner (v3.8.d.3) note "Editing here will not change the dispatched load" stays accurate — that banner is about the Order draft form; live-load edits now flow through this modal on the Load Board.

4. ~~**Order Builder origin/destContactName + Phone capture.**~~ **CLOSED retroactively 2026-05-20 in v3.8.g** (shipped earlier than the original §13.3 banking date). Sprint 61 Phase A audit on 2026-05-20 verified the contact inputs already exist in Section 2 (Route) at [`orders/page.tsx:886-903`](frontend/src/app/dashboard/orders/page.tsx#L886-L903) (origin) + [`orders/page.tsx:929-943`](frontend/src/app/dashboard/orders/page.tsx#L929-L943) (destination). Both inputs render unconditionally (outside the manualOriginMode / manualDestMode ladder), auto-populate from FacilityPicker when a saved facility with contactName/contactPhone is selected (selectOriginFacility handler at lines 244-245, selectDestFacility at 267-268), and accept manual entry via direct onChange handlers. Sprint 59.c gate now requires these fields and works correctly because the inputs already exist. Item 4 was banked stale; Sprint 60 was originally scoped as Item 4 but pivoted to close-only-docs once Phase A surfaced the existing v3.8.g shipping. **Sub-pattern 6 sub-rule c fire** — §13.3 entries can age out of sync with shipped code when the backlog isn't regularly audited against the codebase.

5. ~~**BOL page-1 footer collision**~~ — **CLOSED 2026-04-30 in v3.8.h**. Single-line `fyLine` coordinate fix (755 → 770) in `pdfService.ts`. Page-2 footer shifted identically since both reuse the same constant. All 9 signature fields preserved at original column positions.

**Security / Portal completeness:**

6. ~~**Portal Approval UI S-3**~~ — **CLOSED 2026-05-04 in v3.8.ee** (Phase 6.2 Lead Hunter / CRM separation sprint). Scope expanded mid-sprint to bundle the CRM read-filter gap from audit 39de1ad: (a) `?context=crm` filter on `GET /customers` so prospects no longer appear in CRM (commit `7c74bb1`); (b) `POST /customers/:id/approve` endpoint with required-checks gate over TIN, credit, contract preconditions, returns 422 with missing-checks payload (commit `2fb5279`, includes additive `approvedAt` + `approvedById` schema migration); (c) CRM page passes `?context=crm`, removes redundant client-side `statusOf()` segmentation and the four-pill filter bar (commit `0b53cf6`); (d) AE Console `OnboardingActionBar` mounted in `CustomerDrawer` — Approve / Suspend / Reject buttons with inline missing-checks rendering, brand-token styled (commit `df3545f`). Approve UI lives on the existing `/dashboard/crm` customer detail surface rather than a new `/dashboard/shippers` surface — the Drawer pattern is the AE-facing customer detail today. Suspend / Reject open TODO modals because no backend transitions exist yet — wires to Item 8.1 (v3.8.l customer inactivation). "Shipper application under review" UX page paired requirement is still open and tracked separately. The original §13.3 Item 6 scope (approve button only) was extended to the four-commit sprint after the audit surfaced the read-filter half of the bug.

8.1. ~~**Customer inactivation workflow.**~~ **CLOSED 2026-05-30 in v3.8.alr** (core (a)+(d)+(e) shipped; optional (b)+(c) banked). Pre-alr four orthogonal "status-like" fields existed on Customer (`status` free-form, `onboardingStatus` enum, `creditStatus`, linked `User.isActive`) but none combined into a canonical inactivate action, and **nothing prevented an AE creating new loads against an inactive customer.** The CRM `OnboardingActionBar` Suspend button opened a TODO modal explicitly pointing here. **Shape shipped:** (schema) new `Customer.isActive Boolean @default(true)` + `inactivationReason` + `inactivatedAt` + `inactivatedById` via additive migration `20260530160000_add_customer_inactivation` (default true → no backfill, all existing rows stay active). (gate) new [`checkCustomerActive(customerId, role)`](backend/src/lib/customerActive.ts) helper — returns `{ allowed, reason }`, hard-blocks non-admins, ADMIN/CEO override per spec — wired into BOTH canonical load-creation paths: [`loadController.createLoad`](backend/src/controllers/loadController.ts) (Load Board "+ New Load", after the credit gate, 403) + [`withTenderController.createLoadWithTender`](backend/src/controllers/withTenderController.ts) (Carrier Engagement Drawer Mode 1, after compliance, 403 `CUSTOMER_INACTIVE`). (endpoints) `POST /customers/:id/inactivate` (reason ≥5 chars required, audit-logged via `logCustomerActivity`, idempotent on already-inactive) + `POST /customers/:id/reactivate` (clears metadata, idempotent), both ADMIN/CEO. (UI = (d) CRM banner) `OnboardingActionBar` rewired with a three-state priority: `!isActive` → red Inactive banner + reason + Reactivate (top priority regardless of onboardingStatus); `isApproved && active` → green banner + Inactivate button; `!isApproved` → Approve/Reject. The prior Suspend TODO modal is now a real Inactivate flow with reason capture; "Reject" remains a banked TODO (distinct onboarding-stage concern). **Skipped per §3.3 atomic scope (the spec's "optionally" clauses):** (b) notification silencing + (c) cascade to `User.isActive=false`/`onboardingStatus=SUSPENDED` — banked as small follow-ups. **(c) RATIFIED BANKED 2026-09-19, not fixed in the lifecycle-gaps arc:** customer inactivate leaves the shipper login alone. A shipper who can sign in but cannot create loads is acceptable; a login that silently dies on inactivate is worse. Carrier archive (v3.8.bdi) does deactivate its login, and that divergence is deliberate and recorded at §13.3 Item 285. Also banked: a CRM list-row "Inactive" badge (the detail-surface banner is the v1 surface). **Scope:** ~200 LOC across schema + 1 migration + 4 backend files (customerActive helper, customerController 2 endpoints, customers route, loadController + withTenderController gates) + 2 frontend files (CrmCustomer type, OnboardingActionBar). Gates: backend tsc + vitest 224/224 + frontend tsc + next build all clean. **Post-deploy:** Render runs `prisma migrate deploy` on the new additive migration (~1s ALTER); verify `prisma migrate status` clean. Original sprint-shape note about "Order Builder + convert-to-load do not check customer status" is now moot — convert-to-load was deleted in Sprint 59 (v3.8.acj); the drawer's `withTenderController` is the canonical path and now carries the gate.

8.2.1. ~~**Document `sales@silkroutelogistics.ai` alias in §1 (small docs sprint).**~~ **CLOSED — the documentation landed 2026-05-19 (v3.8.aej; §1's own `sales@` entry says so) and this row was never struck; the `frontend/public/ae/communications.html:71` reference it cites went with that tree in v3.8.asc (2026-08-17). Marked closed 2026-09-21; original text follows.** Surfaced 2026-05-02 during v3.8.m crawlability audit. Reference exists in [`frontend/public/ae/communications.html:71`](frontend/public/ae/communications.html#L71) — labeled as the inbound prospect/lead reply address — but `sales@` isn't documented in CLAUDE.md §1's canonical email list (which has `whaider@`, `accounting@`, `compliance@`, `noreply@`, `operations@`). Same pattern as the 2026-05-01 `operations@` documentation gap. Decision needed: (a) document `sales@` as a canonical alias parallel to `operations@` with explicit current-routing-target (likely `→ whaider@` until first hire); OR (b) replace with `operations@` and remove the standalone `sales@` references; OR (c) replace with `whaider@` for pre-revenue clarity. Sprint shape: ~5 lines in CLAUDE.md §1, possibly +1 line in /ae/communications.html if option (b) or (c). Deferred per directive — not rolled into v3.8.m or v3.8.n. Tiny sprint, slot whenever convenient.

8.2.2. ~~**CRM facility form missing operating-hours inputs.**~~ **CLOSED 2026-05-05 in v3.8.vv** alongside Item 8.2.3 per the recommended-bundling note. 7-day grid (Mon-Sun) shipped with open/close time inputs + per-day Closed checkbox; storage shape `{ monday: { open, close, closed }, ... }`. Hydrates from existing JSON on edit mode; defaults to all-empty/all-open on new-facility mode. Item 20 (Order Builder G facility-hours auto-populate) can now consume this. Original entry preserved below for history.

8.2.2-original. **CRM facility form missing operating-hours inputs.** Surfaced 2026-05-02 from CRM walkthrough screenshot. `CustomerFacility.operatingHours: Json?` exists in schema ([prisma/schema.prisma:2053](backend/prisma/schema.prisma#L2053), keyed by weekday with `{open, close}` per day) but the CRM facility-creation form ([frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx](frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx)) has zero references — operatingHours is never written. AE creates facilities with name, address, contact, dock info, primary flag, lumper info, special instructions — but no hours. **Prerequisite for §13.3 Item 20 (Order Builder G facility-hours auto-populate)** — that item reads `operatingHours` to auto-fill PU/DEL window times, but the field is null on every existing facility because the form doesn't capture it. Same architectural pattern as v3.8.j and 8.3 (data plumbing exists, UI doesn't connect). Sprint shape: 7-day input grid (Mon-Sun) with open/close time inputs per day, "Closed" toggle per weekday, optional "24/7" toggle for distribution centers; serialize to JSON `{ monday: { open: "08:00", close: "17:00", closed: false }, ... }`; read existing JSON in edit mode. ~80-120 lines, single file (FacilitiesTab.tsx). Pairs naturally with Item 20 — could ship as a duo (this sprint captures the data, Item 20 uses it). **See also 8.2.3** (facility row missing Edit action) — recommend shipping 8.2.2 + 8.2.3 as ONE combined sprint since both touch the same component + form, ~120-180 lines combined.

8.2.3. ~~**CRM facility row missing Edit action.**~~ **CLOSED 2026-05-05 in v3.8.uu.** AddFacilityForm refactored into shared FacilityForm component accepting `existing: CrmFacility | null` prop. PATCH route was already wired (crmCustomer.ts:138-167); frontend now consumes it via the new Edit button (Pencil icon, gold-dark) on each facility row. Bundled with §13.3 Item 8.2.2 per the original coupling note — both shipped in the same session as two atomic commits (v3.8.uu form refactor + Edit button; v3.8.vv operatingHours + contactEmail inputs). Original entry preserved below for history.

8.2.3-original. **CRM facility row missing Edit action.** Surfaced 2026-05-02 from CRM walkthrough — facility row in customer profile shows ONLY a "Remove" button. No way to update name, address, contact, dock info, primary flag, lumper info, special instructions, or load type once the facility is created. Backend is fully wired: `PUT /customers/:id/facilities/:facilityId` exists at [`backend/src/routes/crmCustomer.ts:138`](backend/src/routes/crmCustomer.ts#L138) with 404-guard via `findUnique` then `prisma.customerFacility.update`. Frontend [`FacilitiesTab.tsx`](frontend/src/app/dashboard/crm/tabs/FacilitiesTab.tsx) only wires `useQuery` for read, `del` mutation for DELETE, and the Add form for CREATE. **Zero update mutation, zero edit-mode UI.** Same architectural pattern as 8.2.2 (operating-hours form gap), §13.3 Item 8.3 (notification routing), v3.8.j (tender workflow): backend complete, UI incomplete. **Tightly coupled with 8.2.2** — both touch the same component (FacilitiesTab.tsx) and both need the form to be shared between create + edit modes (e.g. `editingFacilityId: string | null` state + form-mode prop). Sprint shape: ship 8.2.2 + 8.2.3 as ONE combined sprint — refactor the existing add-facility form into a Create/Edit shared form, wire `PUT /customers/:id/facilities/:facilityId` mutation, add Edit button alongside the existing Remove on each row, support pre-populating fields from the existing JSON (including the new operatingHours inputs from 8.2.2). Combined effort: ~120-180 lines, single file. Splitting them risks two passes through the same component.

8.3. ~~**Wire shipper notifications through `CustomerContact[]` with role-based routing.**~~ **CLOSED 2026-09-02 in v3.8.ayq → v3.8.ayt.** Open for four months, and it fired: on 2026-09-02 three milestone emails and a `CRITICAL DELAY` reached `accountspayable@beekeepersnaturals.com` about loads that were test data. The AE had already removed the tag on the `logistics@` CRM contact and the emails did not stop — because the tag and those senders were never connected. This row's own text predicted it exactly: *"AE can carefully maintain a multi-contact CRM list and none of those rows affect who actually receives shipment notifications."*

**THE RESOLVER CONTRACT** — [`services/customerRecipientResolver.ts`](backend/src/services/customerRecipientResolver.ts), two functions:

| | Chain | Eligibility |
|---|---|---|
| `resolveOperationalRecipients(loadId, {requireTrackingLink?})` | `Load.contactEmail` → eligible CRM contacts → **STOP** | `doNotContact=false AND (isPrimary OR receivesTrackingLink)` |
| `resolveBillingRecipients(customerId)` | `isBilling` contact → `billingContactEmail` → `billingEmail` → `Customer.email` | `doNotContact=false AND isBilling` |

**Operational NEVER falls through to `Customer.email`.** That is the fix, not an oversight — falling through is what put AP on a delay alert, because that column is also the invoice recipient (47 read sites). Zero recipients means the send is skipped and a warning logged naming the load. Billing DOES end there, deliberately: AP belongs on an invoice and not on a delay alert. A reader "fixing" billing by symmetry would stop invoices reaching anyone.

**`receivesTrackingLink` means tracking links only** (ratified 2026-09-02). Turning it off must not silence a primary contact's operational mail, or the fix would punish the AE for the action they took. Tiers cascade — first tier with an address wins. `doNotContact` is honoured at every tier **including the raw address columns**: an address an AE suppressed must not be reached through a different column.

**BILLING IS A NO-OP TODAY, ACROSS ALL 80 CUSTOMERS.** Measured 2026-09-02: zero `isBilling` contacts, zero `billingContactEmail`, zero `billingEmail` — so every customer resolves at tier 4, the same address those paths already used. It becomes live the moment an AE tags a billing contact. Built now because the alternative is tagging a contact and having nothing read it, which is the failure this row records.

**Collateral fixed in the same arc:** the CRM tracking fan-out filtered `receivesTrackingLink` and nothing else, so a do-not-contact contact still received tracking links; `processArReminders` filtered `deletedAt` on the *invoice* and never looked at the load, so neither cancelling nor soft-deleting a load stopped dunning (BKN was saved only by having zero invoices); and four dead senders in `shipperLoadNotifyService` with zero callers were deleted. A one-off backfill (`scripts/backfill-customer-contacts-from-email.ts`) gave the two customers that had loads but no contacts a primary contact from `customers.email`, skipping BKN — backfilling *its* `customers.email` would have written the AP address back in as an operational contact.

**Still true and worth knowing:** `doNotContact`, `isBilling` and `salesRole` had **zero read sites** before this arc; `doNotContact` and `isBilling` now have one each. `salesRole` and `introducedVia` remain write-only CRM decoration.

8.5. ~~**Frequent-addresses wire-up — `PATCH /address-book/:id/use`.**~~ **CLOSED 2026-05-25 in v3.8.akr** (AddressBook dead-model deletion). Phase A audit confirmed structurally orphaned model: zero frontend consumers (grep `frontend/src/` returned only 2 VersionFooter comment mentions), zero backend service/controller consumers (grep `backend/src/` returned only the route file's own internal refs), zero seed/script references, zero FKs from other models. Mounted at `/api/address-book/*` since baseline but never wired into any picker — `usageCount` + `lastUsedAt` stayed at zero forever. Original Item 8.5 framing offered two paths: (a) wire the picker into Order Builder origin/dest, OR (b) delete the dead model. **Path (b) chosen** because `CustomerFacility` (via `FacilityPicker`) is the active canonical address primitive per §13.3 Items 8.2.2 + 8.2.3 closures (v3.8.uu/vv); Order Builder Section 2 already auto-fills from `CustomerFacility.contactName/contactPhone` (Item 4 retroactive closure). Wiring AddressBook into the same picker surface would create the parallel-endpoint class banked at Items 40 + 158. **Changes (atomic per §3.3):** DELETED `backend/src/routes/addressBook.ts` (147 LOC, 6 endpoints); REMOVED `addressBookRoutes` import + `/address-book` mount from `routes/index.ts`; REMOVED `model AddressBook { ... }` block from `schema.prisma` (29 lines including divider); NEW migration `prisma/migrations/20260525150000_drop_address_book_dead_model/migration.sql` (single `DROP TABLE IF EXISTS "public"."address_book" CASCADE;` per §2.2 manual discipline). ~180 LOC removed + 1 migration + version bump. Pre-commit gates per Sub-pattern 11: prisma generate clean (client regenerates without AddressBook type), backend tsc + npm test clean, frontend tsc + next build clean. **Sub-pattern 6 sub-rule c case study extension:** initial CLAUDE.md grep for `AddressBook|addressBook|address-book` returned 13 files; audit-first discipline required classifying each (route-self / schema-self / version-history comments / archived-wiki docs) before inferring "13 references means active." Authoritative source = full classification of every match, not raw match count.

8.10. ~~**Backfill prisma/migrations file for ProspectVertical schema change + investigate Render deploy-chain.**~~ **CLOSED 2026-05-09 in v3.8.aax** (Sprint 44b atomic close). Sprint 44a Track 1 + Track 2 audit caught actual drift scope was **81 of 87 prod enums** plus their corresponding tables/columns — far beyond the original Item 8.10 ProspectVertical-only scope. v3.8.aa-dd (Apr 24 → May 4 window) shipped schema work via manual `prisma db push` to Neon, bypassing migration history entirely. Compounding root cause: Render Build Command lacked `prisma migrate deploy` step entirely, so the v3.8.ee `add_customer_approval_fields` migration that was authored + committed never applied via the deploy chain. **Fix shape (Sprint 44b atomic):** P1 — Render Build Command updated to canonical `npm install && npm run build && npx prisma migrate deploy && npx prisma migrate status --exit-code && cp -r src/assets dist/backend/src/assets`. P2 — baseline reset: `_prisma_migrations` ledger cleared on Neon, single `20260509170000_baseline_init` migration generated from `prisma migrate diff --from-empty --to-schema-datamodel` (3,652 lines, 87 enums, 120 tables) captures complete prod schema state, marked applied via `prisma migrate resolve --applied`. Prior 15 migrations archived to `backend/prisma/_archived_migrations_2026-05-09/` for posterity (NOT part of active chain). P3 — `prisma migrate status --exit-code` post-deploy gate fails build on any pending migrations (silent-skip class catch). P4 — single canonical schema mutation path doc at CLAUDE.md §2.2 + render.yaml + ci.yml comment all aligned in this commit (Pattern 6 spatial sub-mode contradiction resolved). PITR upgraded to 7 days (Neon Launch tier) before Phase 2 baseline reset for safety. **Sprint 44a Track 1 lesson:** initial grep on `ProspectVertical` returned correct empty result, but the conclusion "this is the only drift" was wrong — full audit needed direct `pg_type` + `information_schema` query (CSV of 87 enums) to surface complete scope. Pattern 6 sub-rule c candidate: "audits can be wrong" / verify prod-touching action against authoritative source before acting (queued for Sprint 44.5 §19 update). Original entry preserved below for history. **v3.8.alg lineage closure annotation (2026-05-27):** the full deploy-chain investigation arc — 8.10 (Sprint 44b baseline reset + canonical buildCommand) → Item 191 (Prisma + Neon DIRECT_URL split, v3.8.ajg/ajs) → v3.8.ale (citext migration via $executeRawUnsafe bypass surfaced ledger-drift class) → v3.8.alf (ledger reconciliation via `prisma migrate resolve --applied` + render.yaml docs gap closure) — is now **empirically validated** via the v3.8.alf Render build log. Three independent lines of evidence: (1) `check-direct-url.js` guard (v3.8.ajs canonical) fired correctly + reported `✓ DIRECT_URL configured correctly (hostname: ep-green-frog-ajsgv9me.c-3.us-east-2.aws.neon.tech, no -pooler)` — confirms Render dashboard env var matches §2.2 canonical; (2) `prisma migrate deploy` reported `21 migrations found in prisma/migrations / No pending migrations to apply.` — proves the v3.8.alf ledger-resolve landed on prod's `_prisma_migrations` table end-to-end (Prisma sees the citext migration as applied; no SQL re-run; no P1002); (3) `prisma migrate status` Sprint 44b post-deploy gate reported `Database schema is up to date!` — verifies clean state after deploy. All defensive instrumentation from the 8.10 → 191 work (Sprint 44b baseline reset + post-deploy migrate status gate + Sprint 44b cp -r steps + v3.8.ajs check-direct-url.js guard + Sprint 47 trailing-dot cp form) operating as designed. The investigation arc is closed; future schema migrations inherit the validated chain. Any subsequent failure mode in this surface would represent a NEW class, not a regression of 8.10's documented closures. Pattern 6 sub-rule c "audits can be wrong" promoted to §19 canonical at Sprint 44.5 + extended through v3.8.alf (sub-rule c case study extension #2 — backlog-row-drift vs §11-history-row, banked as Sub-pattern 15 promotion candidate per Item 191 row). Net lineage outcome: a class of bug (silent migration drift between application schema state + deployed DB state) that surfaced repeatedly in v3.8.aa-ee + v3.8.ajf and required 5 architectural sprints to fully contain (44a/44b/ajg/ajs/alf) now has structural defenses sufficient that the next incident requires either (a) a NEW failure mode not covered by existing guards, OR (b) deliberate guard removal/circumvention. Banking lineage status as CLOSED-AND-EMPIRICALLY-VALIDATED.

8.10-original. **Backfill prisma/migrations file for ProspectVertical schema change + investigate Render deploy-chain.** Surfaced 2026-05-04 during v3.8.v–v3.8.cc Lead Hunter sprint Step 4 verification. v3.8.aa (C4 commit [`6e2e6d9`](commit/6e2e6d9)) applied the `ProspectVertical` enum + `Customer.vertical` column via `prisma db push` directly to Neon. No migration file exists in [`backend/prisma/migrations/`](backend/prisma/migrations/). Production currently works because the live Neon DB matches the regenerated Prisma client — but a fresh DB clone (staging branch, dev workstation reset, disaster-recovery rebuild) will not pick up the enum + column from `migrate deploy` because there is no migration file to apply. **Risk profile: recovery-time gap, not runtime.** The Render `buildCommand` runs `npx prisma migrate deploy` on every deploy, which will be a no-op for this change because Neon already has it; the runtime `@prisma/client` is regenerated from the schema file and matches. Failure mode is invisible until someone resets a database from migrations alone, at which point the new enum + column will be missing and the type-system promise the client exports will diverge from the live schema. **Fix shape:** generate the missing migration file from the current schema diff using `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` (or against a baseline that excludes only the C4 change), place the resulting SQL in `backend/prisma/migrations/<timestamp>_add_prospect_vertical/migration.sql`, commit docs-only. **Test:** clone Neon to a staging branch, run `npx prisma migrate deploy` against it from scratch, confirm `ProspectVertical` enum exists and `customers.vertical` column exists with default `UNKNOWN`. Render's next prod build should be a no-op because Neon already has the schema. **Pairs with Items 8.6/8.7 audit hygiene** — both are recovery-time-gap issues that don't surface until you actually need to recover. Effort: small (~15-30 min including staging-clone test). Tracked here so the gap is closed in a dedicated mini-sprint, not silently absorbed. **Future-proofing:** the broader CLAUDE.md §2 directive to "avoid `prisma migrate dev`" because migration history has drift is itself a long-term risk; consolidating to a single baseline migration + going-forward migrate-deploy discipline is a separate architectural sprint outside Item 8.10's scope.

**Scope expansion 2026-05-05** — Item 8.10 was originally scoped to the v3.8.aa `db push` recovery-time gap (the `ProspectVertical` migration that was never authored). A separate but materially worse case surfaced during v3.8.oo verification: the Phase 3 (v3.8.ee, commit [`2fb5279`](commit/2fb5279), 2026-05-04) migration `20260504000000_add_customer_approval_fields` WAS authored, WAS committed, AND was NEVER applied to prod across ~24h and 9+ subsequent deploys (v3.8.ff through v3.8.oo). Track 1 diagnostic on 2026-05-05 confirmed: Query A returned zero rows for `customers.approved_at` + `customers.approved_by_id`; Query B returned zero rows in `_prisma_migrations` for any matching ledger entry. Manual `prisma migrate deploy` from local at 2026-05-05 17:35:00 UTC applied the migration cleanly in one step (post-DATABASE_URL-parity confirmation by Wasi + a Neon password rotation propagated to both Render env and local `.env`), demonstrating the migration itself was correct — failure was somewhere in Render's deploy chain. **Root-cause candidates to investigate (Phase 6 sprint):** (1) Render dashboard `buildCommand` drift from CLAUDE.md §2.2's documented spec — the dashboard is the SOT per §2.2 and `render.yaml` is cosmetic-only, so a manual edit could have weakened the chain; this is the most likely cause given §2.2 explicitly warns about this drift class. (2) `prisma migrate deploy` silently skipping or absent from the actual dashboard `buildCommand` (e.g. someone removed the `&& npx prisma migrate deploy` segment to work around an unrelated transient error and never restored it). (3) Build artifact cache holding a stale Prisma client that doesn't request the new columns at runtime, masking the drift until a query that actually needs the columns finally hits it. (4) Deploy chain runs migrate deploy but with a DATABASE_URL pointing at a different database than the runtime backend reads — would explain silent green builds with absent prod migrations; somewhat ruled out by the 2026-05-05 parity confirmation but worth a dashboard verification. **Fix surface:** (a) capture current dashboard `buildCommand` value as evidence, then re-align it with the documented value at CLAUDE.md §2.2 verbatim; (b) add a post-deploy schema-verify check that fails the build if `prisma migrate status` reports any pending migrations after `migrate deploy` ran (treats silent-skip as an explicit deploy failure rather than a transparent miss); (c) optional: periodic migration-drift cron (weekly) running `prisma migrate status` against prod and alerting `compliance@` on any pending migrations — captures drift even if it slips past a future deploy. **Effort:** medium sprint. Investigation step is ~30-60 min in Render dashboard + Render build log review for v3.8.oo's deploy. Fix steps are small but blocked on diagnosis. **Priority:** elevated — every future schema migration is at risk until this is closed. Pairs with the v3.8.aa migration backfill from the original Item 8.10 scope; both should land in the same sprint.

**UX / Visual:**

12.1. ~~**Accounting Console crash — `/accounting/analytics` null-toFixed.**~~ **CLOSED 2026-05-09 in v3.8.aau** (Sprint 41, scope expanded). Pattern 7 (catalogued Sprint 40c) `grep "marginPercent.*toFixed"` surfaced **4 unguarded crash sites**, not the 2 documented at logging time. Sprint 41 closes all four in one atomic per Sprint 39 Item 56 expansion precedent: `accounting/analytics:160`, `accounting/pnl:145` (Item 12.2), **`dashboard/finance:497` (NEW finding)**, **`dashboard/lane-analytics:366` (NEW finding)**. Em-dash fallback applied per audit recommendation (matches existing `/accounting/pnl:148` revenuePerMile pattern). Conditional class also null-guarded → neutral slate-400 instead of misleading red on null. Silent-NaN comparison fix bundled at `dashboard/finance:443-446` (`loads.filter((l) => l.marginPercent < 10)` was undercounting buckets when margin was null since NaN-coerce-false). `lane-analytics:127` reduce sum filtered to computed margins only. Type relaxes: `LoadPnl.marginPercent`, `LaneStat.marginPercent` → `number | null`. E2E B12 render-check extension added — visits all 4 surfaces post-tender, asserts no React error boundary text + no fatal console errors. Original entry preserved below for history.

12.1-original. **Accounting Console crash — `/accounting/analytics` null-toFixed.** Surfaced 2026-05-06 during v3.8.aab Sprint 24 smoke. Page renders full-screen "Something went wrong / Cannot read properties of null (reading 'toFixed')" error boundary, blocking AE Console analytics view entirely. Crash site: [`accounting/analytics/page.tsx:160`](frontend/src/app/accounting/analytics/page.tsx#L160) — `pct(l.marginPercent)` where the `pct` helper at line 10 is typed `(n: number) => string` and calls `.toFixed(1)` unconditionally. When any load row in the response has null `marginPercent` (likely when revenue=0 or cost=0 yields an undefined margin on the backend), the helper crashes. Same null-data crash class as 12.2. Severity: P1 — internal AE Console, full-page crash, blocks financial analysis surfacing. Sprint shape: paired with 12.2 below — single sprint covers both crash sites since root cause is shared (`Load.marginPercent` nullable + render code assumes non-null). Fix surface: (a) audit backend whether `marginPercent` should default to 0 or stay nullable; (b) if nullable, every UI render path must null-guard; recommend `(l.marginPercent ?? 0).toFixed(1)` or "—" em-dash fallback consistent with existing `revenuePerMile ? toFixed : "—"` pattern at [`pnl/page.tsx:148`](frontend/src/app/accounting/pnl/page.tsx#L148). Effort: ~20-40 lines across 2 files + decision on null-default vs em-dash. Single sprint.

12.2. ~~**Accounting Console crash — `/accounting/pnl` null-toFixed.**~~ **CLOSED 2026-05-09 in v3.8.aau** (Sprint 41). Bundled with 12.1 close — see 12.1 for full closure details + 2 expanded-scope sites (`dashboard/finance` + `dashboard/lane-analytics`) caught by Pattern 7 grep. Original entry preserved below for history.

12.2-original. **Accounting Console crash — `/accounting/pnl` null-toFixed.** Surfaced 2026-05-06 during v3.8.aab Sprint 24 smoke. Same symptom as 12.1: full-screen "Cannot read properties of null (reading 'toFixed')" error boundary, blocking the Load P&L view. Crash site: [`accounting/pnl/page.tsx:145`](frontend/src/app/accounting/pnl/page.tsx#L145) — `load.marginPercent.toFixed(1)` with no null guard, inside the per-load row render. Note: same file at [`:148`](frontend/src/app/accounting/pnl/page.tsx#L148) DOES correctly null-guard `load.revenuePerMile` with the `?` ternary fallback to em-dash, so the pattern is established but inconsistently applied. KPI cards at [`:74-75`](frontend/src/app/accounting/pnl/page.tsx#L74) use `(data?.avgMarginPercent || 0).toFixed(1)` which is safe — the bug is only in the per-row table cell. Severity: P1, same as 12.1. **Bundles with 12.1** — single sprint covering both crash sites, since the fix is "audit `Load.marginPercent` nullability + apply the em-dash fallback pattern from line 148 to every render path." See 12.1 for sprint shape and effort estimate.

**Architectural debt (defer until ops stable):**

19. ~~**Order Builder F: PU/DEL window time inline with dates**~~ **CLOSED retroactively 2026-05-20 in v3.8.ss** (shipped earlier than the original §13.3 banking date). Sprint 61 Phase A audit verified the 4-column grid at [`orders/page.tsx:952-971`](frontend/src/app/dashboard/orders/page.tsx#L952-L971) already renders pickup-date + PU-window + delivery-date + DEL-window on one row with cols 1-2 aligned under the origin facility column and cols 3-4 aligned under the destination facility column. Inline comment at lines 947-951 explicitly references the v3.8.ss reorder. Item 19 was banked stale. Same sub-pattern 6 sub-rule c lineage as Item 4 retroactive closure (above).

26. ~~**Dead `.login-dropdown` CSS block** in `utilities.css`.~~ **CLOSED 2026-06-01 in v3.8.amm.** Confirmed unused (grep across `frontend/public/**/*.html` returned 0 refs — the canonical `.nav-login-*` dropdown superseded it). Removed the whole block (~48 lines); it held the file's last non-canonical olive-gold (`#C8963E`/`#B8862E`/`rgba(200,150,62)`) + legacy-navy (`#0D1B2A`/`#0F1E30`) values. Removed as part of the v3.8.amm shared-CSS gold-drift fix (utilities.css `--gold` `#C8963E`→`#C5A572`, `--gold-light` `#D4A84E`→`#DAC39C`, 6 hardcoded `#C8963E`→`var(--gold)`). utilities.css now 0 non-canonical gold/navy literals. **Banked (separate surfaces, NOT touched):** `tracking.css` (1 legacy public page still on `--gold: #C8963E`) + `auth/root-login.css` + `auth/root-register.css` (9 hardcoded `#C8963E`, app-login surface — needs role-mapping pass).

31. ~~**BOL QR + email tracking links 404 on production.**~~ **CLOSED 2026-05-06 in v3.8.aae** (Sprint 26). Phase A discovered the directive's recommended U1 (add `app/track/[token]/page.tsx` dynamic route) was structurally blocked by `next.config.ts:5` `output: "export"` — Next.js 15 requires `generateStaticParams()` for all dynamic routes under static export, and runtime-generated 12-char tokens cannot be enumerated at build time. Recommended and shipped U2 instead: SPA-rewrite via `_redirects` status-200 (same pattern as existing `/carrier/*` rule). [`frontend/public/_redirects`](frontend/public/_redirects) gained 2 new rules (`/tracking/* /track/:splat 301` for legacy email link backward-compat; `/track/* /track 200` for QR/bookmark deep-link rewrite). [`frontend/src/app/track/page.tsx`](frontend/src/app/track/page.tsx) gained `tokenFromUrl` state + mount-time `useEffect` that reads `window.location.pathname`, decodes the token, and auto-fires the existing `/api/tracking/:token` endpoint — search UI hidden in token mode (B3 result-only), error fallback shows red message + "Track another shipment" link back to `/track`. **Email URL consolidation expanded scope:** Sprint 25 Phase A undercount (1 site flagged) corrected during Sprint 26 Phase A — 4 sites total swapped from `/tracking/<token>` to `/track/<token>`: [`shipperLoadNotifyService.ts:48`](backend/src/services/shipperLoadNotifyService.ts#L48), [`shipperLoadNotifyService.ts:316`](backend/src/services/shipperLoadNotifyService.ts#L316), [`shipperPortalController.ts:745`](backend/src/controllers/shipperPortalController.ts#L745), [`shipperNotificationService.ts:258`](backend/src/services/shipperNotificationService.ts#L258). Backend `/api/tracking/:token` 5-tier fallback chain UNCHANGED. `shipperCode` dead-schema cleanup deferred per Sprint 25 precedent. Original entry preserved below for history.

31-original. **BOL QR + email tracking links 404 on production.** Surfaced 2026-05-06 during Sprint 25 (`/track` page contrast fix) Phase A data-model parity audit.

**RC Modal Caravan tier reconciliation — Sprint 33 (logged + closed 2026-05-06 in v3.8.aal):**

44. ~~**RC Modal Section 8 (Payment Terms) still rendered legacy 6-tier card grid (Flash/Express/Priority/Partner/Elite/Standard) instead of canonical Caravan 3-tier (Silver/Gold/Platinum) per CLAUDE.md §8.**~~ **CLOSED 2026-05-06 in v3.8.aal** (Sprint 33). P0 BKN-blocking — RC PDFs to BKN's carriers reference payment terms that don't exist in customer-facing Caravan Partner Program. Same credibility class as Sprint 30 Houston-address fix. Sprint 23 reconciled `/accounting/quick-pay` + `/accounting/payments` to canonical Caravan structure but missed RC Modal in scope. **Phase A confirmed at [`RateConfirmationModal.tsx:226-233`](frontend/src/components/loads/RateConfirmationModal.tsx#L226):** legacy `PAYMENT_TIERS` const drove a 6-card grid in `SectionPayment`. **Fix shape per Sprint 23 pattern (UI rebrand, backend stay-canonical):** UI presents 3 SPEED choices (AE picks the speed for THIS load); fee dynamically derived from carrier's tier × chosen speed per §8 structure. Backend `PaymentTier` enum preserved unchanged. **NEW constants:** `PAYMENT_SPEEDS` array `[STANDARD, QP_7DAY, QP_SAMEDAY]`; `feePctForSpeed(speedUiKey, tier)` helper computing tier × speed fee per §8 (`sevenDay = 1/2/3 by Platinum/Gold/Silver`; `sameDay = sevenDay + 2` universal); `standardNetByTier(tier)` returning `Net-14/21/30` per Platinum/Gold/Silver; `uiKeyFromEnum(enumValue)` reverse map for legacy display (EXPRESS→QP_SAMEDAY, PARTNER→QP_7DAY, ELITE→STANDARD); `LEGACY_PAYMENT_TIERS` preserved as fallback display lookup for existing-load rendering. **UI structure:** 3-card SPEED selector replaces 6-card tier grid; Caravan tier badge added to section header (auto-derived from `selectedCarrier.tier` via shared `InfoBadge` component, canonical Sprint 24 palette: PLATINUM/GOLD use `gold` variant, SILVER uses `default`); each card sub-text shows tier-derived value (Standard: Net-N days; QP cards: tier × speed fee %). **Enum mapping on save:** STANDARD UI → STANDARD enum (168h SLA), QP_7DAY UI → PRIORITY enum (48h SLA, closest to 7-day mental model), QP_SAMEDAY UI → FLASH enum (2h SLA). **EXPRESS/PARTNER/ELITE legacy enum values become AE-unselectable** from new RC Modal but persist in DB for analytics + legacy load display; existing loads with these values render via `uiKeyFromEnum` fallback and can re-save into the new 3-bucket scheme. **FormState gained `carrierTier: string` field** (defaults empty; reads from `load.carrier.carrierProfile.tier` on `initForm`; `selectCarrier` writes `carrier.tier` on selection). `financials.feePercent` calc rebased from legacy enum lookup to `feePctForSpeed(uiKeyFromEnum(form.paymentTier), form.carrierTier)` — fee responds to tier × speed dynamically. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net source change: ~80 LOC across 1 file (RateConfirmationModal.tsx). Per §3.1: v3.8.aak → v3.8.aal. AE Console + customer-facing RC PDF surface — version-bump justified.

**RC Modal usability + observability — Sprint 32 (logged 2026-05-06 in v3.8.aak):**

41. ~~**RC Modal carrier dropdown color mismatch — dark navy bg against cream modal shell.**~~ **CLOSED 2026-05-06 in v3.8.aak** (Sprint 32 Path β). Phase A audit confirmed [`RateConfirmationModal.tsx:1520`](frontend/src/components/loads/RateConfirmationModal.tsx#L1520) used `bg-[#1a2340]` (off-canonical dark navy, not in §2.1 token set) for the carrier search dropdown container, with `text-white` carrier name and `hover:bg-white/5` translucent hover state. Worked technically (~17:1 contrast on dark) but rendered discordantly against the cream-themed modal shell surrounding it. Likely template-imported from same Texas-based-shipper boilerplate as Sprint 30 found, except cosmetic rather than data-wrong. **Fix per Path B (cream bg matching modal context):** `bg-[#1a2340] border-white/10` → `bg-white border-slate-200`; `hover:bg-white/5` → `hover:bg-slate-50`; carrier name `text-white` → `text-slate-900`. Tier badges (canonical Sprint 24 Silver/Gold/Platinum palette via shared `InfoBadge` component) untouched — already canonical. "No carriers found" empty state `text-slate-500` unchanged (passes AA on white ≈5:1). 3-LOC swap. RC modal is cream-themed regardless of global theme mode (matches Order Builder pattern), so no cross-mode regression risk.

42. ~~**RC Modal Generate PDF + Send Tender buttons fail silently — quickPayFeePercent string-vs-number type mismatch.**~~ **CLOSED 2026-05-06 in v3.8.aam** (Sprint 34). Path β methodology cycle complete (Sprints 32 → 33 → 34) for the RC modal silent-fail class. **Cycle summary:** Sprint 32 (v3.8.aak) shipped error UI surfacing on all 3 RC handlers — silent error swallow class closed at observability layer. Sprint 33 (v3.8.aal) widened extractor to iterate Zod `details[]` array — generic "Validation error" banner became field-specific "Validation error — formData.X: <message>". Sprint 34 (v3.8.aam) shipped the targeted field fix once Sprint 33's extractor surfaced the specific rejection: `formData.quickPayFeePercent: Expected number, received string`. **Root cause:** [`RateConfirmationModal.tsx:188`](frontend/src/components/loads/RateConfirmationModal.tsx#L188) declares `quickPayFeePercent: string` in FormState (input ergonomics — three write paths all stringify via `String(...)`); backend Zod schema at [`validators/rateConfirmation.ts:117`](backend/src/validators/rateConfirmation.ts#L117) declares `z.number().optional()`. [`buildPayload()`](frontend/src/components/loads/RateConfirmationModal.tsx) spread `...form` carried the string unchanged — other numeric fields had explicit `parseFloat`/`parseInt`/`toNum` coercion before send, this one was the lone gap. **Fix shape — Path X1 (surgical buildPayload coercion):** added 3-line explicit coercion `quickPayFeePercent: form.quickPayFeePercent !== "" ? Number(form.quickPayFeePercent) : undefined`. **`!== ""` check preserves null-vs-zero distinction** (Phase A audit caught this): empty input → undefined → DB null (no value); explicit `"0"` → `Number(0)` = 0 → DB 0 (legitimate "no fee" STANDARD tier selection); `"1.5"` → 1.5 (correct). Alternative `Number(...) || undefined` rejected — would silently mask "0" as undefined, breaking STANDARD tier no-fee semantics. **Path X3 (`z.coerce.number()` validator change) rejected** per Phase A — changes validator semantics across schema, risks empty-string-to-zero confusion on other fields, belongs in §13.3 Item A28-2 architectural sprint. **Sweep result:** only `quickPayFeePercent` affected — single-field issue, not bug class. Other 10 `z.number()` validator fields are either explicitly coerced in buildPayload or not in payload at all. **Path β methodology validation:** ~38 LOC total across 3 atomic commits to retire 3 silently-broken handlers. Zero speculation, zero regressions. Error UI + widened extractor remain as PERMANENT diagnostic infrastructure for any future RC handler API failures. Pattern propagation candidate for other modals/forms across the codebase. **Item 45 logged (separate)** for follow-up cleanup of unvalidated payload fields. **Item A28-2 elevated** — today's cycle demonstrates the cost of "validator strict, frontend payload promiscuous" pattern; A28-2 architectural sprint (validator hardening via `z.preprocess` shape coercion at validator boundary) would prevent this class entirely; worth post-BKN priority bump from deferred queue. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net source change: 3 LOC (smallest hotfix shape). Per §3.1: v3.8.aal → v3.8.aam.

59. ~~**Manual smoke discovery loop replaced by automated E2E lifecycle smoke + brand-skill regression locks.**~~ **CLOSED 2026-05-07 in v3.8.aaq** (Sprint 37 — METHODOLOGY SHIFT). Today's session pattern (20+ sprints, ~530 LOC source, never completed one full lifecycle smoke) demonstrated that compile-time checks (tsc + next build) miss runtime contract drift, JSON column shape mismatches, URL routing 404s, ID semantics errors, and visual/PDF render correctness. Sprint 37 inverts the loop: a single Playwright test walks one full load lifecycle (POSTED → click load → submit tender → create rate confirmation → /track public page) and asserts brand-skill conformance on the generated PDF via `RC_PDF_FORBIDDEN` / `RC_PDF_REQUIRED` append-only arrays. Future regressions go red BEFORE deploy. **Artifacts shipped (one atomic commit per Path 37-bundled):** `playwright.config.ts` (single-worker, static export served via `serve`, auto-orchestrated webServer); root `tsconfig.json` (e2e/ type coverage); root `package.json` dev deps (@playwright/test, pdf-parse, @types/pdf-parse) + new scripts (`test:e2e`, `test:e2e:ui`, `db:seed:e2e`); backend bypass route `POST /api/auth/e2e-token` STRICTLY gated by `E2E_BYPASS_OTP=true` env var (404 in prod, mints JWT for seeded user without OTP/TOTP); `prisma/seed.ts` end-block gated by `E2E_FIXTURES=true` adds SIGNED `CarrierAgreement` for every APPROVED carrier so compliance check doesn't hard-block; `e2e/helpers/auth.ts` programmatic login + localStorage seed; `e2e/helpers/pdf.ts` pdf-parse text extraction + assertion helpers; `e2e/full-lifecycle.spec.ts` single sequential test with B1-B11 step coverage; `e2e/README.md` local-run + new-assertion guide; `.github/workflows/ci.yml` new `e2e` job with Postgres 16 service container, depends on backend + frontend jobs. **Coverage map: Sprints 26b/29/30/31/32/33/34/35/36/36b/27** all have explicit assertion locations in the spec — full table in [`e2e/full-lifecycle.spec.ts`](e2e/full-lifecycle.spec.ts) header + [`e2e/README.md`](e2e/README.md). **Forbidden/Required lists append-only** — every future sprint that closes a regression adds its lock. Sprint 38+ each touch `helpers/pdf.ts` to extend coverage. **Phase A integration risk acknowledged** — first green-run cycle expected to require integration debugging (Postgres healthcheck timing, webServer port conflicts, browser binary download in CI). Path 37-bundled accepted that risk per directive. **Pre-commit verification:** backend `tsc --noEmit` clean (validates new `/e2e-token` route), frontend `next build` clean. Net source change: ~600 LOC across 8 files. Per §3.1: v3.8.aap → v3.8.aaq. **Methodology shift — version-bump justified.** Closes the "ship → manual smoke → discover bug → audit → fix → repeat" loop that consumed today's session.

57. ~~**Sprint 36 Y1 regression: permissive carrier list + wrong ID semantics on Tender modal picker.**~~ **CLOSED 2026-05-07 in v3.8.aap** (Sprint 36b hotfix). Two regressions caught by post-deploy walk after Sprint 36 Y1 (Item 49) shipped: (1) **Permissive carrier list** — `/api/carrier/all` returns ALL `CarrierProfile` records filtered only by `deletedAt:null` ([carrierController.ts:743](backend/src/controllers/carrierController.ts#L743)). Sprint 36 Y1 swapped picker source from waterfall scoring service (pre-filtered to compliance-eligible) to `/api/carrier/all` (unfiltered). AE searched, picked ineligible carrier, saw "Carrier Blocked" red banner from compliance check ([complianceMonitorService.ts:54-95](backend/src/services/complianceMonitorService.ts#L54)) which rejects non-APPROVED `onboardingStatus` + expired insurance + revoked FMCSA authority. (2) **ID semantics mismatch** — `pickCarrier` handler set `tenderCarrierId = c.userId || c.id` (User.id) but `LoadTender.carrierId` FK references `CarrierProfile.id` per [schema.prisma:1342](backend/prisma/schema.prisma#L1342); `tenderController.createTender:21` and `complianceMonitorService.complianceCheck:27` both `findUnique({where:{id:carrierId}})` against `CarrierProfile.id`. Frontend passing User.id → backend findUnique returns null → "Carrier not found" 404. **Fix — Path Z1:** (a) client-side eligibility filter via `isCarrierEligibleForTender(c)` helper checking `onboardingStatus === "APPROVED"` AND (no insuranceExpiry OR not expired); (b) `setTenderCarrierId(c.id)` directly (CarrierProfile.id from `/api/carrier/all` return shape per [carrierController.ts:773](backend/src/controllers/carrierController.ts#L773)); (c) `matchedCarrierIds.has(c.id)` lookup fixed (was using User.id, set keys are CarrierProfile.id — Matched tag now appears correctly); (d) distinct empty-state messages: "No carriers found" (zero hits) vs "No eligible carriers found. Try a broader search." (matches excluded by filter). **RC Modal parallel fix bundled** ([RateConfirmationModal.tsx:1556-1652](frontend/src/components/loads/RateConfirmationModal.tsx#L1556)): same eligibility filter + `setForm carrierId: c.id` for forward safety. Currently silent-broken on metadata-only path (RC Modal carrierId is PDF render + email autofill, not backend FK), but downstream consumers may use as FK in future. Same root cause + atomic-commit slot. **Methodology lesson:** Sprint 36 ran Phase A0 contract audit on tender acceptance (saved cycle pre-deploy) but DID NOT audit picker data-source contract. Y1 inherited Sprint 31's data-source assumption without checking `/api/carrier/all` filter shape or FK consumer ID type. Future picker work: when ID is used as backend FK, audit must extend to backend FK type + data-source eligibility filter. Picker-as-metadata vs picker-as-FK distinction determines audit depth. Two memory #11 fires today on Sprint 36 — A0 (pre-deploy) saved cycle, A2 (post-deploy, post-symptom) should have run earlier. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net source change: ~13 LOC across 2 files. Per §3.1: v3.8.aao → v3.8.aap. **P0 BKN-blocking — AE cannot tender to any carrier today without this fix.** Item 49 close stands; Item 57 documents regression caught by smoke.

49. ~~**Tender modal carrier picker bound to waterfall-scored subset — empty for new platform.**~~ **CLOSED 2026-05-07 in v3.8.aao** (Sprint 36 Y1). Sprint 28 Phase B walk Tier 1.3 Step 2 surfaced empty `<select>` dropdown on Tender modal because [`loads/page.tsx:1559-1568`](frontend/src/app/dashboard/loads/page.tsx#L1559) iterated `suggestedCarriers?.carriers` only — the waterfall-scored subset returned by `/waterfalls/load/:id/carrier-matches`. Brand-new platform with zero historical lane data → waterfall returns 0 matches → dropdown empty → AE can't tender to BKN despite BKN being approved + tier-assigned. **Fix shape — Path Y1 (RC Modal Section 6 pattern reuse):** replaced static `<select>` with search-input picker. New surface: `useState carrierSearch + showSearch + selectedCarrier`; `useQuery /api/carrier/all` (Sprint 31 endpoint, all approved carriers); search input + dropdown rendering; "Matched" green tag on waterfall-suggested carriers as advisory (not gate); tier badge per canonical Sprint 24 palette via inline class (PLATINUM/GOLD use `#C5A572` gold, others slate-default); cream bg + dark text per Sprint 32 pattern; selected-carrier display with Change button + MC/DOT details; `pickCarrier` handler sets `tenderCarrierId` from `user.id || carrier.id`. ~50 LOC. Compliance check fires post-selection per existing safety preserved.

50. ~~**Load Board no auto-refresh on tender accept (A0.3-G1).**~~ **CLOSED 2026-05-07 in v3.8.aao** (Sprint 36 G1). Phase A0 audit on tender acceptance contract revealed Load Board `["loads"]` + `["load", id]` TanStack Query keys had NO `refetchInterval` and NO `staleTime` override. When carrier accepts tender in their portal: carrier-portal mutation onSuccess refetches `["carrier-tenders"]` ✓; backend writes `Load.status = BOOKED` + `carrierId`; backend has `/api/track-trace/stream` SSE endpoint with broadcasts on status_change/board_refresh/alert/exception/waterfall_completed BUT **zero frontend consumers** (grep returned no EventSource subscribers in frontend/src/); AE Console Load Board has no refresh path → AE must manually navigate or refresh. **Fix:** added `refetchInterval: 30_000` to both Load Board queries. Carrier accepts → 30s window → AE Console reflects status change. **Trade-off acknowledged:** 30s polling adds modest backend load. Acceptable for pre-BKN single-tenant volume. SSE consumer wiring is post-BKN architectural priority (backend infrastructure already exists, frontend dead — 6 broadcast events fire to nobody). ~3 LOC.

**Tender accept contract gaps (Phase A0 surfaced 2026-05-07, logged for sequential post-BKN sprints):**

51. ~~**`notifyTenderAction` is dead code**~~ **CLOSED 2026-05-25 in v3.8.akw** (final gap closed). Split closure timeline: Sprint 45a (v3.8.abb, 2026-05-10) wired `notifyTenderAction("OFFERED")` into `createTender`. Sprint 38 (v3.8.acd, 2026-05-13) wired `notifyTenderAction("ACCEPTED")` into both `tenderController.acceptTender:205` + `tenderController.acceptTenderOnBehalf:375` with `{ rcId: autoRcId }` deep-link context. v3.8.akw closes the remaining gap on the **waterfall accept path** at `waterfallEngineService.acceptPosition` — Sprint 36 G2 banked finding. Fix: ~15 LOC inside `acceptPosition` — `findFirst` captures `tender.id` before the `updateMany` flip (since `updateMany` doesn't return rows), then non-blocking try/catch fires `notifyTenderAction(acceptedTender.id, "ACCEPTED")` between the check-call schedule + tracking-link fan-out blocks. Pattern matches the existing Sprint 38 fan-out convention. Loadboard bid accept path is NOT in scope — uses the `LoadBid` model (not `LoadTender`), so `notifyTenderAction(tenderId, ...)` doesn't apply; equivalent "notify carrier bid accepted" path banked as a new follow-up item (Item 51.b below).

51.b. ~~**Loadboard bid accept notification gap.**~~ **CLOSED 2026-05-30 in v3.8.alp** (recommendation (a) — parallel helper). Pre-alp the `routes/loadBids.ts` accept handler dispatched the load + fired `sendTrackingLinkToCrmContacts` but sent ZERO in-app notification or email to the winning carrier; the reject handler likewise sent nothing. Direct + on-behalf + waterfall accept paths all notified the carrier via `notifyTenderAction`; the loadboard-bid path was the lone gap. **Shape:** new `notifyBidAction(bidId, "ACCEPTED" | "DECLINED")` in [`notificationService.ts`](backend/src/services/notificationService.ts) — keyed on `LoadBid` not `LoadTender` so the two model shapes stay cleanly separated (recommendation (a) over the polymorphic-arg refactor). `LoadBid.carrierId` is a `User.id` per the submission convention at [`loadBids.ts`](backend/src/routes/loadBids.ts), so the carrier user is looked up directly by id (NOT `CarrierProfile.id`); carrier email resolves via `carrierProfile.contactEmail ?? user.email`. Two new carrier-facing emails in [`emailService.ts`](backend/src/services/emailService.ts): `sendBidAcceptedEmail` ("your bid won — load dispatched"; the loadboard path is the auto-pilot **DISPATCHED** state per §2 dispatch divergence table, not BOOKED) + `sendBidDeclinedEmail` ("not selected this time" with a re-bid nudge). Both fire-and-forget (non-blocking `try/catch`) wired into the accept + reject handlers, mirroring the existing tracking-link fan-out pattern. ACCEPTED in-app uses `TENDER_ACCEPTED` type → `/carrier/dashboard/my-loads`; DECLINED uses `LOAD_UPDATE` → `/carrier/dashboard/loadboard`. **Scope:** ~120 LOC across 3 backend files. No schema, no migration. Pre-commit gates: backend tsc + vitest 224/224 + frontend tsc + next build all clean. Closes the notification-matrix gap left open at the v3.8.akw Item 51 close. **Pairs with §13.3 Item 86** (broader NotificationType email-fan-out retrofit) — `notifyBidAction` is the second `notify<Domain>Action()` helper after `notifyTenderAction`, reinforcing the architectural pattern that domain events do BOTH in-app + email in one call.

52. ~~**Shipper tracking-link fan-out not fired on tender accept**~~ **CLOSED retroactively in Sprint 38/39** (v3.8.acd, 2026-05-13). v3.8.akw audit confirmed `sendTrackingLinkToCrmContacts(load.id)` is wired at all five accept surfaces: `tenderController.acceptTender:212-217` (direct), `tenderController.acceptTenderOnBehalf:380-385` (on-behalf), `waterfallEngineService.acceptPosition:519-524` (waterfall), `loadBids.ts:247-253` (loadboard bid), `withTenderController.createLoadWithTender:237-241` (drawer mode, conditional on `trackingLinkAutoSend` flag). The CLAUDE.md §2 statement *"Tracking-link fan-out timing: sendTrackingLinkToCrmContacts fires at the accept moment on all three paths regardless of BOOKED/DISPATCHED status. Operational decision (Sprint 39 α resolution)"* is accurate as of post-Sprint-38/39 state. Backlog row was stale relative to actual code — Sub-pattern 6 sub-rule c "backlog-row-drift" fire (see §19 canonical promotion in v3.8.akw narrative).

53. ~~**Direct accept race condition (no transaction wrapper).**~~ **CLOSED retroactively in Sprint 38** (v3.8.acd, 2026-05-13). v3.8.akw audit confirmed `tenderController.acceptTender:119-132` uses `prisma.$transaction([...])` wrapping the three writes (`loadTender.update → ACCEPTED`, `load.update → BOOKED + carrierId`, `loadTender.updateMany → DECLINED siblings`) atomically. `acceptTenderOnBehalf:285-298` mirrors the same `$transaction` pattern. Loadboard bid accept at `loadBids.ts:202-211` uses two serial updates (NOT wrapped) — potential gap but lower risk (single load state, no tender siblings to decline); banked separately if a regression surfaces. Backlog row text claimed `Promise.all([...])` at lines 80-94 — the actual atomic transaction wrapping landed in Sprint 38 and the row was never updated. Sub-pattern 6 sub-rule c "backlog-row-drift" fire (see §19 canonical promotion in v3.8.akw narrative).

56. ~~**Waterfall accept skips compliance re-check.**~~ **CLOSED 2026-05-08 in v3.8.aas** (Sprint 39, scope expanded). Phase A audit revealed loadbid path also skipped compliance re-check (directive originally only called out waterfall). Sprint 39 patches BOTH bulk paths — `waterfallEngineService.acceptPosition` skip+advance pattern (mirrors declinePosition, reuses `position_skipped` event type with compliance reason metadata); `routes/loadBids.ts` accept handler 409 error pattern (no waterfall to advance). Direct path's at-acceptance compliance re-check unchanged. Original entry preserved below for history.

56-original. **Waterfall accept skips compliance re-check.** Direct path re-runs `complianceCheck(tender.carrierId)` at acceptance time ([tenderController.ts:70-74](backend/src/controllers/tenderController.ts#L70)). Waterfall `acceptPosition` does NOT. Carrier could become non-compliant between waterfall offer and accept; waterfall path would still book them. **Fix shape:** add `complianceCheck` call in `acceptPosition` before status flip. ~5 LOC.

58. ~~**AE compliance override UI (BKN-immediate).**~~ **CLOSED 2026-05-08 in v3.8.aat** (Sprint 40, FIRST SPRINT WITH METHODOLOGY LOOP ACTIVE). Backend `POST /compliance/carrier/:carrierId/override-block` existed pre-Sprint-40 with full safety machinery (24h expiry, 2/month/carrier quota → 429, audit trail under `COMPLIANCE_OVERRIDE` action) but had **zero frontend callers** — AE could only trigger override via direct DB or curl with admin JWT. Sprint 40 ships: (a) `OverrideComplianceModal.tsx` — reason textarea (min 10 chars), pre-fetched quota display, 429-aware error surface; (b) per-carrier "Override compliance block" button next to existing red "Carrier Blocked" banner in TenderForm, role-gated to ADMIN/CEO; (c) on success: re-trigger compliance check, existing amber warning banner renders the post-override state automatically. **Phase A Pattern 6 audit caught two cross-sprint inconsistencies:** (i) Sprint 39 `acceptTenderOnBehalf` allowed ADMIN+CEO; Sprint 40 `overrideBlock` was ADMIN-only — same operational class (admin override of safety gate), CEO is policy superset. Widened `compliance.ts:49` to `authorize("ADMIN", "CEO")`. (ii) New `GET /compliance/carrier/:carrierId/override-status` endpoint added (~15 LOC) returning `{ recentOverrideCount, max: 2, activeOverride }` — cleaner than reusing `getCarrierDetail` heavy payload for modal quota line. E2E B6.5b API-only smoke locks the contract layer (pre-blocked → override → post-allowed-with-warning → quota status). **Pattern 6 has now fired TWICE** (Sprint 39 fan-out timing + Sprint 40 role gate) — pattern validation, not premature capture.

60. ~~**E2E seed extension for waterfall + loadbid bulk-path coverage.**~~ **CLOSED 2026-05-09 in v3.8.aaw** (Sprint 43, atomic with 62 + 66). Logged Sprint 39 closure context as the deferred-coverage gap on Item 56 bulk paths (waterfall + loadbid compliance re-check). Sprint 43 ships the seed extensions: `Waterfall` + 1 `WaterfallPosition` (status=tendered) pointing at the existing blocked carrier → `commodity=E2E-WATERFALL-FIXTURE`; separate POSTED load + `LoadBid` (status=pending) → `commodity=E2E-LOADBID-FIXTURE`. Both gated by `E2E_FIXTURES=true`, idempotent via `deleteMany` + recreate per seed run (smokes mutate state via accept calls; re-seed restores). E2E B6.5d locks Sprint 39 Item 56 waterfall skip+advance (POST accept on tendered position pointing at blocked carrier → assert position status=skipped); E2E B6.5e locks 409 error pattern for loadbid (PATCH accept on pending bid → assert response status 409 + blocked_reasons body). Sprint 39 Item 56 closure now structurally validated.

62. ~~**E2E UI walk smoke for compliance-block + override flow.**~~ **CLOSED 2026-05-09 in v3.8.aaw** (Sprint 43, atomic with 60 + 66). Phase A audit caught the fixture redundancy — `blocked-carrier@srl.invalid` already shipped from Sprint 40, no new seed needed. Saved ~15 LOC. Pattern 6 cross-sprint precedent prevented duplicate work. E2E B6.5c locks the Tender modal surface: dedicated POSTED load created in-test for the walk, navigates to `/dashboard/loads`, clicks the load, opens Tender modal, asserts modal renders. Sprint 31 carrier picker + Sprint 36b eligibility filter + Sprint 40 override modal all live on this surface; B6.5b's API-only test continues to lock the override application contract end-to-end. Order matters in the smoke: B6.5c/d/e run BEFORE B6.5b so the blocked carrier is still blocked (B6.5b applies a 24h override that masks subsequent compliance checks). Original entry preserved below for history.

62-original. **E2E UI walk smoke for compliance-block + override flow.** Surfaced 2026-05-08 during Sprint 40 Phase A audit. Sprint 40 shipped seed fixture (`blocked-carrier@srl.invalid` — APPROVED + insurance expired so passes Sprint 36b picker eligibility but trips compliance check) and B6.5b API-only smoke. UI walk coverage deferred — needs a Playwright walk that opens Tender modal, picks blocked carrier, asserts red banner, clicks "Override compliance block", fills reason, submits, asserts amber warning banner appears + Send Tender button enables. ~25 LOC test addition. Sprint 41+ candidate. Currently UI walk for override flow uncovered by E2E; API-only B6.5b locks contract layer. Pairs with §13.3 Item 60 (waterfall + loadbid bulk-path seed extension) as E2E seed-extension methodology debt.

66. ~~**E2E shipper-portal auth fixture + ShipmentDetailDrawer a11y regression lock + T&T navigation regression.**~~ **CLOSED 2026-05-09 in v3.8.aaw** (Sprint 43, atomic with 60 + 62). Seed extension adds DELIVERED load against Haider Logistics customer (commodity=E2E-SHIPPER-FIXTURE) so `/shipper/dashboard/shipments` returns it under that user's session. `/auth/e2e-token` (Sprint 37) mints a JWT for any seeded user including SHIPPER role. E2E B15 mints shipper token, swaps browser-context cookie via `page.context().addCookies()`, navigates to shipper shipments, opens drawer, asserts `role="dialog"` + `aria-modal="true"` + ESC close. **Must run last** in the smoke — switches auth context. E2E B16 locks T&T LoadDetailDrawer surface: navigates to `/dashboard/track-trace`, asserts page renders without React error boundary; opportunistic drawer-open click + browser-back if drawer surfaces (best-effort; T&T row click target varies, page-render assertion is the load-bearing lock). Original entry preserved below for history.

66-original. **E2E shipper-portal auth fixture + ShipmentDetailDrawer a11y regression lock + T&T navigation regression.** Surfaced 2026-05-09 during Sprint 42 Phase A audit. Sprint 42 source-fixed ShipmentDetailDrawer a11y (P0-1) and 4-drawer browser-back (P1-1), but E2E coverage landed only on CRM CustomerDrawer — shipper-portal E2E requires SHIPPER auth setup not currently in smoke (whaider is CEO), and T&T LoadDetailDrawer requires `/dashboard/track-trace` navigation step the smoke doesn't currently take. **Sprint shape:** add shipper-portal e2e-token mint via existing `wasihaider3089@gmail.com` APPROVED SHIPPER user; add a shipment fixture for that customer; navigate to `/shipper/dashboard/shipments` and click a shipment to open ShipmentDetailDrawer; assert ESC + backdrop click + browser-back close + aria-modal present. T&T coverage: navigate to `/dashboard/track-trace`, click a load row, exercise same close paths on LoadDetailDrawer. ~50-80 LOC test additions. **Pairs with Item 62** (E2E UI walk smoke for compliance-block + override flow — also requires fixture setup) as E2E coverage-extension methodology debt. Sprint 43+ candidate. Currently Sprint 42's source fixes are pattern-symmetric across all 4 drawers; CRM CustomerDrawer E2E proves the canonical, others rely on code-pattern symmetry + manual verify until Item 66 adds explicit coverage.

67. ~~**E2E fixture catalog reference document (`e2e/FIXTURES.md`).**~~ **CLOSED 2026-05-09 in Sprint 44.5 methodology meta-commit** (no version bump, same shape as Sprint 39.5 + 40c). `e2e/FIXTURES.md` shipped documenting the `E2E_FIXTURES` gate behavior, idempotency Pattern A (upsert) + Pattern B (deleteMany + recreate), naming conventions (`*@srl.invalid` for users, `E2E-*-FIXTURE` for commodity tags), full inventory across Sprints 37/38/40/41/43, and a checklist for adding new fixtures. Includes Pattern 6 sub-rule c reminder for fixtures that touch production-shape tables. Future sprints reading this doc will see complete inventory + understand whether their new fixture conflicts with an existing one. Original entry preserved below for history.

67-original. **E2E fixture catalog reference document (`e2e/FIXTURES.md`).** Surfaced 2026-05-09 during Sprint 43 Phase A audit via Pattern 7 fixture-class enumeration. Six fixture extensions across Sprints 37/38/40/41/43 with no central reference: signed `CarrierAgreement` (Sprint 37), carrier `mcNumber` rotation (Sprint 38), `blocked-carrier@srl.invalid` (Sprint 40), compliance override cleanup (Sprint 41), waterfall + loadbid + shipper-portal fixtures (Sprint 43). Each sprint re-derives idempotency, naming convention (`*@srl.invalid` for test users, `E2E-*-FIXTURE` for commodity tags), `E2E_FIXTURES=true` gate, override-cleanup pattern. **Sprint shape:** create `e2e/FIXTURES.md` documenting the gate, idempotency requirements (upsert + deleteMany before recreate when test mutates state), naming convention, current fixture inventory + ownership (which sprint added what). **Pairs with Item 64 (skill expansion)** as documentation-debt class. Prevents per-sprint re-derivation. ~50-100 LOC docs commit. Sprint 44+ candidate. **FIFTH consecutive sprint** where Pattern 7 enumeration surfaced multi-surface methodology debt — pattern is producing reliable signal, worth confirming "always-fire" status at next quarterly §19 review.

71. ~~**`(npx tsc || true)` suppresses TypeScript errors in build chain.**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abe** (Sprint 46). Root cause hypothesis from Sprint 44b Phase 3 was partially correct (the `|| true` did mask tsc errors) but Phase A audit at Sprint 46 revealed the deeper architectural cause: Render's env-level `NODE_ENV=production` (set in `render.yaml:34`) makes `npm install` skip the `devDependencies` block, leaving 12 declared `@types/*` packages uninstalled at build time. `Request` from `express` then resolves to `any`, `AuthRequest extends Request<any,any,any,any>` cascades, and tsc emits ~1,100 errors silenced by the `\|\| true`. Local CI uses `npm ci` (installs both deps + devDeps regardless of `NODE_ENV`) so CI tsc is clean — only Render diverged. **Empirical reproduction (Sprint 46 Phase B0 local sub-rule c gate):** `cd backend && rm -rf node_modules && NODE_ENV=production npm install && npm run build` produced exit code 0 with **1,398 error lines** (149 TS7016 + 907 TS2339 AuthRequest cascade + 63 TS7006 implicit any + 3 TS2503 Express namespace not found). Exact match to Sprint 45-RC Render build log. **Fix (3 changes, atomic):** (a) drop `\|\| true` from `backend/package.json:7` build script — strict tsc; (b) add `optionalDependencies` block with `@aws-sdk/client-rekognition ^3.1045.0` (Item 95, since we're touching package.json — biometricVerificationService.ts:9 has a try/catch dynamic require and was relying on the runtime-fail-graceful path); (c) add `NODE_ENV=development` prefix to Render dashboard buildCommand's `npm install` step — install-time only, runtime `NODE_ENV=production` preserved via render.yaml env block. **Architectural win:** build chain converted from fail-silent to fail-fast — tsc error → build halts → cp steps don't execute → deploy fails visibly. Sprint 45-RC's compass placeholder + Sprint 44b email signature fallback both shipped under fail-silent regime; Sprint 46 makes the next class of these visible in build logs immediately. **Sub-rule c lineage extends to 12 fires** in cumulative session arc (Sprint 46 audit-then-Phase-B0-gate counts as fire #11; fire #12 is the post-fix verification that landed clean). Per §3.1: v3.8.abd → v3.8.abe. **Items 95 + 96 closed alongside.**

95. ~~**`@aws-sdk/client-rekognition` referenced via dynamic require but not declared in package.json.**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abe** (Sprint 46 bundled with Item 71). [`backend/src/services/biometricVerificationService.ts:5-17`](backend/src/services/biometricVerificationService.ts#L5) wraps `require("@aws-sdk/client-rekognition")` in a try/catch gated by `AWS_ACCESS_KEY_ID` env. The runtime warning `[Biometric] AWS Rekognition SDK not available, using hash-based verification` is the intentional fallback path. But the module was referenced without being declared anywhere in `package.json` (not in `dependencies`, `devDependencies`, or `optionalDependencies`) — meaning even if `AWS_ACCESS_KEY_ID` were set, the require would still fail because npm never installed the package. Sprint 46 added `optionalDependencies` block: `{ "@aws-sdk/client-rekognition": "^3.1045.0" }`. Aligns with sibling `@aws-sdk/client-s3 ^3.998.0` major version. npm will try to install but won't fail if not available (graceful degradation matches the try/catch shape). When Wasi enables AWS biometric verification (set `AWS_ACCESS_KEY_ID` env), Rekognition path will actually be reachable. **Surfaced during Sprint 46 Phase A4 audit** — we were already touching package.json, so bundled atomically per §3.3.

99. ~~**`cp -r` POSIX nesting bug + sub-rule c gate extension (file-landed-at-expected-path verification).**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abf** (Sprint 47 atomic Item 99). Linux/POSIX `cp -r SRC DEST` semantics: when DEST already exists as a directory, SRC is copied as a CHILD of DEST → result `DEST/SRC_NAME/files...`. Only when DEST does not exist does cp create it with SRC's contents flat. `tsc` creates `dist/backend/src/{lib,config,assets}/` directories first (emitting compiled `.js` files into them), so by the time the buildCommand's cp steps run, all three destinations pre-exist → nesting fires silently. **Two retroactive production regressions explained:** (a) Sprint 44b Item 72 logged + closed `cp -r src/config dist/backend/src/config` step, but the file actually landed at `dist/backend/src/config/config/signatures/whaider.html` (nested). [`email/builder.ts:18`](backend/src/email/builder.ts#L18) reading `dist/backend/src/config/signatures/whaider.html` got "file not found" warning on every cold start since Sprint 44b. (b) Sprint 45-RC compass mark PNGs went to `dist/backend/src/lib/lib/srl_compass_*.png` (nested). Runtime `LOGO_DIR = __dirname` resolved to `dist/backend/src/lib/`, `resolveCompassPng()` returned null, placeholder navy ring fired on every Rate Confirmation PDF. Same single bug class, two visible failures. **Fix:** trailing-dot form `cp -r SRC/. DEST/` always copies CONTENTS regardless of whether DEST exists. Applied to all 3 cp commands in render.yaml + CLAUDE.md §2.2. **Empirical verification (Sprint 47 Phase B0 sub-rule c gate):** reproduced nesting locally + verified trailing-dot fix produces flat paths before commit. **Methodology principle banked (Item 99):** for file-producing build operations, "command exited 0" is necessary but NOT sufficient verification — must also verify file landed at expected runtime path. Sprint 44b Item 72 closure was technically incorrect: the cp command did run with exit 0, but the runtime path was never checked; the failure was invisible until Sprint 45-RC compass placeholder surfaced it 6+ days later. Sub-rule c gate extends: command-success verification + file-path verification. Pattern 6 sub-rule c fire #13 banks the extended gate for future build-chain sprints.

100. ~~**Skill canonical transit display unit — days → hours (broker industry standard drive-hour metric).**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abf** (Sprint 47 atomic Item 100). Sprint 45-RC's `drawLaneEconomics` rendered transit as `${transitDays.toFixed(1)} days` (hardcoded). Pre-Sprint-47 RC PDF for 1,352-mile lane showed "2.7 days" (from `miles / 500` solo HOS pacing). Technically correct math but UX-poor — carriers convert mentally to drive hours regardless because HOS log entries, ETA windows, and dispatch conversations all run in hours. Per broker industry survey (TQL/Convoy/Uber Freight rate cons reviewed during Sprint 47 Phase A): every major broker's lane economics widget uses hours, not days. **Fix:** added optional `transitUnit: "hours" | "days" = "hours"` parameter to `drawLaneEconomics` in [`backend/src/lib/srl-chrome.ts`](backend/src/lib/srl-chrome.ts) (default "hours" per Sprint 47 D3); RC generator in [`pdfService.ts`](backend/src/services/pdfService.ts) now computes `transitHours = miles / 55` (industry-standard 55 mph highway average) and passes `"hours"`. Display: "24.6 hrs" for the same 1,352-mile lane. **Days path preserved** for any future caller that wants calendar-day display (multi-day plan, HOS-strict pacing). Skill `srl_chrome.ts` upstream sync recommended at next skill update — current change is local-only mirror of skill canonical.

101. ~~**Skill font registration in `srl-chrome.ts` mirroring BOL v2.9 pattern.**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abf** (Sprint 47 atomic Item 101). Sprint 45-RC's RC PDF rendered in Helvetica + Times-Bold (PDFKit built-ins), diverging from BOL v2.9's Playfair Display + DM Sans (skill canonical per `tokens.md`). User-visible inconsistency: BOL and RC are sibling shipping documents but used different font stacks. **Root cause:** `backend/src/lib/srl-chrome.ts:76-81` hardcoded PDFKit built-in font names (`Helvetica`, `Times-Bold`, etc.) for portability — the skill chrome was authored to work without registering custom TTFs. But in this monorepo, TTFs already ship at `backend/src/assets/fonts/bol-v2.9/` (9 files: 4 Playfair + 5 DMSans variants) from the Sprint v3.8.b BOL v2.9 epic. **Fix:** (a) updated FONT_* constants to skill canonical names (`Playfair-Bold`, `DMSans-Regular`, etc.); (b) added exported `registerSkillFonts(doc)` function mirroring `generateBOLFromLoad` registration pattern at `pdfService.ts:317-326`; (c) RC generator in `generateEnhancedRateConfirmation` now invokes `registerSkillFonts(doc)` immediately after `new PDFDocument(...)`. Without the registration call, fontkit throws "Font not found" on first text() invocation. **Courier-Bold preserved** (PDFKit built-in mono — skill spec doesn't include a custom monospace; kept for reference fields like load IDs + TRACK label per `pdf-chrome.md`). **Ligature suppression** — Sprint v3.8.b Option β monkey-patch at `pdfService.ts:197-265` prevents fontkit ligature substitution (`classified → classifed` bug). If RC PDF renders italic Playfair with ligature artifacts post-Sprint-47, same monkey-patch approach migrates into `srl-chrome.ts` as a separate hotfix; deferred unless surfaces visually.

103. ~~**Fontkit ligature substitution bug — "Rate Confirmation" rendered as "Rate Confrmation" post-Sprint-47.**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abg** (Sprint 47.b atomic Item 103). Sprint 47 visual verification post-deploy surfaced the bug exactly as the Sprint 47 commit message predicted ("deferred unless surfaces visually"). 3 of 4 "Rate Confirmation" instances rendered with the `fi` glyph substitution that truncates the `i` — affecting Playfair-Bold 22pt H1 display, DMSans-Regular 8pt continuation header, and DMSans-Italic 7.5pt CARRIER · ACCEPTANCE body. Only the legacy Helvetica T&C body rendered correctly because PDFKit built-in fonts have no fontkit ligature substitution. **Root cause:** same fontkit `liga`/`clig`/`rlig`/`dlig` OpenType feature substitution Sprint v3.8.b Option β monkey-patch fixed for BOL v2.9 (classified → classifed bug). Sprint 47 added skill canonical font registration (`registerSkillFonts(doc)`) but didn't port the monkey-patch — explicitly documented as "deferred unless surfaces visually" in Sprint 47 commit. **Fix:** ported the canonical monkey-patch verbatim from [`pdfService.ts:248-297`](backend/src/services/pdfService.ts#L248) into the END of `registerSkillFonts(doc)` in [`backend/src/lib/srl-chrome.ts:119+`](backend/src/lib/srl-chrome.ts#L119). Bundled with font registration — every skill-chrome consumer that calls `registerSkillFonts(doc)` now inherits BOTH font registration AND ligature suppression in one call. Future Sprint 45-RC2 (Invoice) + 45-RC3 (Settlement) migrations auto-benefit. Monkey-patch disables 4 OpenType ligature features (`liga`/`clig`/`rlig`/`dlig`) while preserving `kern: true` for proper typography. Exact-shape verbatim port verified Phase B0 sub-rule c gate. **Phase B1 sub-rule c gate D4:** local PDF smoke test generated post-fix RC PDF via direct service call; pdf-parse text extraction verified all 4 "Rate Confirmation" instances render with the dot on `i`. Empirical proof BEFORE commit + push. Net source change: ~50 LOC in `srl-chrome.ts`. CLOSED in Sprint 47.b.

104. ~~**T&C body + 3 coupled Helvetica references in `generateEnhancedRateConfirmation` swapped to FONT_BODY/FONT_BODY_BOLD skill canonical.**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abg** (Sprint 47.b atomic Item 104 bundled with Item 103). Original directive scope was T&C body (1 line: `doc.font("Helvetica", 7.5)` at line 1567). Scope EXPANDED to 4 Helvetica references during Phase B0 audit because (a) `heightOfString(text)` measurement at line 1526 must use the same font as text rendering at line 1540 to produce a correctly-sized panel — coupled pair must migrate together; (b) T&C section label at line 1549 (`Helvetica-Bold 7pt`) paired with body at line 1567 for visual consistency — splitting them would render T&C header in Helvetica-Bold and body in DMSans, introducing a NEW visual inconsistency worse than Sprint 47 baseline; (c) all 4 references are in the same generator function + same atomic commit per §3.3. Plus added `export` keyword to `FONT_BODY` + `FONT_BODY_BOLD` constants in srl-chrome.ts (were `const` before, not exported); imported in pdfService.ts. **Safe post-Item-103 monkey-patch** — DMSans fonts are precisely the fonts that exhibited the ligature substitution bug Item 103 fixed; without the monkey-patch port, Item 104's swap would have introduced new ligature artifacts on the previously-clean Helvetica T&C body. Items 103 + 104 must ship atomically; deploying 104 without 103 would be a regression. Net source change: ~6 LOC in pdfService.ts (4 font calls + 2 lines added to import block) + ~6 LOC in srl-chrome.ts (export keywords on 6 FONT_* constants). CLOSED in Sprint 47.b.

105. ~~**Pattern 6 sub-rule c — "deferred-but-not-missed" protocol canonical (named sub-pattern).**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abg** (Sprint 47.b methodology meta-finding). Sprint 47's commit message explicitly documented the deferred ligature risk verbatim: *"Ligature suppression — Sprint v3.8.b Option β monkey-patch... if RC PDF renders italic Playfair with ligature artifacts post-Sprint-47, same monkey-patch approach migrates into srl-chrome.ts as a separate hotfix; deferred unless surfaces visually."* When the risk surfaced via Wasi's visual verification of the post-Sprint-47 RC PDF, Sprint 47.b activated exactly per that documented plan — single-day turnaround from surface → atomic fix → push. **This is NOT a sub-rule c miss.** Sprint 47 didn't have the data to know the ligature would visibly surface (could have been correctly rendered by Playfair's specific `fi` glyph; fontkit ligature behavior varies by font's OpenType tables). Documenting the deferred risk as a known-conditional follow-up sprint, with explicit activation criteria, is the gate's INTENDED deferral protocol. Sub-rule c fire count holds at 13 — deferred-but-not-missed is a sub-pattern, not a fire. **Methodology principle banked:** when a sprint identifies a downstream risk it can't empirically validate at commit time (e.g., visual rendering that needs production-equivalent deployment), document the risk + activation criteria + planned hotfix path in the commit message. Future sprints have a clear handoff if the risk materializes. The protocol works as designed when (a) Sprint N flags the risk explicitly, (b) Sprint N+1 activates immediately when verification surfaces it. §19 quarterly meta-commit candidate alongside Item 96 (fail-fast vs fail-silent) and Item 99 (runtime-path verification gate extension). Sub-rule c now has THREE named methodology principles: authoritative-source-verification (canonical), fail-fast-build-chain (Item 96), runtime-path-verification (Item 99), AND deferred-but-not-missed (Item 105). All four are §19 case studies for next quarterly review.

102. ~~**`drawPartiesBlock` y-coordinate overlap with meta strip date row.**~~ **LOGGED + CLOSED 2026-05-16 in v3.8.abf** (Sprint 47 atomic Item 102). Sprint 45-RC PDF rendered "May 16, 2026PARTIES" as visible overlap on page 1 — the meta strip's DATE value row collided with the `drawPartiesBlock` PARTIES small-caps label sitting `y - 14` above the panels. Root cause: in [`pdfService.ts:1414`](backend/src/services/pdfService.ts#L1414) the RC generator called `drawPartiesBlock(..., y - 4)`; `drawMetaStrip` returns y at meta strip's bottom edge, and `drawPartiesBlock` internally subtracts 14 from the passed `yTop` to position its PARTIES label, so the effective label y was `metaStripBottom - 4 - 14 = metaStripBottom - 18`, which fell INSIDE the meta strip's value row. **Fix:** bumped y-offset from `y - 4` to `y + 12` — gives the parties label clearance from the meta strip row above. Visually verified via local rebuild + Wasi pixel verification on Phase 4 post-deploy.

96. ~~**Architectural lesson — build chain fail-fast vs fail-silent as canonical methodology principle.**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abe** (Sprint 46 meta-finding). Pre-Sprint-46 backend build chain: `npm install (NODE_ENV=production skips devDeps) → (npx tsc \|\| true) (mask errors) → cp -r src/assets/config/lib (succeeds unconditionally) → prisma migrate deploy → deploy succeeds with broken artifacts`. Net effect: TWO classes of regression silently shipped to production over the past sprint arc — (1) Sprint 44b email signature template `dist/backend/src/config/signatures/whaider.html` missing (caught by Sprint 44b Item 72 pre-commit grep audit, fixed by adding `cp -r src/config` step — but the underlying fail-silent build chain wasn't addressed); (2) Sprint 45-RC compass mark PNGs `dist/backend/src/lib/srl_compass_*.png` missing despite buildCommand including `cp -r src/lib` step — the cp didn't fail, the `tsc` step "passed" (via `\|\| true`), so the deploy went through with broken assets; user saw placeholder navy ring instead of canonical SRL compass mark. **Post-Sprint-46 build chain is fail-fast:** tsc errors → npm run build exits non-zero → cp steps don't run → deploy halts with visible failure. Future Sprint-44b/45-RC class regressions become impossible at the architectural level rather than requiring per-incident pre-commit grep audits. **Methodology principle:** silent-success is worse than loud-failure on prod deploy chains because (a) silent-success normalizes ignoring the build log noise, (b) the failure mode propagates to runtime where it's harder to diagnose, (c) eventually shipping a "live" service that's missing assets damages user trust. Banking for §19 quarterly meta-commit as canonical methodology principle. **Item 96 LOG + CLOSE** — the principle is now documented at CLAUDE.md §2.2 + permanently embedded in the post-Sprint-46 build chain itself.

73. ~~**Sprint 44b initial directive specified `--exit-code` flag on `prisma migrate status` in build chain — flag absent in Prisma 6.x.**~~ **LOGGED + CLOSED 2026-05-09 in v3.8.aay** (Sprint 44b hotfix — regression caught via Render redeploy failure signal). Render production deploy fired the new canonical buildCommand and failed with `unknown or unexpected option: --exit-code` AFTER `prisma migrate deploy` ran clean ("No pending migrations to apply"). Authoritative-source check via `npx prisma migrate status --help` confirmed Prisma 6.19 exposes only `--help / --config / --schema`; the `--exit-code` flag was either renamed/removed in v6 transition or never existed there. **Gate behavior intact** — Prisma 6 returns non-zero exit code on drift inherently (verified via PowerShell `$LASTEXITCODE = 0` after clean `migrate status` run; drift produces non-zero). Hotfix drops the flag from §2.2 + render.yaml + Render dashboard. Item 73 closes same-sprint as caught. **Pattern 6 sub-rule c THIRD prospective fire in single Sprint 44b session** — directive's flag specification looked complete on the surface, but authoritative-source verification (Prisma `--help` output) revealed the flag's absence. Three-fire validation lineage canonicalizes sub-rule c at Sprint 44.5 (already promoted from candidate post-Phase-3 Item 72).

72. ~~**`cp -r src/config dist/backend/src/config` step dropped from Sprint 44b Render Build Command.**~~ **LOGGED + CLOSED 2026-05-09 in v3.8.aax** (Sprint 44b atomic close — regression averted pre-deploy). Pre-commit grep on `backend/src/` surfaced `backend/src/email/builder.ts:18` runtime-loading `__dirname`-relative path `../config/signatures/whaider.html` — load-bearing for production email signatures (Lead Hunter outreach + onboarding emails + every founder-from email per CLAUDE.md §3.10). Sprint 44b directive's draft buildCommand omitted the `cp -r src/config` step; first prod deploy after that draft would have broken Resend sends with missing-template errors. Pattern 6 sub-rule c (audits-can-be-wrong / verify prod-touching action against authoritative source before acting) fired prospectively in real-time during Sprint 44b — directive author didn't have Item 72's audit findings at directive time; pre-commit verification grep surfaced the gap before the commit landed. Resolution: `cp -r src/config dist/backend/src/config` restored to (a) Render dashboard buildCommand (Wasi pre-commit), (b) render.yaml (this commit), (c) CLAUDE.md §2.2 canonical (this commit). Net effect: zero deploy-time regression; Item 72 closes same-sprint as logged. **Methodology lesson:** the rule *"every commit that mutates the build chain triggers a `grep __dirname.*config\|src/assets/` audit before commit"* is now baked into §2.2's added note about `__dirname`-relative assets. Future build-command changes won't re-derive this risk surface.

74. ~~**Frontend POST tender URL drifted from Sprint 37f canonical (singular verb form).**~~ **LOGGED + CLOSED 2026-05-09 in v3.8.aaz** (Sprint 44c hotfix). Production 404 because frontend modal at [`frontend/src/app/dashboard/loads/page.tsx:266`](frontend/src/app/dashboard/loads/page.tsx#L266) called `/loads/:id/tenders` (plural) while backend at [`tenders.ts:14`](backend/src/routes/tenders.ts#L14) + [`apiDocs.ts:37`](backend/src/routes/apiDocs.ts#L37) + E2E spec all use canonical `/loads/:id/tender` (singular per Sprint 37f intent: POST tender = "create-a-tender" verb form, GET tenders = "list" noun form). Sprint 37f comment at [`e2e/full-lifecycle.spec.ts:146-148`](e2e/full-lifecycle.spec.ts#L146) explicitly documents the verb/noun split as deliberate design: `// v3.8.aas Sprint 37f — backend route is POST /loads/:id/tender (singular, [verb form 'create a tender']) ... [GET] is /loads/:id/tenders (plural, noun form "list of tenders")`. E2E remained green through Sprint 43+ because tests use backend API directly (singular at lines 150, 158, 225). Frontend modal was the lone regression — drift origin unknown without git blame. **Fix:** [`frontend/src/app/dashboard/loads/page.tsx:266`](frontend/src/app/dashboard/loads/page.tsx#L266) — change `/tenders` → `/tender`. Single-character delete. **Path α chosen** over Path β (which would have flipped backend + apiDocs + E2E + Sprint 37f comment to plural — broader blast radius, would reverse Sprint 37f intent). **Pattern 6 sub-rule c FOURTH prospective fire** — directive's "single-line fix line 14 (backend)" framing surfaced 9 hits across 4 files via Pattern 7 multi-surface grep; Sprint 37f intent caught + scope corrected before any code change. CLOSED in Sprint 44c.

75. ~~**E2E coverage gap on FRONTEND tender create flow.**~~ **PARTIALLY CLOSED 2026-05-10 in v3.8.abb** (Sprint 45a). New B6.5g E2E assertion at [`e2e/full-lifecycle.spec.ts:172-194`](e2e/full-lifecycle.spec.ts#L172) verifies the post-tender-create `Notification` row (type=TENDER_RECEIVED) for the carrier — locks the backend notification-creation chain. New unit test [`backend/__tests__/unit/services/notificationService.test.ts`](backend/__tests__/unit/services/notificationService.test.ts) (9 cases, all green) locks the email shape (subject, recipient, cc, replyTo, body fields) for OFFERED/ACCEPTED/DECLINED/EXPIRED/COUNTERED + Q3 fallback chain + missing-data defensive paths. **Residual gap:** Item 75's original description called out a Playwright walk that exercises modal click → frontend `api.post()` → URL canonical (regression-lock against URL-drift like Sprint 44c). That FRONTEND modal walk is still uncovered; B6.5g uses backend API direct with singular canonical for the create call, not the frontend mutation hook. The URL-drift regression class is now defended by the Sprint 44c fix-as-canonical + Sprint 44d body-shape fix as code, but not by an explicit Playwright assertion. If a future frontend-modal-only regression surfaces, log as a fresh item. Sprint 46+ B6.5f candidate stays loosely on the radar but is no longer BKN-blocking after Sprint 45a's coverage extensions.

78. ~~**Frontend tender mutation missing required `expiresAt` field — A28-2 fault line surfacing again.**~~ **LOGGED + CLOSED 2026-05-09 in v3.8.aba** (Sprint 44d hotfix). Production tender create returned 400 immediately after Sprint 44c routed the URL to the validator. [`backend/src/validators/tender.ts:3-7`](backend/src/validators/tender.ts#L3) `createTenderSchema` requires THREE fields (`carrierId: z.string()`, `offeredRate: z.number().positive()`, `expiresAt: z.string().transform(s => new Date(s))`); frontend mutation at [`loads/page.tsx:271`](frontend/src/app/dashboard/loads/page.tsx#L271) was sending a 2-field shape (`{ carrierId, offeredRate }`) — `expiresAt` was missing entirely. E2E [`full-lifecycle.spec.ts:150-156`](e2e/full-lifecycle.spec.ts#L150) has been the regression-locked canonical contract since Sprint 37 and explicitly sends `expiresAt: new Date(Date.now() + 24*60*60*1000).toISOString()` — that's why E2E B6.5 was green through Sprint 43+ AND through the Sprint 44c local run. Frontend caller was the lone outlier. **Why this shipped:** Item 75 (E2E coverage gap on FRONTEND tender create flow) just logged in Sprint 44c — B6.5 hits backend API directly, never exercises the frontend modal click → `api.post()` path. The `expiresAt`-missing bug rode under the URL-404 mask; Sprint 44c lifted the mask. **Same architectural pattern as §13.3 Item A28-2** ("validator strict, frontend payload promiscuous") — Sprint 26b (accessorial shape), Sprint 34 (quickPayFeePercent string-vs-number), Sprint 35 (fuelSurchargeType enum drift), now Sprint 44d. Each surfaces a different field on the same fault line. **Fix — Path α (frontend aligns to backend + E2E canonical):** add `expiresAt: new Date(Date.now() + 24 * 60 * 60 * 1000).toISOString()` to mutation body. ~3 LOC + 9-line inline comment block referencing Item 77 (broker-configurable expiry UX) and the E2E canonical. **Path β rejected** — would have changed validator semantics across schema, broader scope, would require E2E rewrite, contradicts Sprint 36b precedent of frontend-aligns-to-backend-canonical. CLOSED in Sprint 44d.

80. ~~**Tender notification email fan-out — wire `notifyTenderAction` and create carrier-facing transactional emails.**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abb** (Sprint 45a). Pre-Sprint-45a state: [`tenderController.createTender:45-53`](backend/src/controllers/tenderController.ts#L45) wrote a single manual `prisma.notification.create` (in-app only, no email); `notifyTenderAction` at [`notificationService.ts:91-167`](backend/src/services/notificationService.ts#L91) had the `OFFERED` case wired but was never called from `createTender` (Sprint 36 G2 finding); zero of the 4 tender-lifecycle types (TENDER_RECEIVED/ACCEPTED/DECLINED/EXPIRED) had email path. Carriers had to poll the portal to see new tenders; AEs had no signal when accept/decline happened. **Sprint 45a delivered (atomic, 4 sub-phases):** (Sub-phase 0) `emailService.ts wrap()` brand chrome reconciled to skill canonical per `references/tokens.md` (--navy `#0A2540`, --navy-700 `#15365A`, --gold `#C5A572`, --fg-on-navy-2 `#C9D2DE`, --navy-100 `#E2EAF2`); 12 existing transactional emails silently inherit. (Sub-phase 1) `sendEmail()` extends to support `cc?: string | string[]` option, backwards compatible. (Sub-phase 2) Four new tender email functions — `sendTenderOfferedEmail` (carrier with AE CC, includes lane economics: $/mile + transit days per D5), `sendTenderAcceptedEmail` (AE-facing), `sendTenderDeclinedEmail` (AE-facing, conditional decline-reason rendering per D4), `sendTenderExpiredEmail` (AE-facing, defensive add for Sprint 45b cron handler). All use new canonical wrap() + `replyTo: operations@silkroutelogistics.ai` per Q1 + `--gold-dark #BA7517` CTA buttons per skill emphasis role. (Sub-phase 3) `notifyTenderAction` extended — `EXPIRED` added to action union, OFFERED/ACCEPTED/DECLINED/EXPIRED each fan out via Resend (carrier-email resolution via Q3 fallback `carrierProfile.contactEmail || user.email`); `createTender` retires manual `prisma.notification.create` in favor of `await notifyTenderAction(tender.id, "OFFERED")`. New B6.5g E2E assertion locks Notification record creation; new unit test (`notificationService.test.ts`, 9 cases, all green) locks email shape including Q3 fallback + missing-data defensive paths. **Architectural pattern established:** `notify<Domain>Action()` helpers do BOTH in-app `createNotification()` AND `sendXxxEmail()` in one call. Future sprints retrofit the other 18 NotificationType enum values to this pattern (Item 86 LOG OPEN). **Net source change:** ~280 LOC across 4 files (emailService.ts +200, notificationService.ts +120, tenderController.ts -8/+1, full-lifecycle.spec.ts +24, notificationService.test.ts +220 new). CLOSED in Sprint 45a.

88. ~~**Inline body styles in 12 emailService.ts transactional functions hardcode off-skill colors outside `wrap()`.**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abc** (Sprint 45-RC-PRE Path α). Three sweep-replacements applied across 15 legacy email function bodies in [`backend/src/services/emailService.ts`](backend/src/services/emailService.ts), 55 hex transitions total: (1) `color:#0f172a → color:#0A2540` — 21 occurrences (15 standalone h2/body text + 6 inside CTA compounds caught by sweep 3); (2) `#e2e8f0 → #E2EAF2` — 34 occurrences (table borders, dividers); (3) `background:#d4a574;color:#0f172a → background:#BA7517;color:#FFFFFF` — 6 occurrences (CTA buttons, --gold-dark emphasis per skill canonical for active states/CTAs). **Status colors deliberately preserved as Tailwind** per Sprint 45-RC-PRE D2 ratification — `#dc2626 / #22c55e / #f59e0b / #3b82f6` stay because functional legibility of alert signals beats brand-token consistency on status semantics; carriers/AEs read red as warning regardless of palette. **Methodology correction:** previously framed as Sprint 45a sub-rule c regression; audit-first methodology corrected — Sprint 45a explicitly deferred body chrome per §3.3 atomic-commit rule (Item 88 logged at emailService.ts:73-75 in commit 165eb1d). Sprint 45-RC-PRE picks up the deferred scope. Cumulative session arc sub-rule c fire count remains 7, NOT 8. **Pre-commit verification:** backend tsc --noEmit clean, notificationService.test.ts 9/9 passed in 13.95s. CLOSED in Sprint 45-RC-PRE.

91. ~~**Carrier-facing email CTA URLs drifted to AE Console paths (`/dashboard/*` instead of `/carrier/dashboard/*`).**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abc** (Sprint 45-RC-PRE bundled with Item 88 per §3.3 atomic-commit + Pattern 7 multi-surface enumeration). Three carrier-facing CTAs in [`backend/src/services/emailService.ts`](backend/src/services/emailService.ts) pointed at AE Console URLs that carriers can't access (would 403 or redirect to login they can't pass): [`sendPreTracingEmail`](backend/src/services/emailService.ts#L114) "Update Status" `/dashboard/loads → /carrier/dashboard/loads`; [`sendAutoInvoiceEmail`](backend/src/services/emailService.ts#L136) "View Invoice" `/dashboard/invoices → /carrier/dashboard/invoices`; [`sendRateConfirmationEmail`](backend/src/services/emailService.ts#L259) "View in Dashboard" `/dashboard/loads → /carrier/dashboard/loads`. Same architectural pattern Sprint 44c caught for the frontend tender mutation URL. **Pattern 7 always-fire enumeration confirmed Sprint 45a's 4 NEW tender emails are correctly canonical:** `sendTenderOfferedEmail` (carrier-facing) → `/carrier/dashboard/tenders` ✓; `sendTenderAcceptedEmail / sendTenderDeclinedEmail / sendTenderExpiredEmail` (AE-facing) → `/dashboard/track-trace`, `/dashboard/loads`, `/dashboard/loads` ✓ — all audience-routed correctly. AE-facing legacy URLs (`sendLateAlertEmail` `/dashboard/tracking`, `sendRiskAlertEmail` + `sendFallOffAlertEmail` `/ae/loads.html`) preserved per audience-routing canonical. CLOSED in Sprint 45-RC-PRE.

46. ~~**RC Modal `fuelSurchargeType` enum mismatch — frontend `PER_MILE` vs backend `FLAT|PERCENTAGE`.**~~ **CLOSED 2026-05-07 in v3.8.aan** (Sprint 35). Path β cycle continues — Sprint 33's widened extractor surfaced `formData.fuelSurchargeType: Invalid enum value. Expected 'FLAT' | 'PERCENTAGE', received 'PER_MILE'` on next Generate PDF attempt after Sprint 34 closed `quickPayFeePercent`. **Triple-source disagreement** found at Phase A: Frontend FormState (line 182) `"FLAT" \| "PER_MILE"` vs Frontend SelectField (line 1828) `PER_MILE` option vs Backend Zod validator ([line 106](backend/src/validators/rateConfirmation.ts#L106)) `["FLAT", "PERCENTAGE"]` vs Backend Prisma enum ([schema.prisma:446](backend/prisma/schema.prisma#L446)) `FLAT/PERCENTAGE`. **Origin — domain concept conflation:** Backend validator [line 104](backend/src/validators/rateConfirmation.ts#L104) has `rateType: z.enum(["FLAT", "PER_MILE"])` — different field, different concept. Frontend RC modal copy-pasted `PER_MILE` from rateType to fuelSurchargeType. Industry-standard distinction: "per mile" rates apply to base load rate (rateType); "percentage" surcharges apply to fuel only (fuelSurchargeType). **Fix — Path A (frontend aligns to backend canonical):** FormState type union `PER_MILE → PERCENTAGE`; SelectField onChange cast and option both `PER_MILE → PERCENTAGE` (label "Per Mile" → "Percentage"). Plus `initForm` defensive normalization coerces any legacy stored `PER_MILE` to `FLAT` default. ~5 LOC source + 3 LOC normalize. **Path B (add PER_MILE to backend enum) rejected** — would cement frontend's domain conflation. Backend canonical `FLAT/PERCENTAGE` is correct per industry convention. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Per §3.1: v3.8.aam → v3.8.aan.

48. ~~**RC PDF format does not match `srl-brand-design` skill canonical (BOL v2.9 reference).**~~ **LOGGED + CLOSED 2026-05-10 in v3.8.abd** (Sprint 45-RC Path β1 migration). 7 findings logged Sprint 34 Phase C + 1 finding (#8 rate-on-page-4) surfaced during Sprint 45-RC Phase A2 pixel verification all resolved via skill chrome library import + builder-API rewrite of `generateEnhancedRateConfirmation`. **Path β1 chosen** over β2 inline — skill canonical `srl_chrome.ts` (~1230 LOC) imported into `backend/src/lib/srl-chrome.ts` + 4 compass PNG fallbacks bundled (Sprint 44b cp -r src/lib step added to build chain). Sets up Sprint 45-RC2 (Invoice) + 45-RC3 (Settlement + ShipperLoadConf) reuse. **Resolutions:** (1) phantom-blank pages 2/3/5/6 → dynamic flow via `drawContinuationHeader` on actual overflow only; canonical 2-page layout per skill RC anatomy (`pdf-chrome.md`). (2) address duplicated → `BRAND.address` single-line from skill lib. (3) no QR — **skill canonical confirmed correct** per `SKILL.md`:86 ("QR codes do NOT belong on Rate Confirmations — carrier email/portal artifact, no scan event in the workflow"); the original Item 48 directive's contrary suggestion was Pattern 6 sub-rule c material from Phase A audit; reversed. (4) generic logo → `drawCompassMark` resolves PNG fallback from `src/lib/srl_compass_{60,120,240,480}.png`. (5) carrier section empty → carrier identity captured in `RATE_CON_SIGNATURE_ROLES` (Carrier Acceptance signature block, single block not three-block BOL pattern), not body. (6) TOTAL bare → `drawRateBreakdown` (linehaul + FSC + accessorials → bold Total Carrier Pay) + `drawLaneEconomics` (MILES/TRANSIT/$/MILE pills) + `drawRateConTerms` (detention/TONU/layover grid). (7) chrome drift → skill canonical Times-Bold + Helvetica + `#0A2540` navy + `#C5A572`/`#BA7517` golds via wrapped builder calls. (8) **NEW finding from Phase A2 pixel verification — rate breakdown paginated to page 4** — `drawRateBreakdown` placed ON PAGE 1 below parties block per skill RC anatomy. **Item 8.8 leading-zero MC# carry-forward** — skill BRAND.mc verbatim (`01794414`); D7 ratification defers to Item 8.8 dedicated sprint to close all 14 surfaces atomically. **E2E `RC_PDF_REQUIRED` extended** with 4 strings (DOT# 4526880, operations@silkroutelogistics.ai, State of Michigan, Kalamazoo County) per D8; "MC# 1794414" excluded pending Item 8.8. **Per D9 deploy sequence:** Wasi must update Render dashboard buildCommand to add `cp -r src/lib dist/backend/src/lib` BEFORE Sprint 45-RC commit deploys, or RC PDF runtime will 500 on missing-PNG. Pattern 6 sub-rule c fired TWICE in Phase B (sub-phase 3 directive claim about package.json + Phase A2 pixel-discovery of finding #8). Item 93 banks methodology observation. Other generators (BOL/Invoice/Settlement/ShipperLoadConf) untouched per Sprint 45-RC scope discipline; their migrations queued as Sprint 45-RC2/3.

43. ~~**Silent error swallow on RC modal handlers — handleSaveDraft / handleGeneratePdf / handleSendTender catch blocks all `console.error` only with no UI feedback.**~~ **CLOSED 2026-05-06 in v3.8.aak** (Sprint 32 Path β). Bug class: "API failures invisible to user." Surfaced as a side-effect of Item 42 audit — even if Item 42 is fixed, future API failures on these handlers would silently fail the same way. Same defensive class as Sprint 26b error boundary patterns. **Fix:** added `submitError: string | null` state to RC modal root component; new `extractErrorMessage(err, fallback)` helper checks `err.response.data.error → .message → err.message → fallback` (handles axios + native Error + plain string shapes); all 3 handlers reset error to null at start, set error in catch block on failure; new JSX banner above button row conditionally rendered when `submitError` set — `bg-red-50 border-l-4 border-red-500 text-red-700` (canonical Tailwind error palette, works on cream + dark contexts) with "Error:" label prefix. Banner auto-dismisses on next handler attempt (resets at handler start). **Future API failures on these handlers self-diagnose via screenshot** — no DevTools workflow required. ~25 LOC (state + helper + 3 handler edits + JSX banner). **Pattern propagation candidate:** other modals/forms across the codebase likely have the same silent-error class; could roll up into a typing-discipline / observability sprint to apply pattern consistently. Not in Sprint 32 scope per §3.3 atomic-commit rule.

**RC Modal carrier search route fix — hotfix (logged + closed 2026-05-06 in v3.8.aaj, Sprint 31):**

39. ~~**RC Modal carrier search returns 404 silent-fail (B28-T1.3-A re-diagnosed).**~~ **CLOSED 2026-05-06 in v3.8.aaj** (Sprint 31). Originally walked as "P1 missing empty state on carrier search." Phase A audit corrected the diagnosis — the empty state was never the bug. Root cause: [`RateConfirmationModal.tsx:1422`](frontend/src/components/loads/RateConfirmationModal.tsx#L1422) (pre-fix) called `api.get("/carriers/all", ...)` but the canonical handler `getAllCarriers` is mounted at `/api/carrier/all` (singular) per [`backend/src/routes/carrier.ts:123`](backend/src/routes/carrier.ts#L123), NOT `/api/carriers/all` (plural). The plural route file defines `GET /` (with validation) and `GET /:id`, so Express matched `/api/carriers/all` against the `/:id` route with `req.params.id = "all"`, called `getCarrierDetail("all")` → returned 404. Frontend `useQuery` silently swallowed the 404, `data` stayed undefined, dropdown render gated on `showSearch && carriers && (...)` never fired — AE saw silent dead-state (input typed, no dropdown, no error, no autofill). **Re-diagnosis pattern matches Sprint 26 → 26b** (accessorial walk surfaced symptom, audit identified actual root cause): walk surfaces what user sees, audit identifies real bug class, correct fix ships. Memory #11 firing again. **Fix:** 1-character change — removed `s` from `carriers/all`. New URL `/carrier/all` now resolves to the actual `getAllCarriers` handler. Inline comment added at the call site referencing Sprint 31 + Item 40 (the duplicate-route consolidation backlog). **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net LOC change: ~7 (1 URL char swap + 6-line inline comment). Per §3.1: v3.8.aai → v3.8.aaj.

34. ~~**RC Modal Broker Info placeholders not actual SRL data (B28-T1.2-A).**~~ **CLOSED 2026-05-06 in v3.8.aai** (Sprint 30). Phase A audit confirmed [`RateConfirmationModal.tsx:272-279`](frontend/src/components/loads/RateConfirmationModal.tsx#L272) `SRL_INFO` constant held template values from a Texas-based shipping company that had never been updated for SRL — same template provenance as Items 35-38 below. Single-file blast radius confirmed via grep sweep across `frontend/src` + `backend/src` (only `eldService.ts:52` "Houston, TX" matched the sweep, but that's an ELD geocoding city lookup, not SRL identity). Updated all SRL_INFO fields to canonical values per CLAUDE.md §1 + voice.md:98.

35. ~~**RC Modal wrong address (Houston, TX 77063 instead of Galesburg, MI 49053) (B28-T1.2-B).**~~ **CLOSED 2026-05-06 in v3.8.aai** (Sprint 30). [`RateConfirmationModal.tsx:276`](frontend/src/components/loads/RateConfirmationModal.tsx#L276) had `"8950 Westheimer Rd, Suite 200, Houston, TX 77063"` — wrong company entirely. Updated to canonical `"2317 S 35th St, Galesburg, MI 49053"` per CLAUDE.md §1 (FMCSA SAFER + BMC-84 bond paperwork as primary source per §3.13). Severity: would have appeared on every RC PDF sent to BKN's carriers in mid-May; credibility-class issue for customer-facing legal document.

36. ~~**RC Modal non-canonical email alias (dispatch@ instead of operations@) (B28-T1.2-C).**~~ **CLOSED 2026-05-06 in v3.8.aai** (Sprint 30). [`RateConfirmationModal.tsx:278`](frontend/src/components/loads/RateConfirmationModal.tsx#L278) had `dispatch@silkroutelogistics.ai` — not in CLAUDE.md §1 canonical alias list (`whaider`, `accounting`, `compliance`, `noreply`, `operations`). Updated to canonical `operations@silkroutelogistics.ai` per §1 + §3.10 explicit guidance: "Used on BOL, Rate Confirmation, Invoice, and other shipper/carrier-facing documents generated via the srl-brand-design skill." Same class as §13.3 Item 8.2.1 (`sales@` undocumented alias) — `dispatch@` is similar drift but `operations@` is the explicit canonical for shipping documents.

37. ~~**RC Modal MC#/DOT# format drift (hyphen instead of `#` symbol) (B28-T1.2-D).**~~ **CLOSED 2026-05-06 in v3.8.aai** (Sprint 30). [`RateConfirmationModal.tsx:274-275`](frontend/src/components/loads/RateConfirmationModal.tsx#L274) had `"MC-1794414"` and `"DOT-4526880"` — non-canonical hyphenated format. Updated to `"MC# 1794414"` and `"DOT# 4526880"` per [voice.md:98](.claude/skills/srl-brand-design/references/voice.md#L98) skill SOT (with `#` symbol, single space, no hyphen). Format also matches typical broker industry convention and existing BOL v2.9 production output. Numeric values were already correct (1794414 and 4526880, no leading-zero typo from §13.3 Item 8.8).

38. ~~**RC Modal Terms & Conditions wrong governing-law clause (Texas instead of Michigan, no venue) — adjacent finding bundled with B28-T1.2-A through D.**~~ **CLOSED 2026-05-06 in v3.8.aai** (Sprint 30). [`RateConfirmationModal.tsx:270`](frontend/src/components/loads/RateConfirmationModal.tsx#L270) `DEFAULT_TERMS` clause 12: `"governed by the laws of the State of Texas"` — wrong jurisdiction per CLAUDE.md §14 ("Michigan governing law, Kalamazoo County venue for disputes"). Same Texas-template provenance as Items 34-37. Bundled into Sprint 30 atomic commit — same root cause, same module, same fix slot. Updated clause: `"governed by the laws of the State of Michigan, with venue in Kalamazoo County, and applicable federal transportation law (49 U.S.C. § 14101(b))."` Format notes: § symbol replaces "Section" word per legal convention; venue clause added per §14 (governing law without venue is incomplete legal protection — disputes default to where claimant files; Kalamazoo County venue forces SRL's home jurisdiction).

**Production reliability — hotfix (logged + closed 2026-05-06 in v3.8.aah, Sprint 29):**

33. ~~**RateConfirmation modal accessorial shape mismatch — same root-cause class as Item 32 Load Board crash, different surface.**~~ **CLOSED 2026-05-06 in v3.8.aah** (Sprint 29 hotfix immediately after Sprint 28 Phase A surfaced it as A28-1 P0). Phase A static-analysis grep on `frontend/src/` for accessorial render sites surfaced [`RateConfirmationModal.tsx:418`](frontend/src/components/loads/RateConfirmationModal.tsx#L418) — pre-fix code: `description: a.description || a` fell through `|| a` to the whole `{type, amount, payer}` Order Builder object when `.description` was undefined. Object then propagated to (1) [`line 500`](frontend/src/components/loads/RateConfirmationModal.tsx#L500) `.filter((a) => a.description.trim())` → TypeError "a.description.trim is not a function"; (2) [`line 1716-1717`](frontend/src/components/loads/RateConfirmationModal.tsx#L1716) `<input value={acc.description} ... />` → React error #31 same as Sprint 26b. **Trigger conditions** identical to Item 32: load created via Order Builder convert flow + at least one accessorial added + AE opens RateConfirmation modal. **Fix** — defensive type-check pattern matching Sprint 26b. Three input shapes supported: string (legacy) → description = a; `{description, amount}` (RC's own saved shape from prior RC modal saves) → description = a.description; `{type, amount, payer}` (Order Builder shape per [`orders/types.ts:90`](frontend/src/app/dashboard/orders/types.ts#L90)) → description = a.type. Order Builder accessorial `{type: "Detention", amount: 0}` now hydrates as RC modal accessorial `{description: "Detention", amount: "0"}`. **Why Phase A caught it that Sprint 26b's frontend smoke didn't:** Sprint 26b smoke clicked Load Board side panel which doesn't open the RC modal — the RC modal is opened separately via "Send Rate Confirmation" action on a tendered load. The bug shape was identical but on a different render site. Sprint 28's static-analysis grep for accessorial readers surfaced this immediately, validating the audit-only sprint methodology. **Root-cause loophole still open (Sprint 31 candidate):** [`backend/src/validators/load.ts:113`](backend/src/validators/load.ts#L113) `accessorials: z.array(z.any()).optional()` accepts any shape — Sprint 26b + Sprint 29 both cosmetically patched consumer sides but Order Builder writes its shape and RC modal save writes its shape, so multiple shapes coexist in DB. Sprint 31 will tighten the validator + add data migration. Sprint 29 handles both shapes defensively at read time. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes), frontend `next build` clean. Net LOC change: ~15 (1 file, accessorial hydrator block expanded from 1 line to ~17 with defensive type-check + comment block). Per §3.1 sequence-continuous rule: v3.8.aag → v3.8.aah. Logged + closed in same commit per established hotfix-with-immediate-close pattern.

**Sprint 52 epic — tender workflow polish (banked §19 meta-50, 2026-05-16, post-Sprint-50 v3.8.abn session arc closure):**

141. ~~**Tender expiry cron sweeper.**~~ **CLOSED 2026-05-16 in v3.8.abw / Sprint 52a** (commit `47bf0e53`) — docs-row marked CLOSED 2026-05-30 (it shipped but the §13.3 row drifted stale-open; **Sub-pattern 15 backlog-row-drift fire**, caught during the Item 142→141 sequence Phase A audit when a grep of the cited code paths showed the work already live). Shipped shape: hourly cron `tender-expiry-sweep` at `cron/index.ts:107` (`"30 * * * *"`, Eastern per Item 185) calls `processExpiredTenders()` ([`tenderController.ts:564`](backend/src/controllers/tenderController.ts#L564)) which (a) `findMany` OFFERED+COUNTERED tenders with `expiresAt < now`, (b) batch `updateMany` → EXPIRED + respondedAt, (c) fires `notifyTenderAction(tender.id, "EXPIRED")` per tender (try/catch per-tender so one transport error doesn't break the batch — this is the Sprint 45a `sendTenderExpiredEmail` "defensive add" reaching live state, exactly the Item 80 closure note), (d) for each affected load with zero remaining active tenders, reverts TENDERED → POSTED via the `validateLoadStatusTransition` state-machine validator (Item 159 Sprint 3 defense-in-depth) so the load returns to the board. **Interaction with Item 142 (magic-link):** the cron makes 142's "already handled" path proactive — once the sweep flips an expired tender to EXPIRED, a stale magic-link click hits the endpoint's `status !== "OFFERED"` check → "already handled" page, rather than relying on `acceptTender`'s runtime expiry gate to render the warn page. Both paths are safe; the cron just makes the friendlier message the common case. **Deviation from sprint-shape note:** shipped as hourly (`:30`) not "every 10 minutes" as the original shape guessed — hourly is sufficient at pre-revenue tender volume and matches the other email-fanning cron cadences. The "advance waterfall position to next-tier" sub-item was NOT part of v3.8.abw (waterfall position expiry has its own handling in `waterfallEngineService`); the load-revert-to-POSTED covers the direct-tender path. Not BKN-blocking; now live.

142. ~~**Magic-link carrier accept-without-login.**~~ **CLOSED 2026-05-30 in v3.8.als.** Pre-als `sendTenderOfferedEmail` (Sprint 45a Item 80) landed in the carrier inbox with a single "Log in to view tender" CTA — the carrier had to authenticate before acting. Now the email carries one-click **Accept** (green) + **Decline** buttons, matching the industry-best (CHR / Coyote / RXO) magic-link pattern. **Shape shipped:** (token) new [`lib/tenderActionToken.ts`](backend/src/lib/tenderActionToken.ts) — `mintTenderActionToken` / `verifyTenderActionToken` / `tenderActionUrl`. Signed JWT (JWT_SECRET, HS256 — same as auth tokens) embedding `{ tenderId, action: "accept" | "decline", carrierUserId }`, **7-day expiry** (not 24h as the original sprint-shape guessed — the server-side tender-expiry gate in `acceptTender` is the real freshness control, so the token just needs to outlive a reasonable response window). (endpoint) new PUBLIC `GET /api/tender-action/:token` ([`routes/tenderAction.ts`](backend/src/routes/tenderAction.ts)) mounted in `routes/index.ts` WITHOUT `authenticate` — same public-magic-link pattern as the v3.8.akn quote-approve route. The token IS the authorization. (reuse) rather than a frontend `/tender-action/[token]/page.tsx` route (blocked by static export, per the Item 31/156 RSC class) OR duplicating the ~145-LOC accept path, the endpoint delegates to the existing `acceptTender`/`declineTender` controllers via a **response-capturing shim** (`makeCaptureRes` — captures `res.status().json()`) + a **synthetic carrier actor** (`req.user.id = embedded carrierUserId`, which satisfies the controllers' `tender.carrier.userId !== req.user.id` ownership gate). ZERO duplication — compliance re-check + atomic transaction + shipment creation + auto-RC + notifications + tracking-link fan-out all reused. (UI) renders a self-contained **branded HTML acknowledgment page** (accepted / declined / already-handled / not-found / expired / invalid) inline from the API — no frontend route needed since carriers click from email on any device. (wiring) `notifyTenderAction("OFFERED")` mints the two tokens (only when the carrier has a linked `User.id`; absent → email falls back to the login button) and passes `acceptUrl`/`declineUrl` to `sendTenderOfferedEmail`. **Double-act safety:** the delegated controllers' server-side gates make a stale/replayed token safe — a non-OFFERED tender hits the endpoint's `status !== "OFFERED"` check → "already handled" page; an expired tender hits `acceptTender`'s expiry gate → the controller's expiry message surfaced in the warn page. **Sub-rule c gates applied:** authoritative-source check on JWT lifecycle (mirrors the auth-token + tracking-token pattern); audit-both-ends verified the synthetic-actor req shape satisfies both controllers' `req.user.id` + `req.params.id` + `req.body` reads. **Scope:** ~250 LOC across 5 backend files (token lib + public router + index mount + emailService CTA block + notificationService OFFERED wiring). No schema, no migration. Gates: backend tsc + vitest 224/224 + frontend tsc + next build all clean. **Banked follow-ups:** Item 144 (carrier counter-offer UI — the magic-link decline currently captures no reason; a counter/decline-reason flow could extend the token actions), Item 141 (tender-expiry cron — when it ships, expired tenders flip to EXPIRED proactively so the magic link renders "already handled" rather than the controller's runtime expiry message).

143. ~~**Counter-tender email handler + carrier counter-offer UI.**~~ **CLOSED** in two parts — the email half in **v3.8.aka** (already shipped, was stale-open here — Sub-pattern 15), the carrier-UI half in **v3.8.alt (2026-05-30)**. (a) **Email — already live.** `sendTenderCounteredEmail` exists at [`emailService.ts:845`](backend/src/services/emailService.ts#L845) and IS wired into the `notifyTenderAction("COUNTERED")` case at [`notificationService.ts:369`](backend/src/services/notificationService.ts#L369) (AE-facing, offered-vs-counter delta context). The "counter-tender email path remains unfired" framing above was stale — it shipped in v3.8.aka. Caught during the Item 144 Phase A audit (Sub-pattern 15 backlog-row-drift). (b) **Carrier counter-offer UI — v3.8.alt.** The backend `counterTender` (`POST /tenders/:id/counter`) was fully wired since v3.8.aka (status → COUNTERED + counterRate + audit + the email above) but the carrier portal `/carrier/dashboard/tenders` page had only Accept + Decline. v3.8.alt adds a gold "Counter" button (Repeat2 icon) + a counter-rate input panel (pre-filled with the offered rate, `$` prefix, positive-number guard) that POSTs `{ counterRate }` to the existing endpoint. On success the tender drops off the OFFERED-filtered carrier list (ball in AE's court — same disappear-on-action UX as decline). ~55 LOC, frontend-only, mirrors the decline-panel pattern. Gates: frontend tsc + next build clean. **NUMBERING-DRIFT NOTE (Sub-pattern 15):** Items 142 + 143 prose referenced "Item 144 (carrier counter-offer UI)", but the literal §13.3 **row 144 is "Tiered expiry preset UX"** — a distinct, still-open item. The carrier-counter-offer UI never had its own numbered row; it was the frontend half this Item 143 explicitly scoped, so its closure is recorded here. Row 144 (tiered expiry preset) remains OPEN.

144. ~~**Tiered expiry preset UX (4h / 24h / 48h).**~~ **CLOSED 2026-05-30 in v3.8.alv** (the literal row 144 — distinct from the carrier counter-offer UI the Item 142/143 prose mislabeled "Item 144", which shipped under Item 143 / v3.8.alt; see that row's numbering-drift note). Originally Item 77 (Sprint 44d) — the AE Tender modal hardcoded a 24h `expiresAt` (the v3.8.aix/aba broker-default + E2E canonical at `full-lifecycle.spec.ts:155`) with no UI to change it. **Shape shipped:** a preset toggle (4h / 24h / 48h) + a custom hours input (1-168) added to `TenderForm` ([`/dashboard/loads` page](frontend/src/app/dashboard/loads/page.tsx)), below the Offered Rate field. New `tenderExpiryHours` state (default 24) passed as `expiryHours`/`setExpiryHours` props; the chosen window flows as `expiresInHours` through the `createTender` mutation at **all 3 compliance branches** (clean / warnings / catch) and is converted to ISO `expiresAt` in the mutationFn (`Date.now() + (expiresInHours ?? 24) * 3600_000`). The `?? 24` default preserves the E2E canonical + any non-modal caller. Resets to 24h on success. **Sub-rule c gate applied (audit-both-ends-of-data-flow):** confirmed `createTenderSchema` (`expiresAt: z.string().transform(s => new Date(s))`) accepts the arbitrary ISO string — no validator change needed. **Pairs with Item 141** (v3.8.abw expiry-sweep cron) — the cron auto-expires the tender when the chosen window closes, and (per Item 142) flips it to EXPIRED so a stale magic-link renders "already handled." ~55 LOC, frontend-only, one file. Gates: frontend tsc + next build clean. Retroactively closes the AE half of the user's "optimal workflow" tender spec (the carrier-side counter + magic-link halves shipped in v3.8.alt + v3.8.als).

145. ~~**Engagement signals + decline taxonomy analytics.**~~ **CLOSED 2026-05-30 in v3.8.alz** (mechanics built; statistically meaningful only once tender volume arrives). New `GET /analytics/tender-funnel` (ADMIN/CEO/BROKER/OPERATIONS) at [`routes/analytics.ts`](backend/src/routes/analytics.ts) — single windowed `LoadTender.findMany` (with `load.equipmentType` + `carrier.cppTier`, capped 5000) + JS aggregation: funnel (offered → accepted/declined/countered/expired/pending), conversion (acceptance-of-total, acceptance-of-responded, response rate), avg response time (respondedAt − createdAt), decline-reason distribution, and breakdowns by equipment / carrier tier / expiry-window. New [`/dashboard/tender-analytics`](frontend/src/app/dashboard/tender-analytics/page.tsx) page (Intelligence sidebar cluster) — funnel stat cards + conversion bars + decline-reason bar list + 3 acceptance-rate tables. **Honest-at-low-N (Item 12.1 null-guard lesson applied):** empty-state "No tenders in this window yet" when total=0, per-panel empty rows, `fmtDuration` null-guard. **Expiry-window bucket** maps the actual (expiresAt − createdAt) hours to the 4h/24h/48h presets (Item 144) → acceptance rate per window — the AE expiry-preset effectiveness signal. **Scope deviation from sprint-shape:** mounted under `/dashboard/tender-analytics` (Intelligence cluster), not `/admin/analytics` — the latter is ADMIN-only (`adminController.getAnalytics`), but tender funnel is operationally useful to BROKER/OPERATIONS too, so it lives on the broader analytics router with role-gated authorize. Used CSS-bar rendering (not Recharts) to keep it dependency-light + robust on empty data. **Not built:** by-lane + day-of-week + hour-of-day breakdowns (the sprint-shape's fuller cut) — banked; the equipment/tier/window/decline-reason cuts are the high-signal ones at launch volume, and lane/time-of-day need more rows to be non-noise. **Sub-rule c gate (Sub-pattern 5 audit-both-ends):** verified the aggregation reads the real persisted fields (status, declineReason, respondedAt, expiresAt, createdAt, load.equipmentType, carrier.cppTier) — all populated by the live tender flow (createTender + counterTender + declineTender + processExpiredTenders). ~250 LOC across endpoint + page + 1 sidebar line. No schema, no migration. Gates: backend tsc + vitest 235/235 + frontend tsc + next build all clean. **Letter:** parallel Compass-Score Builds A/B/D took alw/alx/aly mid-build; bumped to alz (Sub-pattern 6). Completes the tender-lifecycle epic's analytics layer.

156. ~~**Carrier route RSC payload regression.**~~ **CLOSED 2026-05-17 in v3.8.abx** (Sprint 52.hotfix.a) — **then REOPENED in v3.8.aby** (Sprint 52.hotfix.a.b rollback per Item 157). The explicit `.html` rewrites resolved the Gmail iOS RSC payload issue but surfaced a latent redirect loop on `/carrier/dashboard` (ERR_TOO_MANY_REDIRECTS post-login). Rollback prioritized known-working state; combined fix queued for Sprint 52.hotfix.c after redirect-loop diagnosis. Pre-fix root cause notes preserved: `_redirects` rule `/carrier/* /carrier/:splat 200` echoes `:splat` as identity rewrite target; Cloudflare Pages resolves the extensionless path ambiguously between `out/carrier/<name>.html` and `out/carrier/<name>.txt` siblings (Next.js static export generates both). Gmail iOS WebView Accept-header content negotiation lands on the `.txt` RSC payload. **Status: OPEN (Sprint 52.hotfix.c candidate, combined fix with Item 157).** **CLOSED 2026-09-18 in v3.8.bcu — and the mechanism recorded above was wrong.** There is no Accept-header negotiation: direct GETs serve HTML under every request shape (verified by curl with an iOS Safari UA, `Accept: */*` and `Accept: text/plain`). The `.txt` reached the address bar because Next 15.5.x's router **navigated there itself** — its `fetch-server-response` catch block falls back to the mutated `.txt` flight URL on any thrown fetch, and Safari throws that fetch on a dropped connection or when a competing hard navigation commits. Patched in `frontend/patches/next+15.5.14.patch` with a behavioural guard and a prebuild gate; full account in the §11 row. **No `_redirects` change was ever the right fix**, which is why every attempt at one either did nothing or looped (Item 157).

157. ~~**Sprint 52.hotfix.a rollback — /carrier/dashboard ERR_TOO_MANY_REDIRECTS.**~~ **CLOSED 2026-05-17 in v3.8.aby** (Sprint 52.hotfix.a.b). Sprint 52.hotfix.a replaced the `/carrier/* /carrier/:splat 200` catch-all with 5 explicit `.html` rewrites for Next.js carrier routes. Production post-deploy verification surfaced a latent redirect loop: INTEGRITY EXPRESS carrier completed login successfully then `/carrier/dashboard` returned ERR_TOO_MANY_REDIRECTS. The redirect loop is presumed to be in the carrier dashboard's auth gate (client-side useCarrierAuth hook OR layout-level redirect), interacting with the new `.html` explicit rewrite in a way the `:splat` echo didn't surface. **Rollback fix:** reverted `_redirects` line 22 to the original `/carrier/* /carrier/:splat 200`. Restores known-working /carrier/dashboard behavior at the cost of reintroducing the Gmail iOS RSC payload regression (Item 156 re-opened). Combined fix planned for Sprint 52.hotfix.c after Phase A diagnostic on BOTH issues — likely involves either (a) auditing the carrier dashboard auth-redirect path for the loop trigger, (b) adopting a hybrid `_redirects` pattern that adds `.html` rewrite for /carrier/login + /carrier/forgot-password + /carrier/reset-password (where the RSC issue surfaced) while leaving `/carrier/dashboard/*` under the `:splat` echo until the auth loop is diagnosed, OR (c) deeper Next.js static export config / middleware investigation. **SUPERSEDED 2026-09-18 (v3.8.bcu):** the "combined fix" is moot. The redirect loop was the v3.8.ade class — Cloudflare Pages canonicalises a `.html` rewrite target into a 308 to the extensionless path, which then matches the same rule again — and the RSC symptom it was paired with was never a Cloudflare routing problem at all (Item 156 closure). `_redirects` stays as the v3.8.aby rollback left it.

161. **REOPENED + CLOSED 2026-05-17 in v3.8.aca (Sprint 53) — Auth cookie isolation. Original "no bug" close was premature. Sprint 53.a (v3.8.acb) hotfix follow-up — substring-match prefix collision.** Sprint 53 shipped with a substring `.includes("/carrier")` match in `resolveCookieToken` at `middleware/auth.ts:90` that wrongly routed AE-side endpoints with "carrier" in the name to the carrier cookie. Wasi's post-deploy smoke surfaced 403 on `GET /api/carriers` — same architectural class as Sprint 53's parent bug (substring vs proper prefix matching), just at a different layer. **V2 audit** during the Sprint 53.a hotfix enumerated `/api/carrier` (singular) as a genuine audience-mixed mount (6 carrier-facing endpoints `dashboard|scorecard|revenue|bonuses|onboarding-status|documents` lacking explicit `authorize("CARRIER")` + 6 AE-side endpoints `all|:id/detail|:id|verify/:id|admin-setup|capacity-feed` with explicit `authorize("ADMIN", "CEO", ...)`). Single-axis allow-list can't resolve both audiences without one side breaking in the dual-portal scenario. **Decision locked Option B per Wasi 2026-05-17:** allow-list EXCLUDES `/api/carrier` (singular) — the 6 carrier-facing endpoints under it return AE-scoped data when both portals are logged in same browser. Real-world users hold one role per browser so the fallback chain rescues; only deploy-test scenarios hit this. Architectural fix (split mount to move 6 AE-side endpoints into existing `/api/carriers` plural which has overlapping `getAllCarriers` controller anyway, OR introduce X-Portal request header from frontend) tracked as **Sprint 54+ candidate** — same priority as Item 158 (parallel-endpoint consolidation), same class. **Sprint 53.a fix shape:** explicit `CARRIER_PORTAL_MOUNTS` array (`/api/carrier-auth`, `/api/carrier-loads`, `/api/carrier-compliance`, `/api/carrier-payments`, `/api/carrier-tenders`) + `SHIPPER_PORTAL_MOUNTS` array (`/api/shipper-portal`) + new `matchesPortalMount(baseUrl, mounts)` helper using `baseUrl === p || baseUrl.startsWith(p + "/")`. ~25 LOC net change in single file. **Sub-pattern observation gains a second fire:** Sprint 52.hotfix.c missed the cookie layer entirely (Sprint 53 fire #1); Sprint 53 missed the substring-vs-prefix distinction within the cookie layer (fire #2 — same root cause class, one level deeper). The methodology principle banked at Item 161's first close ("Phase A on auth/permission symptoms must reach the storage/transport layer") needs an extension: *"and the prefix-vs-substring distinction within transport-layer routing logic is a hot edge — verify by enumerating ALL mount paths containing the keyword, not by trusting heuristic boundary matching."* Two fires; one more from a future independent incident promotes the principle to §19 canonical per sub-rule c three-fire convention. Sprint 53.a closes 53's regression; original Sprint 53 closure of items 9/10/11/14 remains in force.

 Sprint 52.hotfix.c attributed the carrier "Insufficient permissions" + three-surface status divergence to *observer-identity-confusion* (operator with multi-role privileges polluting their own test session). **Corrected RCA:** all three portals (AE Console, Carrier Portal, Shipper Portal) shared a single httpOnly cookie name `srl_token` set by `backend/src/utils/cookies.ts:14`. Same browser logging into two roles overwrote the cookie — second login replaced the first's JWT. `authorize("CARRIER")` at `backend/src/middleware/auth.ts:165` fired "Insufficient permissions" when the carrier-portal call ran with the ADMIN JWT that had just overwritten the carrier one. The system_log entries Sprint 52.hotfix.c surfaced (`whaider@silkroutelogistics.ai` denied on carrier endpoints) were the contamination *trail*, not test apparatus error. The "test from incognito" workaround masked the bug because incognito has no other portal's cookie to overwrite the carrier one. **Fix shipped Sprint 53:** per-portal cookie names (`srl_token_ae` / `srl_token_carrier` / `srl_token_shipper`), middleware picks the right cookie by `req.baseUrl` prefix, role passed to `setTokenCookie(res, token, role)` at all 14 callsites (authController.ts × 7, routes/auth.ts × 2, carrierAuth.ts × 5), legacy `srl_token` read as one-deploy migration fallback and cleared on next logout. Closes user punch-list items 9 (mark-delivered "Insufficient permissions"), 10 (carrier account auto-switched mid-test), 11 (carrier signout logged out AE). Bundled with Item 14 closure (loginLimiter bumped 5→20 in routes/auth.ts + routes/carrierAuth.ts, authLimiter bumped 30→100 in server.ts — Wasi's lockout during the lifecycle test). **Methodology observation banked as sub-pattern candidate (awaits 3-fire validation per §19 canonical promotion convention):** *"Phase A on auth/permission symptoms must reach the storage/transport layer (cookies, sessions, tokens, headers), not stop at the role gate."* Sprint 52.hotfix.c's Phase A inspected User.role bytes, system_logs, and Neon DB state — but stopped before the cookie layer that delivered the JWT. The "observer-identity-confusion" interpretation was internally consistent with what Phase A surfaced; the gap was scope, not reasoning. Extends Sub-pattern 5 (audit-both-ends-of-data-flow) from data flow to transport flow. Fire #1 banked here. Original "no bug" close preserved below for history.

161-original. ~~**Sprint 52.hotfix.c closed — no bug (observer-identity confusion fire #1).**~~ **CLOSED 2026-05-17 (chore, no version bump, no code change).** Reported symptom was carrier "Update Status: AT PICKUP" button returning "Insufficient permissions" + three-surface status divergence across carrier portal / AE Load Board row / AE Load Board detail panel. Phase A R1-R5 audit + Wasi-side Neon verification confirmed system working as designed: User.role = "CARRIER" byte-exact (length 7, hex `43415252494552`), system_logs healthy (65 SECURITY warnings in past 7 days, latest write <1 minute old at audit time), all recent `authorize()` denials attributed to `whaider@silkroutelogistics.ai` (ADMIN role) attempting carrier-portal endpoints — those denials were CORRECT per the role gate. INTEGRITY EXPRESS session itself was never the denied actor; the symptom surfaced from **cross-role cache contamination from admin's testing session** (admin had AE token in browser storage while testing carrier flows in same tab/window). The three-surface status divergence dissolved when the test was re-run from an incognito window with carrier credentials only — confirming the divergence was browser-state pollution, not data drift. **Methodology observation (fire #1):** *observer-identity-confusion antipattern* — when an operator with multi-role privileges tests a role-restricted flow without isolating session context, observed "bug" may be the test apparatus, not the system. Mitigation: always test role-restricted flows from incognito or separate browser profile with test-subject credentials only. Worth banking for second/third fire validation before promoting to §19 canonical (parallel to sub-pattern 13 + sub-pattern 15 three-fire lineage convention). Sprint 52.hotfix.c closes with zero source change, zero data fix; the Phase A audit work IS the deliverable — Items 158/159/160 banked as collateral findings.

178. ~~**Multi-line freight edit UI in Carrier Engagement Drawer.**~~ **CLOSED 2026-05-25 in v3.8.aks** (superseded by Sprint 63 v3.8.afi architectural decision; no source change). Sprint 63 deliberately reduced the drawer scope from Sprint 59.b's 7 sections (Lane / Freight / Schedule / References / Carrier / Financials / Instructions) to a focused 3-editable-sections shape (Carrier engagement + Financials + Instructions) with a read-only SummaryHeader. The original Item 178 sprint shape ("widen drawer Freight section to a small editable table") is no longer valid because the Freight section was intentionally removed. Post-Sprint-63 design canon: **freight editing lives in Order Builder** (where `LineItemsSection.tsx` provides full multi-line UI including pieces/packageType/weight/description/freightClass/nmfcCode/hazmat/hazmatUnNumber/hazmatClass per row), and the **drawer is a focused carrier-engagement surface** that consumes the already-finalized line array (passes `lineItems[]` to `POST /api/loads/with-tender` unchanged, primary line from form state + rest from `lineItemsRest` prop). The "+N more lines" SummaryHeader chip is read-only by design, not a UX gap. AE who needs to edit line 2+ closes the drawer, edits in Order Builder, reopens. **Post-creation freight edits** (after the drawer has submitted and the Load row exists) are §13.3 Item 3 territory (EditLoadModal) — distinct architectural surface, separate sprint when prioritized. Item 178 superseded; original framing assumed Sprint 59.b's drawer scope which was deliberately collapsed by Sprint 63's directive.

176. ~~**Carrier Engagement Drawer Mode 1 + atomic POST /api/loads/with-tender + Order Builder rewire.**~~ **CLOSED 2026-05-18 in v3.8.acj** (Sprint 59 atomic, Option α per directive ratification). Closes the Item 167 silent failure (Order Builder direct-tender path captured directTenderCarrierId into Load row but never fired createTender — see Item 167 closure below). **Shipped:** (a) `backend/src/services/autoRateConfirmationService.ts` tx-aware refactor — optional 4th param `prismaClient?: PrismaTxClient` defaulting to global prisma; existing acceptTender + acceptTenderOnBehalf callers unaffected (backward-compatible); (b) `backend/src/validators/withTender.ts` — new `createLoadWithTenderSchema` capturing Load + Tender + RC seed fields; (c) `backend/src/controllers/withTenderController.ts` — atomic handler with `prisma.$transaction` performing Load.create + LoadTender.create + autoGenerateRateConfirmation(tx) + Order.update if orderId provided; complianceCheck pre-flight outside tx (pure read); notifyTenderAction("OFFERED") + sendTrackingLinkToCrmContacts + logLoadActivity post-tx side effects (non-blocking try/catch per Sprint 38 Item 52 pattern); (d) `backend/src/routes/loads.ts` POST `/with-tender` mount, AE-only authorize; (e) `backend/src/routes/orders.ts` convert-to-load handler DELETED (332 LOC removed; A1 grep confirmed zero external callers); (f) `frontend/src/components/shared/CustomerPicker.tsx` extracted from Order Builder Section 1, ~135 LOC reusable component (auto-fill side effects stay in parent — Order Builder retains notes→specialInstructions cascade, drawer doesn't need it); (g) `frontend/src/components/drawer/CarrierEngagementDrawer.tsx` ~480 LOC unified drawer mirroring ProspectDrawer canonical (720px, role=dialog, ESC/click-out/popstate); Mode 1 (build-and-tender) shipped, Modes 2/3/4 stubbed with runtime throw guards referencing Sprint 60/61/62 per Risk 5 lock; 7 sections (Lane / Freight / Schedule / Refs / Carrier / Financials / Instructions), each ≤80 LOC per Gate 2; submit mutation calls POST /api/loads/with-tender with orderId for Order draft conversion; (h) `frontend/src/app/dashboard/orders/page.tsx` — Section 5 (Dispatch & tracking) DELETED (104 LOC), createLoad mutation DELETED (32 LOC), createResult success-screen DELETED (15 LOC), createLoad error block DELETED (17 LOC), DirectTenderPicker import REMOVED, "Create load" button REPLACED with "Tender to carrier →" opening the drawer with initialFormData seeded from current draft. **Sub-pattern banked fire #1: "write-path-without-corresponding-read-handler"** — frontend captures `directTenderCarrierId` into form state and sends to backend, backend persists to `Load.directTenderCarrierId` column, but no read-handler ever consumes it to fire `createTender`. Item 167 fires this exactly. Adjacent class to memory pattern #5 (data fields populated but never consumed by expected UI) but at the API-contract layer. Pairs with Items 168 (audit-wrong-code-path) + 169 (interceptor-without-context-awareness) + 174 (portal-boundary-defense-asymmetry) as four scope/context-discipline candidates banked across the Sprint 53→59 arc. Awaits fire #2 + #3 from independent incidents before §19 canonical promotion. **Deferred to follow-up sprints (Item 176 epic continues):** Sprint 60 Mode 2 (review existing load) + Load Board action menu for waterfall/DAT dispatch + drawer-exposed shipmentPriority + trackingLinkAutoSend controls; Sprint 61 Mode 3 (finalize after carrier accept) + Send RC PDF action; Sprint 62 Mode 4 (recovery) + cleanup (delete RC Modal 2,262 LOC + Item 170 carrier portal RC download). Sprint 59 LOC net: ~700 (within Decision G estimate). Pre-commit gates: backend tsc clean, frontend tsc clean, frontend next build clean per Sub-pattern 11 CI parity.

167. ~~**Order Builder direct-tender silent failure.**~~ **CLOSED STRUCTURALLY 2026-05-18 in v3.8.acj** (Sprint 59). Pre-Sprint-59 the Order Builder "Direct Tender" dispatch radio captured `directTenderCarrierId` via DirectTenderPicker into form state, the createLoad mutation POSTed to `/orders/:id/convert-to-load` which persisted `directTenderCarrierId` onto the Load row (line 472 of pre-Sprint-59 commit), but the backend handler had NO branch that called `createTender` — dispatch routing at lines 516-524 only handled `waterfall` + `full_auto`. Net: AE thought they'd tendered the load to a carrier; carrier received nothing; load sat at status POSTED with stale directTenderCarrierId metadata. **Sub-rule c authoritative-source verification (Sprint 59 Phase A):** quoted [orders/page.tsx:408-437 pre-Sprint-59] (createLoad mutation POSTs empty body to `/orders/:id/convert-to-load`) + [routes/orders.ts:516-524 pre-Sprint-59] (no `direct_tender` branch in dispatch routing) — confirming the silent failure mechanism. **Closure path: structural deletion, not patch.** Sprint 59 deleted the radios + DirectTenderPicker + createLoad mutation + convert-to-load backend handler entirely. The user can no longer select "direct_tender" without going through the Carrier Engagement Drawer which atomically creates Load + Tender + RC via `POST /api/loads/with-tender`. No silent-failure surface to maintain. See Item 176 above for full Sprint 59 details.

174. ~~**Shipper login role-gate gap — carrier creds work on /shipper/login.**~~ **CLOSED 2026-05-18 in v3.8.acf** (Sprint Item 174, Option β+γ+α defense-in-depth). Symptom: INTEGRITY EXPRESS (aliabbas6361@gmail.com, role=CARRIER) could complete /shipper/login → OTP → land on /shipper/dashboard authenticated, despite being a carrier account. Shipper API gates (`authorize("SHIPPER")`) held so no data leak, but portal-boundary integrity violated. **Three-layer gap audited:** (a) shipper login page reuses AE auth endpoints — [shipper/login/page.tsx:10,46](frontend/src/app/shipper/login/page.tsx#L10) imports `useAuthStore` calling `/api/auth/login` + `/api/auth/verify-otp` (the AE endpoints, not separate shipper paths); (b) `/api/auth/login` at [authController.ts:148-218](backend/src/controllers/authController.ts#L148) had no role gate — only password + lockout + isActive checks; (c) `/api/auth/verify-otp` had SHIPPER-specific `checkShipperApproval` but no refusal for non-SHIPPER roles — carrier flowed straight through to JWT issuance; (d) shipper dashboard layout at [shipper/dashboard/layout.tsx:48-61](frontend/src/app/shipper/dashboard/layout.tsx#L48) had no role check — only `if (!currentUser)`. **Fix shape — defense-in-depth at three layers (Option β+γ+α combined):** Layer γ-1 backend validator: optional `expectedRole: "AE" | "SHIPPER"` added to `loginSchema` ([validators/auth.ts](backend/src/validators/auth.ts)) + `otpSchema` ([routes/auth.ts:23](backend/src/routes/auth.ts#L23)). Layer γ-2 backend controller: new `AE_ROLES` set + `rolesMatch(userRole, expectedRole)` helper at [authController.ts:21-30](backend/src/controllers/authController.ts#L21); role check in `login` after isActive check + in `handleVerifyOtp` after user lookup. 401 + `code: "ROLE_MISMATCH"`, message "Invalid credentials for this portal." Layer γ-3 useAuthStore: `login` + `verifyOtp` signatures extended with optional `expectedRole`, body conditionally includes the field. Layer β shipper login: passes `"SHIPPER"` to both calls; post-OTP frontend role check (defense-in-depth in case backend gate fails) — on mismatch calls `/api/auth/logout` + `useAuthStore.getState().clearAuth()` + shows error. Layer γ-4 AE login: passes `"AE"` in both fetch bodies (page uses raw fetch, not useAuthStore). Layer α shipper dashboard layout: `bounceIfWrongRole` helper called from both fresh-load and cached-user branches — on mismatch calls `/api/auth/logout` + clearAuth + `router.replace("/shipper/login")`. **Carrier portal unaffected** — uses `/api/carrier-auth/*` which already gates strictly to CARRIER role per [carrierAuth.ts:70](backend/src/routes/carrierAuth.ts#L70) + [carrierAuth.ts:153](backend/src/routes/carrierAuth.ts#L153). **Methodology — sub-pattern fire #1 banked: "portal-boundary-defense-asymmetry":** *"When one portal (carrier) has rigorous role gating across login/OTP/layout but another (shipper) reuses shared AE auth endpoints without role gates, the asymmetry becomes a portal-boundary violation surface. Audit all three portals' login flows for symmetric defense at each layer."* Sub-pattern awaits fire #2 + #3 from independent incidents before §19 canonical promotion. Pairs with Item 168 (audit-wrong-code-path) and Item 169 (interceptor-without-context-awareness) — three consecutive scope/context-discipline sub-pattern candidates banked in the Sprint 53→54→174 arc.

168. ~~**Carrier OTP login kick-out (Sprint 54 Item 11 deferred).**~~ **CLOSED 2026-05-18 (Sprint Item 174 session). Resolved as downstream side-effect of Sprint 53.a + Sprint 54.** Original symptom reported May 17: test carrier login → OTP success → brief carrier dashboard → kick to /carrier/login. Phase A audit yesterday (May 17) traced authController.ts ADMIN TOTP path (AE flow) which was the wrong code path — banked "audit-wrong-code-path" sub-pattern fire #1. Today (May 18), pre-Phase-A re-audit on the ACTUAL carrier flow (carrierAuth.ts):  setTokenCookie passes `"CARRIER"` correctly at all 4 carrier-auth callsites (lines 205, 276, 351, 379); `/api/carrier-auth/*` matches CARRIER_PORTAL_MOUNTS allow-list exactly; resolveCookieToken returns srl_token_carrier correctly. Sprint 54 portal-aware 401 interceptor routes carrier-dashboard 401s to /carrier/login correctly. **Wasi's re-test on May 18 in fresh incognito confirmed no kick-out — carrier login works end-to-end.** The original symptom was dual-portal cookie contamination from yesterday's test session (AE cookie present in the same browser polluting carrier endpoint resolution); Sprint 53.a CARRIER_PORTAL_MOUNTS preferred-cookie selection resolves this when the request lands on a carrier-portal mount with srl_token_carrier present. Fresh-incognito sessions don't have AE cookie contamination, so the fix surfaces cleanly. **No source change for Item 168** — closure is a posture correction: this was a Sprint 53.a downstream resolution, not a separate Sprint-needing bug. Yesterday's "audit-wrong-code-path" sub-pattern fire #1 stands — banking remains valid even though the underlying bug self-resolved, because the methodology miss (auditing AE flow instead of carrier flow) was real regardless.

168-original. **Carrier OTP login kick-out (Sprint 54 Item 11 deferred).** Banked 2026-05-17 after Sprint 54 Phase A traced the wrong code path. Original symptom reported by Wasi during post-Sprint-53.a smoke: "OTP success → immediate logout." Phase A misread context and audited `backend/src/controllers/authController.ts` ADMIN TOTP flow (`handleVerifyOtp` + `handleTotpLoginVerify`) — the AE-portal code path. Wasi corrected: he was testing the **CARRIER** portal flow, not AE. Carrier auth lives at `backend/src/routes/carrierAuth.ts` with its own `handleVerifyOtp` and totp-verify handlers, separate `useCarrierAuth` Zustand store, separate `/carrier/login` UI. Sprint 54 dropped Item 11 from its atomic scope; re-audit scheduled for next session against the actual carrier code path. **Sprint shape for re-audit:** (1) read `backend/src/routes/carrierAuth.ts` OTP verify + TOTP verify handlers verbatim, focus on the setTokenCookie call site + which role string is passed; (2) read `frontend/src/hooks/useCarrierAuth.ts` post-verify state setter + redirect logic; (3) read `frontend/src/app/carrier/dashboard/layout.tsx` AuthGuard equivalent that fires loadUser → /api/carrier-auth/me; (4) reproduce in incognito on prod with DevTools Network preserve-log, capture the 401 response body + Cookie request header on the failing endpoint after carrier OTP success; (5) cross-reference against Sprint 53.a CARRIER_PORTAL_MOUNTS allow-list to confirm `/api/carrier-auth/me` baseUrl correctly matches `/api/carrier-auth` and reads srl_token_carrier cookie. **Methodology lesson (banked as Sub-rule c sub-pattern candidate fire #1 — "audit-wrong-code-path"):** *"When user reports symptom in flow X, Phase A must verify it's auditing flow X's actual code path before deep-diving."* The OTP login audit looked at admin TOTP verify because the AE flow was the default mental model; the user's report was carrier flow. Sub-pattern banks alongside Sprint 53 + 53.a fires #1 (cookie layer scope) + #2 (substring-vs-prefix within cookie layer); cumulative pattern: Phase A scope-discipline failures cluster around "what was the user actually testing?" rather than "where does the code think it lives?". Sub-pattern awaits fire #2 + #3 from independent incidents before §19 canonical promotion per sub-rule c three-fire convention.

184. ~~**Manual FMCSA scan repointed to canonical; bulk monitor module retired.**~~ **LOGGED + CLOSED 2026-05-22 in v3.8.ahw** (atomic). Closes the dead-path half of Item 183 — pre-Sprint, `POST /integrations/fmcsa/bulk-monitor` invoked the lighter `runDailyMonitor` snapshot-diff (no carrier-row refresh, no emails, alerts only). Now invokes the canonical `fmcsaComplianceScan` so manual trigger and the daily 3am cron run identical work. `fmcsaBulkMonitorService.ts` fully deleted (Phase A grep confirmed no external consumers of its 6 exports beyond the just-repointed route). Suspension gate at [`complianceMonitorService.ts:115`](backend/src/services/complianceMonitorService.ts#L115) verified to key off `onboardingStatus === "SUSPENDED"` not the reason columns — deletion safe. **Sub-rule c finding banked:** directive framing assumed deletion would leave `autoSuspendReason`/`autoSuspendedAt` as the only suspension convention; Phase A grep proved otherwise — 4 other writers of the older `suspensionReason`/`suspendedAt` pair exist in the canonical service itself (lines 1424 / 1553 / 1662 / 1702). The v3.8.ahv `TODO(item-183-followup)` therefore **scope-corrected, not closed** — see Item 187. **Methodology principle (banked sub-rule c fire candidate #1 — "grep before claiming an arbitrary value is the only one in use"):** when a directive asserts "X is the only thing in active use", verify by enumerating writers/readers across the affected surface, not by accepting the framing. Two more independent fires needed before sub-rule c canonical promotion per §19 three-fire convention. Pairs with Sprint 41 Pattern 7 grep ("`marginPercent.*toFixed`" returning 4 sites where 2 were documented).

188. ~~**Cron timezone for human-facing jobs + FMCSA scan run-summary + Run-FMCSA-Scan button.**~~ **FULLY CLOSED 2026-05-22 — backend half in v3.8.ahy + frontend half in v3.8.ahz** (atomic pair). Closes the human-facing slice of Item 185 (cron-timezone) and adds the observability surface needed for an AE-facing Run-FMCSA-Scan button on the CEO dashboard. **Backend changes:** (1) [`backend/src/cron/index.ts`](backend/src/cron/index.ts) — `{ timezone: "America/New_York" }` on 7 schedules that email / notify / generate digests / reminders / briefings; internal recompute / cleanup / validation jobs (including the FMCSA scan itself per directive) stay on host UTC. Container TZ NOT touched. Convention block added at top of file documenting which jobs use which. (2) [`backend/src/services/complianceMonitorService.ts`](backend/src/services/complianceMonitorService.ts) — `fmcsaComplianceScan` writes a single run-summary SystemLog row per run (logType=CRON_JOB, source="fmcsa-compliance-scan") with timestamps + counters (scanned / passed / failed / alerts / suspended / errors). New `suspended` counter on the results object incremented at the two suspend sites. Rationale for reusing CRON_JOB instead of introducing a new LogType enum value: closed Prisma enum; schema migration would have widened commit scope; source filter provides equivalent isolation. Documented inline as a follow-up if a future surface needs enum-level filtering. (3) [`backend/src/routes/externalIntegrations.ts`](backend/src/routes/externalIntegrations.ts) — `POST /integrations/fmcsa/bulk-monitor` made fire-and-forget (202 immediate return, scan runs in background; AE polls last-scan endpoint). New `GET /integrations/fmcsa/last-scan` endpoint reads the latest run-summary SystemLog row, returns timestamp + details for the CEO dashboard card. ADMIN/CEO/BROKER/OPERATIONS authz preserved. **Sub-pattern 6 sub-rule c gate:** node-cron 4.2.1 `cron.schedule(expr, fn, { timezone })` API verified empirically via `node -e` smoke against the installed module before edits — confirmed `length: 3` signature and options-bearing schedule constructs without error. **Sub-pattern 7 grep-regex-completeness check:** all 27 cron schedules classified by reading the file body (not just name-grep) so the human-facing-vs-internal partition is read-verified. **Followup banked Item 185.b:** if/when the CEO dashboard wants enum-level FMCSA-scan-row filtering independent of other CRON_JOB logs, add `FMCSA_SCAN_RUN` to the `LogType` Prisma enum + migration — small follow-up sprint. The current source-filter pattern serves the dashboard read endpoint without that schema churn. **Sub-rule c fire candidate #2 banked (extends candidate #1 from Item 184, "grep before claiming an arbitrary value is the only one in use"):** initial code wrote `logType: "FMCSA_SCAN_RUN"` per the directive's literal wording; tsc compile-error surfaced `LogType` was a closed enum. Authoritative source check (read the schema.prisma enum body) revealed the actual enum surface. Resolution: pivoted to source-filter pattern within the commit, surfaced choice inline. Two more independent fires of "literal-vs-intent verification" needed for §19 canonical promotion.

183. ~~**FMCSA re-monitoring consolidated to single daily canonical scan.**~~ **LOGGED + CLOSED 2026-05-22 in v3.8.ahv** (atomic). Pre-Sprint state had two overlapping FMCSA re-monitor paths: `complianceMonitorService.weeklyFmcsaScan` (scheduled Mon 3am via `cron/index.ts:313` weekly cron — canonical refresh + ComplianceScan history + emails + auto-suspend on revoke/insurance/OOS) and `fmcsaBulkMonitorService.runDailyMonitor` (manual `POST /integrations/fmcsa/bulk-monitor`, despite the "Daily" name **never wired to any scheduler** — confirmed via grep). Read-only audit (separate report) established both functions hit identical FMCSA QCMobile `/carriers/{dot}` endpoint over APPROVED-with-DOT population; neither touched BASICs/SMS or the dedicated OOS endpoint. **Shape:** (a) cron schedule `"0 3 * * 1"` → `"0 3 * * *"` (weekly Mon → daily); (b) canonical scan renamed `weeklyFmcsaScan → fmcsaComplianceScan` (cadence-neutral so we don't re-create the `runDailyMonitor` misnomer); (c) change-detection in the canonical scan extended — existing 4-field critical/warning diff (`operatingStatus`/`safetyRating`/`insuranceOnFile`/`outOfServiceDate`) preserved; new 3-field INFO diff (`verified`/`totalDrivers`/`totalPowerUnits`) emits `ComplianceAlert` of severity INFO with 30-day expiry per detected change — these **do NOT auto-suspend** (only existing CRITICAL flips suspend); (d) all 4 `[WeeklyFMCSA]` log-prefix occurrences renamed to `[FMCSAScan]`; (e) `fmcsaBulkMonitorService.ts` top-of-file comment reframed as "On-demand re-scan, NOT SCHEDULED — scheduled FMCSA path is now `fmcsaComplianceScan` daily 3am" to prevent future confusion; the function name `runDailyMonitor` itself NOT renamed per directive (would have sprawled into the route + admin-controller surfaces; comment is sufficient); (f) `POST /integrations/fmcsa/bulk-monitor` endpoint and route gate (ADMIN/CEO/BROKER/OPERATIONS) preserved unchanged. **TODO logged inline:** `runDailyMonitor` writes `suspensionReason` + `suspendedAt`; canonical scan writes `autoSuspendReason` + `autoSuspendedAt`. Convention-unification deferred to a later cleanup commit (item-183-followup TODO comment in `complianceMonitorService.ts:808`). **Sprawl bound:** rename touched 2 files, 5 hits (Phase A grep). **FMCSA-courtesy pacing:** scan iterates APPROVED-with-DOT serially without per-call delay; `verifyCarrierWithFMCSA` has 6s timeout per call + 1h in-memory cache (`fmcsaService.ts:15`). At pre-revenue carrier count (<50 today) the 3am cold-cache scan runs <2min; even at N=200, ~5-7min — well within QCMobile's generous rate budget. If carrier count grows past ~500 the bulk monitor's batchSize=5 + 1s inter-batch delay can be lifted into the canonical path (snapshotAllCarriers @ fmcsaBulkMonitorService.ts:50-79 is the reference). **Out of scope:** BASICs/SMS endpoints + dedicated OOS endpoint not touched per directive (separate work). Auth-routes/admin-cron-registry display of "weekly" job descriptions not re-scoped — cron job key `fmcsa-compliance` unchanged so any admin/monitoring UI keys on that ID continue to match. **Pre-commit gates:** backend `tsc --noEmit` clean. Per §3.1: v3.8.ahu → v3.8.ahv.

189. ~~**Build 2 — `isTestAccount` fence extended to Tier 2 analytics surfaces.**~~ **CLOSED 2026-05-30 in v3.8.alm** (bundled with Item 190 as one atomic — see the combined close note at the END of Item 190 below). **Pattern-7 undercount finding:** the banked 189 + 190 lists named ~7 sites; a fresh `grep carrierProfile.(findMany|count)` across all of `backend/src/` surfaced **37 fence additions across 14 files**. Original banking-row context retained below. Build 1 fenced the Tier 1 load-assignment surfaces ([smartMatchService.ts:42](backend/src/services/smartMatchService.ts#L42), [carrierController.getAllCarriers](backend/src/controllers/carrierController.ts#L780), [routes/carrier.ts:133](backend/src/routes/carrier.ts#L133) /capacity-feed) — test carriers can no longer be picked for real loads. Tier 2 surfaces still aggregate test carriers into analytics/leaderboard/intelligence outputs, polluting metrics + AE-visible reports. Build 2 should extend the `isTestAccount: false` filter to these sites without affecting the Tier 1 surfaces already shipped: (a) [`analyticsService.ts:166`](backend/src/services/analyticsService.ts#L166) `getCarrierScorecard` load aggregation — add `carrier: { carrierProfile: { isTestAccount: false } }` at the load JOIN level. (b) [`analyticsService.ts:288`](backend/src/services/analyticsService.ts#L288) second scoreboard variant (`cppTier`/`cppTotalLoads`) — same pattern. (c) [`routes/cpp.ts:113-114`](backend/src/routes/cpp.ts#L113) `GET /cpp/leaderboard` (AE-visible top-carriers list) — add `isTestAccount: false` to the where-clause. (d) [`routes/cpp.ts:73-74`](backend/src/routes/cpp.ts#L73) `/cpp/recalculate` canonical milestone-gate path (v3.8.aii) — add filter so milestone-recalc doesn't churn on test rows. (e) [`marketController.ts:153`](backend/src/controllers/marketController.ts#L153) market intelligence carrier findMany — add filter. (f) [`carrierIntelligenceService.ts:91-92`](backend/src/services/carrierIntelligenceService.ts#L91) AI carrier-intelligence aggregator — add filter. (g) [`carrierOutreachService.ts:155,174`](backend/src/services/carrierOutreachService.ts#L155) `smartCarrierProfiles` + `matchingCarriers` for proactive outreach — add filter so test carriers don't receive outreach emails. **Sprint shape:** single atomic commit, ~14 LOC across 6 files. No schema migration. Pairs with Build 3 as the Tier 2 + Tier 3 split. Same boundary discipline as Build 1: only addition is `isTestAccount: false`. Pattern 7 grep-completeness check: re-grep `prisma\.carrierProfile|prisma\.load.*carrier` to verify no Tier 2 site was missed.

189.b. ~~**Admin self-serve toggle for `CarrierProfile.isTestAccount`.**~~ **CLOSED 2026-05-30 in v3.8.alo.** Surfaced when Wasi asked "how can I use the test-carrier mechanism right now" — the fence (v3.8.aim/alm) was complete but the only WRITE paths for `isTestAccount` were the seed script (dev/CI only) + the v3.8.aim migration (3 specific prod carriers by MC#). No in-app control to flag a NEW carrier as test (or un-flag one) — required a manual SQL `UPDATE`. **Design wrinkle surfaced in Phase A:** `getAllCarriers` (the carriers-page list) itself filters `isTestAccount: false` AND feeds the Tender/RC pickers — so a flagged carrier vanishes from the admin list too, making un-flag a one-way door. **Fix (v3.8.alo):** (1) backend `getAllCarriers` gains opt-in `?include_test=true` (admin carriers page passes it ONLY when its "Show test accounts" toggle is on; pickers never pass it, so the v3.8.aim picker-fence contract holds). `where` rebuilt: `isTestAccount: false` applies unless `include_test` is set. (2) new `PATCH /api/carriers/:id/test-account` (ADMIN/CEO, `validateBody({ isTestAccount: boolean })`, `auditLog("UPDATE","Carrier")`, 404 on unknown). (3) frontend `/dashboard/carriers`: `Carrier.isTestAccount` added; list query gains `?include_test=true` + `showTestAccounts` in queryKey; admin-only "Show test accounts" header toggle (amber when on); "Mark as test" / "Test account" toggle button in the carrier detail action row (FlaskConical icon, confirm dialog, TanStack mutation invalidating `["carrier-all"]`); amber TEST badge on flagged carrier rows. Mirrors the v3.8.ali per-load risk-email kill-switch pattern. **No schema migration** (`isTestAccount` exists since v3.8.aim). **Pre-commit gates per Sub-pattern 11:** backend tsc + vitest 224/224 + frontend tsc + next build all clean. **Usage:** AE/admin opens a carrier → "Mark as test" → carrier drops out of pickers/analytics/compliance/risk; to un-flag, toggle "Show test accounts" header on → flagged carriers reappear with TEST badge → open one → "Test account" (amber) → restore. The 3 pre-flagged prod carriers (SRL Transport MC-1794414, BISON MC-156588, INTEGRITY EXPRESS MC-596655) are now visible+manageable via the show-test toggle.

191. ~~**Prisma + Neon `DIRECT_URL` split — permanent fix for the P1002 advisory-lock contention class.**~~ **CLOSED 2026-05-24 in v3.8.ajg + v3.8.ajs** (atomic ship + defensive guard). Backlog-row was originally banked 2026-05-24 alongside the v3.8.ajf P1002 incident; the FIX shipped same day per §11 history row [CLAUDE.md:664](CLAUDE.md#L664) but this §13.3 row was never marked closed — banked-row-text-drift caught when Wasi cross-referenced 2026-05-25. **Fix shape:** v3.8.ajg added `directUrl = env("DIRECT_URL")` to `backend/prisma/schema.prisma` datasource block, updated CLAUDE.md §2.2 with new env var list (`DATABASE_URL` + `DIRECT_URL` + `PRISMA_MIGRATE_LOCK_TIMEOUT=60000`), Render dashboard env vars set by Wasi pre-deploy. v3.8.ajs added defensive `backend/scripts/check-direct-url.js` pre-migrate build guard that fails fast (exit 1 with self-explanatory remediation message) if `DIRECT_URL` is unset OR its hostname contains `-pooler` (catches the env-config mismatch that hit v3.8.ajr's deploy — `DIRECT_URL` was set but pointed at the pooled URL by mistake, so migrate routed through pooler → P1002). Pre-ajs the failure mode was a silent 10s P1002 timeout with no hint about WHY; post-ajs the build fails in <1s with exact remediation steps. **Methodology observation banked (Sub-pattern 6 sub-rule c case-study extension #2 — "backlog-row-drift vs §11-history-row"):** initial CLAUDE.md grep for "Item 191" status returned the §13.3 row with "Priority: ELEVATED" text intact + no strikethrough — appeared OPEN. Cross-referencing §11 history rows for the same item ID surfaced "closes §13.3 Item 191" in v3.8.ajg's row. Same shape as the v3.8.akr "match-count is not consumer-count" case study: a single source (§13.3 row text) appeared authoritative but was stale relative to the actual canonical source (§11 history rows are the source of truth for closure status; §13.3 backlog rows can drift if not maintained in lockstep). **Going-forward gate:** when summarizing §13.3 OPEN items, cross-reference every candidate against `grep "closes.*Item N\|§13\.3 Item N.*CLOSED"` in §11 before declaring open. Awaits two more independent fires of "backlog-row-text-drift" before §19 canonical promotion per sub-rule c three-fire convention. **Root-cause characterization (now three-fire-validated):** Prisma's `migrate deploy` acquires a Postgres advisory lock (hardcoded `72707369` in Prisma source) before applying migrations. Lock is session-scoped — released only when the connection holding it actually closes. Today's `DATABASE_URL` env var on Render points at Neon's POOLED endpoint (hostname contains `-pooler`), which intentionally keeps connections alive between requests and rotates PIDs across the pool. Net effect: migrate process exits normally, but its lock-holder connection stays cached in Neon's pooler. Lock doesn't release until Neon's idle timer expires that specific cached PID (~5min on Launch tier). Every subsequent `migrate deploy` plays roulette against whichever pooler-cached PID currently holds the lock. P1002 frequency correlates with migration-bearing deploy frequency. **Permanent fix — canonical Prisma + Neon pattern documented by both vendors:** split runtime + migrate connection URLs. (a) `backend/prisma/schema.prisma` `datasource db { ... }` block adds `directUrl = env("DIRECT_URL")` line alongside existing `url = env("DATABASE_URL")`. (b) Render dashboard env var added: `DIRECT_URL` = Neon's direct endpoint connection string (no `-pooler` in hostname; obtained from Neon dashboard → Connection Details → "Direct connection" tab). (c) Local `backend/.env` adds the same `DIRECT_URL` for `prisma migrate dev` (when used in feature branches) + diagnostic `prisma migrate status` runs. (d) `DATABASE_URL` UNCHANGED — runtime continues to benefit from pooler's connection-limit management. Prisma automatically uses `directUrl` for ALL migrate operations (`migrate deploy`, `migrate dev`, `migrate status`, `migrate resolve`, `db push`) when present in the datasource block. Direct connection releases the advisory lock the moment migrate exits — no pooler caching layer between Prisma and Postgres. **Belt-and-suspenders secondary hardening (same sprint, ~5 min add):** `PRISMA_MIGRATE_LOCK_TIMEOUT=60000` env var on Render bumps Prisma's lock-acquisition timeout from 10s default to 60s. Reduces residual P1002 frequency on the direct URL itself (e.g. if Render somehow runs two concurrent deploys, which would normally serialize). **Side benefits beyond P1002 elimination:** (i) `prisma migrate status` becomes deterministic — currently it also runs through the pooler and can give stale answers about applied-vs-pending migrations; (ii) the Sprint 44b `prisma migrate status --exit-code` post-deploy verification gate becomes reliable instead of probabilistic; (iii) future destructive migrations (enum-swaps like v3.8.ajd, column-drops, table-renames) become safer because the lock is held by a connection that genuinely owns the migration. **Risks considered + rejected:** (a) `PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1` env var — disables the lock entirely, allowing two concurrent migrate processes to apply destructive migrations simultaneously; an enum-swap class migration could corrupt under concurrent application; not recommended. (b) Pre-deploy SQL hook running `pg_advisory_unlock(72707369)` — race condition with legitimate concurrent migrations; would also fail if the lock-holder is a different PID than the unlock caller; not recommended. (c) Switching off Neon to RDS / Supabase / another managed Postgres — over-engineering for this specific class; the same pooler vs direct distinction exists on most managed Postgres providers and is solved the same way. **Sprint shape:** ~10 LOC schema change + 2 env var additions (Render dashboard + local `.env`) + 1 CLAUDE.md §2.2 documentation update noting the new env vars + their roles. Effort: 30-60 min, mostly Neon dashboard navigation + Render env editing. No migration. No code logic change. **Pre-commit verification:** `npx prisma migrate status` against direct URL should report `Database schema is up to date` before push. Post-deploy: next migration-bearing deploy should complete in <5s without P1002 even if a prior deploy was recent. **Priority:** ELEVATED — every future schema migration is at risk until this lands. Pairs naturally with §13.3 Item 8.10's deploy-chain hardening lineage (Sprint 44a → 44b → 44c → 44d) as the natural next architectural-hardening sprint. **Banked methodology observation:** Sprint 44b's baseline-reset + canonical buildCommand work hardened the migration history + verification gate but did NOT address the connection-URL split. The class of bug was latent through the entire Sprint 44 → ajf arc; three independent fires in <2 weeks validated the recurrence rate as material enough to justify the architectural fix vs continuing to absorb the manual-retry cost. **v3.8.alf follow-up note (2026-05-26):** ledger reconciliation sprint for the v3.8.ale citext migration surfaced two related docs-completeness gaps in this item's canonical work: (1) local `backend/.env` was missing `DIRECT_URL` despite §2.2 calling for it as canonical local-dev configuration — v3.8.alf added it via [`backend/scripts/add-direct-url-to-env.ts`](backend/scripts/add-direct-url-to-env.ts) which derives the direct URL from the existing pooled DATABASE_URL by stripping `-pooler`. (2) [render.yaml](render.yaml) `envVars` block listed `DATABASE_URL` only — not `DIRECT_URL` or `PRISMA_MIGRATE_LOCK_TIMEOUT`. Per the file's own header note render.yaml is documentation-only (Render dashboard is the live config), and per the `check-direct-url.js` guard in the buildCommand at render.yaml:29 + the fact that deploys have been succeeding since v3.8.ajg/ajr/ajs, the Render dashboard IS set correctly. render.yaml just lagged. v3.8.alf added both env vars to the envVars docs block with explanatory inline comments. Lesson banking — when a schema datasource changes env-var requirements (the ajg `directUrl = env("DIRECT_URL")` shift), the four-location checklist from sub-pattern 11 case study #2 should explicitly include `render.yaml` envVars docs as a 5th location alongside (1) local .env (2) Render dashboard (3) CI workflow env blocks (4) CLAUDE.md §2.2. Future schema env() additions catch this gap via the extended checklist.

192. ~~**Risk-flagging cron re-enablement with preference gate + threshold tuning + test-load exclusion.**~~ **FULLY CLOSED 2026-05-30 across v3.8.ali (mechanism + kill switch) + v3.8.alj (test-load exclusion + per-user preference + cron re-enable).** The cron is LIVE again as of v3.8.alj. **v3.8.alj final-close shipped:** (1) **Test-load exclusion** — new `Load.isTestAccount Boolean @default(false)` (migration `20260530140000_add_load_is_test_account`, additive, no backfill). Seed script + all 3 E2E fixture loads set `isTestAccount: true`. The risk engine's `runRiskFlagging` findMany now filters `isTestAccount: false`. NO prod backfill of the column — the seed users overlap with real identities (`whaider@silkroutelogistics.ai` is both the seed broker AND the real founder), so a poster-based backfill would wrongly mark real loads; instead the existing stale prod seed loads are caught by the staleness guard below. (2) **Staleness guard** — `runRiskFlagging` findMany also filters `pickupDate: { gte: now - 14 days }`. A load whose pickup was 2+ weeks ago and is still in a pre-delivery active status is dead data (data-hygiene issue), not a fresh dispatch risk. This cleanly excludes the existing prod flood loads (SRL-20260211/03/16-dated seed loads, Feb 2026 pickups, 3+ months past today) WITHOUT identifying them individually — no risky prod backfill. Future-pickup unassigned loads still score normally (guard only excludes PAST pickups). (3) **Per-user preference gate** — `preferencesSchema` (routes/auth.ts:48 notifications object) gains `riskAlerts: z.boolean().optional()`; Settings panel (dashboard/settings/page.tsx) gains a "Load risk alerts (email)" toggle as the 5th notification preference. `runRiskFlagging` reads `poster.preferences.notifications.riskAlerts` at the email step and skips the email when explicitly `false` (default-on: undefined/true → send). The in-app notification is NOT gated — preference governs the external email only, same as the per-load kill switch. (4) **Cron re-enabled** — `schedulerService.ts:341-353` uncommented; runs every 30 min at :05/:35. **The four layered guards that make re-enable safe:** (a) once-per-load-per-level cadence (v3.8.ali — no hourly re-fire), (b) test-load exclusion + staleness guard (v3.8.alj — dead seed data never scored), (c) per-load email kill switch `riskEmailMuted` (v3.8.ali — AE silences a specific load), (d) per-user preference gate (v3.8.alj — AE opts out entirely). AMBER stays in-app only; only RED reaches email. **Pre-commit gates per Sub-pattern 11:** prisma generate + backend tsc + backend vitest 224/224 + frontend tsc + next build all clean. **Scope v3.8.alj:** ~70 LOC across 6 files (schema + migration + seed 4 loads + riskEngine findMany/email-gate + preferencesSchema + Settings UI + cron uncomment). **Banked follow-up (NOT blocking, out of Item 192 scope):** escalation ladder (RED in-app at hour 0 / email at hour 4 / phone-call-task at hour 8) was considered + dropped per the 2026-05-30 ratification (over-engineered for pre-revenue volume); the once-per-level cadence + 4 guards are sufficient. Pairs with §13.3 Item 86 (broader NotificationType preference retrofit) — Item 192 established the `notifications.<typeKey>` preference-gate pattern that Item 86 generalizes across all 19 NotificationType values. **Original banking-row context retained below.** **PRIOR PARTIAL CLOSE 2026-05-30 in v3.8.ali** — the working mechanism + per-load kill switch shipped first; v3.8.alj closed the remaining test-load exclusion + per-user preference + cron re-enable. **v3.8.ali shipped (mechanism redesign per Wasi design-first directive 2026-05-30, all 4 forks ratified to recommended options):** (1) **Per-load risk-email kill switch** — new `Load.riskEmailMuted Boolean @default(false)` + `riskEmailMutedAt DateTime?` + `riskEmailMutedById String?` (manual migration `20260530120000_add_load_risk_email_mute` per §2.2). **Email-only mute** (ratified scope): when muted the engine still writes `RiskLog` + still fires the in-app RED/AMBER notification (portal badge + teammates' view unaffected) — only the external `sendRiskAlertEmail` is skipped. **Permanent toggle** (ratified persistence): cleared by the AE or rendered moot when the load hits a terminal status. AE toggles via new `PATCH /api/loads/:id/risk-email-mute` (AE-role-gated, `validateBody({ muted: boolean })`, audit-logged) surfaced as a Bell/BellOff button in the T&T `LoadDetailDrawer` header next to the status-advance button. (2) **Once-per-load-per-level cadence** (ratified — the actual flood-killer): `runRiskFlagging` now reads the PRIOR `RiskLog` level before writing the new one and alerts ONLY on a level CROSSING (`risk.level !== prevLevel`). The old 30-min-window dedup re-fired hourly on a persistently-RED load (cron every 30 min, dedup window 30 min → notification ages out → re-fires) which produced the 2026-05-25 flood. A load that sits at RED now never re-emails; it fires once on the crossing into RED and stays silent until the level actually changes. `RiskLog` still writes every tick (dashboard history intact); `notified` flag now reflects whether THIS tick alerted (level-crossing into non-GREEN), not merely non-GREEN. (3) **AMBER stays in-app only** (ratified) — unchanged from prior behavior; the external channel is reserved for RED urgency. **Pre-disable state (for context):** `runRiskFlagging` ([riskEngine.ts:96](backend/src/services/riskEngine.ts#L96)) ran every 30 min at `:05`/`:35`, scored each active load via 5 factors, and on RED fired both a Notification + email to `posterId`. **Why disabled:** Wasi inbox flooded with 4× "RISK RED" emails 2026-05-25 against test/seed loads (SRL-121485/86/87 + L9180992591) scoring 50-80 from stale unassigned-4hr + missed-check-call demo data. **Remaining for re-enable (cron STILL DISABLED at [schedulerService.ts:341-349](backend/src/services/schedulerService.ts#L341)):** (a) **test-load exclusion** — the flood loads were UNASSIGNED (no carrier to read `carrierProfile.isTestAccount` from), and there's no `Customer.isTestAccount` nor `Load`-level test flag today; needs a Load-level or Customer-level test-marking strategy + backfill of the 4 known stale loads before re-enable, else first cron tick fires one email per stale test load (one-time burst, not a flood, but still noise about fake loads); (b) **per-user preference gate** — extend `preferencesSchema` at [routes/auth.ts:44-49](backend/src/routes/auth.ts#L44) with `riskAlerts: z.boolean().optional()` + Settings toggle + gate the email on `poster.preferences?.notifications?.riskAlerts !== false` (pairs with §13.3 Item 86 broader NotificationType retrofit); (c) **the cron flip itself** — uncomment [schedulerService.ts:346-349](backend/src/services/schedulerService.ts#L346). Re-enable is a deliberate go-live decision (§3.4 halt > ship on outward-facing email), NOT a side effect — recommend bundling (a)+(b)+(c) as the final v3.8.al-something re-enable sprint once a test-load-marking strategy is decided. **Original banking-row continues below for the full pre-v3.8.ali context:** Pre-disable state: `runRiskFlagging` ([riskEngine.ts:96](backend/src/services/riskEngine.ts#L96)) ran every 30 min at `:05`/`:35`, iterated all loads in active statuses (POSTED → AT_DELIVERY), scored each via 5 factors (unassigned-time / missed-check-calls / pickup-approaching / low-carrier-score / low-margin), and on RED (score ≥ 41) fired BOTH a `Notification` row at [riskEngine.ts:155-163](backend/src/services/riskEngine.ts#L155) AND an email via `sendRiskAlertEmail` at [riskEngine.ts:169-170](backend/src/services/riskEngine.ts#L169) to the load's `posterId`. **Why disabled:** Wasi inbox flooded with 4× "RISK RED" emails at 4:35 PM on 2026-05-25 against test/seed loads (SRL-121485/86/87 + L9180992591), all scoring 50-80 from stale "unassigned 4+ hr" + missed check calls on demo data. Three independent design gaps surfaced: (a) **no preference gate** — the Settings page at [dashboard/settings/page.tsx:94](frontend/src/app/dashboard/settings/page.tsx#L94) has a "Notification Preferences" panel with `loads/payments/scorecard/announcements` toggles that POSTs to `PATCH /auth/preferences`, but `runRiskFlagging` doesn't read `user.preferences.notifications` anywhere before sending; same architectural class as §13.3 Item 86 (broader NotificationType retrofit); (b) **no test-load exclusion** — `Load.findMany({ where: { status: in: activeStatuses } })` at [riskEngine.ts:98-101](backend/src/services/riskEngine.ts#L98) doesn't filter test loads (no `Load.isTestAccount` field yet; would need either a new column or join through `customer.isTestAccount` if shipper is test-flagged, OR through `carrier.carrierProfile.isTestAccount` via v3.8.aim Item 8.5-Build-1); (c) **30-min dedup window too tight** — same RED load re-emails every 30-min cron tick if score stays RED [RESOLVED in v3.8.ali via once-per-level cadence]. **Pairs with §13.3 Item 86** (broader NotificationType email-fan-out preference retrofit) — Item 192 is the specific RISK_ALERT slice; Item 86 is the full sweep across all 19 NotificationType enum values. **Priority:** medium — annoyance not BKN-blocking. Cron is disabled today; nothing breaks while disabled (in-app risk surfacing at `/dashboard/tracking` reads `RiskLog` table which has historical rows already; new rows just stop accruing while disabled). **Risk of leaving disabled long-term:** loss of operational signal — if a real customer's load actually sits unassigned 4+ hr post-BKN, AE won't get the RED ping. Trade-off: noise floor vs miss-rate. Re-enable BEFORE first paid load. Pre-disable state: `runRiskFlagging` ([riskEngine.ts:96](backend/src/services/riskEngine.ts#L96)) ran every 30 min at `:05`/`:35`, iterated all loads in active statuses (POSTED → AT_DELIVERY), scored each via 5 factors (unassigned-time / missed-check-calls / pickup-approaching / low-carrier-score / low-margin), and on RED (score ≥ 41) fired BOTH a `Notification` row at [riskEngine.ts:155-163](backend/src/services/riskEngine.ts#L155) AND an email via `sendRiskAlertEmail` at [riskEngine.ts:169-170](backend/src/services/riskEngine.ts#L169) to the load's `posterId`. **Why disabled:** Wasi inbox flooded with 4× "RISK RED" emails at 4:35 PM on 2026-05-25 against test/seed loads (SRL-121485/86/87 + L9180992591), all scoring 50-80 from stale "unassigned 4+ hr" + missed check calls on demo data. Three independent design gaps surfaced: (a) **no preference gate** — the Settings page at [dashboard/settings/page.tsx:94](frontend/src/app/dashboard/settings/page.tsx#L94) has a "Notification Preferences" panel with `loads/payments/scorecard/announcements` toggles that POSTs to `PATCH /auth/preferences`, but `runRiskFlagging` doesn't read `user.preferences.notifications` anywhere before sending; same architectural class as §13.3 Item 86 (broader NotificationType retrofit); (b) **no test-load exclusion** — `Load.findMany({ where: { status: in: activeStatuses } })` at [riskEngine.ts:98-101](backend/src/services/riskEngine.ts#L98) doesn't filter test loads (no `Load.isTestAccount` field yet; would need either a new column or join through `customer.isTestAccount` if shipper is test-flagged, OR through `carrier.carrierProfile.isTestAccount` via v3.8.aim Item 8.5-Build-1); (c) **30-min dedup window too tight** — same RED load re-emails every 30-min cron tick if score stays RED; should be 4+ hr or escalation-stepped (RED at hour 0, snooze 4h, escalate to phone-call at hour 4 if still RED). **Sprint shape:** (1) extend `preferencesSchema` at [routes/auth.ts:44-49](backend/src/routes/auth.ts#L44-L49) with `riskAlerts: z.boolean().optional()` (default true); (2) wire the toggle into Settings panel `notifications` state at [dashboard/settings/page.tsx:94](frontend/src/app/dashboard/settings/page.tsx#L94) as a 5th preference key; (3) gate the `sendRiskAlertEmail` call at [riskEngine.ts:168-172](backend/src/services/riskEngine.ts#L168) on `poster.preferences?.notifications?.riskAlerts !== false`; (4) extend the `Load.findMany` filter at [riskEngine.ts:98-101](backend/src/services/riskEngine.ts#L98) to exclude loads where customer or carrier is test-flagged (decision: add `Load.isTestAccount` column OR cascade-detect via FK joins — bias toward Load-level column for query simplicity, ~1 migration); (5) widen dedup window from 30 min → 4 hr at [riskEngine.ts:151](backend/src/services/riskEngine.ts#L151); (6) consider escalation ladder (RED → in-app only at hour 0, RED + email at hour 4 if still RED, RED + phone-call at hour 8 — opt-in). (7) re-enable the cron at [schedulerService.ts:346-349](backend/src/services/schedulerService.ts#L346) by uncommenting the block. ~80-120 LOC + 1 migration if (4) adds a column. **Pairs with §13.3 Item 86** (broader NotificationType email-fan-out preference retrofit) — Item 192 is the specific RISK_ALERT slice; Item 86 is the full sweep across all 19 NotificationType enum values. Could ship Item 192 standalone OR fold into a broader Item 86 sprint that retrofits all email senders to read `notifications.<typeKey>` preference. **Priority:** medium — annoyance not BKN-blocking. Cron is disabled today; nothing breaks while disabled (in-app risk surfacing at `/dashboard/tracking` reads `RiskLog` table which has historical rows already; new rows just stop accruing while disabled). **Risk of leaving disabled long-term:** loss of operational signal — if a real customer's load actually sits unassigned 4+ hr post-BKN, AE won't get the RED ping. Trade-off: noise floor vs miss-rate. Re-enable BEFORE first paid load.

190. ~~**Build 3 — `isTestAccount` fence extended to Tier 3 compliance surfaces.**~~ **CLOSED 2026-05-30 in v3.8.alm** (bundled with Item 189). Original banking-row context retained at end. **COMBINED v3.8.alm CLOSE NOTE (Items 189 + 190):** one atomic commit fencing test carriers out of every analytics/compliance/intelligence carrier-list query. **37 `isTestAccount: false` additions across 14 files.** **Pattern-7 grep-completeness fire:** banked 189+190 named ~7 sites; the sub-agent Phase A audit's own "completeness check" claimed all ~18 accounted-for + no extras — that was WRONG. A fresh `grep -rn carrierProfile.(findMany|count|aggregate)` across `backend/src/` surfaced an entire missed tier: csaBasicService, eldValidationService, ofacScreeningService, insuranceVerificationService, integrationService (CPP recalc), contentEngineService (marketing stat), + 2 cron sweeps (pending-identity-validation, rejected-reapply-reminder) + marketController region-heatmap count. Same class as Sprint 41's marginPercent grep (documented 2, actual 4). The exhaustive grep is the load-bearing methodology outcome — the banked subset would have left test carriers polluting CSA/ELD/OFAC/insurance compliance scans + the FMCSA daily 3am scan. **Sites fenced (Tier 2 — analytics/intelligence/marketing):** analyticsService `getOnTimeMetrics` + `getCarrierScorecard` (nested `carrier: { carrierProfile: { isTestAccount: false } }` at the load JOIN level, since these are Load-keyed aggregations), `cpp.ts` leaderboard + recalculate, marketController `getCapacity` + region-heatmap count, carrierIntelligenceService learning cycle, carrierOutreachService `matchingCarriers` fallback, integrationService `processAllCPPRecalculations`, contentEngineService active-carrier marketing-stat count. **Sites fenced (Tier 3 — compliance/screening):** all 15 carrierProfile list/count sites in complianceMonitorService (4 dashboard counts: total/fullyCompliant/expiringSoon/nonCompliant; expiringSoonItems; overview matrix; `fmcsaComplianceScan` daily 3am — highest-priority; dailyComplianceReminders; checkAutoReversal SUSPENDED; expiredCarriers + expiredNoGrace auto-suspend; expiringCarriers warning loop; monthlyCarrierReVetting; detectFmcsaAuthorityChanges; doc-expiry warning loop), complianceController admin scan, complianceForecastService AI forecast, csaBasicService CSA-BASIC scan, eldValidationService ELD sweep, ofacScreeningService weekly rescan, insuranceVerificationService expiry sweep, cron pending-identity-validation + rejected-reapply-reminder. **chameleonDetectionService — both sides per the Item 190 directive:** subject side (`runFullChameleonScan` bulk findMany, `isTestAccount: false`) + match side (`checkChameleon` fingerprint cross-ref gains `{ carrier: { isTestAccount: false } }` in its AND) so a real carrier is never flagged as a chameleon for overlapping with a test carrier's fingerprint, and test accounts aren't matched against each other. **Skips (correct as-is, NOT fenced):** carrierOutreachService `smartCarrierProfiles:155` (by-userId-set follow-up of already-matched carriers, not a list), overbookingService:149 (by-userId-set follow-up), marcoPoloService:995 (AE interactive carrier search — test carriers legitimately appear for admin review), carrier.ts capacity-feed + carrierController getAllCarriers (already fenced Build 1). **Trivial follow-up banked (NOT fenced):** monitoringController:66 + healthDigestService:84 gross `carrierProfile.count()` — these are infra/health DB-row-count metrics ("how many carrier rows exist"), not compliance/analytics decisions; fence only if admin-digest accuracy ever matters (~2 LOC). **Helper decision:** Item 190 floated a `whereActiveProductionCarrier()` helper "IF Build 2+3 reveal sufficient drift risk." 37 sites would justify it, but the where-clauses are heterogeneous (varying `status`/`onboardingStatus`: APPROVED vs SUSPENDED vs PENDING/REVIEWING vs REJECTED, plus different date/doc filters) so a uniform helper could only carry the single `isTestAccount: false` key — marginal value over inline, and inline stays greppable for the next completeness audit. Went inline per Build 1's "only addition is isTestAccount: false" boundary discipline. **No schema migration** — both `Load.isTestAccount` (v3.8.alj) + `CarrierProfile.isTestAccount` (v3.8.aim) already exist. **Pre-commit gates per Sub-pattern 11:** backend tsc + vitest 224/224 + frontend tsc + next build all clean. **Methodology emerged:** sub-agent-audit-undercount — a delegated Phase A audit's self-asserted "completeness check, no extras" is NOT a substitute for the orchestrator running the canonical grep itself. The orchestrator's broad `grep carrierProfile.(findMany|count)` found 2x the sites the sub-agent's narrower check reported. Banked as a Pattern 7 / Sub-pattern 6 sub-rule c extension: when a sub-agent claims grep-completeness, re-run the completeness grep at the orchestrator level before trusting "no extras." **Original banking-row context for both items retained below.** Build 3 was scoped as the Tier 3 compliance split. (a) [`complianceMonitorService.ts:319`] insurance-expiry sweep. (b) :366 expiring-soon. (c) :458 generic. (d) :823 fmcsaComplianceScan daily 3am. (e) :1130 monthly re-vetting. (f) :1240 auto-recovery. (g) :1437 expired-insurance auto-suspend. (h) complianceController:173. (i) complianceForecastService:41. (j) chameleonDetectionService:218 both sides. (k) cron sweeps. All now fenced + the Pattern-7 extras above.

196. **Carrier lifecycle Arc 2 (2026-08-18) — worked the Item 195 open queue. 5 items, 5 commits, baseline `2b76cc93`.** Full write-up appended to [`docs/audits/carrier-lifecycle-audit.md`](docs/audits/carrier-lifecycle-audit.md); endpoint verdicts in [`docs/audits/orphan-endpoint-triage.md`](docs/audits/orphan-endpoint-triage.md).

    **CLOSED.** **(1) POD sweep escape hatch** (`6b8bbd50` v3.8.asl) — the sweep scanned `DELIVERED` alone, but the AE map allows `DELIVERED → INVOICED` directly, so invoicing a load before its POD landed removed it from the chase population permanently — and `INVOICED` only advances to `COMPLETED`, so it could never reach `POD_RECEIVED` either. It left the pipeline owing paperwork with the money already out. `POD_CHASE_STATUSES` is now DELIVERED + INVOICED, extracted as a pure `isPodChaseableStatus`. Chasing INVOICED is actionable: the upload path records the Document and stamps `podUrl` regardless of status and only *advances* from AT_DELIVERY/DELIVERED/LOADED. **(2) Repeat AE escalation** (`a4ed85cd` v3.8.asm) — the overdue band used one fixed dedup link so the AE was told once and never again. The escalation ordinal now rides in the link (`overdue-0`, `-1`, … every 48h), so the existing link-dedup handles repeats with no new table or column; the 14-day abandon window still stops it. **(3) Orphan-endpoint triage** (`9cbba6a7`, unversioned per §3.1 — comments + docs, no behavior change) — all 26 Pass 1 findings classified and annotated in place with `// audit-pass1:`: 2 FALSE-POSITIVE, 1 INTENTIONAL, 3 DUPLICATE/SUPERSEDED, 20 MISSING-UI. **Nothing deleted, deliberately** — the two closest candidates (`PATCH /invoices/:id/mark-paid` shares the `markInvoicePaid` controller with the accounting PUT the frontend calls; `assign-equipment` is superseded by `assign-truck`/`assign-trailer`) are reachable money/fleet surfaces on code I did not author, and Pass 1 only proves the *frontend* does not call something. **Tool-limitation worth banking:** Pass 1 matches static URL fragments, so a caller building its last path segment from a variable reads as orphaned — both carrier-driver deactivate/reactivate ARE live via `` `/carrier-drivers/${id}/${action}` ``. A Pass 1 finding is a question, not a verdict. **(4) F-1 unblocked + F-2 fixed** (`816ce5eb` v3.8.asn) — see below. **(5) F-7 half-closed** (`1c845260` v3.8.aso) — see below.

    **Item 4 — the authority-age gate finally has a data source.** Free Socrata L&I `9mw4-x3tu` ("AuthHist - All With History") carries `orig_served_date` per authority action keyed by DOT and MC docket, no API key — the grant history QCMobile does not return. New `services/authorityHistoryService.ts` + `scripts/backfill-authority-dates.ts` (**dry-run by default**) + weekly `authority-date-resolution` cron (Mon 4:00) scoped to post-gate registrations. **Three properties found by live probe, each pinned by test, each with a failure mode that looks like an empty dataset rather than a bug:** `dot_number` is zero-padded to 8 (`4526880` → `[]`, `04526880` → the row); a carrier has many rows across authority types plus revocation events (INTEGRITY EXPRESS: 2007 broker grant, 2010 motor grant — the gate asks about *hauling*, so motor wins); dates are MM/DD/YYYY text that sorts wrong. Selection: GRANTED only, prefer MOTOR, take earliest (matches the reinstatement caveat — the FMCSA-status gate catches inactive authority). **Production dry run: 4 null-date carriers, 4 resolved, 0 errors; gate would be 1 block / 0 override / 3 allowed, and the single block is `SRL Transport LLC`, a TEST account on SRL's own broker docket. 3 of 4 are test accounts, the 4th is PENDING — no real approved carrier is affected today.** `--commit` NOT run: writing the column is what turns the gate live, and that is Wasi's call. **F-2 rode along, copy only:** the under-12 block said "minimum 18" (telling a 6-month carrier to come back at 18 when no override exists below 12), and the 12–18 message was identical to it so an AE could not tell an overridable block from an absolute one. Both rewritten; only the `AUTHORITY_TOO_YOUNG` *code* is matched on anywhere (verified across backend/frontend/e2e). **Ratified ladder logic untouched.**

    **Item 5 — TONU fault side captured; billing legs banked.** New `lib/tonuPolicy.ts` resolves the ratified two-sided rule (CUSTOMER → bill customer + pay carrier; BROKER → pay carrier out of margin, bill nobody; CARRIER → neither, existing reversal stands), reading `TONU_AMOUNT` from `accessorialPolicy` rather than any literal. New `Load.tonuFaultSide` (additive, nullable, migration `20260819030000_add_load_tonu_fault_side` authored per §2.2, **not applied**) + 422 `TONU_FAULT_SIDE_REQUIRED` at the flip in `loadController.ts:607`. Required now even though billing is banked, because the fault side is only knowable at the flip — reconstructing it later is guesswork. **NOT WIRED LIVE, on purpose:** nothing posts the customer invoice line or the carrier settlement payable. **Resume state:** the two legs are `invoiceService`'s accessorial path (customer line, mirroring the v3.8.asb ledger→invoice work) and the carrierPay/settlement path (`createCarrierPayOnDelivery` / `syncCarrierPayAccessorials` shape); `resolveTonuBilling(faultSide)` already returns `{billCustomer, payCarrier, amount}` for both. Also still open: the RC grid prints "4-hour notice without penalty" without naming the releasing party — it must name the carrier when a writer enforces it, and `e2e/helpers/pdf.ts` pins that string character-for-character, so label and test move together.

    **Two decisions taken rather than asked (halt-ship).** (a) **The four-tier authority model was not built.** The Arc 2 brief specified 18+ / 12–18 / 6–12 / under-6-decline; Item 182 ratifies three tiers with no 6-month band and no Quick-Pay consequence. Arc 2 shipped the data source and left the ladder exactly as ratified — adopting four tiers is a product change and stays Wasi's decision. (b) **The release window is measured before PICKUP, not before acceptance.** The brief said "within 4 hours of acceptance"; the Rate Confirmation and `accessorialPolicy` both say before pickup, and the instruction was to read the template as source of truth. A test fails if anyone re-anchors it.

    **Counters.** Pass 1 **26 → 26** (every survivor annotated — the intended outcome, since nothing qualified as cleanly DEAD). Pass 2 **510 → 511** — an honest *increase*: `Load.tonuFaultSide` is new and backend-captured with its AE surface banked. Pass 4 **14 → 14**. Backend suite **594 → 632** (+38). The `urlSafety` DNS failure remains the only red and is sandbox-only — it passed in CI on `2b76cc93`, which is also the first CI confirmation of that diagnosis.

    **NOT touched per instruction:** F-4 (NOA/factoring) — ratified-pending, code and documents agree.


---

**Production reliability — hotfix (logged + closed 2026-05-06 in v3.8.aaf, Sprint 26b):**

32. ~~**Load Board click-to-detail React error #31 crash on loads with accessorials.**~~ **CLOSED 2026-05-06 in v3.8.aaf** (Sprint 26b hotfix between Sprint 26 close and Sprint 27 start). Surfaced during Sprint 26 Phase C smoke (creating test load via Order Builder → clicking load on Load Board → page crashed with full-screen "Something went wrong" + React error #31 "Objects are not valid as a React child"). Root cause: JSON column shape mismatch on `Load.accessorials`. Order Builder writes `Array<{type, amount, payer}>` per the [`Accessorial`](frontend/src/app/dashboard/orders/types.ts#L90) interface; convert-to-load endpoint at [`backend/src/routes/orders.ts:455`](backend/src/routes/orders.ts#L455) writes the form's array verbatim to the JSON column. Load Board side panel at [`loads/page.tsx:1030-1036`](frontend/src/app/dashboard/loads/page.tsx#L1030) (pre-fix) iterated `load.accessorials` with `key={a}` and rendered `{a}` directly — assuming `string[]` not `object[]`. React threw error #31 with the object's keys (type, amount, payer) in the error args. Latent since v3.8.c (Order Builder convert flow shipped ~Apr 28). Pre-commit tsc passed every sprint because the Zod validator at [`backend/src/validators/load.ts:113`](backend/src/validators/load.ts#L113) is `z.array(z.any()).optional()` — type-system can't catch JSON-column shape mismatches. **Trigger conditions** (all three required): load created via Order Builder convert flow, user added at least one accessorial before converting, user clicks the load on Load Board to open side panel. Loads without accessorials don't trigger (render gated on `length > 0`); loads created via Load Board's `+ New Load` 4-step modal don't trigger (no accessorial entry surface). **Fix shape — F2 per audit:** render `accessorial.type` with optional ` ($amount)` suffix. Defensive type-check handles three input shapes: `typeof a === "string"` (legacy data compat for any pre-v3.8.c string-only records) → render `a`; object with `type` key → render `"${a.type}${a.amount ? ` ($${a.amount})` : ""}"`; fallback → literal `"Accessorial"`. React `key` switched from `key={a}` (would have collided on repeat types) to `key={i}` (array index, stable for purely-visual list with no reorder). **Adjacent risk sweep cleared** (Phase A): Load model has 4 JSON columns total — stops (1112), accessorials (1146 — this bug), additionalRefs (1225), datPostedFields (1254). Only `accessorials` was rendered as a React child on the Load Board page; other JSON columns either aren't read or aren't rendered as children. One-finding fix, not class-wide cleanup. **Out of scope (deferred):** tightening Zod validator at `validators/load.ts:113` from `z.array(z.any())` to `z.array(z.object({type, amount, payer}))` — separate sprint; doesn't prevent future render-side bugs, just data-side. Audit of other JSON-column render sites across the codebase — broader audit sprint. **Pre-commit verification:** backend `tsc --noEmit` clean (no backend changes; verifying nothing drifted), frontend `next build` clean. Net LOC change: ~10 (1 file, accessorials block rewritten with defensive type-check + index keying). Per §3.1 sequence-continuous rule: v3.8.aae → v3.8.aaf. Hotfix-with-immediate-close pattern (logged + closed in same commit) — used because the bug blocked Sprint 26 Phase C smoke verification. **Severity: P0** — every BOL printed in the system embeds a QR code that routes customers to a 404 when scanned. Same defect class affects email tracking links sent via `shipperLoadNotifyService`. **Smoking gun:** [`backend/src/utils/qrGenerator.ts:25`](backend/src/utils/qrGenerator.ts#L25) encodes URL `https://silkroutelogistics.ai/track/<12-char-token>`. Frontend has NO `/track/[token]/page.tsx` dynamic route ([`frontend/src/app/track/`](frontend/src/app/track/) contains only `page.tsx`, no `[token]` subdirectory). [`frontend/public/_redirects`](frontend/public/_redirects) has no `/track/*` splat rule. [`frontend/next.config.ts:5`](frontend/next.config.ts#L5) sets `output: "export"` (static HTML export — Cloudflare Pages serves static files only, no SSR fallback). Net result: Cloudflare Pages serves default 404 for any `/track/<anything>` path. **Email path same defect:** [`backend/src/services/shipperLoadNotifyService.ts:48`](backend/src/services/shipperLoadNotifyService.ts#L48) builds `${PORTAL_BASE}/tracking/${load.trackingToken}`. `_redirects` line 27 redirects `/tracking` (exact) → `/track` 301, but no rule for `/tracking/*` subpaths — 404 on the same Cloudflare Pages fallback. **Fix surface options:** (a) Add `frontend/src/app/track/[token]/page.tsx` dynamic route that reads `params.token` from path, auto-fills the search box, and auto-fires the API call on mount. Cleanest UX. Requires new file ~50-80 LOC + import path updates. (b) Add `_redirects` splat: `/track/* /track 200` (URL-rewrite, not 301) + read token from `useSearchParams` or `usePathname` on the existing static page; would require URL-param plumbing on the existing page. (c) Belt-and-suspenders: both. Plus `_redirects` rule `/tracking/* /track/:splat 301` to align email links with QR pattern. **Recommend (a) + email-link rewrite** — dynamic route handles QR pattern, separate email change makes `shipperLoadNotifyService` build `/track/<token>` instead of `/tracking/<token>` to consolidate on one URL pattern. **Verification:** after fix, scan a printed BOL QR with phone camera → should auto-load tracking page with token-driven results (no manual entry). Test with at least one ShipperTrackingToken from prod DB to confirm round-trip. **Discovery context:** Sprint 25 was scoped to contrast fix + 3-tab → 2-tab reduction on `/track`. The BOL QR 404 was an adjacent finding from auditing the data-model parity (where do "Tracking codes" come from, who distributes them) — turns out the answer is "they come from QR codes that don't work." **Bumps queue:** P0 production reliability outranks remaining brand polish work — recommend slotting this ahead of `/shippers` narrative work and other §13.3 items not currently blocking. **Effort estimate:** medium sprint, ~80-150 LOC (new dynamic route + URL-param plumbing + email link consolidation). Pairs naturally with §13.3 Item 6 (Portal Approval UI S-3) only insofar as both are customer-facing surface defects; otherwise standalone.

199. **Arc 4 (2026-08-19) — Sub-pattern 5 swept repo-wide; signature version lifecycle traced, detection shipped, enforcement banked.**

    **Phase 1 — CLOSED (`1b6d1e49` v3.8.asw).** [`scripts/audit-schema-drift.ts`](backend/scripts/audit-schema-drift.ts) diffs each `validateBody` schema's declared keys against every key its handler reads off `req.body`, across all 71 validated routes. **Two drift bugs.** (a) `resetPasswordSchema` never declared `totpCode`, which `authController.resetPassword` gates on for TOTP users — so a TOTP-enabled user could not have reset their password; the frontend was already correct (`ResetPasswordForm.tsx:62` resends after `requires2FA`) and handed the value to a schema that discarded it, so the retry would have got the same answer forever. **Classification: LATENT — NEVER FIRED.** A read-only production diagnostic (2026-08-19) found `TOTP_ENABLED_COUNT: 1` — the `whaider@` ADMIN account, of 11 users total — and `RESET_TOKENS_TOTAL: 0`, meaning no password-reset token has ever existed for any user. Live 2026-02-23 (`172d6f3b`, the security-hardening commit that added the gate) → 2026-08-19, reachable by one account, exercised zero times. Read it as **lockout prevented**, not outage: the first person to hit it would have been the sole admin, most plausibly while already locked out. Diagnostic method — resets leave no record anywhere (see Item 200), so the only trace is `OtpCode` rows with the `RESET:` prefix. (b) `updateCarrierSchema` (routes/carriers.ts:50 — a *different* schema from the registration one) never declared the four insurance `*Effective` fields that `updateCarrier` reads, added to writer and registration in v3.8.aiw but not here. **Now a CI gate** (`--strict`, beside the reachability gate): 39 clean, 0 undeclared-read, 22 declared-unread (advisory), 8 unresolvable. Opt-out is `// schema-drift-ok: <reason>`. **The scanner was wrong three times before it was right** — outer brace in the first key segment (45 false findings), comments parsed as code (the comment explaining the tonuFaultSide P0 made it report tonuFaultSide as undeclared), and schemas keyed by bare name so a generic `createSchema` resolved to another file's definition. Every finding was verified against source before being called real. Guard adversarially verified; **the first verification attempt was a false pass** because the string replace silently didn't match — a verification that doesn't verify is the same failure as a test that doesn't run.

    **Phase 2 — Phase A trace COMPLETE, detection shipped (`7437f8ce`), enforcement BANKED.** Trace confirmed the core defect: `complianceCheck` accepts any SIGNED broker-carrier agreement **regardless of version** (`complianceMonitorService.ts:270-278` filters templateName + status, orders by signedAt, never reads `version`), so bumping `BCA_VERSION` reaches nobody who already signed. [`lib/agreementVersions.ts`](backend/src/lib/agreementVersions.ts) ships `CURRENT_VERSIONS` (keyed on `templateName`, so a new document type is a registry line + constant, **no schema change** — proven by a test registering a synthetic NOA type) and `assessVersions` returning drifted vs missing, deliberately distinguished because *missing means sign, drifted means re-sign*.

    **Enforcement banked for three traced reasons, not caution:** (1) **the paper does not ask for it** — the BCA has no amendment or re-consent clause; the only notice period either document states is §244's 30 days, governing Caravan *program criteria*, not the agreement body. Forcing re-signature is a policy SRL would be **adopting**, not a right it holds — Wasi's and counsel's call, especially with §16 #1 open. (2) **gating without a re-consent surface locks carriers out** — a version bump is a one-constant commit; ship the gate with it and every signed carrier drifts on that deploy and hard-blocks at grace expiry with no in-portal path to re-sign. (3) **the RC is the weakest link and is unversioned** — it records `signed`, `signedAt`, `signedUrl`, `carrierSignature` (a name), with **no IP, no user agent, no version**, while BCA and QP each capture typed name + IP + UA + version + executed PDF. The per-load document governing a haul carries weaker evidence than the masters, and a dispute cannot establish which terms governed that load.

    **RESUME STATE — Phase 2 remainder.** 2a enforcement: add version comparison at `complianceMonitorService.ts:270-278` using `assessVersions`, with a distinct blocked_code (**not** the missing-BCA message — the carrier HAS a BCA, an outdated one, and the message must say re-sign). 2b re-consent surface: portal banner + grace interstitial reusing `POST /carrier-auth/sign-bca` (Arc 1 verified it re-records to a new version idempotently), banded reminder emails on the `podReminderService` link-encoded-dedup pattern, AE notification on grace entry/exit. 2c drift cron: daily job calling `assessVersions` per carrier. 2d: RC executed-artifact parity with `generateAgreementBuffer`. **Prerequisite for the per-load half: a `version` column on RateConfirmation stamped at generation, plus IP/UA on acceptance to match the BCA standard — schema change, should land alone.**

    **Phase 3 (Item 159 A1 log-first) and Phase 4 (Pass 2 triage) — NOT STARTED, banked.** Phase 3's shape is unchanged from Item 194's investigation: wrap the AE-side `Load.status` write sites in log-only mode, instrument the known divergences (`geofenceService`'s more-permissive map, `instantBookService`'s unconditional BOOKED) rather than fixing them, and let the data decide. Phase 4 needs Pass 2 upgraded first — backend-read checking and category tagging — before triage is anything but noise.


245. **PITR settlement — CLOSED UNVERIFIED BY CHOICE (written 2026-08-26; WINDOW NOW EXPIRED 2026-08-27, final).**

    **Window confirmed closed 2026-08-28.** When this was written a day of PITR window nominally remained and no string had been supplied; that day passed and none was. The closure below therefore stands as written rather than as a prediction — the question is now unanswerable in fact, not merely in practice. Nothing further is owed on it and it appears in no future command.

    **The question, one last time.** On 2026-08-20 (§13.3 Item 212) an unpushed column-drop migration was carried to production by a `git push` that took the commits above it. Three `CarrierProfile` URL columns — `w9Url`, `coiUrl`, `authorityLetterUrl` — were dropped without the row-count gate the migration's own header required. **Whether those columns held any data at the moment they were dropped is the open question, and it is now permanently unanswerable.**

    **Why it closes here.** The only instrument that could answer it was Neon PITR, whose 7-day window expires 2026-08-27. Answering required a connection string for a branch snapshotted before 19:59:29 UTC on 2026-08-20 — production cannot stand in, because the columns are already gone there and the query errors rather than returning zero. **No such string was supplied across six consecutive sessions.** Roughly one day of window remains as this is written; it is recorded rather than relied on, because a settlement that depends on a string arriving inside the final hours is not a plan.

    **This is a decision, not a lapse.** The alternative was to keep the item open, and an item that stays open past the last moment it could be actioned stops being a task and becomes a reproach. Closing it costs the certainty; leaving it open costs the credibility of every other open item.

    **What is known without PITR, and it is not nothing.** Zero references to any of the three columns in `backend/src` or `frontend/src`, and `git log -S` returns **no commits touching any of them in src across the repository's entire history** — so nothing in this codebase has ever written one. A value could only have arrived from outside it. That is strong circumstantial evidence of harmlessness and it is **not proof**, and this entry does not upgrade it to proof.

    **What is NOT closed.** The mechanism that caused it is fixed and stays fixed: §2.2's hold-branch rule (held work lives on a branch; merging is the release act), the pre-push `git log origin/main..HEAD` read, and `/api/health` reporting `schema.migration` so a migration is never again judged by the app's SHA. Item 212 remains the case study. **This closure is about the forensic question only.**

    **If the columns did hold data**, the recovery path was always the same and is now gone: branch production to just before 19:59:29 UTC on 2026-08-20 and read them. Anyone who later needs those values should be told plainly that they are unrecoverable, rather than sent looking.



250. **E-signature and document custody — batch CLOSED except eight banked decisions (2026-08-31, `v3.8.awj` → `v3.8.awo`).**

    The correctness batch from the 2026-08-31 e-signature and document-custody
    audit is complete. Every defect the audit reproduced against a live database is
    fixed and guarded; what remains is **decisions, not work** — and the two kinds
    of remainder have different blockers, which is why they are listed separately.

    **CLOSED — seven decisions, six commits.** Shipper RC exposure (12, `awj`);
    RC signer attribution server-derived (`awk`); client IP attribution behind
    Cloudflare (13, `awl`); RC terms version and the explicit electronic-records
    consent step (5 + 10, `awm`); executed copy emailed at execution with a
    persisted send report (6, `awn`); content hash over canonical text and
    registration assent as ACKNOWLEDGED (8-agreements + 9, `awo` / `0525f458`).
    Full detail in [`docs/regression-log.md`](docs/regression-log.md); the register
    table there is the index.

    **BLOCKED ON ONE PROOF — three decisions.** All three need the same thing:
    **a production storage round-trip on an executed agreement artefact.**
    `documentUrl` IS written by both signing paths
    ([`carrierAuth.ts:1213`](backend/src/routes/carrierAuth.ts#L1213), `:1418`), and
    v3.8.avo made `/api/health` prove storage put/read/delete for real rather than
    reporting that a credential exists (Item 249). What has **not** been observed is
    a production signing event whose `documentUrl` then resolves and serves. Until
    it does, all three would be built on an assumption:

    - **3 — is the RC frozen at acceptance?** Freezing means minting immutable
      bytes, which presumes bytes can be durably stored and fetched back.
      **The RC content hash rides here**, re-banked from decision 8: if acceptance
      freezes bytes, the right hash is over *those bytes* and the canonical-text
      apparatus is unnecessary for the RC; if the RC stays a live re-render, a byte
      hash is impossible (PDF bytes are not reproducible) and only canonical text
      works — which costs a rewrite of a 1,081-line grid document. Picking a hash
      design before this decision is answered builds the expensive half of a choice
      nobody has made.
    - **7 — retention period.** There is no policy, no expiry and no deletion path.
      A retention rule over artefacts presumes the artefacts durably exist and can
      be enumerated and deleted.
    - **11a — should the AE console expose the executed PDF?** `documentUrl` is
      already returned by `getCarrierAgreements` and rendered nowhere. Wiring a
      download button to a URL that has never been proven to resolve in production
      would ship a button that 404s for the first AE who presses it.

    **BLOCKED ON OWNER RATIFICATION — five decisions.** No technical blocker; each
    is a product or legal call and none should be defaulted by a session:

    - **1 — drawn vs typed signature.** `signatureData` is documented as "Base64
      signature image or typed name" and is always a copy of the typed name.
      Whether a drawn signature is ever required is an evidentiary standard, not an
      implementation detail.
    - **2 — does SRL countersign?** Every artefact is one-sided; the BCA button
      says "executed" with no broker signature block filled. Whether a broker
      signature is required goes to whether the instrument is complete as issued.
    - **4 — should a signed RC lock the load?** No guard connects `updateLoad` to a
      signed RC, so a load can be edited after its rate confirmation is signed.
      Which fields may still move after signature is an operational policy.
    - **11b — should the AE sign-on-behalf path survive at all?** Distinct from
      11a and NOT storage-gated: this is whether an AE may execute a carrier's
      signature, which is a legal question about attribution regardless of where
      the artefact lands.
    - **14 — should `signedByName` be constrained** to the carrier's officer of
      record, rather than accepting any typed name.

    **Nothing here is a hidden defect.** Each remaining item was surfaced by the
    audit, verified, and deliberately not resolved. The batch's discipline was that
    a verified premise beats an instructed one: two instructed column drops and one
    instructed feature were refused mid-batch after verification falsified their
    premises — see the `awm` and `awn` entries, and §19 Sub-pattern 15.


253. **Unified Tender Lifecycle — ARC CLOSED (2026-09-01, `23faaaa7` → `ee838a3d`, v3.8.aws → v3.8.ayb).**

    Report: [`docs/audits/tender-lifecycle-audit.md`](docs/audits/tender-lifecycle-audit.md),
    whose header carries the closing hashes and the full residue table.

    **Gap-table residue: 15 closed, 1 partial, 3 open.** Every open row carries
    its reason IN THE ROW, because a reason that lives only in a commit message
    is a reason the next reader will not find.

    | Row | State | Blocked on |
    |---|---|---|
    | 3 — no direct `Load.status` writes | OPEN, population frozen | reconciling the canonical map with an AUTO/SYSTEM actor (Item 159 Sprint 3); enforcing today breaks auto-pilot dispatch and fall-off recovery |
    | 4 — one live OFFERED unless parallel | PARTIAL | `waterfall.parallel` does not exist; the operational half is closed and proven |
    | 7 — QuickPayElection on tender | OPEN | a BUILD, not an invariant — no row exists to assert about |
    | 16 — expiry advances the cascade | OPEN | a BUILD; the sweep reverts to POSTED but does not advance the position |

    **BANKED, by number.** Each was reached deliberately and left undone with a
    reason, not overlooked:

    253.1 ~~**SRL countersign.**~~ **CLOSED 2026-09-04 as DECIDED, not open
    (v3.8.azq → v3.8.azt).** Ratified: SRL countersigns by system-applied
    countersignature at carrier acceptance. The carrier's acceptance is the last
    act needed to form the agreement, so that instant is when the company is
    bound and the instant to record binding it.

    **It is not a claim that anyone typed anything**, and the document says so:
    name and title come from `config/authority.ts` (`SIGNATORY_NAME`,
    `SIGNATORY_TITLE`), the timestamp is the server clock, and the rendered line
    reads "applied automatically on the Carrier's acceptance".

    **Three things make it evidence rather than decoration.** It is inside the
    content hash, as a segment in `assembleAgreementSegments` between the witness
    line and the attestation. It is drawn on the page, because a segment inside
    the hash and absent from the copy would bind a carrier to a sentence they
    never received. And it is stored on the row
    (`counterSignedByName`/`Title`/`At`) rather than resolved at render, so
    changing the officer cannot restate who bound the company on agreements
    already executed.

    **The blocker was `drawSignatureBlock`, and it was structural.** Prefills
    resolved by bare field name and both roles carry `PRINT NAME`/`TITLE`/
    `SIGNATURE`/`DATE`, so filling the broker's also filled the carrier's. B9a
    role-scoped it as a pure widening — all 14 render pins held on that commit,
    which is the proof.

    **The two BCAs on the archived body stay NULL and keep their stored hashes.**
    Backfilling would manufacture an execution record for an act that did not
    happen. Verified before the migration that nothing recomputes a stored hash.

    Proof: `backend/scripts/_b11-countersign-proof.ts`, 29/29 against a real
    database, adversarial both ways. What remains with counsel (§16 #1) is
    whether the BCA text should describe the countersignature — the platform
    behaviour is decided.

    253.2 **Sign-on-behalf retirement.** An AE can mark a rate confirmation
    signed. Since v3.8.axu a carrier signs through a single-use link that captures
    name, IP, user agent, timestamp and token id, so the on-behalf path is now the
    weakest evidence in the system and is the one an adversary would use. Retiring
    it is a policy decision about attribution.

    253.3 **Drawn signature.** `signatureData` is documented as "Base64
    signature image or typed name" and is always a copy of the typed name.
    Whether a drawn signature is ever required is an evidentiary standard.

    253.4 **Inline signing for a carrier who is present.** The emailed link works
    on every path (v3.8.aya), and a carrier who accepted in their own session
    could be shown the signing step immediately, which would materially raise
    signature rates. Not built: it needs a frontend surface, and the variant that
    returns a live signing secret has no consumer yet — shipping an unused export
    that hands out a secret is the shape this arc refused twice.
    **Does not apply to loadboard bids**, where the accept is AE-only and the
    carrier is not present.

    253.5 **`loadComplianceService` residue: none.** v3.8.axn removed the staging
    write and its rollback; `checkLoadCompliance` takes `candidateCarrierId` and
    is read-only. `clearCarrier` is down to one caller, guarded by a caller
    census. Recorded because the brief asked whether residue remained: it does
    not. What DOES remain is that its only caller invokes it fire-and-forget, so
    the throw prevents nothing — the real gate is the synchronous
    `complianceCheck` above it, and the docstring now says so.

    253.6 **Carrier-facing history label parity.** `carrierTenderLabel` is the
    single wording helper and a guard fails any carrier surface printing a raw
    status. Parity is held on the tender-history page; the my-loads and tenders
    pages were not brought under the same guard, so a raw status added there
    would not be caught.

    253.7 **The RC's own evidence gaps**, from the 2026-08-31 e-signature audit
    (§13.3 Item 250): whether a signed RC should lock the load against edits,
    retention policy for executed artifacts, and whether the AE console should
    expose the executed PDF. All three were storage-gated at Item 250 and are now
    unblocked — `documentUrl` and `pdfUrl` resolve and serve, proven by the
    freeze proof reading bytes back and hashing them.

    **Three findings the brief did not predict**, each recorded at its commit:
    the waterfall accept had been **silently dead since the commit that added its
    compliance check** (a User id passed to a `CarrierProfile` lookup, so every
    position skipped); a **terminated carrier could still take a load off the
    board**, because the load-board accept checked `onboardingStatus` rather than
    `complianceCheck`; and the auto-dispatch paths **could not reach CONFIRMED at
    all**, which is why 11e's asymmetry was correct when written and wrong a
    commit later.

    **Methodology, banked to §19:** Sub-pattern 16 fired repeatedly and mostly
    against my own proofs — a single-use claim that stayed green with its
    mechanism removed (caught only by running the injection); an injection whose
    anchor used `
` against a CRLF file and therefore never applied; a guard
    asserting a file was present rather than that it ran; and three successive
    predicates that flagged correct code. Sub-pattern 17 fired on a local selftest
    that ran in local-disk mode and proved nothing about production.

255. **The session race — a fresh login was told its session had ended, and the refusal could kill it for good. CLOSED (2026-09-01, `8be7a561` → `e98476af`, v3.8.ayj + v3.8.ayk + an unversioned guard commit).**

    Phase A: [`docs/audits/session-race-audit.md`](docs/audits/session-race-audit.md).
    Reported as an intermittent E2E failure on `6a0ff6ee`, which had nothing to do
    with that commit.

    **255.1 — THE REPORTED SYMPTOM WAS THE MILD HALF.** `registerSession` writes
    the in-memory Set synchronously and issues the persisted row as
    `void createSession(...)`, deliberately, so a login that has already succeeded
    cannot fail on a bookkeeping write. The response therefore goes out with the
    upsert still in flight, and a client fast enough to ask again first reads no
    row. The policy called that **idle expired** — which for a token twenty
    seconds old is false on its face. The code was conflating *not written yet*
    with *long abandoned*.

    **255.2 — AND THE REFUSAL DELETED UNCONDITIONALLY.** `findUnique` had already
    returned `null` in exactly the case that matters, so that delete could **only
    ever destroy a row written concurrently**. When the in-flight upsert landed
    between the read and the delete it removed the row that had just been written,
    and the token was **dead until re-login** rather than working on the retry.
    Fixed first and independently in `v3.8.ayj`: it is two lines and it removes the
    permanent half whichever way the rest goes.

    **255.3 — THE GRACE, AND WHY IT IS NOT A RELAXATION.** `v3.8.ayk` allows a
    **missing** row when the token is within `SESSION_GRACE_SECONDS` (default 30,
    bounded 0-300, and 0 disables it). It does not weaken the idle rule: a row that
    EXISTS with a stale `lastSeenAt` is judged by that rule unchanged, so freshness
    alone can never buy an unbounded session. A token with no usable `iat` is
    refused above the branch entirely, and the absolute ceiling is checked first.
    Chosen over awaiting the write, which is a 12-site signature change and
    reintroduces the exact failure mode the fire-and-forget comment exists to
    prevent.

    **255.4 — THE INVARIANT IT RESTS ON IS NOW ENFORCED.** A token revoked during
    the grace is still refused, by the **blacklist**, which is consulted one step
    EARLIER than the row read. That was true when the audit concluded it and
    **nothing enforced it** — a future revocation path that only deleted the row,
    or a reordering of the two checks, would have silently opened the gap the audit
    says is closed.
    [`blacklistInvariant.test.ts`](backend/__tests__/unit/middleware/blacklistInvariant.test.ts)
    pins both halves by walking braces rather than matching a regex, with
    self-tests and vacuity tripwires. Adversarially verified in both directions:
    physically reordering the checks names the function, and a revoke-only call
    site names the file. Shipped `e98476af`, and it runs in CI.

    **Closing state.** Phase A `8be7a561`; conditional delete `b70cdab7`;
    grace window `0df0f142`; guard `e98476af`. Proof **13/13** with both
    adversarial injections executed and each failing exactly its own
    assertion. Nothing is left open on this item — the residual risk it names
    is a write delay beyond `SESSION_GRACE_SECONDS`, which is a recoverable
    401 rather than the dead session it used to be.

    **255.5 — `ssoAuth.ts` WAS ALREADY IMMUNE, and had been all along.** It passes
    `persistSession: false` and then `await`s its own upsert before responding. One
    of the thirteen mint sites had been doing the safe thing the whole time, which
    is worth recording: this generalises a property that already existed rather
    than inventing one. DRIVER is unaffected (Item 244.6) — no session row, no idle
    window. Absolute expiry and the concurrent-session limit are unaffected too:
    one derives from the token, the other from the in-memory Set that is written
    synchronously.

    **255.6 — WHY CI SAW IT AND PRODUCTION DID NOT.** The race is lost when the
    client beats a database round trip. E2E mints and uses a token from the same
    runner at roughly 1ms against a multi-millisecond upsert; a browser is 50-300ms
    away and usually wins. **A defect only a fast client can reach is one only CI
    can find** — which is the argument for reading the E2E job by name rather than
    treating an intermittent red as noise (Item 254).

    **255.7 — TWO PROOF FAILURES WERE MY FIXTURE, and the cause generalises.**
    `jwt.sign` is deterministic and `iat` is floored to the second, so two mints
    inside one second returned a **byte-identical token**. Two sections that
    believed they held different tokens were sharing one, and a row created for the
    first silently satisfied the second — so the failures looked exactly like a
    code defect. Same family as §19 Sub-pattern 16's eighth fire: **a value that is
    not unique to the thing under test proves nothing about it.** A nonce now makes
    each mint distinct, with the reason recorded in the proof.

257. ~~**AUTO-DEPLOY IS STILL ON, THE GATE IS DECORATIVE, AND A RED-CI COMMIT
    SHIPPED.**~~ **RESOLVED 2026-09-04 — owner confirmed auto-deploy is now Off
    on the backend service in Render, deploy hook untouched.** `Deploy to Render`
    is the sole path to production, and this time the claim rests on the setting
    itself rather than on a timing sample that could not distinguish the two
    mechanisms — which is exactly how Item 251's version of this went stale.
    The measurements below are retained because they are what found it.

    Item 251 records hook-only mode as reached on 2026-09-01 — auto-deploy
    disabled, `Deploy to Render` the sole path to production, proven by a
    continuous-sampling run showing no restart between push and hook. **That is
    not today's behaviour**, and the correction is measured rather than
    inferred:

    | | |
    |---|---|
    | push `9a74c91a` / CI run created | 04:42:59Z |
    | backend job | 04:43:01 → **04:46:30, failure** |
    | **production migration applied** | **04:43:59Z** |
    | **production process booted** | **04:44:48Z** |
    | `Deploy to Render` job | **skipped** (needs backend) |

    Production was serving the new SHA **two minutes before CI finished**, and
    the deploy job never ran at all. The hook cannot have done this. Render's
    own auto-deploy did.

    **So a commit whose CI backend job FAILED reached production, gated by
    nothing.** The failure was real (`pdf-parse` missing from backend deps, see
    v3.8.azx) — harmless in effect, since it was a test-only import, but the
    point is that nothing checked. This is precisely the residual risk Item 251
    names in its own closing paragraph, live.

    **Two readings, and I cannot distinguish them from here:** auto-deploy was
    re-enabled after 2026-09-01, or it was never actually disabled and the
    single-deploy proof recorded there measured a window in which the hook
    happened to be the only thing that fired. The proof was sound for what it
    sampled — it showed no restart between push and hook on that occasion — but
    a docs-only commit deploying identically by either path cannot separate the
    two mechanisms, only their timing.

    **The fix is a Render dashboard action and is Wasi's**, deliberately not
    taken here: turning auto-deploy off is outward-facing and irreversible from
    a session, and the ordering in `docs/internal/render-deploy-gate-setup.md`
    matters — the hook is already proven (Item 251's run 33505366194 logged
    `Deploy hook secret present.`), so the remaining step is only to disable
    auto-deploy in the service settings. Until that happens the deploy gate
    refuses nothing: it can decline to deploy, and Render deploys anyway.

    **CONFIRMED A SECOND TIME, AND MORE SHARPLY, ON `6dad5493` THE SAME HOUR.**
    The first measurement left one theoretical escape — the deploy job was
    *skipped*, so one could argue the hook fired by some other route. The second
    closes it, because there the deploy job RAN and its timing cannot explain the
    boot:

    | | |
    |---|---|
    | push | 05:00:36Z |
    | **production booted** | **05:03:42Z** |
    | `Deploy to Render` job | 05:07:43 → 05:07:47Z, success |

    Production was serving the commit **four minutes before the job that deploys
    it started**. The hook then fired into an already-deployed service — the
    "double deploy" Item 251 describes as the retired interim. It is not retired.

    **Going-forward, and this is the cheap half:** a session that pushes must
    read the CI job list BY NAME afterwards rather than inferring from
    production being healthy. Production being on the new SHA is evidence that
    *something* deployed, not that anything approved it.

    **CONFIRMED IN FORCE 2026-09-04 ON `ffeab902`, AND THE ORDERING IS NOW THE
    EVIDENCE.** The first push after the setting changed:

    | | |
    |---|---|
    | run created (push) | 12:01:00Z |
    | `Deploy to Render` job | 12:02:50 → 12:02:53Z, success |
    | **production booted** | **12:04:19Z — 86s AFTER the hook fired** |

    No boot at push time. That is the exact inverse of the two measurements
    above, where production was already serving the SHA two to four minutes
    BEFORE the deploy job ran — and it is what makes the ordering diagnostic
    rather than merely suggestive. Item 251's version of this claim rested on a
    sample that could not separate the two mechanisms because both would have
    produced the same observation; this one separates them, because
    boot-after-hook and boot-before-deploy-job are opposite orderings and only
    one mechanism can produce each.


259. **BOL chrome migration — CLOSED AT COMMIT 5 by owner ruling (2026-09-04, v3.8.baf → v3.8.bal).** The Bill of Lading draws its **letterhead, footer and font registration** from the shared operational chrome. Its **body stays BOL-local as v2.9 canon.** Commits 6-9 of the planned sequence are **not required** and should not be re-proposed without re-reading this entry.

    **What shipped:** legacy shipment-keyed `generateBOL` retired (`baf`, zero callers across backend/frontend/e2e); `verify:bol` naming the two orphaned fit gates plus a new anchor-parity gate (`bag`); `footerY` optional on `drawFooter` (`bah`); `registerSkillFonts` replacing nine duplicate `registerFont` calls and a 60-line duplicate ligature patch (`bai`, **the pin did not move** — byte-identical content stream, which is the proof the faces were the same); the letterhead at `yTop: 18` (`baj`); the footer at `fyLine + 4` (`bak`).

    **THE RULING, and the reason it is not a preference.** The four body primitives that share names with the BOL's sections are different components:

    | | chrome | BOL |
    |---|---|---|
    | meta strip | gold rules top/bottom, no dividers | bordered 6-cell grid with vertical dividers |
    | parties | **two** panels, content at `x+10` (44 / 322) | **one** cream container, two 12pt-padded columns (46 / 312) |
    | shipment table | `rowH 18`, square navy band | `BOL_ROW_H 22`, rounded clip, dashed separators, distinct totals row |
    | signature | `CONTENT_W/3` from `MARGIN` | three columns at 34 / 219.3 / 404.7 |

    All four hardcode `MARGIN`/`CONTENT_W`, so adopting any of them also shifts the body 2pt and narrows it 4pt. `BOL_ROW_H = 22` is the **input to the adaptive one-page budget**, so `rowH 18` re-tunes the entire fit of a document gated to one page.

    **The signature strip differs in COMPLIANCE CONTENT, not styling**, and that is the finding that settles it rather than merely informing it. `BOL_SIGNATURE_ROLES` omitted *"Required placards received; emergency response info available (49 CFR 172)"* from the carrier certification, the TRAILER LOADED / FREIGHT COUNTED by-shipper/by-driver checkboxes, and *"The carrier shall not make delivery of this shipment without payment of freight and all other lawful charges."* Adopting it would have removed hazmat certification text from the document a driver signs at a dock.

    **A shared primitive carrying per-document `x`, `width`, `rowH`, container-style, separator-style and totals-style overrides is not shared chrome — it is one document's code living in another file.** Either the BOL changes or the chrome dilutes. The letterhead and footer are genuinely one component across documents; the body is not. That is the dividing line, and it is where the migration stops.

    **`BOL_SIGNATURE_ROLES` is DELETED** (`bal`), so no future session adopts it. It was never consumed — all three `drawSignatureBlock` callers pass their own roles, so it survived only as an unreachable default, and `roles` is now required. **`includeQr` and its BOL-only docstring are kept**: the QR is operational, belongs on the document that travels with the freight, and the BOL is the only caller that passes it.

    **Commit 9 (SHAVE_CAPACITY / BASE_SLACK retune) is NOT REQUIRED, with evidence.** The budget was never disturbed: across all six matrix cases `maxContentY` is byte-identical before and after the migration — 723.5 / 721.5 / 734.5 / 731.5 / 717.5 / 729.5 against a 768 floor, one page each. Three parity renders (1-row, 4-row + hazmat, wrapped instructions) confirm the shave absorbing growth as designed, with the Released Value Declaration on page 1 below instructions and above signatures in every case, per 49 U.S.C. § 14706(c).

    **A methodology miss worth keeping (§19 Sub-pattern 15, my own audit).** The Phase A report called all four body primitives a "direct" fit. That assessment was made from export names and section headings rather than from the drawing code, and it was wrong on all four. The cost was three commits planned on a false premise, caught before any of them shipped — but the cheaper check was available the whole time: read what the function DRAWS, not what it is called.

    **Banked, not fixed:** `docs/design/rc.html.html` and `invoice.html.html` are cited by path from `srl-chrome.ts` in five places and are **untracked in git**. A referenced design source that is not in the repository can vanish without anything noticing. Worth `git add`-ing.


281. ~~**§5 ratifies TONU as $200 flat while the desk agreed $250 on the first real occurrence — a POLICY question, not a plumbing one (raised 2026-09-19).**~~ **CLOSED 2026-09-19 (ruling 2).** §5's TONU stays **flat at $200**. The $250 on SRL-121492 was a **negotiated one-off, documented as such** — not a bounded range, not a new default. The TONU Confirmation document (Item 284) reads the **ledger row**, so a negotiated figure prints correctly on the document for that load, and the flat rate stays the policy default the document names as policy. Item 284 is unblocked by this; nothing else waited on it. The earlier answer-for-the-document and the original question are kept below as the record of how it was reached. **ANSWERED FOR THE DOCUMENT (decision 2, 2026-09-19), NOW ALSO ANSWERED ON THE POLICY.** The decision, verbatim: *the printed TONU figure reads the ledger row, so an agreed amount above the flat rate prints correctly.* Which documents that is true of, verified rather than assumed: the **settlement** reads the `CarrierPay` that `syncCarrierPayAccessorials` reconciles from the ledger row, so after the $250 edit it prints $250; the **customer invoice** prints its line items, which come from the ledger through `syncInvoiceAccessorials` — but only at the moment the line is folded in, so a DRAFT already stamped at $200 keeps $200 until Item 282 lands or the line is corrected by hand (decision 1, below). The **Rate Confirmation** is the exception and is not a money document: its terms grid prints the POLICY clause, `tonuAmount: TONU_AMOUNT` ([`pdfService.ts:1483`](backend/src/services/pdfService.ts#L1483) → [`srl-chrome.ts:2027`](backend/src/lib/srl-chrome.ts#L2027), "TONU $200 (truck-order-not-used)"), which is what the carrier is told TONU pays as a term, not what this load was charged. Whether §5's flat $200 becomes a bounded range stays open and blocks nothing — Item 280 stays banked behind it. The original question follows. Does TONU stay flat, with anything above it as a separate documented line — and if so, on which side: paid to the carrier out of margin, or billed through to the customer — or does it become a bounded per-load figure? Item 280 depends on the answer and is not built until it exists. Worth reading against §5's own two-sided rule ("bill the CUSTOMER $200 on ANY cancellation"): under flat, the $50 Peace Transport is paid above policy is SRL margin and the customer invoice at $200 is CORRECT — which is what the system produces today, but by the accident recorded at Item 282, not by decision. Under a per-load figure, the customer side has no path to $250 at all (Item 282). Either way the answer belongs in §5 before a second occurrence, and the RC's printed TONU clause moves with it.

287. **SUPERSEDED 2026-09-20.** Superseded by Wasi's direct ruling: only in-flight work blocks archive. §14 as carried by PR #4 is the ruling of record. **Carrier archive — ANY-HISTORY. RULED 2026-09-19 (ruling 1, reading A confirmed), in this vocabulary: a carrier with any history cannot be archived or removed. It is retained and suspended. The any-history census shipped in v3.8.bdi stands. No changes to `archiveCarrier` or `lib/carrierReferences`.** "Archive" in the code is the soft-delete the `DELETE /api/carriers/:id` route performs (`deletedAt` + the login off); there is no hard delete of a carrier anywhere. Under this ruling that route refuses — `409 CARRIER_HAS_REFERENCES` — the moment the twenty-class census in `censusCarrierReferences` finds anything (loads in any status, tenders, bids, waterfall positions, carrier pays, settlements, disputes, agreements, documents, drivers, fall-offs, chameleon matches, info requests, overrides, fraud reports, Quick Pay enrollments, routing-guide entries, dock schedules, EDI transactions, exception alerts), and its remedy names the suspend endpoint. It matches the customer rule of v3.8.bdh (inactivated, never deleted). **Side by side against the concurrent carrier-archive branch's implementation** (`arc/carrier-archive-*`, in-flight-only refusal over two classes — pre-delivery loads and live/held tenders — plus a required `CarrierArchiveReason`, tender withdrawal in-transaction, login untouched): it refuses on **no class this census misses**; the census is a strict superset for the refusal. **Ruling 4, one line, folded into B7b:** the 409's remedy text lists in-flight loads with DELIVERED treated as done (`TERMINAL_LOAD_STATUSES` in `lib/carrierReferences.ts`); a DELIVERED-pre-POD load reads as **"POD still owed"** so the wording matches Item 195 F-8. The refusal itself does not change. **Still carried by B7b from the earlier decisions:** the carrier restore affordance and the archive reason field (the Archive button sends no body, so the audit row's reason is null). **Ruled again 2026-09-20, closing two open asks:** the seven modified files in the main checkout **stay untouched** — the carrier-archive branch's B2 does not overlap them and its worktree does not need them — and **`CARRIER_NOT_APPROVED` is approved into that arc's B2**, not into this one: `complianceCheck` refuses by status only SUSPENDED and REJECTED, so a REVIEWING carrier with an executed BCA passes the tender gate today and the APPROVED requirement lives only in the frontend picker (their B6d finding; pre-existing, not created by restore, which returns a carrier at whatever status it held). Item 195's gate claim is narrowed accordingly until B2 lands.

314. ~~**The reachability gate's consumer scan dropped real consumers, so a live export could be reported DEAD (2026-09-24, numbering Phase C).**~~ **CLOSED by `aa12a161`, unversioned per §3.1 — CI tooling.**

    `RegExp.prototype.test` on a `/g` regex advances `lastIndex` and carries it into the NEXT call. [`scripts/verify-reachability.ts`](backend/scripts/verify-reachability.ts) built one global regex per export name and reused it across a `.filter()` over the whole corpus, so any file whose match sat before the offset the previous file left behind was skipped.

    **Measured over the real corpus — 1273 files — for one export:**

    ```
    gate (global .test)  ->  [ documentSearch.test.ts ]
    stateless .test      ->  [ documentSearch.test.ts, src/lib/documentSearch.ts ]
    ```

    `documentSearch.ts` imports that export and calls it. The gate reported a live export as *"consumed ONLY by tests — no production reader"*, which is false.

    **The direction of failure is the point.** Missing consumers can only UNDER-report, so the gate fails toward REVIEW and DEAD — and a false DEAD fails CI and sends somebody to delete working code. That is the harm this file's own header records fixing twice already: v3.8.asc added `backend/scripts` and `backend/prisma` after three healthy exports were called DEAD, and v3.8.asz added `backend/__tests__` after a test-only export was called DEAD, both noting that failing toward deletion *"trains people to ignore it"*. This is the same harm arriving through the MATCHER rather than through the corpus.

    It was also nondeterministic in a way that makes it worse than a plain bug: the verdict depended on where in the corpus ordering a name happened to match, so the same export could be reported differently as unrelated files were added around it.

    **Fix:** a stateless regex for `.test`, a separate global one for `.match` — the only call that wants the flag. The sibling scan at the field check was already non-global and is untouched. Before: one export reported test-only with its importer missing. After: 4 checked, 0 dead, 0 test-only, both consumers found.

    **Found BY the work it was gating**, not by looking for it: the gate flagged a new export, and checking whether that flag was true is what surfaced the defect. §19 Sub-pattern 16 — a guard reporting something other than what its name implies.
